# TASK-S-002 R1 执行记录

## 本轮依据的 A2 指令摘要

1. 当前唯一任务为 `TASK-S-002：本地验收回归工具包`。
2. B2 仅交付本地验收/冒烟回归工具，不进入主线业务开发。
3. 允许修改范围仅限：
   - `07_后端/lingyi_service/tools/acceptance_smoke/`
   - `07_后端/lingyi_service/scripts/`
   - `03_需求与设计/06_交付与验收/`
4. 禁止修改 `app/models`、`app/services`、`app/routers`、`app/schemas`、`migrations`、`tests`、`06_前端`，禁止进入 TASK-005/TASK-006。

## 本轮验收标准摘要

1. 支持参数：`--module`、`--base-url`、`--token`、`--report-dir`。
2. 从独立 JSON case 文件读取测试用例，并覆盖 BOM/Workshop/Production/Subcontract 四模块 13 个关键接口。
3. 按顺序调用接口并记录结果，支持 `depends_on`、`skip_if_missing`、变量提取与模板替换。
4. 输出 `report.json` 与 `report.md`，字段满足任务单约束。
5. 退出码规则：
   - 全通过返回 `0`
   - 存在失败、跳过、配置错误、连接失败或环境阻塞返回 `1`
6. 提供 README/执行手册，便于非开发人员复核执行。

## 本轮实施动作

1. 实现并升级 `tools/acceptance_smoke/run_smoke.py`：
   - 仅 HTTP 调用
   - `expected_code` 断言
   - 前置依赖/跳过策略
   - 变量提取与 `{{var}}` 模板替换
   - 结构化 JSON/Markdown 报告
2. 完成 13 个关键接口 case 配置：
   - `cases/bom_cases.json`
   - `cases/workshop_cases.json`
   - `cases/production_cases.json`
   - `cases/subcontract_cases.json`
3. 补齐 `config.example.json` 与 `tools/acceptance_smoke/README.md`。
4. 交付脚本入口 `scripts/run_acceptance_smoke.sh`（保留 `scripts/run_task002_acceptance_smoke.sh` 兼容入口）。
5. 补齐交付文档：
   - `03_需求与设计/06_交付与验收/TASK-S-002/README.md`
   - `03_需求与设计/06_交付与验收/TASK-S-002/本地验收回归执行手册.md`
6. 新增 C2 审计清单：
   - `03_需求与设计/06_交付与验收/TASK-S-002/TASK-S-002_C2审计清单.md`
7. 生成样例报告：
   - `sample_reports/report.json`
   - `sample_reports/report.md`
   - `failure_sample/report.json`
   - `failure_sample/report.md`

## 关键命令与结果

```bash
python3 -m py_compile 07_后端/lingyi_service/tools/acceptance_smoke/run_smoke.py
```

- 结果：返回码 `0`，语法检查通过。

```bash
python3 -u - <<'PY'
# 启动本地 mock API（覆盖 TASK-S-002 四模块 13 接口）
...
PY
```

- 结果：启动 `http://127.0.0.1:18080` mock 服务用于通过场景验证。

```bash
bash 07_后端/lingyi_service/scripts/run_acceptance_smoke.sh \
  --config 07_后端/lingyi_service/tools/acceptance_smoke/config.example.json
```

- 结果：返回码 `0`，全量 13 case 通过并生成 `sample_reports/report.json` 与 `sample_reports/report.md`。

```bash
bash 07_后端/lingyi_service/scripts/run_acceptance_smoke.sh \
  --module all \
  --base-url http://127.0.0.1:18081 \
  --token demo-token \
  --report-dir ../../03_需求与设计/06_交付与验收/TASK-S-002/failure_sample
```

- 结果：返回码 `1`，生成失败样例报告（连接拒绝+依赖跳过，真实反映环境阻塞）。

```bash
git -c core.quotePath=false diff --name-status -- \
  07_后端/lingyi_service/app/models \
  07_后端/lingyi_service/app/services \
  07_后端/lingyi_service/app/routers \
  07_后端/lingyi_service/app/schemas \
  07_后端/lingyi_service/migrations \
  07_后端/lingyi_service/tests \
  06_前端
```

- 结果：无输出，未对禁止路径产生 tracked diff。

## 结果摘要

1. 工具已满足本地可执行、可复核、可审计。
2. 参数、case 读取、顺序执行、结构化报告、退出码规则均已实现并验证。
3. case 覆盖达到 13 个关键接口，并记录 BOM/Subcontract 历史路径与实际路由差异。
4. `report.json`/`report.md` 字段与内容满足任务单要求（通过率、失败明细、跳过原因、耗时统计、执行命令）。
5. 未修改禁止路径，未触达 TASK-005/TASK-006。

## 提交清洁度整改（A2 回流要求）

1. 已清理旧归属重复清单：移除 `03_需求与设计/05_审计记录/TASK-S-002_C2审计清单.md`，统一以
   `03_需求与设计/06_交付与验收/TASK-S-002/TASK-S-002_C2审计清单.md` 为唯一审计清单。
2. 已清理工具缓存产物：移除 `07_后端/lingyi_service/tools/acceptance_smoke/__pycache__/run_smoke.cpython-314.pyc`（及同目录缓存）。
3. 对禁止范围未跟踪项执行“明确排除”：
   - `02_源码/`
   - `06_前端/lingyi-pc/README.md`
   以上路径仅作为仓库现存未跟踪项，不属于 TASK-S-002 提交范围，后续仅按白名单路径提交。
4. 已保留限制声明：缺少可用本地 API 环境时，`--module all` 全通过返回 `0` 需在可用环境补测。
