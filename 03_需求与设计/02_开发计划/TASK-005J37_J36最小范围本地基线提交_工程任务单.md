# TASK-005J37 J36 最小范围本地基线提交工程任务单

- 任务编号：TASK-005J37
- 模块：款式利润报表 / 前端只读契约门禁
- 优先级：P0
- 任务类型：本地白名单基线提交 / docs evidence
- 前置审计：审计意见书第 155 份，TASK-005J36 已通过
- 更新时间：2026-04-15 14:15 CST
- 作者：技术架构师

## 一、任务目标

将 TASK-005J36 已通过审计的最小范围改动纳入本地 git 基线，形成稳定交付点。只允许提交 J36 的 3 个白名单文件，不允许带入临时探针、历史未跟踪文件、TASK-006、后端或 workflow。

本任务只做本地基线提交和证据记录，不新增业务代码，不修改门禁逻辑，不开放款式利润快照创建入口，不进入 TASK-006。

## 二、前置结论

审计意见书第 155 份结论：TASK-005J36 通过。

已验证：

- `bind` 预绑定绕过已关闭。
- `call.call` 中转绕过已关闭。
- `Function.prototype.apply.call(...)`、`Array.prototype.reduce.apply.call(...)`、`Array.prototype.reduce.call.apply(...)`、`Array.prototype.reduce.bind.call(...)`、`Array.prototype.reduce.bind.apply(...)`、`Reflect.apply(Array.prototype.reduce.bind, ...)` 已被拦截。
- 污染后 `new Worker(args[0], args[1])` 已被拦截。
- 前端 `npm run verify` 通过。
- 后端 style-profit API 只读回归通过。
- 临时审计探针写入的 `src/App.vue` 已还原。
- TASK-006 继续阻塞。

## 三、白名单文件

本任务只允许提交以下 3 个文件：

1. `/Users/hh/Desktop/领意服装管理系统/06_前端/lingyi-pc/scripts/check-style-profit-contracts.mjs`
2. `/Users/hh/Desktop/领意服装管理系统/06_前端/lingyi-pc/scripts/test-style-profit-contracts.mjs`
3. `/Users/hh/Desktop/领意服装管理系统/03_需求与设计/02_开发计划/TASK-005J36_同步数组迭代方法Bind与CallCall门禁整改证据.md`

如果证据文件尚未创建，先创建该证据文件；证据文件只记录 J36 验证结果、提交范围、禁改边界扫描，不写 TASK-006 内容。

## 四、禁止提交

禁止提交以下任何路径：

- `/Users/hh/Desktop/领意服装管理系统/07_后端/**`
- `/Users/hh/Desktop/领意服装管理系统/.github/**`
- `/Users/hh/Desktop/领意服装管理系统/02_源码/**`
- `/Users/hh/Desktop/领意服装管理系统/06_前端/lingyi-pc/src/App.vue`
- `/Users/hh/Desktop/领意服装管理系统/03_需求与设计/**/TASK-006*`
- `/Users/hh/Desktop/领意服装管理系统/03_需求与设计/05_审计记录.md`
- `/Users/hh/Desktop/领意服装管理系统/03_需求与设计/05_审计记录/审计官会话日志.md`
- `.pytest-postgresql-*.xml`
- `coverage/**`
- `dist/**`
- `node_modules/**`
- 任意临时 probe、审计 scratch、备份文件

## 五、必须先执行的检查

在暂存前必须执行：

```bash
cd /Users/hh/Desktop/领意服装管理系统

git status --short

git diff -- 06_前端/lingyi-pc/src/App.vue

git diff --name-only
```

要求：

1. `git diff -- 06_前端/lingyi-pc/src/App.vue` 必须为空。
2. 如 `App.vue` 有 diff，必须停止并回报，不得提交。
3. 如出现白名单外必须提交的文件，必须停止并回报，不得自行扩大范围。
4. 不允许使用 `git add .` 或 `git add -A`。

## 六、验证命令

提交前必须执行并在证据文件中记录摘要：

```bash
cd /Users/hh/Desktop/领意服装管理系统/06_前端/lingyi-pc
npm run check:style-profit-contracts
npm run test:style-profit-contracts
npm run verify
npm audit --audit-level=high

cd /Users/hh/Desktop/领意服装管理系统/07_后端/lingyi_service
.venv/bin/python -m pytest -q tests/test_style_profit_api.py tests/test_style_profit_api_permissions.py tests/test_style_profit_api_audit.py tests/test_style_profit_api_errors.py
.venv/bin/python -m py_compile $(find app tests -name '*.py' -print)
```

最低验收结果：

- 前端 `check:style-profit-contracts` 通过。
- 前端 `test:style-profit-contracts` 通过，场景数不得低于 `475`。
- 前端 `verify` 通过。
- 前端 `npm audit --audit-level=high` 为 `0 vulnerabilities`。
- 后端只读回归通过，至少 `34 passed`。
- 后端 `py_compile` 通过。

## 七、白名单暂存命令

只能使用以下命令暂存：

```bash
cd /Users/hh/Desktop/领意服装管理系统

git add -- \
  '06_前端/lingyi-pc/scripts/check-style-profit-contracts.mjs' \
  '06_前端/lingyi-pc/scripts/test-style-profit-contracts.mjs' \
  '03_需求与设计/02_开发计划/TASK-005J36_同步数组迭代方法Bind与CallCall门禁整改证据.md'
```

暂存后必须执行：

```bash
git diff --cached --name-only
```

输出必须只包含上述 3 个白名单文件。

如果出现任何其他文件，必须立即执行：

```bash
git restore --staged <非白名单文件>
```

不得提交非白名单文件。

## 八、提交命令

确认 staged 只有 3 个白名单文件后，执行：

```bash
git commit -m "test: close style profit iteration descriptor edge cases"
```

提交后执行：

```bash
git show --stat --oneline --name-only HEAD

git status --short
```

## 九、证据文件必须包含

`TASK-005J36_同步数组迭代方法Bind与CallCall门禁整改证据.md` 必须包含：

1. 任务编号：TASK-005J36。
2. 前置审计：审计意见书第 155 份通过。
3. 验证命令摘要。
4. `npm run test:style-profit-contracts` 场景数。
5. 后端只读回归结果。
6. `py_compile` 结果。
7. `App.vue` diff 为空确认。
8. `git diff --cached --name-only` 白名单确认。
9. commit hash。
10. commit 文件清单。
11. TASK-006 未解锁声明。

## 十、验收标准

- HEAD commit 存在。
- commit 文件清单只包含 3 个白名单文件。
- 未提交 `App.vue`。
- 未提交后端、workflow、`02_源码`、TASK-006、审计记录、运行生成物。
- 证据文件已记录验证摘要和 commit hash。
- 工作区中允许存在历史未跟踪文件，但不得被暂存或提交。
- TASK-006 继续阻塞。

## 十一、禁止事项

- 禁止 `git add .`。
- 禁止 `git add -A`。
- 禁止提交 `src/App.vue`。
- 禁止提交审计记录文件。
- 禁止提交 TASK-006 文件。
- 禁止修改业务逻辑。
- 禁止修改后端。
- 禁止修改 workflow。
- 禁止 push。
- 禁止配置 remote。
