# TASK-005F11 F9 证据格式修正工程任务单

- 任务编号：TASK-005F11
- 模块：款式利润报表 / 本地基线证据
- 版本：V1.0
- 更新时间：2026-04-14 16:50 CST
- 作者：技术架构师
- 优先级：P0
- 前置依赖：TASK-005F10 审计有条件通过，审计意见书第 113 份
- 执行角色：后端工程师
- 审计要求：完成后必须提交审计官复审；复审通过前不得进入 TASK-005G、前端创建入口或 TASK-006

## 1. 任务目标

修复已提交的 `TASK-005F9_本地基线提交证据.md` 正文格式损坏与字段缺失，使 F9 证据文件可以独立支撑审计复盘。

本任务只允许 docs-only 修改，不得触碰后端、前端、workflow、JUnit 生成物或 TASK-006。

## 2. 必须修复的问题

文件：

`/Users/hh/Desktop/领意服装管理系统/03_需求与设计/02_开发计划/TASK-005F9_本地基线提交证据.md`

必须修复：

1. 删除孤立 `d` 行。
2. 删除乱码残留，例如 `�入口或 TASK-006`。
3. 补全回归测试命令名称。
4. 补全 PostgreSQL 双 JUnit 文件名。
5. 补全 `.pytest-postgresql-*.xml` 未提交说明。
6. 补全 `git add . / git add -A` 未使用说明。
7. 补全结论字段，不能出现空标签。
8. 保留提交前 HEAD、提交后 HEAD 和 commit message。
9. 保留 staged 文件清单和 `git show` 摘要。
10. 保留 `git status --short` 摘要，明确历史未跟踪目录和 JUnit 生成物仍未提交。

## 3. 本任务边界

### 3.1 允许修改 / 新增

只允许以下文件：

- `03_需求与设计/02_开发计划/TASK-005F9_本地基线提交证据.md`
- `03_需求与设计/02_开发计划/TASK-005F11_F9证据格式修正_工程任务单.md`
- `03_需求与设计/02_开发计划/当前 sprint 任务清单.md`
- `03_需求与设计/01_架构设计/03_技术决策记录.md`
- `03_需求与设计/01_架构设计/06_模块设计_款式利润报表.md`
- `03_需求与设计/01_架构设计/架构师会话日志.md`
- `03_需求与设计/05_审计记录.md`
- `03_需求与设计/05_审计记录/审计官会话日志.md`

### 3.2 禁止修改 / 禁止提交

禁止提交：

- `.github/**`
- `07_后端/**`
- `06_前端/**`
- `02_源码/**`
- `00_交接与日志/**`
- `01_需求与资料/**`
- `03_环境与部署/**`
- `04_测试与验收/**`
- `05_交付物/**`
- `.pytest-postgresql-*.xml`
- `TASK-006*`

禁止执行：

- `git add .`
- `git add -A`
- `git commit --amend`
- force push
- 任何代码修改

## 4. 推荐修正文档结构

将 `TASK-005F9_本地基线提交证据.md` 修成以下结构：

1. 基本信息
   - 任务编号
   - 记录时间
   - 修正时间
   - 提交前 HEAD：`e8654f93e5e056bf14199f3ba19431d34ac326b4`
   - 提交后 HEAD：`81c3cfa25acc77b0a57ae00a282fecb8dca81550`
   - commit message：`chore: baseline task 005f profit postgres gate`
2. staged 文件清单
3. 排除文件清单
4. 回归测试结果
5. PostgreSQL 双 JUnit 非 skip 结果
6. `git show --stat --name-only HEAD` 摘要
7. `git status --short` 摘要
8. 结论

## 5. 回归测试字段必须写清楚

回归测试结果不得出现空命令名。

必须写成：

```text
1. .venv/bin/python -m pytest -q tests/test_ci_postgresql_gate.py：12 passed
2. .venv/bin/python -m pytest -q tests/test_subcontract_settlement_postgresql.py tests/test_style_profit_subcontract_postgresql.py：5 passed, 8 skipped
3. .venv/bin/python -m pytest -q：641 passed, 13 skipped
4. .venv/bin/python -m unittest discover：624 tests, OK (skipped=1)
5. .venv/bin/python -m py_compile $(find app tests -name '*.py' -print)：通过
```

如实际证据中数字不同，以 F9 审计报告和 F9 证据原始记录为准，但命令名必须完整。

## 6. PostgreSQL JUnit 字段必须写清楚

必须写成：

```text
1. 07_后端/lingyi_service/.pytest-postgresql-subcontract-settlement.xml
   - tests=4, skipped=0, failures=0, errors=0
2. 07_后端/lingyi_service/.pytest-postgresql-style-profit-subcontract.xml
   - tests=4, skipped=0, failures=0, errors=0
```

并明确：

```text
上述 JUnit XML 为本地运行时产物，未纳入 commit。
```

## 7. 格式防回退扫描

修复后必须执行：

```bash
rg -n '^d$|�|：$|^\s*-\s*$|未使用  /|结果文件未纳入提交|^1\.\s*：|^2\.\s*：|^3\.\s*：|^4\.\s*：|^5\.\s*：' \
  /Users/hh/Desktop/领意服装管理系统/03_需求与设计/02_开发计划/TASK-005F9_本地基线提交证据.md
```

要求：无输出。

## 8. Staging 要求

执行前先查看：

```bash
cd /Users/hh/Desktop/领意服装管理系统

git status --short
```

如存在非白名单 staged 文件，先取消 staging：

```bash
git restore --staged <非白名单路径>
```

只允许显式 add：

```bash
git add -- \
  '03_需求与设计/02_开发计划/TASK-005F9_本地基线提交证据.md' \
  '03_需求与设计/02_开发计划/TASK-005F11_F9证据格式修正_工程任务单.md' \
  '03_需求与设计/02_开发计划/当前 sprint 任务清单.md' \
  '03_需求与设计/01_架构设计/03_技术决策记录.md' \
  '03_需求与设计/01_架构设计/06_模块设计_款式利润报表.md' \
  '03_需求与设计/01_架构设计/架构师会话日志.md' \
  '03_需求与设计/05_审计记录.md' \
  '03_需求与设计/05_审计记录/审计官会话日志.md'
```

## 9. 提交前验证

必须执行：

```bash
git diff --cached --name-only

git diff --cached --name-only | rg -n '^(\.github/|06_前端/|07_后端/|02_源码/|00_交接与日志/|01_需求与资料/|03_环境与部署/|04_测试与验收/|05_交付物/|.*\.pytest-postgresql-.*\.xml|.*TASK-006)' && exit 1 || true
```

要求 staged 文件只包含第 8 章白名单。

## 10. 提交命令

```bash
git commit -m "docs: fix task 005f9 baseline evidence"
```

禁止 amend，禁止 push。

## 11. 提交后验证

```bash
git show --stat --oneline --name-only HEAD

git status --short
```

要求：

1. HEAD 只包含 docs-only 白名单文件。
2. 不包含 `.github/**`。
3. 不包含 `07_后端/**`。
4. 不包含 `06_前端/**`。
5. 不包含 `02_源码/**`。
6. 不包含 `.pytest-postgresql-*.xml`。
7. 不包含 TASK-006。

## 12. 验收标准

□ `TASK-005F9_本地基线提交证据.md` 无孤立 `d` 行。  
□ 无乱码 `�`。  
□ 回归测试命令名完整。  
□ PostgreSQL 双 JUnit 文件名完整。  
□ `.pytest-postgresql-*.xml` 未提交说明完整。  
□ `git add . / git add -A` 未使用说明完整。  
□ 格式防回退扫描无输出。  
□ 本次 commit 为 docs-only。  
□ 未提交 `.github/**`、`07_后端/**`、`06_前端/**`、`02_源码/**`、`.pytest-postgresql-*.xml` 或 TASK-006。  
□ 审计官复审通过前不得进入 TASK-005G、前端创建入口或 TASK-006。  

## 13. 交付说明要求

工程师交付时必须说明：

1. commit hash。
2. commit message。
3. 修复了哪些格式问题。
4. 格式防回退扫描结果。
5. `git show --stat --name-only HEAD` 摘要。
6. `git status --short` 摘要。
7. 确认未修改后端、前端、workflow、JUnit 生成物和 TASK-006。
