# TASK-164I 后164归口总账收口与剩余差异冻结报告

## 1. 当前差异总览（只读）

- tracked diff 总数：
  - 命令：`git -C '/Users/hh/Desktop/领意服装管理系统' diff --name-only | wc -l`
  - 结果：`41`
- untracked 总数：
  - 命令：`git -C '/Users/hh/Desktop/领意服装管理系统' ls-files --others --exclude-standard | wc -l`
  - 结果：`80710`
- diff stat 摘要：
  - 命令：`git -C '/Users/hh/Desktop/领意服装管理系统' diff --stat`
  - 结果：`41 files changed, 3600 insertions(+), 495 deletions(-)`

## 2. TASK-164A baseline(40) 对照结果

- baseline 来源：`TASK-164A_Lingyi当前tracked_diff基线归口冻结报告.md`（原始 40 项）。
- 对照命令：
  - baseline 提取：`rg '^\\| \`.*\` \\|' TASK-164A...报告.md | awk -F'\`' '{print $2}'`
  - current 提取：`git -c core.quotePath=false diff --name-only`
  - 差集：`comm -13 <(baseline) <(current)`
- 差异结论：
  - `new_tracked_diff_after_TASK_164A` 仅 1 项：
    - `06_前端/lingyi-pc/src/api/quality.ts`
  - baseline 40 项无缺失（`comm -23 <(baseline) <(current)` 为空）

解释：
- 新增项 `quality.ts` 来自 `TASK-164H FIX1` 的最小修复（quality contract blocker），非越权扩散。

## 3. TASK-164B~164H 归口矩阵（含审计 PASS 锚点）

| 归口组 | 覆盖差异 | 归口结论 | 审计锚点 |
|---|---|---|---|
| TASK-164B | `.gitignore`、`06_前端/lingyi-pc/vite.config.ts` | 已归口收敛 | `03_需求与设计/05_审计记录/TASK-164B_开发配置tracked_diff安全收敛_C审计任务单.md:74` |
| TASK-164C | `scripts/frontend-contract-engine.mjs`、`scripts/test-frontend-contract-engine.mjs`、`src/api/request.ts` | `HISTORICAL_TASK_OUTPUT_VERIFIED` | `.../TASK-164C_..._C审计任务单.md:73` |
| TASK-164D | `src/api/factory_statement.ts`、`FactoryStatementList.vue`、`FactoryStatementDetail.vue` | `HISTORICAL_TASK_OUTPUT_VERIFIED` | `.../TASK-164D_..._C审计任务单.md:81` |
| TASK-164E | `src/api/sales_inventory.ts`、`SalesInventorySalesOrderList.vue`、`SalesInventoryStockLedger.vue` | `HISTORICAL_TASK_OUTPUT_VERIFIED` | `.../TASK-164E_..._C审计任务单.md:77` |
| TASK-164F | `src/api/warehouse.ts`、`WarehouseDashboard.vue`、`warehouse.py`、`schemas/warehouse.py`、`erpnext_warehouse_adapter.py`、`warehouse_service.py` | `HISTORICAL_TASK_OUTPUT_VERIFIED` | `.../TASK-164F_..._C审计任务单.md:73` |
| TASK-164G (+FIX1) | production 前后端/测试白名单；`router/HomePage` 从 164G 剔除为 `PARTIAL_ONLY`，转由 164H 处理 | production 语义范围已归口 | `.../TASK-164G_..._C审计任务单.md:78`；`.../TASK-164G_FIX1_..._C复审任务单.md:72` |
| TASK-164H (+FIX1) | `src/router/index.ts`、`src/views/HomePage.vue`、`src/api/quality.ts` | `HISTORICAL_TASK_OUTPUT_VERIFIED`（FIX1 收口） | `.../TASK-164H_FIX1_..._C审计任务单.md:78` |

补充：
- `TASK-164H` 原轮为 BLOCKED，已由 `TASK-164H FIX1` 通过并闭合。
- 因此 `TASK-164A` baseline 的 business/config tracked diff + 后续新增 `quality.ts` 均已进入明确归口链。

## 4. 仍未归口差异判定

### 4.1 tracked business diff

- 结论：`NO_REMAINING_UNOWNED_BUSINESS_DIFF`
- 依据：
  - baseline 22 项 business tracked diff 已被 164C/164D/164E/164F/164G(+FIX1)/164H(+FIX1) 覆盖。
  - baseline 的 config/unknown（`.gitignore`、`vite.config.ts`）已由 164B 收敛并审计通过。
  - baseline 后新增 tracked 仅 `quality.ts`，已由 164H FIX1 收口并审计通过。

### 4.2 untracked business diff

- 结论：`NONE`（在 TASK-164 归口语义内）
- 说明：
  - `HomePage.vue` 虽为 untracked，但已在 `TASK-164H(+FIX1)` 纳入审计闭环，不再视作“未归口 business diff”。
  - 其余大量 untracked 为历史任务单/报告/审计单与杂项，不应被误判为当前业务实现未归口。

## 5. TASK-164 相关 untracked 归类

- 命令：`git -c core.quotePath=false ls-files --others --exclude-standard | rg 'TASK-164|HomePage.vue'`
- 总数：`31`
- 分类：
  - `03_需求与设计/02_开发计划/TASK-164*`：`21`（任务单/归口报告）
  - `03_需求与设计/05_审计记录/TASK-164*`：`9`（C 审计/复审任务单）
  - `06_前端/lingyi-pc/src/views/HomePage.vue`：`1`

## 6. parked blockers 保留说明

以下阻塞槽位不因 TASK-164I 改变：
- `TASK-152A`：继续 parked blocker（外部审批/联调窗口类）
- `TASK-090I`：继续未放行
- `TASK-110B`：继续未放行

只读证据锚点：`03_需求与设计/02_开发计划/工程师会话日志.md` 中 `TASK-150A/150D/152A/155A/156A` 条目均保持上述口径。

## 7. 下一步建议（三选一）

- `NO_REMAINING_UNOWNED_BUSINESS_DIFF_CLOSE_164_SERIES`

理由：
- 164A baseline 的 tracked 差异归口链已完整闭环；
- 后续新增 tracked（仅 `quality.ts`）也已纳入 164H FIX1 审计通过；
- 当前不再存在“仍需后164继续拆分归口”的未归口 business tracked diff。
