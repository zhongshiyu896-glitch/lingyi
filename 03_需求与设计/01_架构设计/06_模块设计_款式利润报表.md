# 模块设计：款式利润报表

- 模块名：款式利润报表
- 任务编号：TASK-005
- 版本：V6.18
- 更新时间：2026-04-15 15:43 CST
- 作者：技术架构师
- 责任模块：FastAPI `reports/style-profit` + Vue3 `style_profit` 视图 + ERPNext `Sales Order / Sales Invoice / Stock Ledger Entry / Item Price / Item`
- 当前结论：TASK-005L 工程侧已输出本地封版复审证据，并建议 TASK-005 标记为本地封版完成；但该结论必须先经 TASK-005L1 审计复核。TASK-005L1 只审计 L 证据是否足以支撑本地封版完成，不修改前后端业务代码，不开放创建快照入口，TASK-006 继续阻塞。

## 1. 业务目标

款式利润报表用于按款式、销售订单和时间范围分析利润，输出标准成本、实际成本、差异、利润金额和利润率。该模块依赖 ERPNext 销售与库存事实，也依赖 FastAPI 自建的 BOM、外发、工票和生产计划事实。

## 2. 系统边界

### 2.1 ERPNext 负责

| ERPNext 能力 | 用途 | TASK-005 使用方式 |
| --- | --- | --- |
| `Sales Order` | 销售订单和订单金额候选来源 | 读取订单、客户、公司、订单行、销售单价、数量 |
| `Sales Invoice` | 实际确认收入候选来源 | TASK-005A 必须评估是否优先于 Sales Order |
| `Purchase Receipt` | 采购成本参考与异常排查来源 | 只作为参考来源写入 source map，不直接计入 `actual_material_cost` |
| `Stock Ledger Entry` | 库存实际成本候选来源 | 读取库存价值变动和实际成本 |
| `Item` | 款式和物料主数据 | 校验 item、物料名称、单位、公司范围 |
| `Role / User Permission` | 权限事实源 | 权限源不可用时 fail closed |

### 2.2 FastAPI 自建负责

| 自建模块 | 用途 |
| --- | --- |
| BOM 管理 | 标准材料用量、损耗、标准工序成本 |
| 外发加工管理 | 外发加工费、扣款、结算锁定事实 |
| 工票/车间管理 | 实际工票数量、撤销数量、计件工资 |
| 生产计划集成 | 销售订单、BOM、Work Order、Job Card 映射关系 |
| 款式利润报表 | 利润快照、明细、标准实际对比、报表查询 |

### 2.3 前端负责

1. 款式利润列表页。
2. 利润快照生成入口。
3. 利润明细钻取页。
4. 标准成本和实际成本对比展示。
5. 筛选项：公司、日期、款式、销售订单、客户。
6. 导出、打印和权限按钮联动。

## 3. 数据流候选

1. ERPNext `Sales Order / Sales Invoice` 提供收入候选事实。
2. BOM 展开结果提供标准材料成本和标准工序成本。
3. ERPNext `Stock Ledger Entry` 提供实际材料成本事实，`Purchase Receipt` 仅作采购成本参考与异常排查。
4. 工票模块提供实际工票成本。
5. 外发模块提供加工费、扣款和结算事实。
6. 生产计划模块提供 SO、BOM、Work Order、Job Card 的关联桥。
7. TASK-005 最终生成不可变利润快照和利润明细。

## 4. 候选数据模型

| 表名 | 用途 | 初步关键字段 | 冻结状态 |
| --- | --- | --- | --- |
| `ly_schema.ly_style_profit_snapshot` | 利润快照主表 | `snapshot_no`, `company`, `sales_order`, `item_code`, `revenue_amount`, `standard_cost_amount`, `actual_cost_amount`, `profit_amount`, `profit_rate`, `calculated_at` | TASK-005A 只盘点，不建表 |
| `ly_schema.ly_style_profit_detail` | 利润明细 | `snapshot_id`, `cost_type`, `source_type`, `source_name`, `amount`, `formula_code` | TASK-005A 只盘点，不建表 |
| `ly_schema.ly_cost_allocation_rule` | 费用分摊规则 | `rule_name`, `company`, `cost_type`, `allocation_basis`, `status` | TASK-005A 只盘点，不建表 |

## 5. 候选 API

| 接口 | 方法 | 路径 | 用途 | TASK-005A 状态 |
| --- | --- | --- | --- | --- |
| 查询款式利润 | GET | `/api/reports/style-profit/` | 分页查询利润快照或计算结果 | 只盘点，不实现 |
| 生成利润快照 | POST | `/api/reports/style-profit/snapshot` | 生成不可变利润快照 | 只盘点，不实现 |
| 查询利润明细 | GET | `/api/reports/style-profit/{snapshot_id}` | 查看收入、材料、工票、外发明细 | 只盘点，不实现 |
| 标准实际对比 | GET | `/api/reports/style-profit/compare` | 输出标准成本与实际成本差异 | 只盘点，不实现 |

## 6. 利润口径冻结前的历史待决策项（已在第 11 章冻结）

1. 收入口径（SO vs SI）已冻结为双口径，见 `11.1`。
2. 实际材料成本口径（PR vs SLE）已冻结为 SLE，见 `11.3`。
3. 外发成本口径已冻结为结算优先、验货兜底，见 `11.3`。
4. 工票成本已冻结为撤销后净数量口径，见 `11.3`。
5. 快照策略已冻结为“重算新快照、不覆盖旧快照”，见 `11.5`。
6. 资源权限已冻结为 `company + item_code + sales_order`，见 `11.5`。
7. 费用分摊已冻结为 V1 默认不纳入，见 `11.4`。

## 7. TASK-005A 执行口径

TASK-005A 只允许输出开发前基线盘点报告，必须回答：

1. 现有代码中是否已有利润相关实现。
2. 上游模块是否已具备可用数据。
3. 利润公式中每一项的事实来源是什么。
4. 哪些口径需要架构决策。
5. 哪些权限、审计、幂等、快照问题会阻塞 TASK-005B。
6. 前端页面和 API client 是否已有可复用基础。
7. TASK-005B 到 TASK-005H 应如何拆分。

## 8. 禁止范围

1. 不得新增后端模型、schema、router、service。
2. 不得新增 Alembic 迁移。
3. 不得新增前端 api、view、router、store。
4. 不得注册 `/api/reports/style-profit/` 路由。
5. 不得调用 ERPNext 写接口。
6. 不得修改 TASK-004C13 平台闭环配置。
7. 不得进入 TASK-006。

## 9. 验收标准

1. `TASK-005_款式利润报表_开发前基线盘点.md` 已输出。
2. 报告覆盖现有实现、上游依赖、利润口径、数据模型草案、API 草案、权限审计、前端缺口、风险和后续拆分。
3. 报告明确 TASK-004C13 未闭环前不得启动 TASK-005B 工程实现。
4. 代码目录 `06_前端 / 07_后端 / .github / 02_源码` 无业务变更。
5. 工程师会话日志已追加。

## 10. TASK-005A1 文档证据链补提交

审计意见书第 80 份确认 TASK-005A 作为只读开发前基线盘点有条件通过，但指出 TASK-005A 关键文档尚未进入 git 提交链，且不得把盘点通过误解为 TASK-005B 可开工。V0.2 冻结以下证据链整改口径。

### 10.1 提交范围

只允许将以下文档纳入 docs-only 提交：

1. TASK-005A 工程任务单。
2. TASK-005A 基线盘点报告。
3. 款式利润报表模块设计。
4. 技术决策记录。
5. 当前 sprint 任务清单。
6. 架构师会话日志。
7. 工程师会话日志。
8. 第 80 份审计记录和审计官会话日志。
9. TASK-005A1 工程任务单。

### 10.2 禁止范围

1. 不得 stage `06_前端/**`。
2. 不得 stage `07_后端/**`。
3. 不得 stage `.github/workflows/**`。
4. 不得 stage `02_源码/**`。
5. 不得 stage `04_测试与验收/**`、`05_交付物/**`。
6. 不得新增迁移。
7. 不得注册 `/api/reports/style-profit/`。
8. 不得进入 TASK-005B/TASK-006。

### 10.3 门禁说明

1. TASK-005A1 docs-only commit 只代表“只读盘点证据入库”。
2. TASK-005A1 docs-only commit 不代表利润报表工程实现放行。
3. TASK-004C13 平台闭环和审计复审通过前，TASK-005B/TASK-006 继续阻塞。

## 11. TASK-005B 利润口径设计冻结

审计意见书第 83 份确认 TASK-004C14 通过，TASK-005B 可从“等待 GitHub URL”的阻塞中释放，但只能进入“利润口径设计冻结 / ADR / 任务单”阶段，不得直接进入模型、迁移、API 或前端实现。V0.3 冻结以下 V1 利润口径。

### 11.1 收入口径

1. `estimated_revenue_amount` 来自 ERPNext `Sales Order` 已提交订单行，用于生产前或未开票场景。
2. `actual_revenue_amount` 来自 ERPNext `Sales Invoice` 已提交发票行，用于财务确认利润。
3. V1 默认展示 `actual_revenue_amount`；无已提交发票时展示 `estimated_revenue_amount`，并标记 `revenue_status=estimated`。
4. draft/cancelled Sales Invoice 不得计入实际收入。
5. Sales Order 与 Sales Invoice 不得重复计入收入。

### 11.2 标准成本口径

1. 标准材料成本公式：`standard_material_cost = sum(bom_exploded_required_qty * standard_unit_cost)`。
2. `standard_unit_cost` 来源优先级：ERPNext Item Price 有效采购价 -> ERPNext Item valuation_rate -> unresolved。
3. 标准工序成本公式：`standard_operation_cost = sum(bom_operation_rate * planned_qty)`。
4. 本厂工序使用 BOM 工序计件工价，外发工序使用 BOM `subcontract_cost_per_piece`。
5. 标准价或工价缺失时必须标记 unresolved，不得静默按 0 计算。

### 11.3 实际成本口径

1. 实际材料成本使用 ERPNext `Stock Ledger Entry` 口径：`actual_material_cost = sum(abs(stock_value_difference))`。
2. `Purchase Receipt` 仅作为采购成本参考和异常排查来源，不直接作为 V1 实际材料成本。
3. 实际工票成本公式：`actual_workshop_cost = sum(net_ticket_qty * wage_rate_snapshot)`，其中 `net_ticket_qty = register_qty - reversal_qty`。
4. 工票必须能归属到目标 Sales Order / Production Plan / Work Order / Job Card，无法归属时标记 `workshop_cost_unresolved`。
5. 实际外发成本采用结算优先、验货兜底：已结算/锁定用结算锁定净额，未结算但已验货用验货净额并标记 `subcontract_status=provisional`。
6. 扣款金额只作为明细展示；已使用 `net_amount` 时不得重复扣减。

### 11.4 费用分摊与利润公式

1. V1 默认不纳入制造费用/管理费用分摊：`allocated_overhead_amount = 0`。
2. 报表必须显示 `allocation_status=not_enabled`。
3. 若后续启用费用分摊，必须另行 ADR 冻结分摊基准。
4. `revenue_amount = actual_revenue_amount if revenue_status=actual else estimated_revenue_amount`。
5. `actual_total_cost = actual_material_cost + actual_workshop_cost + actual_subcontract_cost + allocated_overhead_amount`。
6. `standard_total_cost = standard_material_cost + standard_operation_cost`。
7. `profit_amount = revenue_amount - actual_total_cost`。
8. `profit_rate = profit_amount / revenue_amount`；收入为 0 时 `profit_rate = null`。

### 11.5 快照、幂等、权限与审计

1. 利润快照创建后不可修改；重算必须生成新 `snapshot_no`。
2. 快照必须保存公式版本、数据来源、来源单据号、金额和 unresolved 状态。
3. `POST /api/reports/style-profit/snapshot` 必须要求 `idempotency_key`。
4. 幂等唯一键为 `company + idempotency_key`。
5. `request_hash` 必须包含 `company / item_code / sales_order / from_date / to_date / revenue_mode / include_provisional_subcontract / formula_version`。
6. 权限动作冻结为 `style_profit:read`、`style_profit:snapshot_create`、`style_profit:export`。
7. 资源权限维度冻结为 `company + item_code + sales_order`。
8. 权限来源沿用 ERPNext Role / User Permission 聚合，权限源不可用必须 fail closed。
9. 401、403、503 必须写安全审计；快照生成、幂等 replay、幂等冲突、unresolved 快照必须写操作审计。
10. 日志不得写 Authorization/Cookie/token/password/secret 或原始 SQL 明文。

### 11.6 当前禁止范围

1. TASK-005B 只冻结口径，不实现后端模型、迁移、schema、router、service。
2. TASK-005B 不新增前端 api、view、router、store。
3. TASK-005B 不注册 `/api/reports/style-profit/` 路由。
4. TASK-005B 不进入 TASK-005C/TASK-006。

## 12. TASK-005C 模型迁移与来源映射设计

审计意见书第 85 份确认 TASK-005B1 通过，利润口径已对齐 ADR-079。TASK-005C 可进入“模型 / 迁移 / 来源映射设计”阶段，但不得直接进入完整后端 API、前端页面或加工厂对账单。

### 12.1 允许范围

1. 允许新建款式利润快照、明细、来源映射、费用分摊规则的 SQLAlchemy 模型。
2. 允许新建对应 Alembic 迁移，迁移必须可从空库自举，也必须兼容已存在 `ly_schema`。
3. 允许新建 Pydantic schema，但仅用于模型字段和来源映射 DTO，不开放路由。
4. 允许新建 `StyleProfitSourceService` 来源映射骨架，只做 ERPNext 与本地事实源定位，不计算最终利润。
5. 允许补模型、迁移和来源映射单元测试。

### 12.2 数据表冻结

| 表名 | 用途 | 关键约束 |
| --- | --- | --- |
| `ly_schema.ly_style_profit_snapshot` | 利润快照主表 | `snapshot_no` 唯一；`company + idempotency_key` 唯一；保存公式版本、收入状态、标准/实际成本、利润金额、利润率、unresolved 数量 |
| `ly_schema.ly_style_profit_detail` | 利润明细表 | 关联 `snapshot_id`；按 `detail_type` 区分 `revenue/material/workshop/subcontract/overhead/adjustment` |
| `ly_schema.ly_style_profit_source_map` | 来源单据映射表 | 关联 `snapshot_id/detail_id`；记录 ERPNext 或本地来源类型、单据号、行号、金额和数量 |
| `ly_schema.ly_cost_allocation_rule` | 费用分摊规则表 | V1 仅建表保留，默认不启用；启用前必须另行 ADR |

### 12.3 来源映射冻结

1. 收入来源必须同时支持 `Sales Order` 预计收入和 `Sales Invoice` 实际收入。
2. 已提交 `Sales Invoice` 优先作为实际收入；无已提交发票时才使用已提交 `Sales Order` 预计收入，并标记 `revenue_status=estimated`。
3. `Sales Invoice` 与 `Sales Order` 行级映射必须保存 `doctype/name/line_no/item_code/qty/rate/amount`，不得重复计入收入。
4. 实际材料成本只允许从 ERPNext `Stock Ledger Entry` 汇总，使用 `sum(abs(stock_value_difference))`。
5. `Stock Ledger Entry` 纳入规则：必须归属目标 `company + item_code + sales_order/work_order/production_plan`，必须为已提交库存事实，必须排除取消、冲销、非目标仓库或无法归属行。
6. `Purchase Receipt` 只允许作为参考来源写入 source map，不得直接进入 `actual_material_cost`。
7. 本厂工票成本必须来自 `YS Workshop Ticket` 净数量和工价快照。
8. 外发成本必须优先使用结算锁定净额，未结算但已验货时才使用 provisional 验货净额。
9. 生产计划映射只负责把 `Sales Order -> Production Plan -> Work Order -> Job Card` 关系提供给材料和工票归属，不在 TASK-005C 计算利润。

### 12.4 禁止范围

1. 不得修改 `/07_后端/lingyi_service/app/main.py` 注册利润报表路由。
2. 不得新建或启用 `/api/reports/style-profit/` API。
3. 不得实现完整 `style_profit_service.py` 计算服务。
4. 不得新增或修改 Vue 前端 api、view、router、store。
5. 不得进入 TASK-006 加工厂对账单。
6. 不得重新引入 `Purchase Receipt` 直接计算实际材料成本。
7. 不得把 `profit_amount` 写成固定示例或旧公式。

### 12.5 验收门禁

1. 迁移可创建 4 张利润相关表，并包含唯一约束、外键和关键索引。
2. 模型字段覆盖 ADR-079 的收入、成本、利润、幂等、公式版本和 unresolved 状态。
3. 来源映射测试覆盖 Sales Invoice 优先、Sales Order 兜底、Stock Ledger Entry 纳入/排除、Purchase Receipt 仅参考。
4. 全量后端测试、unittest 和 py_compile 通过。
5. 静态扫描确认未修改前端、未注册利润 API、未进入 TASK-006。
6. TASK-005C 审计通过后，才允许进入 TASK-005D 利润快照计算服务。

## 13. TASK-005C1 审计阻断整改

TASK-005C 审计结论为有条件通过，但明确要求先完成 TASK-005C1，之后才允许进入 TASK-005D。V0.5 冻结以下整改口径。

### 13.1 source_map 审计追溯字段

1. `ly_style_profit_source_map` 必须能追溯到 `snapshot_id` 和可选 `detail_id`。
2. `snapshot_id` 必须为非空外键，关联 `ly_style_profit_snapshot.id`。
3. `detail_id` 可空，关联 `ly_style_profit_detail.id`；excluded/unresolved 来源允许为空。
4. source_map 必须补齐 `source_system/source_doctype/source_status/qty/unit_rate/amount/currency/warehouse/posting_date/raw_ref`。
5. source_map 必须拆分 `style_item_code` 和 `source_item_code`，前者为利润报表款式，后者为来源行物料或款式。
6. 唯一约束必须改为 `snapshot_id + source_system + source_doctype + source_name + source_line_no`，避免新快照复用同一来源行时冲突。
7. `raw_ref` 必须脱敏，不得保存 Authorization、Cookie、token、password、secret 或原始 SQL。

### 13.2 来源状态 fail closed

1. `_is_submitted()` 不得在缺少 `docstatus/status` 时默认放行。
2. `docstatus == 1` 才可判定为已提交；`docstatus` 存在但非 1 必须排除。
3. `docstatus` 缺失时，只允许显式白名单 `status` 通过。
4. `status` 缺失、空、unknown、draft、cancelled、canceled、closed、void、return 必须排除。
5. `is_cancelled=true` 必须排除。
6. 被排除来源必须保留 `source_status_unknown` 或 `not_submitted_or_cancelled` 等原因，便于审计复核。

### 13.3 snapshot 复核字段

1. `ly_style_profit_snapshot` 必须补齐 `from_date`、`to_date`、`revenue_mode`、`unresolved_count`。
2. `revenue_mode` 默认 `actual_first`，限定 `actual_first/actual_only/estimated_only`。
3. `request_hash` 必须继续包含 `from_date/to_date/revenue_mode`，并继续排除 `created_at/operator/request_id`。
4. 增加 `company + item_code + from_date + to_date` 查询索引。

### 13.4 费用分摊默认状态

1. `ly_cost_allocation_rule.status` 默认值必须为 `disabled`。
2. V1 不允许启用费用分摊；`active` 仅作为未来状态保留。
3. 模型、迁移和测试必须一致。

### 13.5 SLE 款式编码与材料编码语义

1. 利润报表入参 `item_code` 表示款式成品编码，应在代码中明确为 `style_item_code`。
2. ERPNext `Stock Ledger Entry.item_code` 在材料成本场景下通常是材料编码，应映射为 `source_item_code` 或 `material_item_code`。
3. 不得用 `SLE.item_code != style_item_code` 直接排除真实材料成本。
4. SLE 行若能通过 `work_order / production_plan_id / sales_order` 归属到目标款式或订单，可纳入材料成本。
5. 若提供 BOM 物料集合，SLE 材料编码必须在集合内；不在集合内时排除并标记 `material_item_not_in_bom`。
6. 若既无工单/生产计划/销售订单桥，也无 BOM 物料集合验证，则不得纳入，标记 `unable_to_link_order_or_material_scope`。
7. `Purchase Receipt` 仍只允许作为参考来源，不得进入 `actual_material_cost`。

### 13.6 放行条件

1. TASK-005C1 定向测试、全量 pytest、unittest、py_compile 通过。
2. 静态扫描确认未注册 `/api/reports/style-profit/`，未修改前端，未进入 TASK-005D/TASK-006。
3. TASK-005C1 审计通过后，才允许下发 TASK-005D 利润快照计算服务。

## 14. TASK-005C2 默认不纳入与状态白名单收口

TASK-005C1 审计确认 `_is_submitted()` fail closed、source_map 主体追溯字段、snapshot 复核字段、费用分摊默认 disabled、SLE 款式/材料编码语义已完成主体整改，但仍要求先完成 TASK-005C2，之后才可评估 TASK-005D。

### 14.1 include_in_profit 默认 false

1. `ly_style_profit_source_map.include_in_profit` 是利润来源纳入开关，默认必须为 false。
2. 模型、迁移和 DTO 默认值必须一致为 false。
3. 新来源初始必须为 false，只有通过提交状态、资源归属、材料范围和排除规则后，才允许显式设置 true。
4. `mapping_status` 默认建议为 `unresolved`，不得默认 `mapped`。
5. `mapping_status != 'mapped'` 时，`include_in_profit` 必须为 false。

### 14.2 source_map 字段契约

1. `snapshot_id` 非空，`detail_id` 可空。
2. `source_system` 长度 64，非空，限定 `erpnext/fastapi/manual`。
3. `source_doctype` 长度 140，非空。
4. `source_name` 长度 140，非空。
5. `source_line_no` 长度 140，非空，默认空字符串。
6. `source_status` 长度 64，非空，缺失时统一为 `unknown`；`unknown` 不允许纳入利润。
7. `style_item_code` 长度 140，非空；`source_item_code` 长度 140，可空。
8. `amount` 非空默认 0；`qty/unit_rate/currency/warehouse/posting_date` 可空。
9. `mapping_status` 非空默认 `unresolved`，限定 `mapped/unresolved/excluded`。
10. `raw_ref` 可空但必须脱敏。

### 14.3 索引与唯一约束

1. 唯一约束：`snapshot_id + source_system + source_doctype + source_name + source_line_no`。
2. 索引：`snapshot_id + mapping_status`。
3. 索引：`company + sales_order + style_item_code`。
4. 索引：`source_system + source_doctype + source_name`。
5. 索引：`detail_id`。

### 14.4 status-only 白名单

1. `docstatus` 是首选事实字段；`docstatus == 1` 才可判定 submitted。
2. `docstatus` 存在但不是 1 时，直接 false，不再看 status。
3. `is_cancelled=true` 直接 false。
4. `docstatus` 缺失时，必须识别 `source_doctype`；未知 doctype 一律 false。
5. `Stock Ledger Entry` 不允许 status-only 放行，必须有 `docstatus=1`。
6. `Purchase Receipt` 不允许通过 status-only 进入利润成本。
7. `Sales Invoice` status-only 白名单只允许 `paid/unpaid/overdue/partly paid`。
8. `Sales Order` status-only 白名单只允许 `to deliver and bill/to bill/to deliver/completed`。
9. `closed/draft/cancelled/canceled/void/return/unknown/空字符串` 一律 false。

### 14.5 放行条件

1. TASK-005C2 定向测试、全量 pytest、unittest、py_compile 通过。
2. 静态扫描确认未注册 `/api/reports/style-profit/`，未修改前端，未进入 TASK-005D/TASK-006。
3. TASK-005C2 审计通过后，才允许下发 TASK-005D 利润快照计算服务。

## 15. TASK-005C3 利润快照期间索引补齐

TASK-005C2 审计确认 `include_in_profit=false` 默认值、source_map 字段契约、主要复合索引和 status-only 白名单已完成整改，但发现 `ly_style_profit_snapshot` 仍缺少期间查询索引。该问题虽然不影响当前功能测试，但会影响后续按期间复核利润快照的性能和契约完整性。

### 15.1 必补索引

1. 索引名称：`idx_ly_style_profit_snapshot_company_item_period`。
2. 目标表：`ly_schema.ly_style_profit_snapshot`。
3. 字段顺序：`company, item_code, from_date, to_date`。
4. 用途：支持按公司、款式、期间查询利润快照。
5. 该索引不得替代现有 `company + item_code + sales_order` 索引；订单查询和期间查询必须同时保留。

### 15.2 放行条件

1. SQLAlchemy 模型包含该索引。
2. Alembic 迁移创建该索引，schema 指向 `ly_schema`。
3. 测试覆盖索引存在性和字段顺序。
4. 定向测试、全量 pytest、unittest、py_compile 通过。
5. 静态扫描确认未注册 `/api/reports/style-profit/`，未修改前端，未进入 TASK-005D/TASK-006。
6. TASK-005C3 审计通过后，才允许下发 TASK-005D 利润快照计算服务。

## 16. TASK-005C4 本地仓库基线提交

TASK-005C3 审计通过，利润模型、schema、来源映射服务、迁移和测试的阻断项已关闭。进入 TASK-005D 前，必须先把 TASK-005C~C3 的新增代码和配套文档纳入本地 git 基线，避免后续利润快照计算服务建立在未提交文件之上。

### 16.1 基线纳入范围

1. 后端利润模型：`app/models/style_profit.py` 与必要的 `app/models/__init__.py`。
2. 利润 schema：`app/schemas/style_profit.py`。
3. 来源映射服务：`app/services/style_profit_source_service.py`。
4. 迁移：`migrations/versions/task_005c_create_style_profit_tables.py`。
5. 测试：`tests/test_style_profit_models.py`、`tests/test_style_profit_source_mapping.py`。
6. TASK-005C~C4 任务单、模块设计、ADR、Sprint 清单、架构师日志、审计记录和必要工程日志。
7. TASK-005C4 本地仓库基线提交证据。

### 16.2 提交规则

1. 只能使用白名单 `git add -- <path>`，禁止 `git add .`、`git add -A`、`git add --all`。
2. 提交前必须跑定向 pytest、全量 pytest、unittest、py_compile。
3. 提交前必须执行禁改边界扫描，确认未触碰 `06_前端`、`.github`、`02_源码`、`app/main.py`、`app/routers/**`。
4. staged 文件必须全部在 TASK-005C4 白名单内。
5. 本地 commit message 固定为 `feat: add style profit model source mapping baseline`。
6. 本任务不允许 push，不允许配置 remote。
7. 本任务不允许进入 TASK-005D/TASK-006。

### 16.3 放行条件

1. 本地 commit 已形成，且提交内容仅限白名单。
2. 证据文件记录提交前 HEAD、提交后 HEAD、验证命令结果、staged 文件清单和禁改边界扫描结果。
3. 审计复核确认 TASK-005C4 通过后，才允许下发 TASK-005D 利润快照计算服务。

## 17. TASK-005D 利润快照计算服务

TASK-005C4 审计意见书第 90 份已通过，当前本地基线 HEAD 为 `67a995c2933fac3bb269e3f668252a03b93d1238`。TASK-005D 可进入后端利润快照计算服务开发，但不得扩大到 API、前端、迁移或 TASK-006。

### 17.1 允许范围

1. 新建 `app/services/style_profit_service.py`，实现 `StyleProfitService.create_snapshot()`。
2. 新建利润快照计算服务测试：`tests/test_style_profit_service.py`、`tests/test_style_profit_snapshot_calculation.py`、`tests/test_style_profit_snapshot_idempotency.py`。
3. 允许修改 `app/schemas/style_profit.py`，仅补服务级入参/出参 DTO。
4. 允许修改 `app/services/style_profit_source_service.py`，仅补只读来源聚合 helper，不得放宽 TASK-005C1/C2 已冻结的状态和 `include_in_profit` 规则。
5. 允许补 `tests/test_style_profit_source_mapping.py` 回归用例。

### 17.2 禁止范围

1. 不得修改 `app/main.py`。
2. 不得新增或修改 `app/routers/**`。
3. 不得注册 `/api/reports/style-profit/`。
4. 不得新增 Alembic 迁移。
5. 不得修改 Vue 前端 `06_前端/lingyi-pc/**`。
6. 不得修改 `.github/**`、`02_源码/**`。
7. 不得进入 TASK-006 加工厂对账单。

### 17.3 快照计算口径

1. 幂等唯一键：`company + idempotency_key`。
2. `request_hash` 必须包含 `company / item_code / sales_order / from_date / to_date / revenue_mode / include_provisional_subcontract / formula_version`。
3. `request_hash` 必须排除 `created_at / operator / request_id / snapshot_no`。
4. 相同 `company + idempotency_key + request_hash` 必须 replay 首次快照，不新增任何行。
5. 相同 `company + idempotency_key` 但不同 `request_hash` 必须返回或抛出 `STYLE_PROFIT_IDEMPOTENCY_CONFLICT`。
6. 快照、明细、来源映射必须同事务写入；服务内不得 `commit()`，只允许 `flush()`。
7. `revenue_mode=actual_first` 时，已提交 `Sales Invoice` 优先；无已提交发票时才使用已提交 `Sales Order`。
8. `revenue_mode=actual_only` 时，无已提交发票必须收入为 0 并写 unresolved。
9. `revenue_mode=estimated_only` 时，只使用已提交 `Sales Order`。
10. `actual_material_cost` 只允许来自 ERPNext `Stock Ledger Entry`，公式为 `sum(abs(stock_value_difference))`。
11. `Purchase Receipt` 只允许作为参考来源，不得进入实际材料成本。
12. `actual_workshop_cost = sum((register_qty - reversal_qty) * wage_rate_snapshot)`。
13. 外发成本优先使用结算锁定净额；未结算验货只有在 `include_provisional_subcontract=true` 时才允许 provisional 纳入。
14. V1 不启用费用分摊，`allocated_overhead_amount=0`，`allocation_status=not_enabled`。
15. `actual_total_cost = actual_material_cost + actual_workshop_cost + actual_subcontract_cost + allocated_overhead_amount`。
16. `profit_amount = revenue_amount - actual_total_cost`。
17. `profit_rate = profit_amount / revenue_amount`；收入为 0 时 `profit_rate=null`。
18. 存在 unresolved 时 `snapshot_status=incomplete`；无 unresolved 时 `snapshot_status=complete`。

### 17.4 明细与来源映射

1. 每个纳入利润的收入/成本项必须生成 `ly_style_profit_detail`。
2. 每个 detail 必须至少有一条 `ly_style_profit_source_map`，V1 固定费用分摊 0 除外。
3. source_map 初始 `include_in_profit=false`，只有最终纳入利润且 `mapping_status=mapped` 的来源才允许设为 true。
4. `source_status=unknown` 不得纳入利润。
5. `raw_ref` 必须脱敏，不得保存 Authorization、Cookie、token、password、secret 或原始 SQL。
6. `unresolved_count` 必须等于可复核的 unresolved detail/source_map 数量。

### 17.5 放行条件

1. TASK-005D 定向 pytest、全量 pytest、unittest、py_compile 通过。
2. 测试覆盖收入模式、SLE 材料成本、Purchase Receipt 参考、工票成本、外发成本、费用分摊、利润公式、幂等 replay、幂等冲突、unresolved、source_map fail closed。
3. 静态扫描确认未注册 API、未修改前端、未新增迁移、未进入 TASK-006。
4. TASK-005D 审计通过后，才允许评估 TASK-005E API 层或报表查询层任务单。

## 18. TASK-005D1 利润快照计算服务审计整改

审计意见书第 92 份确认 TASK-005D 主体实现有条件通过，但 request_hash、unresolved、SLE 追溯、缺工价、收入状态和 idempotency_key 校验仍存在阻断项。TASK-005D1 必须先关闭这些问题，审计通过前不得启动 TASK-005E/API 层或 TASK-006。

### 18.1 request_hash 来源输入口径

1. `request_hash` 不再只包含 header 字段。
2. `request_hash` 必须包含所有影响利润结果的规范化来源输入。
3. 必须纳入 `sales_invoice_rows`、`sales_order_rows`、`bom_material_rows`、`bom_operation_rows`、`stock_ledger_rows`、`workshop_ticket_rows`、`subcontract_rows`、`allowed_material_item_codes`、`work_order`。
4. 必须排除 `operator`、`request_id`、`created_at`、`updated_at`、`snapshot_no`、`audit_id`、数据库自增 `id` 和敏感字段。
5. 来源行顺序不得影响 hash；必须按稳定键排序后参与 hash。
6. 同 header 不同来源金额必须触发 `STYLE_PROFIT_IDEMPOTENCY_CONFLICT`，不得 replay 旧快照。

### 18.2 unresolved 必须可追溯

1. 所有 `revenue_status=unresolved` 场景必须写 unresolved detail/source_map。
2. 无有效 Sales Invoice、无有效 Sales Order、收入来源全部 draft/cancelled/unknown，均必须使 `snapshot_status=incomplete`。
3. `unresolved_count` 必须大于 0 且可通过 detail/source_map 复核。
4. SLE 状态未知、缺 `docstatus`、未提交、取消或无法归属时，不得静默丢弃。
5. 需要影响材料成本完整性的异常 SLE 必须写 unresolved source_map，不计入 `actual_material_cost`。
6. `Purchase Receipt` 继续只作为 reference/excluded 来源，不计入实际材料成本。

### 18.3 缺工价 fail closed

1. 标准工序工价缺失、空字符串、非法 Decimal 时，必须 unresolved。
2. 工票净数量大于 0 且工价缺失、空字符串、非法 Decimal 时，必须 unresolved。
3. 缺工价来源不得 `mapping_status=mapped`。
4. 缺工价来源不得 `include_in_profit=true`。
5. 合法 0 工价必须是字段显式存在且可解析为 Decimal(0)，并有测试证明。

### 18.4 收入状态与幂等键校验

1. `StyleProfitRevenueSourceDTO` 必须保留真实 `source_status`。
2. Sales Invoice 的 `paid/unpaid/overdue/partly paid` 状态必须可追溯。
3. Sales Order 的 `to bill/to deliver/to deliver and bill/completed` 状态必须可追溯。
4. `docstatus=1` 但原状态缺失时，才允许写 `submitted`。
5. `idempotency_key` 必须在服务层或 DTO 层校验长度不超过 128。
6. 超长 `idempotency_key` 必须返回业务异常，建议错误码 `STYLE_PROFIT_INVALID_IDEMPOTENCY_KEY`，不得进入数据库写入。

### 18.5 放行条件

1. 第 92 份审计列出的 6 个问题全部修复。
2. 定向 pytest、全量 pytest、unittest、py_compile 通过。
3. 禁改扫描确认未注册 API、未修改前端、未新增迁移、未进入 TASK-005E/TASK-006。
4. TASK-005D1 审计通过后，才允许评估 TASK-005E API 层任务单。

## 19. TASK-005D2 实际成本归属与事务兜底整改

审计意见书第 93 份确认 TASK-005D1 已关闭第 92 份的主要缺口，但仍发现 workshop/subcontract 实际成本可跨公司、跨订单、跨款式污染利润快照，且计算异常后半快照仍可能被调用方误提交。V1 同时固化 `sales_order` 为强制非空，避免无订单归属快照进入 complete 状态。

### 19.1 实际成本来源归属

1. workshop/subcontract 来源必须按 `company + item_code + sales_order` 做资源归属校验。
2. `company` 存在且不匹配时，必须排除，不得纳入利润。
3. `item_code/style_item_code` 存在且不匹配时，必须排除，不得纳入利润。
4. `sales_order` 存在且不匹配时，必须排除，不得纳入利润。
5. `work_order/job_card` 只作为辅助归属桥，不得覆盖明确冲突的 company、item_code 或 sales_order。
6. 来源缺少 `sales_order` 且没有可验证工单/工序卡桥接时，必须 unresolved，不得纳入利润。
7. 错范围来源可写 excluded source_map；无法确认归属但可能相关的来源必须写 unresolved source_map。
8. 错范围和 unresolved 来源不得影响 `actual_workshop_cost`、`actual_subcontract_cost`、`actual_total_cost`。

### 19.2 服务事务兜底

1. `create_snapshot()` 首次写入路径必须使用 `session.begin_nested()` 保存点或等价保存点机制。
2. replay 路径不得新增行。
3. 幂等冲突路径不得新增行。
4. 首次写入过程中任意业务异常、SQLAlchemy 异常或未知异常，都必须回滚本次保存点。
5. 保存点回滚后，调用方即使误执行 `session.commit()`，也不得留下本次 snapshot/detail/source_map。
6. 服务内仍不得调用外层 `session.commit()`。

### 19.3 sales_order 强制非空

1. TASK-005D V1 中 `sales_order` 强制非空。
2. DTO 或服务校验必须拒绝 `None`、空字符串和空白字符串。
3. 建议错误码：`STYLE_PROFIT_SALES_ORDER_REQUIRED`。
4. 空 `sales_order` 必须在写入 snapshot/detail/source_map 前失败。
5. 空 `sales_order` 不得生成 unresolved 快照；V1 直接拒绝请求。
6. 来源解析不得在 expected_order 为空时把无 sales_order 来源行当作有效收入。

### 19.4 放行条件

1. 第 93 份审计列出的 3 个问题全部修复。
2. TASK-005D1 已修复项不得回退。
3. 定向 pytest、全量 pytest、unittest、py_compile 通过。
4. 禁改扫描确认未注册 API、未修改前端、未新增迁移、未进入 TASK-005E/TASK-006。
5. TASK-005D2 审计通过后，才允许评估 TASK-005D3 本地基线提交或 TASK-005E API 层任务单。

## 20. TASK-005D3 关键归属字段 Fail Closed 整改

审计意见书第 94 份确认 TASK-005D2 已基本修复错公司、错订单、错款式、保存点和空 `sales_order` 问题，但仍发现 workshop/subcontract 来源缺 `company` 或缺 `item_code/style_item_code` 时，仅凭 `sales_order` 匹配仍会 mapped 计入利润。该问题在一个销售订单多款式或来源数据缺关键字段时会污染利润快照。

### 20.1 严格归属口径

1. workshop/subcontract 来源必须能验证 `company + item_code + sales_order` 三者全部匹配，才允许纳入利润。
2. 缺 `company` 时默认 unresolved，不得仅凭 `sales_order` mapped。
3. 缺 `item_code/style_item_code` 时默认 unresolved，不得仅凭 `sales_order` mapped。
4. 缺 `sales_order` 时继续按 TASK-005D2 口径 unresolved 或业务错误处理，不得 mapped。
5. 只有通过可信桥接补全并验证 `company + item_code + sales_order` 三者全部匹配后，才允许 mapped。
6. 当前没有真实桥接查询能力时，不得把 `work_order/job_card/subcontract_order` 字段存在本身当作可信桥接。

### 20.2 scope helper 规则

1. scope 校验必须先检查硬字段完整性，再判断桥接匹配。
2. 硬字段存在但冲突时，必须 excluded。
3. 硬字段缺失时，必须 unresolved。
4. 桥接只允许补全缺失字段或辅助验证，不得覆盖明确冲突字段。
5. helper 必须返回 `mapped/excluded/unresolved` 和明确 reason。
6. 缺 `company` 建议 reason 为 `company_scope_missing`。
7. 缺 `item_code/style_item_code` 建议 reason 为 `item_scope_missing`。

### 20.3 放行条件

1. 第 94 份审计列出的 P1 问题已修复。
2. 工票/外发缺 company、缺 item_code 的四类反向测试全部覆盖。
3. TASK-005D1/D2 已修复项不得回退。
4. 定向 pytest、全量 pytest、unittest、py_compile 通过。
5. 禁改扫描确认未注册 API、未修改前端、未新增迁移、未进入 TASK-005E/TASK-006。
6. TASK-005D3 审计通过后，才允许评估 TASK-005D4 本地基线提交或 TASK-005E API 层任务单。

## 21. TASK-005D4 本地仓库基线提交

审计意见书第 95 份确认 TASK-005D3 通过，D 系列利润快照服务已经完成当前计算服务层阻断整改。进入 TASK-005E/API 前，必须先把 TASK-005D~D3 的利润服务、DTO、来源映射调整、测试、任务文档、审计记录和证据文件纳入本地仓库基线，避免后续 API 审计对象漂移。

### 21.1 基线纳入范围

1. 利润快照服务：`app/services/style_profit_service.py`。
2. 利润 DTO 调整：`app/schemas/style_profit.py`。
3. 来源映射调整：`app/services/style_profit_source_service.py`。
4. 测试：`tests/test_style_profit_service.py`、`tests/test_style_profit_snapshot_calculation.py`、`tests/test_style_profit_snapshot_idempotency.py`、`tests/test_style_profit_source_mapping.py`。
5. TASK-005D~D4 任务文档。
6. 款式利润报表模块设计、技术决策记录、当前 Sprint 清单、架构师日志、工程师日志、审计记录和审计官日志。
7. TASK-005D4 本地仓库基线提交证据。

### 21.2 提交规则

1. 禁止 `git add .`、`git add -A`、`git add --all`。
2. 必须使用 `git add -- <精确文件路径>`。
3. 提交前必须跑定向 pytest、全量 pytest、unittest、py_compile。
4. 提交前必须执行禁改扫描，确认未触碰 `06_前端`、`.github`、`02_源码`、`app/main.py`、`app/routers/**`、`migrations/**`、TASK-005E、TASK-006。
5. commit message 固定为 `feat: add style profit snapshot service baseline`。
6. 本任务不允许 push，不允许配置 remote。
7. 本任务不允许修改业务代码，不允许进入 API 或前端。

### 21.3 放行条件

1. 本地 commit 已形成，且内容仅限 D4 白名单。
2. D4 证据文件记录提交前后 HEAD、验证结果、暂存清单、白名单核对结论和禁改扫描结果。
3. 审计复核确认 TASK-005D4 通过后，才允许评估 TASK-005E API 权限与审计基线任务单。
4. TASK-006 继续保持阻塞。

## 22. TASK-005D5 D4 证据提交后 HEAD 占位修正

审计意见书第 96 份确认 TASK-005D4 代码基线已建立，利润服务与新增测试已纳入 git 跟踪，禁改边界和定向利润测试均通过。唯一保留问题是 D4 证据文件中的“提交后 HEAD”仍为占位符。该问题不涉及业务逻辑，但会影响后续 TASK-005E/API 审计证据链完整性，因此进入 API 前必须先完成 docs-only 修正。

### 22.1 修正范围

1. 只允许修正 `TASK-005D4_利润快照服务本地仓库基线提交证据.md` 的“提交后 HEAD”字段。
2. “提交后 HEAD”必须写为 `47c1728a4eb2ca16549f6478d3bdb5af95b12b1a`。
3. 可追加工程师会话日志说明本次 docs-only 修正。
4. 不得改写 D4 证据中的测试结果、扫描结果、提交前 HEAD 或其他事实字段。
5. 不得修改后端、前端、`.github`、`02_源码`、迁移、API router 或 `app/main.py`。
6. 不得新增或修改 TASK-005E/TASK-006 文件。
7. 不得 push，不得配置 remote/origin。

### 22.2 放行条件

1. D4 证据文件不再存在 `<本次 commit 后回填于执行回报>`。
2. D4 证据文件不再存在 `<pending>` 或 `TODO` 占位。
3. D4 证据文件能 grep 到 `47c1728a4eb2ca16549f6478d3bdb5af95b12b1a`。
4. 禁改扫描确认未修改后端、前端、`.github`、`02_源码`、TASK-005E 和 TASK-006。
5. 本地 docs-only commit 已形成，commit message 建议为 `docs: fix TASK-005D4 post-commit HEAD evidence`。
6. TASK-005D5 审计通过后，才允许下发 TASK-005E/API 权限与审计基线任务单。
7. TASK-006 继续保持阻塞。

## 23. TASK-005D6 D5 任务单文档基线提交

审计意见书第 97 份确认 TASK-005D5 已通过，D4 证据提交后 HEAD 已回填为真实 SHA，且 D5 修正 commit `ff1f8dd` 仅修改 D4 证据文件，未触碰后端、前端、`.github`、`02_源码`。但审计同时指出，D5 任务单本身仍未跟踪。架构决策为：D5 任务单需要纳入文档基线，并与本次 D6 任务单和必要架构文档更新一起做 docs-only 提交。

### 23.1 基线纳入范围

1. `TASK-005D5_D4证据提交后HEAD占位修正_工程任务单.md` 必须入库。
2. `TASK-005D6_D5任务单文档基线提交_工程任务单.md` 必须入库。
3. 当前 Sprint 清单、款式利润报表模块设计、技术决策记录、架构师日志可纳入同一 docs-only 提交。
4. 工程师日志、审计记录、审计官日志如有本轮新增变化，可纳入同一 docs-only 提交。
5. 禁止修改后端、前端、`.github`、`02_源码`、迁移、API router 或 `app/main.py`。
6. 禁止新增或修改 TASK-005E/TASK-006 文件。
7. 禁止 push，不得配置 remote/origin。

### 23.2 放行条件

1. D5 任务单已纳入本地 git 跟踪。
2. D6 任务单已纳入本地 git 跟踪。
3. 本地 docs-only commit 已形成。
4. 禁改扫描确认未修改后端、前端、`.github`、`02_源码`、TASK-005E 和 TASK-006。
5. TASK-005D6 审计通过后，才允许下发 TASK-005E/API 权限与审计基线任务单。
6. TASK-006 继续保持阻塞。

## 24. TASK-005E API 权限与审计基线

TASK-005D6 已完成 D 系列利润服务与证据文档基线，允许进入 TASK-005E 任务单准备阶段。TASK-005E 的目标不是改利润公式，而是在既有利润快照服务之上建立生产级 API 壳，补齐鉴权、资源权限、安全审计、操作审计、统一错误信封和 PostgreSQL 验证门禁。

### 24.1 执行前置门禁

1. TASK-005E 任务单必须先交审计官审查。
2. 审计官确认任务单通过前，工程师不得实现 API。
3. TASK-005E 审计通过后，才允许修改后端 API 文件。
4. TASK-006 继续阻塞，不得与 TASK-005E 混入同一交付。

### 24.2 API 边界

1. API 前缀固定为 `/api/reports/style-profit`。
2. V1 暴露快照列表、快照详情、快照创建三个接口。
3. `/api/auth/actions?module=style_profit` 必须返回款式利润动作权限。
4. 创建快照 API 只允许接收 selector：`company/item_code/sales_order/from_date/to_date/revenue_mode/include_provisional_subcontract/formula_version/idempotency_key/work_order`。
5. API 禁止接收并信任客户端传入的收入、材料、工票、外发、Stock Ledger 明细。
6. 来源事实必须由服务端采集器读取 ERPNext/FastAPI 可信来源。
7. 本任务不改 D 系列利润公式、映射口径和事务保存点语义。

### 24.3 权限与审计

1. 新增 `style_profit:read` 和 `style_profit:snapshot_create` 权限动作。
2. 所有接口必须接入 `get_current_user`。
3. 所有接口必须先校验动作权限。
4. 所有接口必须按 `company + item_code` 校验资源权限。
5. ERPNext 权限源不可用必须 fail closed。
6. 401、403、503 权限相关拒绝必须写安全审计。
7. 创建快照成功/失败必须写操作审计。
8. 详情读取成功/失败必须写操作审计；审计失败不得返回详情。
9. 列表成功可不写操作审计，但失败必须形成安全或操作审计。

### 24.4 错误信封

1. 成功响应统一 `{code,message,data}`。
2. 分页响应放在 `data.items/total/page/page_size`。
3. 错误响应统一 `{code,message,data}`。
4. 禁止顶层 `detail`。
5. 禁止 `detail=str(exc)`。
6. 禁止 SQL、堆栈、Authorization、Cookie、Token、Secret、Password 进入响应或普通日志。

### 24.5 PostgreSQL 验证门禁

1. 必须新增款式利润 API PostgreSQL 集成测试文件。
2. destructive 测试必须同时要求 `POSTGRES_TEST_DSN` 和 `POSTGRES_TEST_ALLOW_DESTRUCTIVE=true`。
3. 数据库名必须匹配 `_test` 后缀或 `lingyi_test_` 前缀。
4. PostgreSQL 测试必须覆盖同 key 并发创建、幂等冲突、提交失败回滚、资源权限拒绝。
5. 本项目是本地项目，不等待 GitHub Hosted Runner。
6. 无本地 PostgreSQL DSN 时允许普通回归安全 skip，但进入前端联调或发布前必须补真实非 skip 证据。

### 24.6 放行条件

1. TASK-005E 任务单审计通过。
2. API 实现仅触碰白名单后端文件和测试文件。
3. 权限、资源权限、审计、错误信封测试全部覆盖。
4. 定向 pytest、全量 pytest、unittest、py_compile 通过。
5. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
6. TASK-005E 审计通过后，才允许评估 TASK-005F 前端或报表联调任务。

## 25. TASK-005E1 API 权限审计基线实现预派发

为避免 TASK-005E 任务单审计通过后再次等待架构派单，先输出 TASK-005E1 预派发任务单。该任务单不是开工许可，只有在 TASK-005E 待审计版任务单通过审计官审查后才允许执行。

### 25.1 前置门禁

1. TASK-005E 任务单必须先通过审计。
2. 若审计官要求修改 TASK-005E 任务单，则先整改 TASK-005E，不得执行 TASK-005E1。
3. TASK-005E1 执行前，工程师回报必须明确 `TASK-005E 任务单审计：通过`。
4. TASK-006 继续阻塞，不得与 API 实现混入同一交付。

### 25.2 实现范围

1. 新增 `app/routers/style_profit.py`。
2. 注册 `/api/reports/style-profit`。
3. 新增 `style_profit:read`、`style_profit:snapshot_create`。
4. 补齐 `/api/auth/actions?module=style_profit`。
5. 实现快照列表、详情、创建接口。
6. 新增服务端来源采集器，禁止信任客户端来源明细。
7. 补齐 401/403/503 安全审计。
8. 补齐创建和详情操作审计。
9. 补齐统一错误信封。
10. 补齐 PostgreSQL 安全门禁测试。

### 25.3 禁止范围

1. 不得修改利润公式。
2. 不得修改已通过审计的 D 系列计算口径。
3. 不得新增或修改 migrations。
4. 不得修改前端、`.github`、`02_源码`。
5. 不得新增或修改 TASK-006。

### 25.4 放行条件

1. TASK-005E 任务单审计通过。
2. TASK-005E1 定向 pytest、全量 pytest、unittest、py_compile 通过。
3. 权限、资源权限、安全审计、操作审计、错误信封和 PostgreSQL 门禁测试覆盖通过。
4. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
5. TASK-005E1 审计通过后，才允许评估 TASK-005F 前端联调或报表页面任务。

## 26. TASK-005E2 API 审计阻断整改

审计意见书第 99 份确认 TASK-005E1 API 主体方向成立，但存在 2 个 P1 和 2 个 P2 阻断项。TASK-005E2 必须先修复这些问题，审计通过前不得进入前端联调、导出、打印或 TASK-006。

### 26.1 来源采集 fail closed

1. `StyleProfitApiSourceCollector` 不得空实现成功。
2. 默认 collector 未配置真实来源时必须返回 `STYLE_PROFIT_SOURCE_UNAVAILABLE`。
3. 来源采集失败不得创建 snapshot/detail/source_map。
4. 服务端无法区分“真实无数据”和“采集未实现/不可用”时，必须按不可用处理。
5. 测试可注入 fake collector 返回可信来源，但生产默认路径不得依赖 fake 数据。
6. collector fail closed 必须写创建失败操作审计。
7. fake collector 返回空 rows 时同样不得创建零值快照。

### 26.2 权限矩阵冻结

| 角色 | style_profit:read | style_profit:snapshot_create |
| --- | --- | --- |
| System Manager | 是 | 是 |
| Finance Manager | 是 | 是 |
| Production Manager | 是 | 否 |
| Sales Manager | 是 | 否 |
| Viewer | 否 | 否 |

static 与 ERP role 聚合必须同时按上表修正。`Production Manager` 不得创建利润快照，`Finance Manager` 是创建快照的主业务角色。

### 26.3 鉴权顺序

1. 登录后必须先做动作权限校验。
2. 列表接口先校验 `style_profit:read`，再校验 `company/item_code` 必填，再做资源权限。
3. 创建接口在业务字段校验、客户端来源字段拒绝、idempotency 校验前，必须先校验 `style_profit:snapshot_create`。
4. 无动作权限用户即使 payload 非法，也必须返回 `403 + AUTH_FORBIDDEN` 并写安全审计。
5. 授权用户的非法 payload 才返回对应 400 业务错误。

### 26.4 操作审计覆盖

1. 列表接口在动作权限通过后的业务校验失败、数据库读取失败必须写操作失败审计。
2. 创建接口在动作权限通过后的客户端来源字段拒绝、参数非法、collector fail closed、service 业务异常必须写创建失败操作审计。
3. 必需操作审计写入失败必须返回 `AUDIT_WRITE_FAILED`。
4. 详情成功/失败操作审计不得回退。
5. 权限拒绝必须写安全审计。

### 26.5 放行条件

1. 第 99 份审计列出的 4 个问题全部关闭。
2. API 定向 pytest、全量 pytest、unittest、py_compile 通过。
3. PostgreSQL 测试安全门禁保留。
4. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
5. TASK-005E2 审计通过后，才允许评估 TASK-005E3 基线提交或 TASK-005F 前端联调任务。

## 27. TASK-005E3 详情接口鉴权前置整改

TASK-005E2 复审确认第 99 份的 4 个主阻断项基本闭环，但详情接口仍存在一个中危信息枚举风险：`GET /api/reports/style-profit/snapshots/{snapshot_id}` 先查询 snapshot，再校验 `style_profit:read` 动作权限。无读权限用户可通过“存在 ID 返回 403、不存在 ID 返回 404”的差异探测利润快照是否存在。

### 27.1 鉴权顺序冻结

详情接口顺序必须固定为：

1. 登录鉴权。
2. 立即校验 `style_profit:read` 动作权限。
3. 动作权限失败返回 `403 + AUTH_FORBIDDEN`，并写安全审计。
4. 动作权限通过后，才允许查询 snapshot。
5. snapshot 不存在返回 `404 + STYLE_PROFIT_NOT_FOUND`。
6. snapshot 存在后，按 `snapshot.company + snapshot.item_code` 做资源权限校验。
7. 资源权限通过后，才允许读取 detail/source_map。
8. 详情成功写操作审计。
9. 详情操作审计失败返回 `AUDIT_WRITE_FAILED`，不得返回详情。

### 27.2 存在性枚举防护

1. 无 `style_profit:read` 用户访问已存在 snapshot_id，必须返回 403。
2. 无 `style_profit:read` 用户访问不存在 snapshot_id，也必须返回 403。
3. 上述两个响应不得暴露 ID 是否存在。
4. 有 `style_profit:read` 用户访问不存在 snapshot_id，才允许返回 404。
5. 有 `style_profit:read` 但无资源权限用户访问存在 snapshot_id，返回 403。

### 27.3 放行条件

1. 详情接口动作权限已前置到 snapshot 查询前。
2. 无读权限存在/不存在 ID 均返回 403。
3. 授权用户不存在 ID 返回 404。
4. 安全审计、详情操作审计和 `AUDIT_WRITE_FAILED` 语义不回退。
5. 定向 pytest、全量 pytest、unittest、py_compile 通过。
6. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
7. TASK-005E3 审计通过后，才允许评估 TASK-005E4 本地基线提交或 TASK-005F 前端联调任务。

## 28. TASK-005E4 API 本地仓库基线提交

TASK-005E3 已关闭详情接口存在性枚举风险，款式利润 API 权限审计基线进入可收口状态。下一步不得直接进入前端联调或真实来源 adapter，必须先将 E~E3 的 API 代码、测试和文档纳入本地仓库基线。

### 28.1 基线纳入范围

1. `app/main.py` 中的 style_profit router 注册。
2. `app/routers/style_profit.py`。
3. `app/services/style_profit_api_source_collector.py`。
4. `app/core/permissions.py`、`app/core/error_codes.py`、`app/services/permission_service.py`。
5. `app/schemas/style_profit.py`。
6. `tests/test_style_profit_api*.py`。
7. TASK-005E~E4 任务文档。
8. 当前 Sprint 清单、模块设计、技术决策记录、架构师日志、工程师日志、审计记录和审计官日志。
9. TASK-005E4 本地仓库基线提交证据。

### 28.2 提交规则

1. 禁止 `git add .`、`git add -A`、`git add --all`。
2. 必须使用精确 `git add -- <文件路径>`。
3. 提交前必须跑 API 定向 pytest、全量 pytest、unittest、py_compile。
4. 提交前必须执行禁改扫描，确认未触碰前端、`.github`、`02_源码`、migrations 和 TASK-006。
5. commit message 建议为 `feat: add style profit API permission audit baseline`。
6. 禁止 push，禁止配置 remote/origin。

### 28.3 放行条件

1. 本地 commit 已形成，且内容仅限 E4 白名单。
2. E4 证据文件记录提交前后 HEAD、验证结果、暂存清单、白名单核对结论和禁改扫描结果。
3. TASK-005E4 审计通过后，才允许评估 TASK-005F 前端联调或真实来源 adapter 任务。
4. TASK-006 继续保持阻塞。

## 29. TASK-005F 真实服务端来源 Adapter

TASK-005E4 已关闭 API 权限审计基线并形成本地提交 `e8654f9`。当前剩余主风险是 `StyleProfitApiSourceCollector` 仍为 fail-closed 空采集器，无法生成真实利润快照。因此 TASK-005F 必须先实现服务端真实来源 Adapter，再评估前端创建入口。

### 29.1 来源采集边界

1. 收入来源从 ERPNext `Sales Invoice / Sales Invoice Item` 和 `Sales Order / Sales Order Item` 读取。
2. 实际收入优先使用已提交 `Sales Invoice`；无已提交 SI 时，按冻结口径允许使用已提交或已审批 `Sales Order` 作为订单收入。
3. 标准材料和标准工序来源从本地 active/default BOM 读取。
4. 实际材料成本来源从 ERPNext `Stock Ledger Entry` 读取，且必须受 BOM 允许物料编码约束。
5. `Purchase Receipt` 只作为采购参考和异常排查来源，不直接计入 `actual_material_cost`。
6. 工票来源从本地工票表读取，必须满足可信 `company + item_code + sales_order/work_order/job_card` 归属。
7. 外发来源从本地外发表读取，必须满足可信 `company + item_code + sales_order/work_order` 归属。
8. 缺关键归属字段的工票或外发来源不得仅凭 `sales_order` 纳入利润。

### 29.2 Fail Closed 原则

1. ERPNext 来源不可用、认证失败、超时或返回结构不可信时，必须返回 `STYLE_PROFIT_SOURCE_UNAVAILABLE`，不得创建 snapshot。
2. 无有效 SI/SO 收入来源时，必须返回 `STYLE_PROFIT_REVENUE_SOURCE_REQUIRED`。
3. 无 active/default BOM 时，必须返回 `STYLE_PROFIT_BOM_REQUIRED`。
4. 数据库读取失败必须返回 `DATABASE_READ_FAILED`。
5. 客户端提交收入、成本、工票、外发或 SLE 明细必须继续拒绝，不得绕过服务端采集。

### 29.3 幂等与审计

1. request_hash 必须包含真实来源输入。
2. 相同 `company + idempotency_key` 但来源数量、金额、状态、source_name 或 source_status 变化，必须返回幂等冲突。
3. 来源行排序必须稳定，避免 request_hash 漂移。
4. Collector fail closed 必须写创建失败操作审计。
5. 权限、资源权限、安全审计和操作审计沿用 TASK-005E4，不得回退。

### 29.4 放行条件

1. `StyleProfitApiSourceCollector` 不再是空采集器。
2. 可信 SI/SO + active/default BOM 存在时，可以生成非空来源快照。
3. ERPNext 来源不可用、无收入、无 BOM、SLE 不可用等场景均 fail closed。
4. 工票和外发缺关键归属字段时不被纳入利润。
5. 客户端来源 payload 仍被拒绝。
6. 定向 pytest、全量 pytest、unittest、py_compile 通过。
7. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
8. TASK-005F 审计通过后，才允许评估 TASK-005G 前端只读/创建入口联调。

## 30. TASK-005F1 SLE 归属与外发来源阻断整改

TASK-005F 审计发现：当前真实来源 Adapter 已开始读取 ERPNext 和本地事实，但 SLE 实际材料成本归属仍不安全，外发成本存在静默漏采集风险。TASK-005F1 必须先关闭这些阻断项。

### 30.1 SLE 入账双门槛

一条 `Stock Ledger Entry` 要计入 `actual_material_cost`，必须同时满足：

1. 材料范围合法：`item_code` 命中 active/default BOM 的 `allowed_material_item_codes`。
2. 归属桥接可信：能证明该 SLE 属于当前 `company + item_code + sales_order` 或当前 `company + item_code + work_order`。

BOM 白名单只能证明“材料属于该款 BOM 可用材料范围”，不能证明“该库存流水属于本销售订单或本生产工单”。因此仅命中 BOM 白名单不得计入利润。

### 30.2 SLE 状态 fail closed

1. Adapter 不得给 SLE 默认补 `docstatus=1`。
2. Adapter 不得给 SLE 默认补 `status=submitted`。
3. Adapter 不得给 SLE 默认补 `is_cancelled=false`。
4. SLE 缺 `docstatus/status/is_cancelled` 等可信状态字段时，必须 unresolved 或 fail closed。
5. `docstatus != 1`、取消态或未知态 SLE 不得计入利润。

### 30.3 允许的 SLE 归属桥接

按优先级允许以下桥接：

1. SLE 明确带 `sales_order` 或 `custom_ly_sales_order`，且等于 selector.sales_order。
2. SLE 明确带 `work_order` 或 `custom_ly_work_order`，且等于 selector.work_order，或可通过本地生产计划映射到 selector.sales_order + selector.item_code + selector.company。
3. SLE 的 `voucher_type/voucher_no` 指向 ERPNext Work Order，且可通过可信 Work Order adapter 或本地生产计划映射到当前销售订单、款式和公司。
4. SLE 明确带 `production_plan` 或 `custom_ly_production_plan`，且可映射到当前销售订单、款式和公司。
5. SLE 明确带 `job_card` 或 `custom_ly_job_card`，且可通过 Job Card/工票链路映射到当前 Work Order，再映射到当前销售订单、款式和公司。

桥接缺失或解析失败时，该 SLE 必须写 source_map unresolved，`include_in_profit=false`。

### 30.4 外发来源不得静默空返回

`load_subcontract_rows()` 不允许继续直接返回空列表。必须：

1. 读取外发候选事实。
2. 能证明 `company + item_code + sales_order/work_order` 归属时，按结算锁定金额优先、验货净额兜底采集。
3. 存在候选事实但缺可信桥接时，写 source_map unresolved，`include_in_profit=false`。
4. 如果现有外发表缺销售/生产桥接字段，必须在交付说明中明确，并以 unresolved 方式暴露，不得把外发成本伪装为 0。

### 30.5 放行条件

1. SLE 缺可信状态不得计入利润。
2. SLE 命中 BOM 物料但无销售/生产桥接不得计入利润。
3. 外发候选事实不得静默消失。
4. source_map 能追溯 SLE 和外发未纳入原因。
5. 补齐 SLE 状态、SLE 无桥接、外发候选未采集、ERPNext 结构异常反向测试。
6. 定向 pytest、全量 pytest、unittest、py_compile 通过。
7. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
8. TASK-005F1 审计通过后，才允许评估 TASK-005F2 或 TASK-005G。

## 31. TASK-005F2 外发利润归属桥接字段与补数

TASK-005F1 通过后，外发来源已可见且会 unresolved，但由于当前外发候选仍缺 `sales_order/work_order/production_plan_id` 可信桥接，外发成本不能宣称已完整计入利润。TASK-005F2 决定补字段和补数，而不是仅前端提示。

### 31.1 外发桥接字段

外发单主表必须补齐：

1. `sales_order`
2. `sales_order_item`
3. `production_plan_id`
4. `work_order`
5. `job_card`
6. `profit_scope_status`
7. `profit_scope_error_code`
8. `profit_scope_resolved_at`

外发验货事实必须复制外发单上的桥接字段，形成验货发生时的利润归属快照。后续外发单桥接变更不得自动重写历史验货事实，历史事实只能通过明确补数工具修复。

### 31.2 归属规则

1. 外发成本计入利润必须满足 `company + item_code + sales_order/work_order` 可信归属。
2. 传入 `production_plan_id` 时，必须校验生产计划存在，且 `company/item_code/bom_id` 与外发单一致。
3. 传入 `work_order` 时，必须通过 `ly_production_work_order_link` 反查到同一 `company/item_code/sales_order`。
4. 传入 `sales_order` 时，必须与生产计划或 Work Order 映射一致，不能只信任前端传入。
5. 缺桥接字段的新外发单允许创建，但必须 `profit_scope_status='unresolved'`，且不得计入利润。

### 31.3 历史补数

1. 历史补数必须支持 dry_run，且 dry_run 真正只读。
2. execute 只在唯一可信匹配时回填。
3. 多个生产计划匹配时保持 unresolved，原因 `SUBCONTRACT_SCOPE_AMBIGUOUS`。
4. 无匹配时保持 unresolved，原因 `SUBCONTRACT_SCOPE_UNTRUSTED`。
5. 数据库读取失败必须 `DATABASE_READ_FAILED`。
6. 写失败必须 rollback 并返回 `DATABASE_WRITE_FAILED`。

### 31.4 利润纳入口径

1. `profit_scope_status='ready'` 且匹配当前 `company + item_code + sales_order/work_order` 的外发验货事实才可计入利润。
2. inspection 有桥接快照字段时，以 inspection 快照字段为准。
3. inspection 缺桥接但 order ready 时，可读取 order 桥接，但 source_map 必须记录 `bridge_source='subcontract_order'`。
4. order 与 inspection 都缺桥接时，写 source_map unresolved，`include_in_profit=false`。
5. 结算锁定金额优先，未结算验货净额兜底受 `include_provisional_subcontract` 控制。

### 31.5 放行条件

1. 外发单和外发验货事实具备桥接字段。
2. 迁移可自举、幂等，不破坏历史数据。
3. 新建外发单缺桥接时 unresolved，不强行计入利润。
4. 新建外发单带合法生产计划/工单桥接时 ready。
5. 历史补数 dry_run 真正只读。
6. 历史补数 execute 只在唯一可信匹配时回填。
7. 款式利润只纳入 ready 且匹配当前 sales_order/work_order 的外发成本。
8. 定向 pytest、全量 pytest、unittest、py_compile 通过。
9. 禁改扫描确认未修改前端、`.github`、`02_源码` 和 TASK-006。
10. TASK-005F2 审计通过后，才允许评估 TASK-005F3 或 TASK-005G。

## 32. TASK-005F3 Work Order 严格匹配与补数审计整改

TASK-005F2 审计发现，外发利润计入在 `company + item_code + sales_order` 匹配后提前 mapped，导致同销售订单、同款式、不同 Work Order 的外发成本可能被计入当前 Work Order 快照。TASK-005F3 必须关闭该高危问题。

### 32.1 报表粒度

款式利润快照支持两个粒度：

1. 销售订单 + 款式汇总：selector 不传 `work_order`，汇总同一 `company + sales_order + item_code` 下的 ready 外发成本。
2. Work Order 快照：selector 传 `work_order`，必须严格限定同一 `company + sales_order + item_code + work_order`。

只要 selector 带 `work_order`，外发成本必须匹配该 Work Order。不得仅凭同销售订单、同款式提前纳入。

### 32.2 Work Order 匹配规则

1. 先校验 `profit_scope_status == ready`。
2. 再校验 `company`。
3. 再校验 `item_code`。
4. 再校验 `sales_order`。
5. 如果 selector.work_order 非空，必须继续校验 `work_order` 或可信反查链路。
6. `row.work_order` 非空但不等于 selector.work_order 时，不得计入利润。
7. `row.work_order` 为空且无法通过 `production_plan_id/job_card/subcontract_order` 反查时，必须 unresolved。

### 32.3 外发状态和期间门禁

1. 外发 `profit_scope_status` 缺失、空字符串、空白字符串、null 或未知值，一律 fail closed。
2. 只有 `profit_scope_status='ready'` 才允许进入范围匹配。
3. 外发来源期间过滤必须使用业务验货时间 `inspected_at`。
4. `inspected_at` 为空时不得用 `created_at` 静默兜底，必须 unresolved。

### 32.4 补数审计

1. 历史补数 execute 必须写操作审计。
2. 审计内容必须包含 operator、筛选范围、扫描数量、ready/unresolved/ambiguous/untrusted 数量和实际 updated_count。
3. 操作审计写失败必须 rollback，并返回/抛出 `AUDIT_WRITE_FAILED`。
4. dry_run 不得产生业务更新，`updated_count=0`。

### 32.5 放行条件

1. selector 带 work_order 时，外发成本严格匹配该 work_order。
2. 同 SO、同款式、错 Work Order 的外发成本不得计入当前 Work Order 快照。
3. selector 不带 work_order 时，允许按 SO + 款式汇总 ready 外发成本。
4. 外发行缺 `profit_scope_status` 不得计入利润。
5. 外发期间过滤使用 `inspected_at`。
6. 补数 execute 写操作审计，审计失败 rollback。
7. 定向 pytest、全量 pytest、unittest、py_compile 通过。
8. TASK-005F3 审计通过后，才允许评估 TASK-005F4 或 TASK-005G。

## 33. TASK-005F4 外发来源查询下推与 PostgreSQL 证据

TASK-005F3 通过后，审计保留的最优先风险是外发来源仍先按 `company/item_code` 拉取候选，再在 Python 层按 `inspected_at` 过滤期间。数据量上来后会形成利润快照大查询。TASK-005F4 必须把期间过滤下推到数据库，并补 PostgreSQL 非 skip 证据。

### 33.1 查询下推口径

1. 外发来源正常查询必须使用 `LySubcontractInspection.inspected_at` 过滤期间。
2. `from_date` 非空时，数据库查询必须包含 `inspected_at >= from_date`。
3. `to_date` 非空时，数据库查询必须包含 `inspected_at <= to_date` 的日期结束边界。
4. `inspected_at IS NULL` 不得进入正常成本候选查询。
5. `created_at` 不得作为默认 fallback。
6. Python 层只允许做最终安全校验和 source_map 解释，不得负责主要期间过滤。

### 33.2 缺 inspected_at 诊断

缺 `inspected_at` 的外发候选如需诊断，必须单独查询：

1. 限制 `company + item_code`。
2. 设置最大返回数量。
3. 超过限制时写聚合诊断项。
4. 不得计入利润。
5. source_map 原因码为 `SUBCONTRACT_INSPECTED_AT_REQUIRED`。

### 33.3 PostgreSQL 证据

1. 新增 `tests/test_style_profit_subcontract_postgresql.py`。
2. 至少 4 条 `@pytest.mark.postgresql` 非 skip 用例。
3. 沿用 `POSTGRES_TEST_DSN`、`POSTGRES_TEST_ALLOW_DESTRUCTIVE=true` 和测试库名安全门禁。
4. JUnit 硬门禁必须能识别全 skip 并失败。
5. 无 DSN 本地路径可以 skip，但交付证据不得伪装非 skip 已通过。

### 33.4 放行条件

1. 外发来源正常查询把 `inspected_at` 期间过滤下推到数据库。
2. 缺 `inspected_at` 不计入利润，诊断集合有限制。
3. Work Order 严格匹配不回退。
4. PostgreSQL marker 测试至少 4 条。
5. CI/JUnit 硬门禁能识别全 skip 并失败。
6. 定向 pytest、全量 pytest、unittest、py_compile 通过。
7. TASK-005F4 审计通过后，才允许评估 TASK-005F5 本地基线提交或 TASK-005G。

## 34. TASK-005F5 PostgreSQL 门禁目标恢复与双 JUnit 断言

TASK-005F4 审计发现，`scripts/run_postgresql_ci_gate.sh` 被改为只执行款式利润 PostgreSQL 测试，但该脚本仍由 `subcontract-postgresql-gate` required check 调用，导致 TASK-002H 外发结算并发门禁被静默替换。TASK-005F5 必须修复该门禁漂移。

### 34.1 门禁组合口径

保留单一脚本，但脚本必须同时运行两组 PostgreSQL 测试，并分别生成/断言 JUnit：

1. TASK-002H 外发结算 PostgreSQL 门禁：`tests/test_subcontract_settlement_postgresql.py`，必须保留 4 条非 skip。
2. TASK-005F4 款式利润外发 PostgreSQL 门禁：`tests/test_style_profit_subcontract_postgresql.py`，必须保留 4 条非 skip。

不得用新测试替代旧门禁，不得只做总量断言。

### 34.2 脚本要求

1. `run_postgresql_ci_gate.sh` 必须先检查 `POSTGRES_TEST_DSN`、`POSTGRES_TEST_ALLOW_DESTRUCTIVE=true` 和 `.venv/bin/python`。
2. 必须生成 `.pytest-postgresql-subcontract-settlement.xml`。
3. 必须生成 `.pytest-postgresql-style-profit-subcontract.xml`。
4. 两份 JUnit 必须分别断言 `tests=4, skipped=0`。
5. 任一组失败、skip 或测试数量不符，脚本必须失败。

### 34.3 目标一致性测试

`tests/test_ci_postgresql_gate.py` 必须断言脚本同时包含：

1. `tests/test_subcontract_settlement_postgresql.py`
2. `tests/test_style_profit_subcontract_postgresql.py`
3. 两份不同 JUnit 文件名
4. 两组 `--expected-tests 4 --expected-skipped 0`

并增加反向测试，证明移除 settlement 目标会失败。

### 34.4 放行条件

1. settlement PG gate 被恢复并保留。
2. style-profit PG gate 被保留。
3. 两组 JUnit 分别断言。
4. CI gate 目标一致性测试能防止门禁目标漂移。
5. README 和证据文件不伪装非 skip 结果。
6. 定向 pytest、全量 pytest、unittest、py_compile 通过。
7. TASK-005F5 审计通过后，才允许评估 TASK-005F6 或 TASK-005G。

## 35. TASK-005F6 Workflow 双 JUnit 上传与 PostgreSQL 证据回填

TASK-005F5 已恢复双 PostgreSQL 门禁脚本，但审计发现 workflow 仍上传旧 `.pytest-postgresql.xml`，而脚本实际生成 `.pytest-postgresql-subcontract-settlement.xml` 和 `.pytest-postgresql-style-profit-subcontract.xml`。如果不修复，Hosted Runner 或本地 workflow 证据会缺失真实 JUnit artifact。

### 35.1 Workflow 证据口径

1. `.github/workflows/backend-postgresql.yml` 必须上传 settlement JUnit。
2. `.github/workflows/backend-postgresql.yml` 必须上传 style-profit JUnit。
3. 旧 `.pytest-postgresql.xml` 不再作为 PostgreSQL hard gate 审计证据。
4. 推荐使用两个 artifact 名称：
   - `postgresql-settlement-junit`
   - `postgresql-style-profit-junit`
5. workflow 仍必须调用 `bash scripts/run_postgresql_ci_gate.sh`。
6. workflow 仍必须保留 PostgreSQL service。

### 35.2 静态防回退口径

`tests/test_ci_postgresql_gate.py` 必须同时校验脚本目标和 workflow artifact 路径，至少覆盖：

1. settlement 测试目标存在。
2. style-profit 测试目标存在。
3. settlement JUnit 文件名存在。
4. style-profit JUnit 文件名存在。
5. workflow 不得只上传旧 `.pytest-postgresql.xml`。
6. workflow artifact 名称或 path 能区分 settlement 与 style-profit。
7. 反向测试能证明 artifact path 回退会失败。

### 35.3 非 skip 证据口径

1. 如果当前有 `POSTGRES_TEST_DSN`，必须实跑 `scripts/run_postgresql_ci_gate.sh`，并回填两份 JUnit 的 `tests=4, skipped=0, failures=0, errors=0`。
2. 如果当前没有 `POSTGRES_TEST_DSN`，证据文件必须保持 pending，不得伪装为非 skip 通过。
3. 所有 DSN、账号、密码、token 必须脱敏，不得写入文档或日志。

### 35.4 放行条件

1. workflow 双 JUnit 上传路径修复。
2. 静态一致性测试覆盖 workflow artifact path。
3. README 说明双 JUnit artifact 和旧文件废弃。
4. 非 skip 证据真实回填，或明确 pending。
5. 定向 pytest、全量 pytest、unittest、py_compile 通过。
6. 禁改扫描确认未修改前端、`02_源码`、迁移、业务代码和 TASK-006。
7. TASK-005F6 审计通过后，才允许评估 TASK-005G。

## 36. TASK-005F7 PostgreSQL 真实非 Skip 证据闭环

TASK-005F6 已修复 workflow 双 JUnit 上传，但审计确认真实 PostgreSQL 非 skip 证据仍未闭环。TASK-005F7 只做证据闭环，不做功能开发。

### 36.1 真实 PostgreSQL 证据口径

1. 必须使用一次性 PostgreSQL 测试库。
2. 数据库名必须匹配 `lingyi_test_*` 或 `*_test`。
3. 必须显式设置 `POSTGRES_TEST_ALLOW_DESTRUCTIVE=true`。
4. 必须执行 `scripts/run_postgresql_ci_gate.sh`。
5. settlement JUnit 必须为 `tests=4, skipped=0, failures=0, errors=0`。
6. style-profit JUnit 必须为 `tests=4, skipped=0, failures=0, errors=0`。
7. DSN、密码、token、Cookie、Authorization、私钥必须脱敏。
8. 如果执行失败，只能记录失败证据并停止，不得在本任务内修改业务代码。

### 36.2 禁改边界

TASK-005F7 禁止修改：

1. 前端。
2. `02_源码`。
3. 后端业务代码。
4. 后端测试代码。
5. PostgreSQL gate 脚本。
6. 迁移文件。
7. `.github`。
8. TASK-006 文件。

### 36.3 放行条件

1. `TASK-005F7_PostgreSQL真实非Skip证据.md` 写明执行时间、执行库、脱敏 DSN、命令和两份 JUnit 指标。
2. settlement JUnit 非 skip 通过。
3. style-profit JUnit 非 skip 通过。
4. 敏感信息扫描通过。
5. 定向 pytest、全量 pytest、unittest、py_compile 通过。
6. 禁改扫描通过。
7. TASK-005F7 审计通过后，才允许评估 TASK-005G。

## 37. TASK-005F8 PostgreSQL 外发 Schema 基线与双门禁复跑

TASK-005F7 首次真实 PostgreSQL non-skip 执行失败，settlement 集成测试在 seed 阶段报 `ly_subcontract_order.sales_order` 缺列。该失败说明 PostgreSQL 测试迁移链未纳入 TASK-005F2 外发利润桥接字段。

### 37.1 外发基础 Schema 决策

TASK-005F2 外发利润桥接字段从本版本起视为外发模块基础 schema。所有外发 PostgreSQL 集成测试都必须纳入这些字段，不能只在款式利润测试中可见。

基础字段至少包括：

1. `sales_order`
2. `sales_order_item`
3. `production_plan_id`
4. `work_order`
5. `job_card`
6. `profit_scope_status`
7. `profit_scope_error_code`
8. `profit_scope_resolved_at`

适用表：

1. `ly_subcontract_order`
2. `ly_subcontract_inspection`

### 37.2 PostgreSQL 测试 Schema 口径

1. 优先执行完整 Alembic head。
2. 如暂不能执行完整 Alembic head，必须显式纳入 `task_005f2_subcontract_profit_scope_bridge.py`。
3. 禁止使用 `metadata.create_all()` 作为已存在表的迁移替代。
4. seed 数据前必须执行 schema assertion。
5. schema assertion 必须覆盖 `sales_order/work_order/profit_scope_status`。

### 37.3 放行条件

1. settlement PostgreSQL fixture schema 与当前外发基础 schema 对齐。
2. settlement JUnit 为 `tests=4, skipped=0, failures=0, errors=0`。
3. style-profit JUnit 为 `tests=4, skipped=0, failures=0, errors=0`。
4. 证据文件记录真实 PostgreSQL non-skip 结果。
5. 禁改扫描确认未进入前端、`.github`、`02_源码`、TASK-006。
6. TASK-005F8 审计通过后，才允许评估 TASK-005G。

## 38. TASK-005F9 款式利润 F 阶段本地基线提交

TASK-005F8 已关闭真实 PostgreSQL 门禁问题，但审计指出关键迁移、测试、防回退和证据文件尚未形成本地基线提交。TASK-005F9 只做白名单 staging、验证和本地 commit，不做新功能。

### 38.1 白名单提交口径

本地基线提交只允许纳入：

1. TASK-005F~F8 后端代码。
2. TASK-005F2 外发利润桥接迁移。
3. PostgreSQL 双门禁脚本、workflow 和 README。
4. TASK-005F~F8 测试。
5. TASK-005F~F9 任务单和证据。
6. 架构文档、ADR、Sprint 清单、审计记录和会话日志。

禁止纳入：

1. 历史未跟踪大目录。
2. `02_源码`。
3. `06_前端`。
4. `.pytest-postgresql-*.xml` 运行时产物。
5. TASK-006 文件。

### 38.2 提交前门禁

1. 禁止 `git add .`。
2. 禁止 `git add -A`。
3. 必须用显式文件白名单 `git add -- <paths>`。
4. 必须用 `git diff --cached --name-only` 复核 staged 文件。
5. 必须确认 staged 文件只包含白名单。
6. 必须复跑定向 pytest、全量 pytest、unittest、py_compile。

### 38.3 放行条件

1. 本地 commit 成功。
2. commit 范围只包含白名单。
3. commit 不包含 `.pytest-postgresql-*.xml`。
4. commit 不包含前端、`02_源码` 或 TASK-006。
5. 证据文件记录提交前后 HEAD、staged 清单、排除项和测试结果。
6. TASK-005F9 审计通过后，才允许评估 TASK-005G。

## 39. TASK-005F10 F9 证据 Docs-Only 补提交

TASK-005F9 审计已通过，但 `TASK-005F9_本地基线提交证据.md` 仍未进入本地仓库。TASK-005F10 只做 docs-only 补提交，不允许修改后端、前端、workflow、迁移、测试或运行时产物。

### 39.1 Docs-Only 提交口径

允许提交：

1. `TASK-005F9_本地基线提交证据.md`
2. `TASK-005F10_F9证据DocsOnly补提交_工程任务单.md`
3. 架构设计、ADR、Sprint 清单、架构师日志、审计记录和审计官日志

禁止提交：

1. `.github/**`
2. `07_后端/**`
3. `06_前端/**`
4. `02_源码/**`
5. `.pytest-postgresql-*.xml`
6. TASK-006 文件
7. 历史未跟踪大目录

### 39.2 Staging 安全口径

1. 禁止 `git add .`。
2. 禁止 `git add -A`。
3. 如存在已 staged 的非白名单文件，必须先非破坏性取消 staging。
4. 只允许显式 `git add -- <白名单文件>`。
5. 提交前必须执行 `git diff --cached --name-only` 并确认只包含白名单。

### 39.3 放行条件

1. F9 证据文件进入 docs-only commit。
2. commit 不包含后端、前端、workflow、运行时 JUnit、历史大目录或 TASK-006。
3. 提交后记录 HEAD 和文件清单。
4. TASK-005F10 审计通过后，才允许评估 TASK-005G。

## 40. TASK-005F11 F9 证据格式修正

TASK-005F10 已把 F9 证据纳入本地仓库，但审计发现证据正文有孤立 `d` 行、命令名称缺失、JUnit 文件名缺失、结论字段缺失和乱码残留。TASK-005F11 只修正文档内容，不修改代码。

### 40.1 修复范围

必须修复：

1. 删除孤立 `d` 行。
2. 删除乱码字符。
3. 补全回归测试命令名。
4. 补全 PostgreSQL 双 JUnit 文件名。
5. 补全 `.pytest-postgresql-*.xml` 未提交说明。
6. 补全 `git add . / git add -A` 未使用说明。
7. 补全结论字段。

### 40.2 禁改边界

TASK-005F11 只能修改 F9/F11 证据和架构/审计文档，禁止提交：

1. `.github/**`
2. `07_后端/**`
3. `06_前端/**`
4. `02_源码/**`
5. `.pytest-postgresql-*.xml`
6. TASK-006 文件

### 40.3 放行条件

1. 格式防回退扫描无输出。
2. docs-only commit 成功。
3. commit 不包含后端、前端、workflow、运行时 JUnit、历史大目录或 TASK-006。
4. TASK-005F11 审计通过后，才允许评估 TASK-005G。

## 41. TASK-005G 款式利润前端只读联调

TASK-005F11 审计意见书第 114 份已通过，F9/F10/F11 证据链闭环。TASK-005G 进入前端只读页面联调阶段，只接入已通过后端审计的利润快照列表与详情 GET 接口。

### 41.1 范围

允许实现：

1. `src/api/style_profit.ts` 只读 API 封装。
2. `StyleProfitSnapshotList.vue` 利润快照列表页。
3. `StyleProfitSnapshotDetail.vue` 利润快照详情页。
4. `/reports/style-profit` 和 `/reports/style-profit/detail` 两个只读路由。
5. `check-style-profit-contracts.mjs` 与反向测试。
6. `npm run verify` 纳入 style-profit 前端契约门禁。

禁止实现：

1. 前端创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. `style_profit:snapshot_create` 按钮权限。
4. `idempotency_key` 出现在前端 style-profit 业务文件。
5. ERPNext `/api/resource` 直连。
6. TASK-006。

### 41.2 只读页面契约

列表页必须要求 `company` 与 `item_code` 后才允许查询，避免前端触发全量利润快照扫描。详情页必须从列表行 `id` 跳转，进入页面后先加载 `style_profit` 模块动作权限，无 `style_profit:read` 时不得调用详情接口。

### 41.3 权限与审计口径

前端只做按钮/入口层控制，后端仍是权限权威。前端不得根据角色名称判断权限，只能读取 `/api/auth/actions?module=style_profit` 聚合出的 `button_permissions.read`。

### 41.4 门禁

TASK-005G 必须新增 style-profit 前端契约脚本，覆盖以下回潮：裸 `fetch()`、ERPNext 直连、创建快照 POST、`snapshot_create`、`style_profit:snapshot_create`、`idempotency_key`、创建/生成/重算文案、缺少 `loadModuleActions('style_profit')`、未使用统一 `request()`。

### 41.5 放行条件

1. `npm run check:style-profit-contracts` 通过。
2. `npm run test:style-profit-contracts` 通过且包含反向测试。
3. `npm run verify` 通过。
4. `npm audit --audit-level=high` 通过。
5. 后端 style-profit API 定向回归和全量回归通过。
6. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
7. 审计官复审通过后，才允许评估 TASK-005H 或 TASK-006。

## 42. TASK-005H 款式利润全局只读边界门禁收口

TASK-005G 审计意见书第 115 份已通过，款式利润前端只读列表/详情联调完成。审计提示后续需防止全局导航、公共按钮、其他页面或 store 绕过 style-profit 只读边界，因此 TASK-005H 只做前端只读边界二次收口。

### 42.1 范围

允许实现：

1. 扩大 `check-style-profit-contracts.mjs` 扫描范围到 `src/api/**`、`src/views/**`、`src/router/**`、`src/stores/**`、`src/App.vue`、`src/main.ts` 和可选 `src/components/**`。
2. 补 `test-style-profit-contracts.mjs` 反向测试，覆盖 App、其他页面、store、router 绕过场景。
3. 详情页 `canRead` 前置阻断反向测试。
4. 普通财务页面内部审计字段展示治理。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. `style_profit:snapshot_create` 或 `snapshot_create` 前端映射。
4. TASK-006。

### 42.2 全局门禁口径

style-profit 只读边界不只属于 `src/views/style_profit/**`。任何全局导航、App、其他页面、公共 store 或未来公共组件，只要出现创建利润快照入口或写权限动作，都必须被契约脚本拦截。

### 42.3 内部字段展示口径

`request_hash`、`idempotent_replay` 等技术字段不得作为财务业务摘要主字段展示。若保留，必须移入“审计信息”折叠区并标注“仅供审计复核”；否则从普通详情页移除。

### 42.4 放行条件

1. `npm run check:style-profit-contracts` 通过。
2. `npm run test:style-profit-contracts` 通过，且包含全局绕过反向测试。
3. `npm run verify` 通过。
4. 后端 style-profit API 定向回归通过。
5. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
6. 审计官复审通过后，才允许评估 TASK-005I 或 TASK-006。

## 43. TASK-005I 款式利润中文泛化写入口门禁

TASK-005H 审计意见书第 116 份已通过，但审计提示当前只读契约扫描仍依赖路径、`style_profit/style-profit` 标识和固定创建类文案。TASK-005I 继续收口中文泛化写入口，防止通过“款式利润计算”“利润报表重算”“毛利核算”等文案绕过只读边界。

### 43.1 范围

允许实现：

1. 在 `check-style-profit-contracts.mjs` 中新增中文语义门禁矩阵。
2. 在 `test-style-profit-contracts.mjs` 中新增中文泛化写入口反向测试。
3. 如现有只读文案误触门禁，可做最小文案修正。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. `style_profit:snapshot_create`、`snapshot_create`、`idempotency_key`。
4. TASK-006。

### 43.2 中文语义口径

门禁不得粗暴禁止全部“款式利润/利润快照/利润报表”字样。只读词如“款式利润报表”“利润快照列表”“查看详情”“查询”“利润明细”“来源追溯”必须允许；写动作词如“新建、创建、生成、重算、重新计算、计算、核算、提交、保存、一键生成”与领域词近距离组合时必须拦截。

### 43.3 放行条件

1. 中文泛化写入口反向测试通过。
2. 合法只读文案 fixture 通过。
3. `npm run verify` 通过。
4. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
5. 审计官复审通过后，才允许评估 TASK-005J 或 TASK-006。

## 44. TASK-005I1 中文语义白名单绕过整改

TASK-005I 审计意见书第 117 份有条件通过，但发现 `shouldIgnoreSemanticMatch()` 按 substring 豁免整段语义匹配，导致“白名单只读词 + 写动作词”组合可绕过门禁。TASK-005I1 只修复该高危绕过点。

### 44.1 范围

允许实现：

1. 修复 `shouldIgnoreSemanticMatch()`，禁止 substring 直接豁免整段命中。
2. 补充 `款式利润报表计算`、`利润快照列表生成`、`利润金额重新计算` 三条反向测试。
3. 保持合法只读文案 fixture 通过。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。

### 44.2 白名单决策

只读白名单只能保护纯只读短语本身，不能保护包含写动作词的更长表达。只要匹配窗口内出现写动作词，应优先判定为违规。

### 44.3 放行条件

1. 三条审计复现用例均失败。
2. TASK-005I 原反向测试未删除、未降级。
3. 合法只读文案 fixture 通过。
4. `npm run verify` 通过。
5. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
6. 审计官复审通过后，才允许评估 TASK-005J。

## 45. TASK-005J 款式利润只读说明文案防误杀与门禁基线

TASK-005I1 审计意见书第 118 份已通过，中文语义白名单绕过已关闭。审计提示语义门禁收紧后可能误杀“利润计算说明”“利润率计算规则”等纯说明文案。TASK-005J 用成功 fixture 固化合法只读说明文案，避免后续通过恢复 substring 白名单来解决误杀。

### 45.1 范围

允许实现：

1. 新增合法只读说明文案成功 fixture。
2. 新增说明文案出现在按钮/菜单等交互入口时失败的反向测试。
3. 保持 TASK-005H/I/I1 既有门禁不回退。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。

### 45.2 只读说明文案口径

“利润计算说明”“利润率计算规则”“实际成本计算口径说明”等纯说明文案可在帮助文本、提示文本、折叠说明区中出现，但不得出现在按钮、菜单、路由、函数名或可触发操作的变量名中。

### 45.3 放行条件

1. 合法说明文案 fixture 通过。
2. 交互入口反向测试失败。
3. 不恢复 substring 白名单。
4. `npm run verify` 通过。
5. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
6. 审计官复审通过后，才允许评估 TASK-005K。

## 46. TASK-005J1 多行交互上下文门禁整改

TASK-005J 审计意见书第 119 份有条件通过，但发现只读说明文案交互上下文识别只看文案所在行，导致多行按钮、菜单或 action 配置绕过门禁。TASK-005J1 只修复该高危绕过点。

### 46.1 范围

允许实现：

1. 扩展交互上下文检测到前后窗口或未闭合标签块。
2. 补多行 `<el-button>`、`<button>`、`<el-menu-item @click>`、`onClick` action 配置和多行路由反向测试。
3. 保持非交互说明文案通过。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。

### 46.2 交互上下文口径

交互上下文不能只看单行。只要说明文案位于未闭合的按钮、菜单、点击标签块或 action 配置窗口内，就必须判定为交互入口并失败。

### 46.3 放行条件

1. 多行交互上下文反向测试通过。
2. 非交互说明文案成功 fixture 继续通过。
3. TASK-005H/I/I1/J 既有测试未删除、未降级。
4. `npm run verify` 通过。
5. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
6. 审计官复审通过后，才允许评估 TASK-005K。

## 47. TASK-005J2 Action 对象级交互上下文门禁整改

TASK-005J1 审计意见书第 120 份有条件通过，但发现长多行 action 配置中 `label` 与 `onClick` 间隔超过固定窗口半径时仍会绕过。TASK-005J2 要求 action 配置检测升级为对象级/block 级检测。

### 47.1 范围

允许实现：

1. 新增 action 对象级检测。
2. 补长间隔 `label + onClick/handler/command/onSelect` 反向测试。
3. 补合法 action 成功 fixture。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。

### 47.2 对象级检测口径

同一个 action 对象或菜单节点内，只要出现利润说明/利润领域 label 与交互字段 `onClick/handler/action/command/onSelect`，不得因字段间隔超过 300 字符而放行。固定窗口可以保留为辅助，但不能作为唯一判断。

### 47.3 放行条件

1. 长 action 配置反向测试失败。
2. 合法 action 成功 fixture 通过。
3. TASK-005H/I/I1/J/J1 既有测试未删除、未降级。
4. `npm run verify` 通过。
5. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
6. 审计官复审通过后，才允许评估 TASK-005K。

## 48. TASK-005J3 Action 祖先对象链门禁整改

TASK-005J2 审计意见书第 121 份有条件通过，但发现父对象存在 `onClick`、子对象 `meta` 内存在 `label: '利润计算说明'` 时仍可绕过。TASK-005J3 要求 action 配置检测升级为祖先对象链检测。

### 48.1 范围

允许实现：

1. 新增 action 祖先对象链检测。
2. 补 `父 onClick + 子 meta label` 反向测试。
3. 补 `handler/command/onSelect + props/extra/payload label` 反向测试。
4. 保留合法只读说明对象成功 fixture。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。

### 48.2 祖先链检测口径

任一子对象、`meta`、`props`、`extra`、`payload`、`children` 节点内出现利润说明类 `label/title/text/description` 时，必须向上检查全部祖先对象。只要祖先对象存在 `onClick/handler/action/command/onSelect/onCommand/callback/execute` 等交互字段，就按交互入口失败，不得按最小子对象直接放行。

### 48.3 放行条件

1. 嵌套 metadata 反向测试失败。
2. 祖先交互字段与后代利润说明文案间隔超过 1000 字符仍失败。
3. 合法只读说明对象成功 fixture 继续通过。
4. TASK-005H/I/I1/J/J1/J2 既有测试未删除、未降级。
5. `npm run verify` 通过。
6. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
7. 审计官复审通过后，才允许评估 TASK-005K。

## 49. TASK-005J4 JSON 风格引号键与真实长距离 Fixture 门禁整改

TASK-005J3 审计意见书第 122 份有条件通过，但发现 JSON 风格引号键仍可绕过：父对象使用 `"onClick": openHelp`，子对象使用 `"label": "利润计算说明"` 时仍会通过。另发现现有 `repeat(1200)` fixture 写入的是源码文本，不是真实 1200 字符距离。

### 49.1 范围

允许实现：

1. 新增裸键、单引号键、双引号键统一识别。
2. 补 JSON 风格引号键祖先链反向测试。
3. 补真实 1200 字符距离 fixture 与距离断言。
4. 保留合法只读说明对象成功 fixture。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。

### 49.2 引号键检测口径

Action 门禁必须同时识别裸键、单引号键、双引号键以及混合风格键。祖先链检测不得因 `"onClick"`、`'handler'`、`"meta"`、`"label"` 等 JSON 风格键而失效。

### 49.3 真实长距离 Fixture 口径

长距离测试必须向临时 fixture 写入实际 1200 个以上字符，并断言交互字段与利润说明文案之间真实距离超过 1200。禁止使用源码文本 `'x'.repeat(1200)` 冒充长距离覆盖。

### 49.4 放行条件

1. JSON 风格双引号键反向测试失败。
2. 单引号键与混合键反向测试失败。
3. 真实长距离 fixture 距离断言通过。
4. 合法只读说明对象成功 fixture 继续通过。
5. TASK-005H/I/I1/J/J1/J2/J3 既有测试未删除、未降级。
6. `npm run verify` 通过。
7. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
8. 审计官复审通过后，才允许评估 TASK-005K。

## 50. TASK-005J5 对象方法简写交互字段门禁整改

TASK-005J4 审计意见书第 123 份有条件通过，但发现对象方法简写仍可绕过：`label: '利润计算说明'` 与 `onClick() { openHelp() }` 在真实长距离下可通过门禁。TASK-005J5 要求交互字段识别同时覆盖属性赋值和对象方法简写。

### 50.1 范围

允许实现：

1. 新增 `onClick() {}`、`handler() {}`、`submit() {}` 等对象方法简写识别。
2. 补引号方法名与 async 方法简写反向测试。
3. 保留真实长距离 fixture 和距离断言。
4. 保留合法只读说明对象成功 fixture。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。

### 50.2 方法简写检测口径

Action 门禁必须同时识别 `key: value` 和 `key() {}` 两类对象成员。`onClick() {}`、`handler() {}`、`submit() {}`、`execute() {}`、`onSelect() {}` 以及引号方法名 `'onClick'() {}`、`"onClick"() {}` 都应被视为交互字段。

### 50.3 放行条件

1. 对象方法简写反向测试失败。
2. 引号方法名反向测试失败。
3. async 方法简写反向测试失败。
4. 真实长距离 fixture 距离断言通过。
5. 合法只读说明对象成功 fixture 继续通过。
6. TASK-005H/I/I1/J/J1/J2/J3/J4 既有测试未删除、未降级。
7. `npm run verify` 通过。
8. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
9. 审计官复审通过后，才允许评估 TASK-005K。

## 51. TASK-005J6 计算属性键交互字段门禁整改

TASK-005J5 审计意见书第 124 份有条件通过，但发现计算属性键仍可绕过：`label: '利润计算说明'` 与 `['onClick']: openHelp` 在真实长距离下可通过门禁。TASK-005J6 要求交互字段识别同时覆盖普通属性、引号属性、方法简写和计算属性键。

### 51.1 范围

允许实现：

1. 新增 `['onClick']:`、`["handler"]()`、`['submit']()` 等计算属性键识别。
2. 补计算属性 label/meta 反向测试。
3. 保留真实长距离 fixture 和距离断言。
4. 保留合法只读说明对象成功 fixture。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。

### 51.2 计算属性键检测口径

Action 门禁必须识别 `['onClick']`、`["handler"]`、`['submit']`、`['execute']`、`['onSelect']` 等计算属性交互键，也必须识别 `['label']`、`["meta"]`、`["description"]` 等计算属性利润说明键。计算属性键必须接入祖先对象链检测。

### 51.3 放行条件

1. 计算属性交互键反向测试失败。
2. 计算属性利润说明字段反向测试失败。
3. 真实长距离 fixture 距离断言通过。
4. 合法只读说明对象成功 fixture 继续通过。
5. TASK-005H/I/I1/J/J1/J2/J3/J4/J5 既有测试未删除、未降级。
6. `npm run verify` 通过。
7. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
8. 审计官复审通过后，才允许评估 TASK-005K。

## 52. TASK-005J7 非字面量计算属性 Action 键禁用门禁

TASK-005J6 审计意见书第 125 份通过，但保留一个正则静态门禁天然边界：非字面量计算属性键如 `[ACTION_KEY]: openHelp`、`[actionMap.onClick]()` 无法可靠识别。TASK-005J7 决策为短期禁止此类写法，若未来需要支持再单独升级 TypeScript/Babel AST 解析。

### 52.1 范围

允许实现：

1. 新增非字面量 computed action key 禁用规则。
2. 补 `[ACTION_KEY]`、`[actionMap.onClick]`、`[getActionKey()]` 反向测试。
3. 保留字符串字面量 computed key 成功/失败既有规则。
4. 可在前端 README 中补 style-profit 编码约定。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。
4. 在本任务中引入 AST 解析大改。

### 52.2 禁用口径

style-profit 业务面禁止使用非字面量 computed action key。凡 action/menu/toolbar/button/command 配置中出现 `[ACTION_KEY]`、`[actionMap.onClick]`、`[getActionKey()]`、`[labelKey]` 等运行时表达式键，必须门禁失败。允许 `['onClick']`、`["handler"]`、`['label']` 等字符串字面量 computed key 继续按既有规则处理。

### 52.3 放行条件

1. 非字面量 computed action key 反向测试失败。
2. 字符串字面量 computed key 既有测试未回退。
3. 合法只读说明对象成功 fixture 继续通过。
4. TASK-005H/I/I1/J/J1/J2/J3/J4/J5/J6 既有测试未删除、未降级。
5. `npm run verify` 通过。
6. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
7. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。

## 53. TASK-005J8 跨行非字面量计算属性 Action 键门禁整改

TASK-005J7 审计意见书第 126 份有条件通过，但发现非字面量 computed key 表达式内部换行仍可绕过，例如 `[actionMap
  .onClick]: openHelp`。TASK-005J8 要求跨行捕获完整 `[...]` 表达式，并继续禁止非字面量 computed action key。

### 53.1 范围

允许实现：

1. 新增跨行 computed key 捕获。
2. 补 `[actionMap
  .onClick]`、`[
ACTION_KEY
]`、`[getActionKey(
)]` 反向测试。
3. 保留字符串字面量 computed key 成功/失败既有规则。
4. 保留真实长距离 fixture 和距离断言。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。
4. 在本任务中引入 AST 解析大改。

### 53.2 跨行 computed key 口径

门禁必须捕获完整跨行 `[...]` 表达式。捕获后应规范化空白并判断是否为字符串字面量：`['onClick']`、`["handler"]` 可继续按既有规则处理；`[ACTION_KEY]`、`[actionMap . onClick]`、`[getActionKey('profit')]` 等非字面量表达式必须失败。

### 53.3 放行条件

1. 跨行非字面量 computed key 反向测试失败。
2. 跨行字符串字面量 computed key 不被误杀。
3. 真实长距离跨行 computed key fixture 距离断言通过。
4. TASK-005H/I/I1/J/J1/J2/J3/J4/J5/J6/J7 既有测试未删除、未降级。
5. `npm run verify` 通过。
6. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
7. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。

## 54. TASK-005J9 Style Profit 契约门禁 AST 化 Computed Key 收口

TASK-005J8 审计意见书第 127 份有条件通过，但发现 computed key 内部方括号仍可绕过，例如 `[actionMap['onClick']]: openHelp`。TASK-005J9 决策为停止继续叠加正则补丁，使用项目已有 TypeScript compiler API 解析对象成员和 computed key。

### 54.1 范围

允许实现：

1. 使用 `typescript` devDependency 创建 `SourceFile` 并遍历对象字面量。
2. 对 `.vue` 文件提取 `<script setup>` 与 `<script>` 后做 AST 扫描。
3. 将 computed key 分类为 literal / dynamic / unknown。
4. 对动态 computed action key 和 unknown key 在 style-profit 语境下 fail closed。
5. 保留 Vue template 文本禁线扫描。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。
4. 新增第三方 parser 依赖。

### 54.2 AST 化口径

对象成员识别必须覆盖 `PropertyAssignment`、`MethodDeclaration`、`ShorthandPropertyAssignment`、`SpreadAssignment` 和 `ComputedPropertyName`。字符串字面量 computed key 继续按既有门禁处理；非字面量 computed key 如 `[ACTION_KEY]`、`[actionMap['onClick']]`、`[getActionKey()]`、模板字符串变量必须 fail closed。

### 54.3 放行条件

1. 内部方括号 computed key 反向测试失败。
2. 函数调用和模板字符串动态 key 反向测试失败。
3. spread + 利润说明 label fail closed。
4. 字符串字面量 computed key 既有测试不回退。
5. Vue script / script setup 中 action 对象被 AST 扫描覆盖。
6. TASK-005H/I/I1/J/J1/J2/J3/J4/J5/J6/J7/J8 既有测试未删除、未降级。
7. `npm run verify` 通过。
8. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
9. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。

## 55. TASK-005J10 Style Profit 动态 Computed Key 全域 Fail Closed 门禁整改

TASK-005J9 审计意见书第 128 份有条件通过，但发现 AST 逻辑仍依赖 `actions/menu/button` 等 action-like 容器名。对象命名为 `items` 等中性名称时，`[ACTION_KEY]` 仍可绕过。TASK-005J10 要求在 style-profit surface 内对 `dynamic_computed_key / unknown_key` 全域 fail closed。

### 55.1 范围

允许实现：

1. 移除 dynamic/unknown computed key 对 action-like 容器名的依赖。
2. 对 style-profit surface 内所有对象字面量的 dynamic/unknown computed key fail closed。
3. 补 `items/rows/configs/columns` 等中性容器名反向测试。
4. 保留字符串字面量 computed key 成功/失败既有规则。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。
4. 新增第三方 parser 依赖。

### 55.2 全域 Fail Closed 口径

在 style-profit surface 内，只要任一 `ObjectLiteralExpression` 出现 `dynamic_computed_key` 或 `unknown_key`，必须 fail closed。不得再要求对象变量名必须叫 `actions`、`menus`、`buttons`、`toolbar`。`label: '查看详情'`、`label: '查询'`、`label: '返回'` 不能豁免动态 computed key。

### 55.3 放行条件

1. 中性容器名 + dynamic computed key 反向测试失败。
2. Vue script setup 中中性容器名反向测试失败。
3. 真实长距离 + 中性容器名反向测试失败。
4. 字符串字面量 computed key 既有测试不回退。
5. TASK-005H/I/I1/J/J1/J2/J3/J4/J5/J6/J7/J8/J9 既有测试未删除、未降级。
6. `npm run verify` 通过。
7. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
8. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。

## 56. TASK-005J11 运行时动态属性注入门禁整改

TASK-005J10 审计意见书第 129 份有条件通过，但发现对象创建后的运行时动态属性注入仍可绕过，例如 `item[ACTION_KEY] = goDetail` 与 `Object.defineProperty(item, ACTION_KEY, { value: goDetail })`。TASK-005J11 要求把动态属性写入、定义、设置、合并路径纳入 AST 门禁。

### 56.1 范围

允许实现：

1. 检测 `ElementAccessExpression` 左值赋值。
2. 检测 `Object.defineProperty`、`Object.defineProperties`、`Reflect.set`。
3. 检测 `Object.assign` source object 中的 dynamic / unknown computed key。
4. 补运行时动态属性注入反向测试。
5. 保留读取型动态 key 的非阻断边界。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。
4. 新增第三方 parser 依赖。

### 56.2 动态属性注入 Fail Closed 口径

在 style-profit surface 内，凡出现非字面量 `ElementAccessExpression` 左值赋值，或 `Object.defineProperty/Object.defineProperties/Reflect.set/Object.assign` 引入 dynamic / unknown computed key，必须 fail closed。读取型 `const value = item[ACTION_KEY]` 暂不作为本任务阻断目标，除非同时构成 action 入口或写入。

### 56.3 放行条件

1. `item[ACTION_KEY] = goDetail` 反向测试失败。
2. `Object.defineProperty(item, ACTION_KEY, ...)` 反向测试失败。
3. `Reflect.set(item, ACTION_KEY, ...)` 反向测试失败。
4. `Object.defineProperties/Object.assign` 动态 key 反向测试失败。
5. Vue script setup 动态属性注入反向测试失败。
6. 读取型动态 key 成功 fixture 不被误杀。
7. TASK-005H/I/I1/J/J1/J2/J3/J4/J5/J6/J7/J8/J9/J10 既有测试未删除、未降级。
8. `npm run verify` 通过。
9. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
10. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。

## 57. TASK-005J12 运行时显式 Action Key 注入门禁整改

TASK-005J11 审计意见书第 130 份有条件通过，但发现运行时显式字面量 action key 注入仍可绕过，例如 `Object.defineProperty(item, 'onClick', { value: openHelp })`、`Object.assign(item, { onClick: openHelp })`、`item['onClick'] = openHelp`。TASK-005J12 要求把显式 action key 的运行时写入、定义、设置、合并路径纳入门禁。

### 57.1 范围

允许实现：

1. 检测 `item['onClick'] = ...`、`item.onClick = ...` 等显式 action key 写入。
2. 检测 `Object.defineProperty`、`Object.defineProperties`、`Reflect.set`、`Object.assign` 中显式 action key 注入。
3. 补显式 action key 运行时注入反向测试。
4. 保留对象字面量中合法只读 action 的成功 fixture。

禁止实现：

1. 创建利润快照入口。
2. `POST /api/reports/style-profit/snapshots` 调用。
3. 后端、workflow、`02_源码` 或 TASK-006 修改。
4. 新增第三方 parser 依赖。

### 57.2 显式 Action Key 运行时注入口径

在 style-profit surface 内，`onClick/handler/action/command/onSelect/onCommand/callback/execute/submit/onConfirm/onSubmit/click/open` 等显式 action key 不允许通过运行时赋值、defineProperty、Reflect.set、Object.assign 等方式注入。对象字面量中合法只读 action 继续按既有规则判断；本任务禁止的是对象创建后的运行时注入。

### 57.3 放行条件

1. `item['onClick'] = ...` 与 `item.onClick = ...` 反向测试失败。
2. `Object.defineProperty/Object.defineProperties/Reflect.set/Object.assign` 显式 action key 反向测试失败。
3. 只读导航 label 不得豁免运行时注入。
4. 对象字面量中合法只读 action 不被误杀。
5. 非 action 字段运行时写入不被误杀。
6. TASK-005H/I/I1/J/J1/J2/J3/J4/J5/J6/J7/J8/J9/J10/J11 既有测试未删除、未降级。
7. `npm run verify` 通过。
8. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
9. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 58. TASK-005J13 运行时 ActionKey 等价语法门禁整改

### 58.1 触发原因

审计意见书第 131 份确认：TASK-005J12 已拦截标准运行时显式 action key 注入，但仍存在等价语法绕过：

1. `Object['defineProperty'](item, 'onClick', { value: openHelp })`
2. `Reflect['set'](item, 'onClick', openHelp)`
3. `Object['assign'](item, { onClick: openHelp })`
4. `const defineProperty = Object.defineProperty; defineProperty(item, 'onClick', ...)`
5. `const action = { onClick: openHelp }; Object.assign(item, action)`

这些路径本质上仍是在款式利润只读页面运行时注入交互入口，必须继续 fail closed。

### 58.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中 callee 等价识别、别名表和 `Object.assign` source 变量追踪。
2. `test-style-profit-contracts.mjs` 中补审计复现反向测试和必要成功 fixture。
3. TASK-005J13 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 58.3 检测口径

1. `Object.defineProperty/Object.defineProperties/Object.assign/Reflect.set` 的点访问与字符串字面量括号访问必须等价处理。
2. 本地别名 `const alias = Object.defineProperty/Object.assign/Reflect.set` 后的调用必须等价处理。
3. `Object.assign` 的所有 source 参数必须逐一检查。
4. source 参数为同文件简单对象字面量变量时，必须追踪其 action key。
5. source 参数为动态、无法解析或含 spread 时，在 style-profit surface 内必须 fail closed。
6. `查看详情/查询/返回/利润计算说明/利润率计算规则/款式利润报表` 等只读文案不得豁免运行时 action 注入。
7. 非 action 字段写入与纯读取动态 key 不应误杀。

### 58.4 放行条件

1. 审计意见书第 131 份列出的 6 类绕过全部变为失败 fixture。
2. TASK-005H/I/I1/J/J1/J2/J3/J4/J5/J6/J7/J8/J9/J10/J11/J12 既有反向测试未删除、未跳过、未降级。
3. `npm run check:style-profit-contracts` 通过。
4. `npm run test:style-profit-contracts` 通过。
5. `npm run verify` 通过。
6. 后端 style-profit API 定向回归通过。
7. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
8. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 59. TASK-005J14 解构与命名空间别名门禁整改

### 59.1 触发原因

审计意见书第 132 份确认：TASK-005J13 已关闭 bracket callee、直接函数别名、变量 source/descriptor 等绕过，但以下别名类路径仍可绕过：

1. `const { defineProperty } = Object; defineProperty(item, 'onClick', ...)`
2. `const { assign } = Object; assign(item, { onClick: ... })`
3. `const { set } = Reflect; set(item, 'onClick', ...)`
4. `const Obj = Object; Obj.defineProperty(item, 'onClick', ...)`
5. `const R = Reflect; R.set(item, 'onClick', ...)`

这些路径仍是运行时 action key 注入，必须等价拦截。

### 59.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中 Object/Reflect 解构别名、解构重命名和命名空间别名识别。
2. `test-style-profit-contracts.mjs` 中补第 132 份审计复现反向测试。
3. TASK-005J14 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 59.3 检测口径

1. `const { defineProperty/defineProperties/assign } = Object` 必须映射为 Object 原 API。
2. `const { defineProperty: dp, assign: merge } = Object` 必须映射为 Object 原 API。
3. `const { set } = Reflect` 与 `const { set: reflectSet } = Reflect` 必须映射为 Reflect 原 API。
4. `const Obj = Object` 与 `const R = Reflect` 必须作为命名空间别名处理。
5. 命名空间别名下的点访问和字符串字面量括号访问都必须识别。
6. 别名来源不明、被重新赋值或与局部函数冲突时，在 style-profit surface 内遇到疑似 action 注入必须 fail closed。
7. TASK-005J13 已关闭的第 131 份绕过不得回潮。

### 59.4 放行条件

1. 第 132 份审计列出的 5 类绕过全部变为失败 fixture。
2. 解构重命名场景补齐失败 fixture。
3. 命名空间别名的点访问和括号访问补齐失败 fixture。
4. TASK-005H 到 TASK-005J13 既有反向测试未删除、未跳过、未降级。
5. `npm run check:style-profit-contracts` 通过。
6. `npm run test:style-profit-contracts` 通过。
7. `npm run verify` 通过。
8. 后端 style-profit API 定向回归通过。
9. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
10. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 60. TASK-005J15 Runtime Mutator 高级别名门禁整改

### 60.1 触发原因

审计意见书第 133 份确认：TASK-005J14 已关闭声明式解构别名和命名空间别名绕过，但以下 runtime mutator 高级别名路径仍可绕过：

1. 赋值式解构：`let defineProperty; ({ defineProperty } = Object)`。
2. bind 别名：`const defineProperty = Object.defineProperty.bind(Object)`。
3. call/apply：`Object.defineProperty.call(...)`、`Object.defineProperty.apply(...)`。
4. globalThis/window 命名空间：`const Obj = globalThis.Object`、`const R = window.Reflect`。

这些路径仍然等价于源 mutator API，必须 fail closed。

### 60.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中赋值式解构、bind、call/apply、globalThis/window Object/Reflect 来源识别。
2. `test-style-profit-contracts.mjs` 中补第 133 份审计复现反向测试。
3. TASK-005J15 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 60.3 检测口径

1. 对象解构赋值 `({ defineProperty/assign } = Object)` 与 `({ set } = Reflect)` 必须映射回源 API。
2. `.bind(Object/Reflect)` 产生的别名必须映射回源 API。
3. `.call()` 必须剥离 thisArg 后按源 API 参数解析。
4. `.apply()` 在数组字面量参数下必须按数组元素解析源 API 参数。
5. `.apply()` 参数无法解析时，在 style-profit surface 内必须 fail closed。
6. `globalThis.Object/window.Object/globalThis.Reflect/window.Reflect` 必须作为 Object/Reflect 等价来源。
7. TASK-005J13/J14 已关闭绕过不得回潮。

### 60.4 放行条件

1. 第 133 份审计列出的绕过全部变为失败 fixture。
2. 赋值式解构、bind、call、apply、globalThis/window 命名空间均有失败 fixture。
3. TASK-005H 到 TASK-005J14 既有反向测试未删除、未跳过、未降级。
4. `npm run check:style-profit-contracts` 通过。
5. `npm run test:style-profit-contracts` 通过。
6. `npm run verify` 通过。
7. 后端 style-profit API 定向回归通过。
8. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
9. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 61. TASK-005J16 Runtime Mutator Sink 变体门禁整改

### 61.1 触发原因

审计意见书第 134 份确认：TASK-005J15 已关闭赋值式解构基础形态、bind/call/apply、globalThis/window 命名空间别名，但以下 sink 变体仍可绕过：

1. `Reflect.apply(Object.defineProperty, Object, [...])`。
2. `Reflect.apply(Object.assign, Object, [...])`。
3. `Reflect.apply(Reflect.set, Reflect, [...])`。
4. `({ 'defineProperty': dp } = Object)`。
5. `({ ['defineProperty']: dp } = Object)`。
6. `const { Object: Obj } = globalThis`。

这些路径仍然等价于源 mutator API，必须 fail closed。

### 61.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中 `Reflect.apply`、字符串属性解构、计算属性解构、globalThis/window 解构命名空间识别。
2. `test-style-profit-contracts.mjs` 中补第 134 份审计复现反向测试。
3. TASK-005J16 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 61.3 检测口径

1. `Reflect.apply` 第一个参数为源 mutator API 时，第三个参数数组必须按源 API 参数位置解析。
2. `Reflect['apply']` 与 Reflect 命名空间别名下的 `apply` 必须等价处理。
3. `Reflect.apply` 第三个参数无法静态还原时，在 style-profit surface 内必须 fail closed。
4. 字符串属性解构必须按真实属性名映射。
5. 字符串字面量计算属性解构必须按真实属性名映射。
6. 非字面量计算属性解构作为 mutator source 时必须 fail closed。
7. `globalThis/window` 解构出的 `Object/Reflect` 必须作为源命名空间处理。
8. TASK-005J13/J14/J15 已关闭绕过不得回潮。

### 61.4 放行条件

1. 第 134 份审计列出的 6 类绕过全部变为失败 fixture。
2. Reflect.apply、Reflect['apply']、namespace apply 都有失败 fixture。
3. 字符串属性解构、计算属性解构、非字面量计算属性解构都有失败 fixture。
4. globalThis/window 解构命名空间有失败 fixture。
5. TASK-005H 到 TASK-005J15 既有反向测试未删除、未跳过、未降级。
6. `npm run check:style-profit-contracts` 通过。
7. `npm run test:style-profit-contracts` 通过。
8. `npm run verify` 通过。
9. 后端 style-profit API 定向回归通过。
10. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
11. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 62. TASK-005J17 Runtime Mutator 中转调用门禁整改

### 62.1 触发原因

审计意见书第 135 份确认：TASK-005J16 已关闭 `Reflect.apply`、字符串/计算属性解构、`globalThis/window` 命名空间解构绕过，但以下中转路径仍可绕过：

1. 逗号表达式：`(0, Object.defineProperty)(...)`。
2. 条件表达式命名空间：`const Obj = true ? Object : Object`。
3. 数组容器中转：`const mutators = [Object.defineProperty]; mutators[0](...)`。
4. 对象容器中转：`const mutators = { dp: Object.defineProperty }; mutators.dp(...)`。

这些路径仍然等价于源 mutator API，必须 fail closed。

### 62.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中逗号表达式 callee、条件表达式命名空间、数组容器中转、对象容器中转识别。
2. `test-style-profit-contracts.mjs` 中补第 135 份审计复现反向测试。
3. TASK-005J17 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 62.3 检测口径

1. `CallExpression` callee 归一化时，逗号表达式必须取最后表达式继续解析。
2. 条件表达式两个分支均解析为同一源命名空间时，必须映射回源命名空间。
3. 条件表达式任一分支无法解析或分支不一致时，在 style-profit surface 内必须 fail closed。
4. 简单数组字面量容器必须建立索引到源 mutator API 的映射。
5. 数组动态索引、spread、无法还原时，在 style-profit surface 内必须 fail closed。
6. 简单对象字面量容器必须建立属性到源 mutator API 的映射。
7. 对象动态属性、spread、无法还原时，在 style-profit surface 内必须 fail closed。
8. TASK-005J13/J14/J15/J16 已关闭绕过不得回潮。

### 62.4 放行条件

1. 第 135 份审计列出的 4 类绕过全部变为失败 fixture。
2. 逗号表达式 callee、条件表达式命名空间、数组容器、对象容器都有失败 fixture。
3. 动态索引、spread、无法还原容器在 style-profit surface 内 fail closed。
4. TASK-005H 到 TASK-005J16 既有反向测试未删除、未跳过、未降级。
5. `npm run check:style-profit-contracts` 通过。
6. `npm run test:style-profit-contracts` 通过。
7. `npm run verify` 通过。
8. 后端 style-profit API 定向回归通过。
9. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
10. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 63. TASK-005J18 Runtime Mutator 源引用禁用门禁收口

### 63.1 触发原因

审计意见书第 136 份确认：TASK-005J17 已关闭直接 comma、conditional、array、object container 中转调用，但以下路径仍可绕过：

1. 函数返回：`getMutator()()`。
2. 箭头函数返回：`() => Object.assign`。
3. IIFE：`(() => Object.defineProperty)()()`。
4. 内联字面量：`[Object.defineProperty][0]`、`({ dp: Object.defineProperty }).dp`。
5. 嵌套容器：`[[Object.defineProperty]][0][0]`。
6. 包装容器：`Object.freeze({ dp: Object.defineProperty })`。
7. 条件容器：`condition ? [Object.defineProperty] : [Object.defineProperty]`。

这些复现说明继续逐个补语法变体不是稳定策略。由于款式利润前端在当前阶段是只读页面，不需要 runtime mutator API，因此本任务收口为源引用禁用策略。

### 63.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中 runtime mutator 源引用禁用、容器中转 fail closed、函数返回/IIFE fail closed。
2. `test-style-profit-contracts.mjs` 中补第 136 份审计复现反向测试和必要成功 fixture。
3. TASK-005J18 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 63.3 检测口径

1. style-profit surface 内禁用 `Object.defineProperty/Object.defineProperties/Object.assign/Reflect.set/Reflect.apply` 源引用。
2. `globalThis/window`、字符串括号访问、命名空间别名、解构别名、bind/call/apply、Reflect.apply、中转容器等所有等价来源均视为源引用。
3. 函数返回、箭头函数返回、IIFE 返回 mutator 源引用时 fail closed。
4. 数组/对象字面量、嵌套容器、条件容器、包装容器中出现 mutator 源引用时 fail closed。
5. 源引用存在但未调用，也必须 fail closed。
6. 允许对象 spread 替代 `Object.assign`，但对象 spread 中若出现 action key 或不可解析 source，继续按既有 action 门禁处理。
7. TASK-005J13/J14/J15/J16/J17 已关闭绕过不得回潮。

### 63.4 放行条件

1. 第 136 份审计列出的新增绕过全部变为失败 fixture。
2. 源引用未调用也失败。
3. 容器存在但未调用也失败。
4. 对象 spread 成功 fixture 不被误杀。
5. 普通说明文案、非 action 字段写入、纯读取动态 key 不被误杀。
6. TASK-005H 到 TASK-005J17 既有反向测试未删除、未跳过、未降级。
7. `npm run check:style-profit-contracts` 通过。
8. `npm run test:style-profit-contracts` 通过。
9. `npm run verify` 通过。
10. 后端 style-profit API 定向回归通过。
11. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
12. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 64. TASK-005J19 动态成员名与源引用隐藏门禁整改

### 64.1 触发原因

审计意见书第 137 份确认：TASK-005J18 已接入 runtime mutator 源引用禁用，并关闭函数返回、IIFE、内联/嵌套/包装容器绕过，但以下源引用隐藏仍可绕过：

1. `Object['define' + 'Property'](...)`。
2. `Object['ass' + 'ign'](...)`。
3. `Reflect['s' + 'et'](...)`。
4. `globalThis['Object']['assign'](...)`。
5. `window['Reflect']['set'](...)`。
6. `Reflect.get(Object, 'defineProperty')(...)`。
7. `Object.defineProperty?.(...)`。

这些路径仍然等价于 runtime mutator 源引用或调用，必须 fail closed。

### 64.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中字符串折叠、动态成员名归一化、多级 element access、Reflect.get、optional chain 识别。
2. `test-style-profit-contracts.mjs` 中补第 137 份审计复现反向测试和必要成功 fixture。
3. TASK-005J19 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 64.3 检测口径

1. 字符串字面量、模板字面量、字符串拼接表达式必须安全静态折叠。
2. `Object/Reflect/globalThis/window` 及其别名的动态成员访问，key 可折叠命中 mutator 时 fail closed。
3. `Object/Reflect/globalThis/window` 及其别名的动态成员访问，key 无法静态证明安全时 fail closed。
4. `globalThis/window` 多级 element access 必须递归归一化。
5. `Reflect.get(target, key)` 必须映射为对应源引用；target/key 任一无法静态确认安全时 fail closed。
6. optional property access 和 optional call 必须等价普通访问/调用处理。
7. TASK-005J13-J18 已关闭绕过不得回潮。

### 64.4 放行条件

1. 第 137 份审计列出的新增绕过全部变为失败 fixture。
2. 动态成员名、字符串拼接、模板字面量、多级 element access、Reflect.get、optional chain 均有失败 fixture。
3. 不可解析动态 key 在 style-profit surface 内 fail closed。
4. 普通业务字典读取和安全对象读取不被误杀。
5. TASK-005H 到 TASK-005J18 既有反向测试未删除、未跳过、未降级。
6. `npm run check:style-profit-contracts` 通过。
7. `npm run test:style-profit-contracts` 通过。
8. `npm run verify` 通过。
9. 后端 style-profit API 定向回归通过。
10. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
11. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 65. TASK-005J20 运行时代码生成入口禁用门禁整改

### 65.1 触发原因

审计意见书第 138 份确认：TASK-005J19 已关闭动态成员名、`Reflect.get`、optional chain 等源引用隐藏绕过，但以下运行时代码生成入口仍可生成受禁 mutator 并绕过静态门禁：

1. `Function('return Object.assign')()`。
2. `new Function('return Object.defineProperty')()`。
3. `eval('Reflect.set')`。
4. `globalThis.Function('return Object.assign')()`。
5. `window.Function('return Reflect.set')()`。

款式利润前端当前为只读页面，不需要任何运行时代码生成能力，因此必须在 style-profit surface 内禁用。

### 65.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中 `eval/Function/new Function/globalThis.Function/window.Function` 及其别名、动态成员、间接调用识别。
2. `test-style-profit-contracts.mjs` 中补第 138 份审计复现反向测试和必要成功 fixture。
3. TASK-005J20 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 65.3 检测口径

1. `eval`、间接 eval、optional eval、window/globalThis eval 必须 fail closed。
2. `Function`、`new Function`、optional Function、window/globalThis Function 必须 fail closed。
3. `Function/eval` 的直接别名、解构别名、赋值式解构别名、命名空间别名必须 fail closed。
4. `globalThis/window` 对 `Function/eval` 的动态成员访问必须静态折叠或 fail closed。
5. `Function/eval` 源引用进入变量、数组、对象、函数返回、IIFE、条件表达式、包装容器时，即使未调用也 fail closed。
6. 不解析代码生成字符串内容；入口出现即 fail closed。
7. TASK-005J13-J19 已关闭绕过不得回潮。

### 65.4 放行条件

1. 第 138 份审计列出的运行时代码生成绕过全部变为失败 fixture。
2. eval、Function、new Function、globalThis/window Function/eval 均有失败 fixture。
3. 别名、解构、动态成员、源引用未调用场景均有失败 fixture。
4. 普通业务函数、普通构造器、说明文案、字段读取、对象 spread 成功 fixture 不被误杀。
5. TASK-005H 到 TASK-005J19 既有反向测试未删除、未跳过、未降级。
6. `npm run check:style-profit-contracts` 通过。
7. `npm run test:style-profit-contracts` 通过。
8. `npm run verify` 通过。
9. 后端 style-profit API 定向回归通过。
10. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
11. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 66. TASK-005J21 字符串定时器与 Constructor 链禁用门禁整改

### 66.1 触发原因

审计意见书第 139 份确认：TASK-005J20 已关闭直接 `eval/Function/new Function/globalThis.Function` 等运行时代码生成入口，但以下派生入口仍可绕过：

1. `setTimeout("Object.assign(...)")`。
2. `setInterval("Reflect.set(...)")`。
3. `window.setTimeout("Object.defineProperty(...)")`。
4. `(() => {}).constructor(...)`。
5. `[]['filter']['constructor'](...)`。
6. `({}).constructor.constructor(...)`。

这些路径仍属于运行时代码生成或 Function 派生入口，必须 fail closed。

### 66.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中字符串 timer、timer 别名/间接调用、constructor 链、constructor 源引用中转识别。
2. `test-style-profit-contracts.mjs` 中补第 139 份审计复现反向测试和必要成功 fixture。
3. TASK-005J21 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 66.3 检测口径

1. `setTimeout/setInterval` 第一参数为字符串、模板字面量、字符串拼接或不可证明非字符串时，必须 fail closed。
2. `window/globalThis` timer、timer 别名、解构别名、间接调用、动态成员访问必须等价处理。
3. 任意表达式上的 `.constructor(...)` 或 `['constructor'](...)` 调用必须 fail closed。
4. `.constructor.constructor(...)` 与 `['constructor']['constructor'](...)` 必须 fail closed。
5. `Function.prototype.constructor` 必须等价视为 Function 构造器源引用。
6. constructor 源引用进入变量、数组、对象、函数返回、IIFE、条件表达式、包装容器时，即使未调用也 fail closed。
7. 函数回调 timer 和已知函数引用 timer 允许通过。
8. TASK-005J13-J20 已关闭绕过不得回潮。

### 66.4 放行条件

1. 第 139 份审计列出的字符串 timer 与 constructor 链绕过全部变为失败 fixture。
2. timer 字符串、模板字符串、字符串拼接、变量字符串、别名、间接调用、动态成员访问均有失败 fixture。
3. constructor 调用、constructor.constructor 调用、constructor 源引用未调用场景均有失败 fixture。
4. 函数回调 timer、函数引用 timer、普通字段读取、普通说明文案、new Date 成功 fixture 不被误杀。
5. TASK-005H 到 TASK-005J20 既有反向测试未删除、未跳过、未降级。
6. `npm run check:style-profit-contracts` 通过。
7. `npm run test:style-profit-contracts` 通过。
8. `npm run verify` 通过。
9. 后端 style-profit API 定向回归通过。
10. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
11. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 67. TASK-005J22 Timer call/apply 等价调用门禁整改

### 67.1 触发原因

审计意见书第 140 份确认：TASK-005J21 已封住直接字符串定时器与 constructor 链主路径，但以下 timer 等价调用仍可绕过：

1. `setTimeout.call(window, "...")`。
2. `setTimeout.apply(window, ["...", 0])`。
3. `window.setTimeout.call(window, "...")`。
4. `globalThis.setInterval.apply(globalThis, ["...", 0])`。

这些路径仍然是字符串 timer 执行代码，必须接入统一参数归一逻辑。

### 67.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中 timer `.call/.apply/Reflect.apply` descriptor 归一与 callback 参数解析。
2. `test-style-profit-contracts.mjs` 中补第 140 份审计复现反向测试和必要成功 fixture。
3. TASK-005J22 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 67.3 检测口径

1. timer `.call()` 必须剥离 thisArg，第二参数作为真实 callback。
2. timer `.apply()` 必须读取第二参数数组，第一个数组元素作为真实 callback。
3. `Reflect.apply(timer, thisArg, args)` 必须读取第三参数数组，第一个数组元素作为真实 callback。
4. apply 参数不是数组字面量、含 spread 或无法还原时，在 style-profit surface 内 fail closed。
5. timer 别名、命名空间、动态成员、间接调用必须共用同一 timer descriptor 归一逻辑。
6. 函数回调与已知函数引用 timer 允许通过。
7. TASK-005J13-J21 已关闭绕过不得回潮。

### 67.4 放行条件

1. 第 140 份审计列出的 timer `.call/.apply` 绕过全部变为失败 fixture。
2. `Reflect.apply(timer, ...)` 有失败 fixture。
3. timer alias 下 `.call/.apply` 有失败 fixture。
4. apply 参数无法静态还原 fail closed。
5. 函数回调 timer 和已知函数引用 timer 成功 fixture 不被误杀。
6. TASK-005H 到 TASK-005J21 既有反向测试未删除、未跳过、未降级。
7. `npm run check:style-profit-contracts` 通过。
8. `npm run test:style-profit-contracts` 通过。
9. `npm run verify` 通过。
10. 后端 style-profit API 定向回归通过。
11. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
12. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 68. TASK-005J23 动态模块加载禁用门禁整改

### 68.1 触发原因

审计意见书第 141 份确认：TASK-005J22 已关闭 timer `.call/.apply/Reflect.apply` 字符串 callback 绕过，但以下动态模块加载入口仍可绕过：

1. `import('data:text/javascript,export default Object.assign')`。
2. `import('data:text/javascript,export default Reflect.set')`。
3. `const url = 'data:text/javascript,...'; import(url)`。
4. `URL.createObjectURL(new Blob([...]))` 后再动态 import。

这些路径可以在运行时加载内联模块代码并返回受禁 mutator，必须 fail closed。

### 68.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中动态 import 协议校验、变量 import fail closed、Blob URL module loading、Worker/script Blob URL 代码加载识别。
2. `test-style-profit-contracts.mjs` 中补第 141 份审计复现反向测试和必要成功 fixture。
3. TASK-005J23 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 68.3 检测口径

1. `import('data:...')`、`import('blob:...')`、`import('javascript:...')`、`import('http(s):...')` 必须 fail closed。
2. 动态 import source 无法静态证明为安全本地路径时必须 fail closed。
3. `import(/* @vite-ignore */ url)` 不得豁免。
4. `URL.createObjectURL(new Blob(...))` 用于生成可执行模块 URL 时必须 fail closed。
5. Blob 脚本 MIME 包括 `text/javascript/application/javascript/text/ecmascript` 等必须 fail closed。
6. 直接 Blob URL Worker/script 代码加载入口必须 fail closed。
7. 仅允许字符串字面量本地路径动态 import，例如 `@/views/...`、`./...`、`../...`，且不得带协议或逃逸项目根。
8. TASK-005J13-J22 已关闭绕过不得回潮。

### 68.4 放行条件

1. 第 141 份审计列出的动态模块加载绕过全部变为失败 fixture。
2. data/blob/http/javascript import 均有失败 fixture。
3. 变量 import、无法静态证明安全 import、vite-ignore import 均 fail closed。
4. Blob URL module loading、Worker/script Blob URL code loading 均 fail closed。
5. 静态本地懒加载 import 成功 fixture 不被误杀。
6. TASK-005H 到 TASK-005J22 既有反向测试未删除、未跳过、未降级。
7. `npm run check:style-profit-contracts` 通过。
8. `npm run test:style-profit-contracts` 通过。
9. `npm run verify` 通过。
10. 后端 style-profit API 定向回归通过。
11. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
12. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 69. TASK-005J24 Blob URL 与 Worker 加载门禁整改

### 69.1 触发原因

审计意见书第 142 份确认：TASK-005J23 已关闭 data/blob 动态 import 主路径，但以下路径仍可绕过：

1. `URL.createObjectURL.call(URL, new Blob(...))`。
2. `URL.createObjectURL.apply(URL, [new Blob(...)])`。
3. `const make = URL.createObjectURL; const blobUrl = make(new Blob(...)); script.src = blobUrl`。
4. `new Worker(blobUrl)`。
5. `new Worker('data:text/javascript,...', { type: 'module' })`。

这些路径仍可生成或加载运行时代码，必须 fail closed。

### 69.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中 URL.createObjectURL call/apply/别名、Blob URL 传播、Worker/SharedWorker 高危 URL、script src 高危 URL 识别。
2. `test-style-profit-contracts.mjs` 中补第 142 份审计复现反向测试和必要成功 fixture。
3. TASK-005J24 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 69.3 检测口径

1. URL.createObjectURL 的直接调用、call、apply、别名、解构别名、命名空间别名必须 fail closed。
2. URL.createObjectURL 结果进入变量后，该变量进入 import/Worker/SharedWorker/script src 必须 fail closed。
3. Blob 脚本 MIME 或无法静态确认安全的 Blob 必须 fail closed。
4. Worker/SharedWorker 的 data/blob/http/https/javascript 协议 URL 必须 fail closed。
5. Worker/SharedWorker 的变量 URL、模板表达式、函数返回、条件表达式等无法证明本地安全路径时 fail closed。
6. script.src 或 setAttribute('src') 指向高危协议或 Blob URL 标记变量时 fail closed。
7. 静态本地 worker 可允许，但 worker 文件必须位于项目内并接受既有扫描。
8. TASK-005J13-J23 已关闭绕过不得回潮。

### 69.4 放行条件

1. 第 142 份审计列出的 Blob URL 与 Worker 绕过全部变为失败 fixture。
2. URL.createObjectURL call/apply/别名均有失败 fixture。
3. Blob URL 变量传播到 import/Worker/script 均有失败 fixture。
4. Worker/SharedWorker 高危 URL 和未知 URL 均 fail closed。
5. 静态本地 worker 成功 fixture 不被误杀。
6. TASK-005H 到 TASK-005J23 既有反向测试未删除、未跳过、未降级。
7. `npm run check:style-profit-contracts` 通过。
8. `npm run test:style-profit-contracts` 通过。
9. `npm run verify` 通过。
10. 后端 style-profit API 定向回归通过。
11. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
12. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。


## 70. TASK-005J25 Worker 构造器别名门禁整改

### 70.1 触发原因

审计意见书第 143 份确认：TASK-005J24 已收口 Blob URL 与 Worker 直接加载主路径，但以下 Worker 构造器别名仍可绕过：

1. `const W = Worker; new W('data:text/javascript,...')`。
2. `const SW = SharedWorker; new SW('data:text/javascript,...')`。
3. `const G = globalThis; new G.Worker('data:text/javascript,...')`。

这些路径说明 Worker 构造器识别逻辑没有接入已有 alias / namespace alias / conditional alias 体系，必须统一。

### 70.2 架构边界

本任务只允许修改 style-profit 前端契约门禁脚本和测试，不允许开放创建快照入口，不允许进入 TASK-006。

允许范围：

1. `check-style-profit-contracts.mjs` 中 Worker/SharedWorker 构造器别名、命名空间别名、解构别名、条件别名、容器中转识别。
2. `test-style-profit-contracts.mjs` 中补第 143 份审计复现反向测试和必要成功 fixture。
3. TASK-005J25 证据文档。

禁止范围：

1. 后端业务、API、迁移、`.github` workflow、`02_源码`。
2. 前端创建快照入口、POST 调用、`snapshot_create` 权限动作暴露。
3. TASK-006 任务单或工程实现。
4. 新增第三方 parser 依赖。

### 70.3 检测口径

1. Worker/SharedWorker 直接别名必须映射回构造器。
2. globalThis/window 命名空间别名必须映射回命名空间。
3. globalThis/window 解构出的 Worker/SharedWorker 必须映射回构造器。
4. 条件表达式两分支均为同类构造器时映射回构造器；无法解析或分支不一致时 fail closed。
5. 数组/对象容器中的 Worker/SharedWorker 构造器必须追踪；动态索引、spread 或无法还原时 fail closed。
6. 无论构造器来源如何，必须调用同一套 Worker URL 校验逻辑。
7. 静态本地 worker 可允许，但必须接受既有扫描。
8. TASK-005J13-J24 已关闭绕过不得回潮。

### 70.4 放行条件

1. 第 143 份审计列出的 Worker 构造器别名绕过全部变为失败 fixture。
2. 直接别名、命名空间别名、解构别名、条件别名、容器中转均有失败 fixture。
3. Worker URL 校验逻辑只有一套，不允许直接构造器和别名构造器分叉。
4. 静态本地 worker 成功 fixture 不被误杀。
5. TASK-005H 到 TASK-005J24 既有反向测试未删除、未跳过、未降级。
6. `npm run check:style-profit-contracts` 通过。
7. `npm run test:style-profit-contracts` 通过。
8. `npm run verify` 通过。
9. 后端 style-profit API 定向回归通过。
10. commit 不包含后端、workflow、`02_源码`、JUnit 生成物或 TASK-006。
11. 审计官复审通过后，才允许评估 TASK-005K 或 TASK-005 收尾封版。

## 71. TASK-005J26 Worker 等价构造入口门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J26_Worker等价构造入口门禁整改_工程任务单.md`
- 前置审计：审计意见书第 144 份
- 结论：有条件通过 / 有问题
- 阻断点：Worker/SharedWorker 构造器别名主路径已修复，但 `Worker.bind(null)`、`const BW = Worker.bind(null)`、`Reflect.construct(Worker, args)`、`const rc = Reflect.construct; rc(Worker, args)` 等等价构造入口仍可绕过。

### 71.1 架构决策

1. Worker 构造 sink 不再只挂在 `NewExpression` 检测上，必须抽象为统一 constructor descriptor。
2. `new Worker(...)`、`new Alias(...)`、`new (Worker.bind(...))(...)`、`Reflect.construct(Worker, args)` 必须归一到同一套 Worker URL 校验。
3. `Worker.bind(...)` 和 `SharedWorker.bind(...)` 返回值必须映射为对应构造器。
4. `Reflect.construct` 及其别名、解构别名、bracket callee、optional chain 形态必须识别。
5. Reflect.construct 参数数组无法静态证明安全时必须 fail closed。
6. 静态本地 worker 是唯一可放行的 Worker 创建路径。
7. 款式利润快照创建入口继续禁止，TASK-006 继续单独阻塞。

### 71.2 工程门禁

1. 必须新增第 144 份审计绕过样例的反向测试。
2. 必须保留 TASK-005J13 到 TASK-005J25 所有既有反向测试。
3. 必须证明 `npm run verify`、`npm audit --audit-level=high`、后端 style-profit API 定向回归通过。
4. 不得修改后端、workflow、`02_源码`、TASK-006 或运行生成物。

## 72. TASK-005J27 动态构造器返回值门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J27_动态构造器返回值门禁整改_工程任务单.md`
- 前置审计：审计意见书第 145 份
- 结论：有条件通过 / 有问题
- 阻断点：`Worker.bind(...)` 与 `Reflect.construct(...)` 主路径已修复，但 `const getWorker = () => Worker; new (getWorker())('data:...')` 等函数返回构造器路径仍可绕过。

### 72.1 架构决策

1. Worker 构造入口从 constructor descriptor 继续升级为 constructor invocation descriptor。
2. `new`、`Reflect.construct`、`bind`、函数返回、IIFE、条件返回必须统一解析构造器来源和 URL 参数。
3. 函数返回 Worker/SharedWorker 或其别名时，必须映射为对应构造器。
4. 函数返回未知构造器且携带高危 URL 时，必须 fail closed。
5. `new unknownCtor(highRiskUrl)` 与 `Reflect.construct(unknownCtor, [highRiskUrl])` 必须同口径 fail closed。
6. 静态本地 worker 是唯一可放行 Worker 创建路径。
7. 款式利润快照创建入口继续禁止，TASK-006 继续单独阻塞。

### 72.2 工程门禁

1. 必须新增第 145 份审计绕过样例的反向测试。
2. 必须保留 TASK-005J13 到 TASK-005J26 所有既有反向测试。
3. 必须证明 `npm run verify`、`npm audit --audit-level=high`、后端 style-profit API 定向回归通过。
4. 不得修改后端、workflow、`02_源码`、TASK-006 或运行生成物。

## 73. TASK-005J28 Unknown Constructor 字符串 URL 严格门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J28_UnknownConstructor字符串URL严格门禁整改_工程任务单.md`
- 前置审计：审计意见书第 146 份
- 结论：有条件通过 / 有问题
- 阻断点：函数返回 Worker/SharedWorker + 高危协议主路径已修复，但 unknown constructor 分支仍会放行普通字符串路径，例如 `new (getWorker())('/runtime/style-profit-worker.js', { type: 'module' })`。

### 73.1 架构决策

1. unknown constructor 不得比 known Worker 更宽松。
2. unknown constructor + URL-like 第一参数在 style-profit surface 内必须 fail closed。
3. URL-like 包括普通字符串路径、协议 URL、模板 URL、变量 URL、`new URL(...)`、函数返回 URL 和 Blob URL 标记变量。
4. `new unknownCtor(url)` 与 `Reflect.construct(unknownCtor, [url])` 必须同口径。
5. 安全非 Worker 构造器必须通过集中白名单明确证明，不得借 unknown 分支放行。
6. 静态本地 worker 是唯一可放行 Worker 创建路径，且构造器必须可证明为 Worker/SharedWorker 或明确安全别名。
7. 款式利润快照创建入口继续禁止，TASK-006 继续单独阻塞。

### 73.2 工程门禁

1. 必须新增第 146 份审计绕过样例的反向测试。
2. 必须保留 TASK-005J13 到 TASK-005J27 所有既有反向测试。
3. 必须证明 `npm run verify`、`npm audit --audit-level=high`、后端 style-profit API 定向回归通过。
4. 不得修改后端、workflow、`02_源码`、TASK-006 或运行生成物。

## 74. TASK-005J29 NewExpression Spread 参数归一化门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J29_NewExpressionSpread参数归一化门禁整改_工程任务单.md`
- 前置审计：审计意见书第 147 份
- 结论：有条件通过 / 有问题
- 阻断点：ordinary unknown constructor URL 参数已修复，但 `new unknownCtor(...args)` 的 `SpreadElement` 被直接传入 URL 分类，未按 Reflect.construct 的参数数组口径处理，最终可绕过。

### 74.1 架构决策

1. `NewExpression` 与 `Reflect.construct` 必须复用同一套 invocation arguments descriptor。
2. `SpreadElement` 不能直接进入 URL 分类函数，必须先静态展开或 fail closed。
3. unknown constructor + spread/rest/数组中转中的 URL-like 参数必须 fail closed。
4. 参数不可静态还原时，在 style-profit surface 内必须 fail closed。
5. safe known constructor 的 spread 放行必须由集中白名单明确证明。
6. 静态本地 worker 是唯一可放行 Worker 创建路径，且参数来源必须可静态证明。
7. 款式利润快照创建入口继续禁止，TASK-006 继续单独阻塞。

### 74.2 工程门禁

1. 必须新增第 147 份审计绕过样例的反向测试。
2. 必须保留 TASK-005J13 到 TASK-005J28 所有既有反向测试。
3. 必须证明 `npm run verify`、`npm audit --audit-level=high`、后端 style-profit API 定向回归通过。
4. 不得修改后端、workflow、`02_源码`、TASK-006 或运行生成物。

## 75. TASK-005J30 Spread 数组状态跟踪门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J30_Spread数组状态跟踪门禁整改_工程任务单.md`
- 前置审计：审计意见书第 148 份
- 结论：有条件通过 / 有问题
- 阻断点：`new unknownCtor(...args)` spread 主绕过已修复，但数组变量声明后的 `args[0] = ...`、`alias[0] = ...`、`args.splice(...)`、`args.unshift(...)` 等运行时变更未污染静态缓存。

### 75.1 架构决策

1. array literal map 不再只是值缓存，必须升级为数组状态跟踪。
2. 数组元素写入、别名写入、mutating method、数组逃逸都必须污染数组或 fail closed。
3. `NewExpression` spread 与 `Reflect.construct` 参数数组必须使用同一数组状态模型。
4. tainted/escaped/unknown 数组用于 Worker/unknown constructor 构造时必须 fail closed。
5. clean readonly 数组可继续静态展开，但必须证明从声明到使用点无 mutation/escape。
6. safe known constructor 的数组 spread 放行必须由集中白名单明确证明。
7. 款式利润快照创建入口继续禁止，TASK-006 继续单独阻塞。

### 75.2 工程门禁

1. 必须新增第 148 份审计绕过样例的反向测试。
2. 必须保留 TASK-005J13 到 TASK-005J29 所有既有反向测试。
3. 必须证明 `npm run verify`、`npm audit --audit-level=high`、后端 style-profit API 定向回归通过。
4. 不得修改后端、workflow、`02_源码`、TASK-006 或运行生成物。

## 76. TASK-005J31 函数调用副作用数组污染门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J31_函数调用副作用数组污染门禁整改_工程任务单.md`
- 前置审计：审计意见书第 149 份
- 结论：有条件通过 / 有问题
- 阻断点：函数声明提升和闭包副作用会让 `poison(); new Worker(...args); function poison(){ args[0] = 'data:...' }` 在运行时先污染数组，但门禁按函数体源码位置误判污染发生在 Worker 之后。

### 76.1 架构决策

1. 函数体内数组污染不再按函数体源码位置直接生效，必须通过 function side-effect summary 在调用点生效。
2. FunctionDeclaration 必须按 JS 提升语义纳入函数表。
3. 函数表达式、箭头函数、函数别名、call/apply/bind 必须应用同一副作用 summary。
4. 闭包副作用、IIFE、副作用函数返回值必须污染调用点之后的数组状态。
5. 未知函数调用、数组逃逸或无法证明无副作用时，在 style-profit surface 内必须 conservative fail closed。
6. `NewExpression` spread 与 `Reflect.construct` 参数数组继续使用同一数组状态模型。
7. 款式利润快照创建入口继续禁止，TASK-006 继续单独阻塞。

### 76.2 工程门禁

1. 必须新增第 149 份审计绕过样例的反向测试。
2. 必须保留 TASK-005J13 到 TASK-005J30 所有既有反向测试。
3. 必须证明 `npm run verify`、`npm audit --audit-level=high`、后端 style-profit API 定向回归通过。
4. 不得修改后端、workflow、`02_源码`、TASK-006 或运行生成物。

## 77. TASK-005J32 解构 Alias 数组状态跟踪门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J32_解构Alias数组状态跟踪门禁整改_工程任务单.md`
- 前置审计：审计意见书第 150 份
- 结论：有条件通过 / 有问题
- 阻断点：数组/对象解构 alias 未进入数组状态图，`const [alias] = [args]; alias[0] = 'data:...'; new Worker(...args)` 仍可绕过。

### 77.1 架构决策

1. `ArrayBindingPattern / ObjectBindingPattern` 必须进入数组状态图。
2. 解构 alias 与原数组必须共享同一 `array_id`，无法静态还原时标记 unknown/escaped 并 fail closed。
3. 解构 alias 的元素写入、mutating method、逃逸必须污染原数组。
4. 函数 side-effect summary 必须识别函数内解构 alias 污染，并按调用点生效。
5. Reflect.construct 参数数组继续使用同一数组状态图。
6. Rest/default/computed 解构无法证明安全时必须 fail closed。
7. 款式利润快照创建入口继续禁止，TASK-006 继续单独阻塞。

### 77.2 工程门禁

1. 必须新增第 150 份审计绕过样例的反向测试。
2. 必须保留 TASK-005J13 到 TASK-005J31 所有既有反向测试。
3. 必须证明 `npm run verify`、`npm audit --audit-level=high`、后端 style-profit API 定向回归通过。
4. 不得修改后端、workflow、`02_源码`、TASK-006 或运行生成物。

## 78. TASK-005J33 循环与参数解构 Alias 门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J33_循环与参数解构Alias门禁整改_工程任务单.md`
- 前置审计：审计意见书第 151 份
- 结论：有条件通过 / 有问题
- 阻断点：变量声明/赋值式解构 alias 已修复，但 `for (const alias of [args])`、`function poison([alias])`、`[[args]].forEach(([alias]) => ...)` 仍未进入数组状态图。

### 78.1 架构决策

1. BindingPattern 解析必须抽成统一入口，不得只覆盖变量声明/赋值式解构。
2. `for...of` 绑定、函数参数、箭头函数参数、函数表达式参数、回调参数必须进入数组状态图。
3. 解构 alias 与原数组必须共享同一 `array_id`，无法静态还原时必须 fail closed。
4. 函数 side-effect summary 必须识别参数解构 alias 污染，并按调用点生效。
5. 回调参数解构对静态可还原集合必须精确追踪，不可还原集合必须 fail closed。
6. Reflect.construct 参数数组继续使用同一数组状态图。
7. 款式利润快照创建入口继续禁止，TASK-006 继续单独阻塞。

### 78.2 工程门禁

1. 必须新增第 151 份审计绕过样例的反向测试。
2. 必须保留 TASK-005J13 到 TASK-005J32 所有既有反向测试。
3. 必须证明 `npm run verify`、`npm audit --audit-level=high`、后端 style-profit API 定向回归通过。
4. 不得修改后端、workflow、`02_源码`、TASK-006 或运行生成物。

## 79. TASK-005J34 同步数组迭代方法 Callback Alias 门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J34_同步数组迭代方法CallbackAlias门禁整改_工程任务单.md`
- 前置审计：审计意见书第 152 份
- 设计结论：同步数组迭代 callback sink 不得继续使用简单白名单，必须改为 method descriptor map，并明确每个方法的 current item 参数位置。

### 79.1 方法描述符口径

| 方法 | 当前元素参数位置 | 门禁要求 |
| --- | --- | --- |
| `forEach/map/some/every/filter/find/findIndex/findLast/findLastIndex/flatMap` | callback 第 1 个参数 | 参数解构 alias 必须绑定 iterable item 并进入数组状态图 |
| `reduce/reduceRight` | callback 第 2 个参数 | accumulator 不得误判为 item，currentValue 解构 alias 必须绑定 iterable item |

### 79.2 安全策略

1. `runtimeArrayIterationMethodNameSet` 必须升级为 method descriptor map。
2. descriptor 必须包含方法是否同步执行 callback、当前元素参数位置、无法静态还原时的 fail-closed 策略。
3. `reduce/reduceRight` 不得简单复用普通方法第 1 参数口径。
4. `flatMap/findIndex/findLast/findLastIndex` 必须与 `find` 同口径。
5. 未知 iterable、未知 callback、未知元素来源只要影响 tracked Worker args，必须 fail closed。
6. `new Worker(...args)` 与 `Reflect.construct(Worker, args)` 必须共用同一数组状态图。
7. TASK-006 仍需单独架构放行。

### 79.3 验收门禁

1. `reduce/reduceRight/flatMap/findIndex/findLast/findLastIndex` 的 callback 参数解构污染反向测试必须失败。
2. `reduce/reduceRight` 必须按 callback 第 2 个参数绑定 current item。
3. 未知 collection 的同步 callback 污染必须 fail closed。
4. clean readonly 同步迭代成功 fixture 不得误杀。
5. TASK-005J13-J33 已关闭绕过不得回潮。
6. 禁止开放款式利润快照创建入口。
7. 禁止进入 TASK-006。

## 80. TASK-005J35 同步数组迭代方法等价调用门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J35_同步数组迭代方法等价调用门禁整改_工程任务单.md`
- 前置审计：审计意见书第 153 份
- 设计结论：同步数组迭代方法必须建立统一调用 descriptor，direct call、`.call`、`.apply`、`Reflect.apply` 必须共用同一套 method descriptor 和 current item 参数位规则。

### 80.1 统一调用 Descriptor

| 字段 | 说明 |
| --- | --- |
| `methodName` | 同步数组迭代方法名 |
| `iterableExpression` | 被迭代对象，direct call receiver 或 call/apply thisArg |
| `callbackExpression` | 同步执行的 callback |
| `initialValueExpression` | `reduce/reduceRight` 的 initialValue |
| `argumentMode` | `direct/call/apply/reflect_apply` |

### 80.2 安全策略

1. `Array.prototype.reduce.call(...)` 必须还原为 `methodName=reduce`，第 1 实参作为 iterable，第 2 实参作为 callback，第 3 实参作为 initialValue。
2. `Array.prototype.reduce.apply(...)` 必须静态展开 argsArray，无法展开时 fail closed。
3. `Reflect.apply(Array.prototype.reduce, thisArg, argsArray)` 必须还原 target、thisArg 和 argsArray，无法还原时 fail closed。
4. `reduce/reduceRight` 在所有调用模式下 current item 参数位置仍为 1。
5. `flatMap/findIndex/findLast/findLastIndex` 在所有调用模式下 current item 参数位置仍为 0。
6. 字面量 bracket key 如 `Array.prototype['reduce']` 必须归一；非字面量 key 无法证明安全时 fail closed。
7. TASK-006 仍需单独架构放行。

### 80.3 验收门禁

1. `.call/.apply/Reflect.apply` 等价调用污染反向测试必须失败。
2. `.apply/Reflect.apply` 参数数组无法静态展开时必须 fail closed。
3. method target 无法静态还原时必须 fail closed。
4. clean readonly 等价调用成功 fixture 不得误杀。
5. TASK-005J13-J34 已关闭绕过不得回潮。
6. 禁止开放款式利润快照创建入口。
7. 禁止进入 TASK-006。

## 81. TASK-005J36 同步数组迭代方法 Bind 与 CallCall 门禁整改

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J36_同步数组迭代方法Bind与CallCall门禁整改_工程任务单.md`
- 前置审计：审计意见书第 154 份
- 设计结论：同步数组迭代方法的 `bind` 预绑定与 `call.call` 中转必须纳入统一 `IterationInvocationDescriptor`，不得在 direct/call/apply/Reflect.apply 主路径之外形成旁路。

### 81.1 统一 Descriptor 范围

| 入口 | 处理要求 |
| --- | --- |
| direct call | 已由 J35 descriptor 覆盖，必须保留 |
| `.call` | 已由 J35 descriptor 覆盖，必须保留 |
| `.apply` | 已由 J35 descriptor 覆盖，必须保留 |
| `Reflect.apply` | 已由 J35 descriptor 覆盖，必须保留 |
| `.bind` | 必须记录 bound function summary，并在调用点合并 boundArgs 与 laterArgs |
| `call.call` | 必须还原为目标方法的 `.call(thisArg, ...args)` |

### 81.2 安全策略

1. `Array.prototype.<method>.bind(thisArg, ...boundArgs)` 必须生成 bound iteration function summary。
2. bound summary 必须记录 `methodName`、bound thisArg、bound callback、bound initialValue。
3. bound function 调用时必须合并 boundArgs 与 laterArgs 后再进入 method descriptor。
4. `Function.prototype.call.call(Array.prototype.<method>, thisArg, ...args)` 必须还原为目标 method 的 call descriptor。
5. `Array.prototype.<method>.call.call(Array.prototype.<method>, thisArg, ...args)` 必须同口径处理。
6. target、thisArg、boundArgs、laterArgs、call.call 参数无法静态还原时必须 fail closed。
7. TASK-006 仍需单独架构放行。

### 81.3 验收门禁

1. `Array.prototype.reduce.bind([[args]])(...)` 污染反向测试必须失败。
2. `Array.prototype.reduce.bind([[args]], callback, 0)()` 污染反向测试必须失败。
3. `Function.prototype.call.call(Array.prototype.reduce, ...)` 污染反向测试必须失败。
4. `Array.prototype.reduce.call.call(Array.prototype.reduce, ...)` 污染反向测试必须失败。
5. `findIndex/flatMap/reduceRight` 的 bind/call.call 等价入口必须同口径失败。
6. clean readonly bind/call.call 成功 fixture 不得误杀。
7. TASK-005J13-J35 已关闭绕过不得回潮。
8. 禁止开放款式利润快照创建入口。
9. 禁止进入 TASK-006。

## 82. TASK-005J37 J36 最小范围本地基线提交

- 任务单：`/03_需求与设计/02_开发计划/TASK-005J37_J36最小范围本地基线提交_工程任务单.md`
- 前置审计：审计意见书第 155 份，TASK-005J36 通过
- 设计结论：J36 通过后不继续扩大门禁整改范围，先做 3 个白名单文件的本地基线提交，形成稳定审计基线。

### 82.1 白名单提交范围

只允许提交:

1. `06_前端/lingyi-pc/scripts/check-style-profit-contracts.mjs`
2. `06_前端/lingyi-pc/scripts/test-style-profit-contracts.mjs`
3. `03_需求与设计/02_开发计划/TASK-005J36_同步数组迭代方法Bind与CallCall门禁整改证据.md`

### 82.2 禁止范围

1. 禁止提交 `06_前端/lingyi-pc/src/App.vue`，审计临时探针必须保持还原。
2. 禁止提交 `07_后端/**`。
3. 禁止提交 `.github/**`。
4. 禁止提交 `02_源码/**`。
5. 禁止提交 `TASK-006*`。
6. 禁止提交审计记录文件和运行生成物。
7. 禁止 `git add .` 或 `git add -A`。

### 82.3 验收门禁

1. `git diff --cached --name-only` 只能包含 3 个白名单文件。
2. `git diff -- 06_前端/lingyi-pc/src/App.vue` 必须为空。
3. 证据文件必须记录验证命令、场景数、后端回归、commit hash 和文件清单。
4. commit 后不得自动解锁 TASK-006。

## 83. TASK-005K 款式利润模块封版盘点

- 任务单：`/03_需求与设计/02_开发计划/TASK-005K_款式利润模块封版盘点_工程任务单.md`
- 前置审计：审计意见书第 156 份，TASK-005J37 通过
- 前置基线：`f0fc6c92e46354eeb44add119267359ea919a74e`
- 设计结论：J36/J37 当前本地基线已通过审计，下一步不再继续扩大门禁整改范围，先做 TASK-005 全链路封版盘点，判断是否具备本地封版复审条件。

### 83.1 封版盘点目标

1. 汇总 TASK-005A 至 TASK-005J37 的任务单、设计、ADR、审计意见和本地提交证据。
2. 核验前端只读契约门禁、后端 style-profit API 回归、PostgreSQL 财务门禁证据和本地 git 基线。
3. 输出 `TASK-005K_款式利润模块封版盘点证据.md`。
4. 给出“建议 / 不建议 TASK-005 进入本地封版复审”的结论。
5. 明确 TASK-006 仍需单独架构任务单和审计放行。

### 83.2 允许范围

只允许创建或更新：

1. `/03_需求与设计/02_开发计划/TASK-005K_款式利润模块封版盘点证据.md`

### 83.3 禁止范围

1. 禁止修改 `06_前端/**`。
2. 禁止修改 `07_后端/**`。
3. 禁止修改 `.github/**`。
4. 禁止修改 `02_源码/**`。
5. 禁止修改 `TASK-006*`。
6. 禁止开放款式利润快照创建入口。
7. 禁止把 TASK-005K 通过解释为 TASK-006 放行。

### 83.4 封版判定口径

1. 审计意见书第 156 份必须为通过。
2. commit `f0fc6c92e46354eeb44add119267359ea919a74e` 必须保持 J37 白名单范围。
3. 前端 `check:style-profit-contracts`、`test:style-profit-contracts`、`verify`、`npm audit --audit-level=high` 必须通过。
4. 后端 style-profit API 定向回归和 `py_compile` 必须通过。
5. PostgreSQL 双 JUnit 真实非 skip 证据必须存在；如本轮不能重跑，必须明确引用已审计通过的 F8/F9 证据，不得伪造。
6. `App.vue` 不得有临时探针残留。
7. `.pytest-postgresql-*.xml` 不得被暂存或提交。
8. TASK-006 继续阻塞。

## 84. TASK-005K1 K 证据本地基线提交

- 任务单：`/03_需求与设计/02_开发计划/TASK-005K1_K证据本地基线提交_工程任务单.md`
- 前置审计：审计意见书第 157 份，TASK-005K 通过
- 设计结论：TASK-005K 已允许进入 TASK-005 本地封版复审，但 K 证据文件仍未跟踪；在封版复审前，必须先用白名单 docs-only commit 将 K 证据纳入本地基线。

### 84.1 白名单提交范围

只允许提交：

1. `/03_需求与设计/02_开发计划/TASK-005K_款式利润模块封版盘点证据.md`

### 84.2 禁止范围

1. 禁止提交 `06_前端/**`。
2. 禁止提交 `07_后端/**`。
3. 禁止提交 `.github/**`。
4. 禁止提交 `02_源码/**`。
5. 禁止提交 `TASK-006*`。
6. 禁止提交审计记录。
7. 禁止提交 `.pytest-postgresql-*.xml`。
8. 禁止提交历史未跟踪目录。
9. 禁止使用 `git add .` 或 `git add -A`。

### 84.3 验收门禁

1. `git diff --cached --name-only` 只能包含 K 证据文件。
2. commit message 建议为 `docs: add task 005K closeout evidence`。
3. `git show --name-only HEAD` 只能包含 K 证据文件。
4. `git diff -- 06_前端/lingyi-pc/src/App.vue` 必须为空。
5. TASK-006 继续阻塞。

## 85. TASK-005L TASK-005 本地封版复审

- 任务单：`/03_需求与设计/02_开发计划/TASK-005L_TASK-005本地封版复审_工程任务单.md`
- 前置审计：审计意见书第 158 份，TASK-005K1 通过
- 前置基线：`1da795333d20ed8ecfb2308da623358668272458`
- 设计结论：K 证据已纳入本地基线，可以进入 TASK-005 本地封版复审；本复审只判断 TASK-005 是否可标记为“本地封版完成”，不代表生产发布，不代表平台 required-check 闭环，不自动解锁 TASK-006。

### 85.1 复审范围

1. TASK-005A/B/C/D/E/F/G/H/I/J/K/K1 全链路证据。
2. 利润口径、来源映射、快照计算、权限审计、真实来源 Adapter、PostgreSQL 财务门禁、前端只读联调和只读契约门禁。
3. 当前 HEAD、K1 commit、运行产物、历史未跟踪文件和禁改范围。
4. TASK-006 阻塞状态。

### 85.2 允许范围

只允许创建或更新：

1. `/03_需求与设计/02_开发计划/TASK-005L_TASK-005本地封版复审证据.md`

### 85.3 禁止范围

1. 禁止修改 `06_前端/**`。
2. 禁止修改 `07_后端/**`。
3. 禁止修改 `.github/**`。
4. 禁止修改 `02_源码/**`。
5. 禁止修改数据库迁移文件。
6. 禁止修改 `TASK-006*`。
7. 禁止提交 `.pytest-postgresql-*.xml`。
8. 禁止开放款式利润快照创建入口。
9. 禁止把本地封版写成生产发布。
10. 禁止把 TASK-005L 通过解释为 TASK-006 放行。

### 85.4 封版判定口径

只有同时满足以下条件，才允许建议 TASK-005 标记为本地封版完成：

1. 审计意见书第 158 份为通过。
2. K 证据已纳入 commit `1da795333d20ed8ecfb2308da623358668272458`。
3. 前端只读门禁全部通过。
4. 后端 style-profit 回归全部通过。
5. PostgreSQL 双 JUnit 证据存在且未伪造。
6. 未发现创建快照入口回潮。
7. 未发现 TASK-006 文件变更或实现入口。
8. 剩余风险均已列明。

## 86. TASK-005L1 TASK-005 本地封版复审证据审计

- 任务单：`/03_需求与设计/02_开发计划/TASK-005L1_TASK-005本地封版复审证据审计_任务单.md`
- 前置任务：TASK-005L 本地封版复审证据已输出
- 前置证据：`/03_需求与设计/02_开发计划/TASK-005L_TASK-005本地封版复审证据.md`
- 设计结论：TASK-005L 已建议 TASK-005 标记为本地封版完成，但该建议必须先经审计官复核；审计通过后，才允许架构师输出 TASK-005 本地封版完成记录。

### 86.1 审计范围

1. TASK-005L 证据文件完整性。
2. 当前 HEAD 与 K1 commit 基线。
3. 前端只读门禁、后端回归、PostgreSQL 证据披露。
4. 禁改范围、运行产物、`App.vue` 探针残留。
5. TASK-006 阻塞状态。

### 86.2 审计结论口径

审计结论只能是：

1. 通过：允许 TASK-005 标记为本地封版完成；TASK-006 继续阻塞。
2. 有条件通过：修复问题前不得标记 TASK-005 本地封版完成；TASK-006 继续阻塞。
3. 不通过：TASK-005 不得封版；TASK-006 继续阻塞。

### 86.3 禁止范围

1. 禁止把 TASK-005L 证据直接当作最终封版结论。
2. 禁止把本地封版写成生产发布。
3. 禁止把本地封版写成平台 required-check 闭环。
4. 禁止把 TASK-005L1 通过解释为 TASK-006 放行。

## 87. 版本记录

| 版本 | 更新时间 | 作者 | 说明 |
| --- | --- | --- | --- |
| V6.18 | 2026-04-15 15:43 CST | 技术架构师 | 根据 TASK-005L 工程侧封版复审证据，新增 TASK-005L1 审计复核口径，要求审计通过后才允许标记 TASK-005 本地封版完成，TASK-006 继续阻塞 |
| V6.17 | 2026-04-15 15:09 CST | 技术架构师 | 根据审计意见书第 158 份，新增 TASK-005L 本地封版复审口径，要求最终核验 TASK-005 全链路证据并判定是否建议本地封版完成，TASK-006 继续阻塞 |
| V6.16 | 2026-04-15 15:00 CST | 技术架构师 | 根据审计意见书第 157 份，新增 TASK-005K1 K 证据本地基线提交口径，要求仅提交 TASK-005K 封版盘点证据文件，TASK-006 继续阻塞 |
| V6.15 | 2026-04-15 14:42 CST | 技术架构师 | 根据审计意见书第 156 份，新增 TASK-005K 款式利润模块封版盘点口径，要求汇总 TASK-005 全链路证据并判定是否建议本地封版复审，TASK-006 继续阻塞 |
| V6.14 | 2026-04-15 14:15 CST | 技术架构师 | 根据审计意见书第 155 份，新增 TASK-005J37 J36 最小范围本地基线提交口径，限定只提交 J36 三个白名单文件 |
| V6.13 | 2026-04-15 13:33 CST | 技术架构师 | 根据审计意见书第 154 份，新增 TASK-005J36 同步数组迭代方法 Bind 与 CallCall 门禁整改口径，要求 bind 预绑定和 call.call 中转统一进入 IterationInvocationDescriptor |
| V6.12 | 2026-04-15 12:48 CST | 技术架构师 | 根据审计意见书第 153 份，新增 TASK-005J35 同步数组迭代方法等价调用门禁整改口径，要求 direct call、call、apply、Reflect.apply 共用统一数组迭代调用 descriptor |
| V6.11 | 2026-04-15 12:00 CST | 技术架构师 | 根据审计意见书第 152 份，新增 TASK-005J34 同步数组迭代方法 Callback Alias 门禁整改口径，要求从简单白名单升级为 method descriptor map 并补齐 reduce/reduceRight/flatMap/findIndex/findLast/findLastIndex |
| V6.10 | 2026-04-15 11:16 CST | 技术架构师 | 根据审计意见书第 151 份，新增 TASK-005J33 循环与参数解构 Alias 门禁整改口径，要求 for...of、函数参数、箭头函数参数和回调参数解构进入数组状态图 |
| V6.9 | 2026-04-15 10:38 CST | 技术架构师 | 根据审计意见书第 150 份，新增 TASK-005J32 解构 Alias 数组状态跟踪门禁整改口径，要求 ArrayBindingPattern/ObjectBindingPattern 进入数组状态图，解构 alias 写入污染原数组 |
| V6.8 | 2026-04-15 10:07 CST | 技术架构师 | 根据审计意见书第 149 份，新增 TASK-005J31 函数调用副作用数组污染门禁整改口径，要求函数声明提升、闭包副作用、函数别名和 call/apply/bind 按调用点污染数组 |
| V6.7 | 2026-04-15 09:27 CST | 技术架构师 | 根据审计意见书第 148 份，新增 TASK-005J30 Spread 数组状态跟踪门禁整改口径，要求数组从值缓存升级为状态跟踪，写入、别名、mutating method 和逃逸必须污染或 fail closed |
| V6.6 | 2026-04-15 09:05 CST | 技术架构师 | 根据审计意见书第 147 份，新增 TASK-005J29 NewExpression Spread 参数归一化门禁整改口径，要求 new 与 Reflect.construct 参数归一化同口径，SpreadElement 先静态展开或 fail closed |
| V6.5 | 2026-04-15 08:42 CST | 技术架构师 | 根据审计意见书第 146 份，新增 TASK-005J28 Unknown Constructor 字符串 URL 严格门禁整改口径，要求 unknown constructor 不再放行普通字符串 Worker URL，并与 known Worker URL 规则同口径 |
| V6.4 | 2026-04-15 08:11 CST | 技术架构师 | 根据审计意见书第 145 份，新增 TASK-005J27 动态构造器返回值门禁整改口径，要求将 new、Reflect.construct、bind、函数返回和 IIFE 统一收敛到 constructor invocation descriptor |
| V6.3 | 2026-04-15 07:44 CST | 技术架构师 | 根据审计意见书第 144 份，新增 TASK-005J26 Worker 等价构造入口门禁整改口径，要求将 Worker 构造 sink 统一到 constructor descriptor 层，覆盖 Worker.bind 与 Reflect.construct 等等价入口 |
| V6.2 | 2026-04-15 07:22 CST | 技术架构师 | 根据审计意见书第 143 份，新增 TASK-005J25 Worker 构造器别名门禁整改口径，要求将 Worker/SharedWorker 构造器识别接入统一 alias/namespace/conditional alias 解析体系 |
| V6.1 | 2026-04-15 06:58 CST | 技术架构师 | 根据审计意见书第 142 份，新增 TASK-005J24 Blob URL 与 Worker 加载门禁整改口径，要求收口 URL.createObjectURL call/apply/别名、Blob URL 传播、Worker/SharedWorker 高危 URL 与 script src 高危 URL |
| V6.0 | 2026-04-15 01:18 CST | 技术架构师 | 根据审计意见书第 141 份，新增 TASK-005J23 动态模块加载禁用门禁整改口径，要求禁用 data/blob/http/javascript import、变量动态 import、Blob URL module loading 与无法静态证明安全的动态 import |
| V5.9 | 2026-04-15 00:56 CST | 技术架构师 | 根据审计意见书第 140 份，新增 TASK-005J22 Timer call/apply 等价调用门禁整改口径，要求把 timer codegen 接入统一 call descriptor 与参数归一逻辑 |
| V5.8 | 2026-04-15 00:30 CST | 技术架构师 | 根据审计意见书第 139 份，新增 TASK-005J21 字符串定时器与 Constructor 链禁用门禁整改口径，要求禁用字符串 setTimeout/setInterval 与 .constructor/.constructor.constructor 派生 Function 链 |
| V5.7 | 2026-04-15 00:01 CST | 技术架构师 | 根据审计意见书第 138 份，新增 TASK-005J20 运行时代码生成入口禁用门禁整改口径，要求禁用 eval、Function、new Function、globalThis/window Function/eval 及其别名、动态成员和间接调用 |
| V5.6 | 2026-04-14 23:39 CST | 技术架构师 | 根据审计意见书第 137 份，新增 TASK-005J19 动态成员名与源引用隐藏门禁整改口径，要求检测字符串拼接、模板字面量、globalThis/window 多级 element access、Reflect.get 和 optional chain 绕过 |
| V5.5 | 2026-04-14 23:16 CST | 技术架构师 | 根据审计意见书第 136 份，新增 TASK-005J18 Runtime Mutator 源引用禁用门禁收口口径，要求从逐语法追踪升级为源引用禁用，覆盖函数返回、IIFE、内联/嵌套/包装/条件容器绕过 |
| V5.4 | 2026-04-14 22:55 CST | 技术架构师 | 根据审计意见书第 135 份，新增 TASK-005J17 Runtime Mutator 中转调用门禁整改口径，要求检测逗号表达式 callee、条件表达式命名空间、数组容器中转和对象容器中转绕过 |
| V5.3 | 2026-04-14 22:36 CST | 技术架构师 | 根据审计意见书第 134 份，新增 TASK-005J16 Runtime Mutator Sink 变体门禁整改口径，要求检测 Reflect.apply、字符串/计算属性解构、globalThis/window 解构命名空间绕过 |
| V5.2 | 2026-04-14 22:12 CST | 技术架构师 | 根据审计意见书第 133 份，新增 TASK-005J15 Runtime Mutator 高级别名门禁整改口径，要求检测赋值式解构、bind/call/apply、globalThis/window Object/Reflect 命名空间绕过 |
| V5.1 | 2026-04-14 21:57 CST | 技术架构师 | 根据审计意见书第 132 份，新增 TASK-005J14 解构与命名空间别名门禁整改口径，要求检测 Object/Reflect 解构别名、解构重命名和命名空间别名绕过 |
| V5.0 | 2026-04-14 21:38 CST | 技术架构师 | 根据审计意见书第 131 份，新增 TASK-005J13 运行时 ActionKey 等价语法门禁整改口径，要求检测 Object['defineProperty']、Reflect['set']、Object['assign']、本地别名调用和 Object.assign 变量 source 合并绕过 |
| V4.9 | 2026-04-14 21:15 CST | 技术架构师 | 根据审计意见书第 130 份，新增 TASK-005J12 运行时显式 Action Key 注入门禁整改口径，要求检测 item['onClick']、item.onClick、Object.defineProperty、Object.assign 等显式 action key 注入 |
| V4.8 | 2026-04-14 21:01 CST | 技术架构师 | 根据审计意见书第 129 份，新增 TASK-005J11 运行时动态属性注入门禁整改口径，要求检测 ElementAccessExpression 左值赋值、Object.defineProperty、Reflect.set 等动态写入路径 |
| V4.7 | 2026-04-14 20:45 CST | 技术架构师 | 根据审计意见书第 128 份，新增 TASK-005J10 动态 computed key 全域 fail closed 口径，移除对 actions/menu/button 等容器名的依赖 |
| V4.6 | 2026-04-14 20:22 CST | 技术架构师 | 根据审计意见书第 127 份，新增 TASK-005J9 Style Profit 契约门禁 AST 化 computed key 收口口径，停止继续叠正则补丁并使用 TypeScript AST 解析对象成员 |
| V4.5 | 2026-04-14 20:07 CST | 技术架构师 | 根据审计意见书第 126 份，新增 TASK-005J8 跨行非字面量计算属性 Action 键门禁整改口径，要求捕获跨行 `[...]` 表达式并禁止动态 action key |
| V4.4 | 2026-04-14 19:51 CST | 技术架构师 | 根据审计意见书第 125 份，新增 TASK-005J7 非字面量计算属性 Action 键禁用门禁口径，明确 style-profit 前端禁止 [ACTION_KEY] 等动态 action key |
| V4.3 | 2026-04-14 19:35 CST | 技术架构师 | 根据审计意见书第 124 份，新增 TASK-005J6 计算属性键交互字段门禁整改口径，要求识别 ['onClick'] / ["handler"]() / ['submit']() 等合法 JS 计算属性键 |
| V4.2 | 2026-04-14 19:22 CST | 技术架构师 | 根据审计意见书第 123 份，新增 TASK-005J5 对象方法简写交互字段门禁整改口径，要求识别 onClick()/handler()/submit() 等方法简写 |
| V4.1 | 2026-04-14 19:07 CST | 技术架构师 | 根据审计意见书第 122 份，新增 TASK-005J4 JSON 风格引号键与真实长距离 fixture 门禁整改口径，要求识别双引号/单引号对象键并用真实 1200 字符距离覆盖 |
| V4.0 | 2026-04-14 18:48 CST | 技术架构师 | 根据审计意见书第 121 份，新增 TASK-005J3 Action 祖先对象链门禁整改口径，要求检查子 meta/props/extra 利润说明文案的全部祖先 action 对象 |
| V3.9 | 2026-04-14 18:28 CST | 技术架构师 | 根据审计意见书第 120 份，新增 TASK-005J2 Action 对象级交互上下文门禁整改口径，要求长 action 配置不再依赖固定窗口判断 |
| V3.8 | 2026-04-14 18:14 CST | 技术架构师 | 根据审计意见书第 119 份，新增 TASK-005J1 多行交互上下文门禁整改口径，要求识别跨行按钮、菜单和 action 配置中的只读说明文案写入口绕过 |
| V3.7 | 2026-04-14 18:02 CST | 技术架构师 | 根据审计意见书第 118 份，新增 TASK-005J 只读说明文案防误杀与门禁基线口径，要求用成功 fixture 保护合法说明文案，禁止恢复 substring 白名单 |
| V3.6 | 2026-04-14 17:52 CST | 技术架构师 | 根据审计意见书第 117 份，新增 TASK-005I1 中文语义白名单绕过整改口径，要求 shouldIgnoreSemanticMatch 不再按 substring 豁免写动作组合，并补三条审计复现反向测试 |
| V3.5 | 2026-04-14 17:39 CST | 技术架构师 | 根据审计意见书第 116 份，新增 TASK-005I 中文泛化写入口门禁口径，要求拦截“款式利润”等领域词与创建/生成/重算/核算等写动作词组合，继续禁止创建快照入口和 TASK-006 |
| V3.4 | 2026-04-14 17:22 CST | 技术架构师 | 根据审计意见书第 115 份，新增 TASK-005H 全局只读边界门禁收口口径，要求 style-profit 禁线扫描覆盖全局导航、公共页面、store 和其他视图，继续禁止创建快照入口和 TASK-006 |
| V3.3 | 2026-04-14 17:02 CST | 技术架构师 | 根据审计意见书第 114 份，新增 TASK-005G 款式利润前端只读联调口径，限定只接入列表/详情 GET 接口并补前端契约门禁，继续禁止创建快照入口和 TASK-006 |
| V3.2 | 2026-04-14 16:50 CST | 技术架构师 | 根据审计意见书第 113 份，新增 TASK-005F11 F9 证据格式修正口径，要求 docs-only 清理孤立字符、乱码和缺失命令/JUnit 字段 |
| V3.1 | 2026-04-14 16:40 CST | 技术架构师 | 根据审计意见书第 112 份，新增 TASK-005F10 F9 证据 docs-only 补提交口径，要求仅提交 F9 证据和架构/审计文档，禁止带入后端、前端、workflow、运行时 JUnit 或历史大目录 |
| V3.0 | 2026-04-14 16:26 CST | 技术架构师 | 根据审计意见书第 111 份，新增 TASK-005F9 款式利润 F 阶段本地基线提交口径，要求白名单提交 F~F8 关键代码、迁移、测试、门禁和证据，禁止纳入运行时 JUnit 与历史大目录 |
| V2.9 | 2026-04-14 16:20 CST | 技术架构师 | 根据审计意见书第 110 份，新增 TASK-005F8 PostgreSQL 外发 schema 基线修复口径，确认 TASK-005F2 桥接字段为外发基础 schema 并要求真实 PG 双门禁复跑通过 |
| V2.8 | 2026-04-14 16:05 CST | 技术架构师 | 根据审计意见书第 109 份，新增 TASK-005F7 PostgreSQL 真实非 skip 证据闭环口径，要求一次性测试库跑出 settlement/style-profit 两份 JUnit 的 4 passed / 0 skipped 证据 |
| V2.7 | 2026-04-14 15:55 CST | 技术架构师 | 根据审计意见书第 108 份，新增 TASK-005F6 Workflow 双 JUnit 上传与 PostgreSQL 证据回填口径，要求 workflow 上传 settlement/style-profit 两份 JUnit 且非 skip 证据不得伪装 |
| V2.6 | 2026-04-14 15:40 CST | 技术架构师 | 根据审计意见书第 107 份，新增 TASK-005F5 PostgreSQL 门禁目标恢复与双 JUnit 断言口径，要求保留 TASK-002H 外发结算门禁且新增款式利润门禁不得覆盖旧门禁 |
| V2.5 | 2026-04-14 15:15 CST | 技术架构师 | 根据审计意见书第 106 份，新增 TASK-005F4 外发来源查询下推与 PostgreSQL 证据口径，要求把 inspected_at 期间过滤下推到数据库并补非 skip 门禁 |
| V2.4 | 2026-04-14 14:55 CST | 技术架构师 | 根据审计意见书第 105 份，新增 TASK-005F3 Work Order 严格匹配与补数审计整改口径，冻结传入 work_order 时外发成本必须严格匹配工单 |
| V2.3 | 2026-04-14 14:15 CST | 技术架构师 | 根据审计意见书第 104 份，新增 TASK-005F2 外发利润归属桥接字段与补数口径，要求通过字段、迁移和安全补数让外发成本具备可信入账条件 |
| V2.2 | 2026-04-14 13:50 CST | 技术架构师 | 根据审计意见书第 103 份，新增 TASK-005F1 整改口径，冻结 SLE 入账必须同时满足 BOM 材料范围和销售/生产归属桥接，并要求外发来源不得静默空返回 |
| V2.1 | 2026-04-14 13:20 CST | 技术架构师 | 根据 TASK-005E4 审计通过结论，新增 TASK-005F 真实服务端来源 Adapter 口径，要求先实现可信来源采集，再评估前端创建入口 |
| V2.0 | 2026-04-14 13:07 CST | 技术架构师 | 根据 TASK-005E3 审计通过结论，新增 TASK-005E4 本地仓库基线提交口径，要求 API 代码、测试、任务文档和审计记录入库后再进入前端联调或真实来源 adapter |
| V1.9 | 2026-04-14 12:58 CST | 技术架构师 | 根据 TASK-005E2 复审意见，新增 TASK-005E3 详情接口鉴权前置整改口径，要求无读权限用户无法通过 403/404 差异枚举利润快照存在性 |
| V1.8 | 2026-04-14 12:34 CST | 技术架构师 | 根据审计意见书第 99 份，新增 TASK-005E2 阻断整改口径，要求 collector fail closed、权限矩阵回归冻结口径、鉴权顺序优先、早期失败审计补齐 |
| V1.7 | 2026-04-14 11:50 CST | 技术架构师 | 新增 TASK-005E1 API 实现预派发口径，避免审计通过后空转，但明确 TASK-005E 任务单审计未通过前不得执行 |
| V1.6 | 2026-04-14 11:42 CST | 技术架构师 | 根据 TASK-005D6 审计通过结论，新增 TASK-005E API 权限与审计基线口径，要求任务单先审计，且明确鉴权、资源权限、安全/操作审计、统一错误信封和 PostgreSQL 门禁 |
| V1.5 | 2026-04-14 11:33 CST | 技术架构师 | 根据审计意见书第 97 份，新增 TASK-005D6 文档基线门禁，要求 D5 任务单和 D6 文档更新入库后再进入 TASK-005E/API |
| V1.4 | 2026-04-14 11:25 CST | 技术架构师 | 根据审计意见书第 96 份，新增 TASK-005D5 docs-only 证据修正口径，要求把 D4 证据提交后 HEAD 占位符修正为真实 commit 后再进入 API |
| V1.3 | 2026-04-14 11:08 CST | 技术架构师 | 根据审计意见书第 95 份，新增 TASK-005D4 本地仓库基线提交口径，要求进入 API 前先白名单提交 D~D3 服务、测试、文档和审计记录 |
| V1.2 | 2026-04-14 10:56 CST | 技术架构师 | 根据审计意见书第 94 份，新增 TASK-005D3 整改口径，明确 workshop/subcontract 缺 company 或缺 item_code 时必须 fail closed，不得仅凭 sales_order 纳入利润 |
| V1.1 | 2026-04-14 10:36 CST | 技术架构师 | 根据审计意见书第 93 份，新增 TASK-005D2 整改口径，修复 workshop/subcontract 实际成本归属、create_snapshot 保存点回滚兜底，并固化 V1 sales_order 强制非空 |
| V1.0 | 2026-04-14 10:15 CST | 技术架构师 | 根据审计意见书第 92 份，新增 TASK-005D1 整改口径，修复 request_hash 来源输入、收入 unresolved、异常 SLE、缺工价、收入 source_status 和 idempotency_key 长度校验 |
| V0.9 | 2026-04-14 07:59 CST | 技术架构师 | 根据 TASK-005C4 审计通过结论，新增 TASK-005D 利润快照计算服务边界，限定只做后端服务与测试，不注册 API、不改前端、不新增迁移、不进入 TASK-006 |
| V0.8 | 2026-04-14 07:21 CST | 技术架构师 | 根据 TASK-005C3 审计意见，新增 TASK-005C4 本地仓库基线提交口径，要求利润模型、服务、迁移、测试和文档白名单纳入本地 commit |
| V0.7 | 2026-04-14 06:58 CST | 技术架构师 | 根据 TASK-005C2 审计意见，新增 TASK-005C3 整改口径，要求补齐利润快照期间查询索引 `company + item_code + from_date + to_date` |
| V0.6 | 2026-04-14 01:59 CST | 技术架构师 | 根据 TASK-005C1 审计意见，新增 TASK-005C2 整改口径，冻结 include_in_profit 默认 false、source_map 字段契约、复合索引和 status-only 白名单 |
| V0.5 | 2026-04-14 01:28 CST | 技术架构师 | 根据 TASK-005C 审计意见，新增 TASK-005C1 整改口径，冻结来源状态 fail closed、source_map 审计字段、snapshot 复核字段、费用分摊默认 disabled 和 SLE 材料编码语义 |
| V0.4 | 2026-04-14 00:30 CST | 技术架构师 | 根据审计意见书第 85 份，新增 TASK-005C 模型、迁移与来源映射设计边界，冻结利润表结构和 ERPNext/FastAPI 来源映射规则 |
| V0.3 | 2026-04-14 00:11 CST | 技术架构师 | 根据审计意见书第 83 份，新增 TASK-005B 利润口径设计冻结，冻结收入、材料、工票、外发、费用分摊、快照、幂等、权限与审计口径 |
| V0.2 | 2026-04-13 23:29 CST | 技术架构师 | 根据审计意见书第 80 份，新增 TASK-005A1 文档证据链补提交口径，明确证据入库不等于 TASK-005B 放行 |
| V0.1 | 2026-04-13 23:14 CST | 技术架构师 | 初版款式利润报表模块设计，冻结 TASK-005A 仅做开发前基线盘点，不允许工程实现 |
