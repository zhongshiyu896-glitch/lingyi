# 技术决策记录

- 模块名：技术决策记录
- 版本：V1.108
- 更新时间：2026-04-14 16:40 CST
- 作者：技术架构师

## ADR-001：BOM 模块唯一契约冻结

- 决策时间：2026-04-11 21:55 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001 BOM 管理
- 审计来源：审计意见书第 3 份，BOM 模块契约不一致
- 结论：采用 `ly_apparel_* + /api/bom/` 作为 BOM 模块唯一契约

### 背景

审计发现 BOM 模块存在两套契约：

1. 模块设计旧版使用 `ly_style / ly_style_bom / ly_style_bom_item / ly_style_bom_version`，接口为 `/api/bom/styles`、`/api/bom/style-boms`。
2. 当前工程实现和任务卡使用 `ly_apparel_bom / ly_apparel_bom_item / ly_bom_operation`，接口为 `/api/bom/`。

该冲突会导致外发加工、生产计划、工票和款式利润模块无法稳定引用 BOM 地基。

### 决策

从本决策生效起，BOM 模块唯一契约如下：

| 类型 | 标准契约 |
| --- | --- |
| BOM 主表 | `ly_schema.ly_apparel_bom` |
| BOM 物料明细表 | `ly_schema.ly_apparel_bom_item` |
| BOM 工序明细表 | `ly_schema.ly_bom_operation` |
| 款式事实源 | ERPNext `public.tabItem` |
| API 前缀 | `/api/bom/` |
| 详情接口 | `GET /api/bom/{bom_id}` |
| 更新接口 | `PUT /api/bom/{bom_id}` |
| 默认接口 | `POST /api/bom/{bom_id}/set-default` |
| 发布接口 | `POST /api/bom/{bom_id}/activate` |
| 展开接口 | `POST /api/bom/{bom_id}/explode` |

废弃以下契约，不允许新开发继续引用：

1. `ly_schema.ly_style`
2. `ly_schema.ly_style_bom`
3. `ly_schema.ly_style_bom_item`
4. `ly_schema.ly_style_bom_version`
5. `/api/bom/styles`
6. `/api/bom/style-boms`

### 约束

1. 款式资料只引用 ERPNext `Item`，不在 FastAPI 自建款式事实表。
2. 外发加工、生产计划、工票和款式利润引用 BOM 时，只能使用 `ly_apparel_bom.id`。
3. 同一 `item_code` 只能有一个 `status='active' AND is_default=true` 的 BOM。
4. PostgreSQL 必须增加部分唯一索引：`UNIQUE(item_code) WHERE is_default = true AND status = 'active'`。
5. `set-default` 必须使用事务和行级锁，防止并发产生多个默认 BOM。

### 影响

1. `01_模块设计_BOM管理.md` 已更新到 V1.1。
2. `当前 sprint 任务清单.md` 与 `TASK-001_BOM管理_工程任务单.md` 保持 `ly_apparel_* + /api/bom/`。
3. 如工程代码中仍存在 `ly_style_*` 迁移或接口，工程师必须迁移或废弃。

### 验收

1. 设计目录内不再出现 `/api/bom/styles` 或 `/api/bom/style-boms` 作为有效新契约。
2. BOM 模块设计、任务单、代码实现均引用 `ly_apparel_bom / ly_apparel_bom_item / ly_bom_operation`。
3. 审计复查时，BOM 契约不一致问题应关闭。


## 版本记录

| 版本 | 更新时间 | 作者 | 说明 |
| --- | --- | --- | --- |
| V1.0 | 2026-04-11 21:55 CST | 技术架构师 | 新增 ADR-001，冻结 BOM 模块唯一契约 |
| V1.5 | 2026-04-12 10:23 CST | 技术架构师 | 新增 ADR-005，明确 ERPNext User Permission 查询失败必须 fail closed |
| V1.6 | 2026-04-12 10:35 CST | 技术架构师 | 新增 ADR-006，明确 BOM 401/403/503 权限拒绝必须形成安全审计闭环 |
| V1.7 | 2026-04-12 11:01 CST | 技术架构师 | 新增 ADR-007，明确 `AUDIT_WRITE_FAILED` 只能用于审计写入失败 |
| V1.8 | 2026-04-12 11:22 CST | 技术架构师 | 新增 ADR-008，明确提交阶段数据库异常必须归类为 `DATABASE_WRITE_FAILED` |
| V1.9 | 2026-04-12 11:37 CST | 技术架构师 | 新增 ADR-009，明确普通日志脱敏与测试环境变量强制隔离 |
| V1.10 | 2026-04-12 11:54 CST | 技术架构师 | 新增 ADR-010，明确 `X-Request-ID/request_id` 必须白名单规范化 |
| V1.11 | 2026-04-12 12:07 CST | 技术架构师 | 新增 ADR-011，明确 `request_id` 必须拦截语义敏感词 |
| V1.12 | 2026-04-12 12:21 CST | 技术架构师 | 新增 ADR-012，明确工票为本地工资事实源、Job Card 为同步对象、工价按工票快照 |
| V1.13 | 2026-04-12 13:17 CST | 技术架构师 | 新增 ADR-013，明确工票写接口必须按 Job Card 派生资源做权限校验 |
| V1.14 | 2026-04-12 13:52 CST | 技术架构师 | 新增 ADR-014，明确工价读取和维护必须按 Item/Company 资源权限闭环 |
| V1.15 | 2026-04-12 14:22 CST | 技术架构师 | 新增 ADR-015，明确批量导入系统级异常不得包装为行级失败 |
| V1.16 | 2026-04-12 14:38 CST | 技术架构师 | 新增 ADR-016，明确 Job Card 同步必须采用 outbox/after-commit/异步重试 |
| V1.17 | 2026-04-12 15:09 CST | 技术架构师 | 新增 ADR-017，明确 outbox `event_key` 必须采用短前缀 + 完整 hash，禁止截断 digest |
| V1.28 | 2026-04-12 18:30 CST | 技术架构师 | 新增 ADR-028，明确 ERPNext Item 候选查询不可用必须 fail closed |
| V1.29 | 2026-04-12 19:15 CST | 技术架构师 | 新增 ADR-029，明确内部 Worker 响应中 `skipped_forbidden*` 为废弃诊断兼容字段 |
| V1.30 | 2026-04-12 20:08 CST | 技术架构师 | 新增 ADR-030，冻结外发模块 V1.1 生产级契约 |
| V1.31 | 2026-04-12 21:25 CST | 技术架构师 | 新增 ADR-031，明确 TASK-002B 阶段回料/验货旧演示成功路径必须 fail closed |
| V1.32 | 2026-04-12 21:51 CST | 技术架构师 | 新增 ADR-032，明确外发资源权限必须以本地 company 事实字段为准 |
| V1.33 | 2026-04-12 22:21 CST | 技术架构师 | 新增 ADR-033，明确外发迁移必须自洽且创建单据必须校验 BOM 款式一致 |
| V1.34 | 2026-04-13 09:24 CST | 技术架构师 | 新增 ADR-034，明确外发发料必须通过 outbox 异步创建 ERPNext Material Issue |
| V1.35 | 2026-04-13 10:33 CST | 技术架构师 | 新增 ADR-035，明确发料幂等必须前置且 ERPNext event_key 找回必须校验 docstatus |
| V1.36 | 2026-04-13 10:58 CST | 技术架构师 | 修订 ADR-034，明确外发发料 `event_key` 不包含 `issue_batch_no` |
| V1.37 | 2026-04-13 11:01 CST | 技术架构师 | 新增 ADR-036，明确外发回料必须通过 outbox 异步创建 ERPNext Material Receipt |
| V1.38 | 2026-04-13 12:06 CST | 技术架构师 | 新增 ADR-037，明确外发库存同步 retry 必须按 outbox_id + idempotency_key + stock_action 精确定位 |
| V1.39 | 2026-04-13 12:31 CST | 技术架构师 | 新增 ADR-038，明确外发验货金额公式、ERPNext 边界和旧 retry 选择器清理 |
| V1.40 | 2026-04-13 13:05 CST | 技术架构师 | 新增 ADR-039，明确验货幂等必须包含 receipt_batch_no，详情返回 inspections，验货迁移独立化 |
| V1.41 | 2026-04-13 13:26 CST | 技术架构师 | 新增 ADR-040，明确外发验货并发不超量测试、详情鉴权后读子表和 TASK-002F 独立迁移要求 |
| V1.42 | 2026-04-13 13:41 CST | 技术架构师 | 新增 ADR-041，明确 PostgreSQL 并发验货测试必须真实执行并进入 TASK-002F3 交付门禁 |
| V1.43 | 2026-04-13 13:58 CST | 技术架构师 | 新增 ADR-042，明确外发前端按钮必须以后端权限动作和单据状态共同驱动 |
| V1.81 | 2026-04-14 00:30 CST | 技术架构师 | 新增 ADR-080，明确 TASK-005C 仅允许模型、迁移与来源映射设计，不得进入 API、前端或 TASK-006 |
| V1.82 | 2026-04-14 01:28 CST | 技术架构师 | 新增 ADR-081，明确 TASK-005C1 必须修复来源状态 fail closed、source_map 审计追溯字段、snapshot 复核字段、费用分摊默认状态和 SLE 材料编码语义 |
| V1.83 | 2026-04-14 01:59 CST | 技术架构师 | 新增 ADR-082，明确利润来源默认不纳入、source_map 字段契约、复合索引和 status-only 白名单 |
| V1.84 | 2026-04-14 06:58 CST | 技术架构师 | 新增 ADR-083，明确利润快照必须补齐公司、款式、期间查询索引 |
| V1.85 | 2026-04-14 07:21 CST | 技术架构师 | 新增 ADR-084，明确 TASK-005C~C3 新增利润模型必须纳入本地仓库基线后才允许进入 TASK-005D |
| V1.86 | 2026-04-14 07:59 CST | 技术架构师 | 新增 ADR-085，明确 TASK-005D 只做利润快照计算服务，不注册 API、不改前端、不新增迁移、不进入 TASK-006 |
| V1.87 | 2026-04-14 10:15 CST | 技术架构师 | 新增 ADR-086，明确 TASK-005D1 必须修复利润快照 request_hash、unresolved、SLE、缺工价、收入状态和幂等键校验 |
| V1.88 | 2026-04-14 10:36 CST | 技术架构师 | 新增 ADR-087，明确 TASK-005D2 必须修复实际成本归属、事务保存点兜底，并固化 V1 sales_order 强制非空 |
| V1.89 | 2026-04-14 10:56 CST | 技术架构师 | 新增 ADR-088，明确 workshop/subcontract 缺 company 或缺 item_code 必须 fail closed，不得仅凭 sales_order 纳入利润 |
| V1.90 | 2026-04-14 11:08 CST | 技术架构师 | 新增 ADR-089，明确 TASK-005D4 必须在进入 API 前完成利润快照服务本地白名单基线提交 |
| V1.91 | 2026-04-14 11:25 CST | 技术架构师 | 新增 ADR-090，明确 TASK-005D5 必须修正 D4 证据提交后 HEAD 占位后才允许进入 TASK-005E/API |
| V1.92 | 2026-04-14 11:33 CST | 技术架构师 | 新增 ADR-091，明确 TASK-005D6 必须将 D5 任务单和 D6 文档更新纳入本地 docs-only 基线后才允许进入 TASK-005E/API |
| V1.93 | 2026-04-14 11:42 CST | 技术架构师 | 新增 ADR-092，明确 TASK-005E API 必须先审计任务单，并以鉴权、资源权限、安全/操作审计、统一错误信封和 PostgreSQL 门禁为核心 |
| V1.94 | 2026-04-14 11:50 CST | 技术架构师 | 新增 ADR-093，允许预派发 TASK-005E1 API 实现任务单，但执行前置门禁必须是 TASK-005E 任务单审计通过 |
| V1.95 | 2026-04-14 12:34 CST | 技术架构师 | 新增 ADR-094，明确 TASK-005E2 必须修复来源采集 fail closed、权限矩阵、鉴权顺序和早期失败审计 |
| V1.96 | 2026-04-14 12:58 CST | 技术架构师 | 新增 ADR-095，明确款式利润详情接口必须先校验 read 动作权限再查询 snapshot，防止 403/404 存在性枚举 |
| V1.97 | 2026-04-14 13:07 CST | 技术架构师 | 新增 ADR-096，明确 TASK-005E4 必须将 E~E3 API 权限审计基线纳入本地白名单提交后才允许进入前端联调或真实来源 adapter |
| V1.98 | 2026-04-14 13:20 CST | 技术架构师 | 新增 ADR-097，明确 TASK-005F 必须实现真实服务端来源 Adapter，禁止前端 payload 作为利润来源事实 |
| V1.99 | 2026-04-14 13:50 CST | 技术架构师 | 新增 ADR-098，明确 SLE 实际材料成本必须同时满足 BOM 材料范围和销售/生产归属桥接，外发来源不得静默漏采集 |
| V1.100 | 2026-04-14 14:15 CST | 技术架构师 | 新增 ADR-099，明确外发成本完整计入利润必须补销售订单、生产计划、工单桥接字段和安全补数策略 |
| V1.101 | 2026-04-14 14:55 CST | 技术架构师 | 新增 ADR-100，明确款式利润传入 work_order 时外发成本必须严格匹配当前 Work Order，禁止仅凭同 SO 同款提前入账 |
| V1.102 | 2026-04-14 15:15 CST | 技术架构师 | 新增 ADR-101，明确外发来源 inspected_at 期间过滤必须下推到数据库，并以 PostgreSQL 非 skip 证据作为财务封版门禁 |
| V1.103 | 2026-04-14 15:40 CST | 技术架构师 | 新增 ADR-102，明确新增 PostgreSQL 门禁不得覆盖既有外发结算门禁，单一脚本必须分别断言 settlement 与 style-profit 两组 JUnit |
| V1.104 | 2026-04-14 15:55 CST | 技术架构师 | 新增 ADR-103，明确 workflow 必须上传 PostgreSQL 双 JUnit 证据，旧 `.pytest-postgresql.xml` 不再作为 hard gate artifact |
| V1.105 | 2026-04-14 16:05 CST | 技术架构师 | 新增 ADR-104，明确 TASK-005 财务封版前必须取得 PostgreSQL 真实非 skip JUnit 证据，且证据库必须是一次性测试库 |
| V1.106 | 2026-04-14 16:20 CST | 技术架构师 | 新增 ADR-105，明确 TASK-005F2 外发利润桥接字段属于外发基础 schema，所有外发 PostgreSQL 集成测试必须纳入 |
| V1.107 | 2026-04-14 16:26 CST | 技术架构师 | 新增 ADR-106，明确 TASK-005F 阶段进入 G 前必须完成白名单本地基线提交，禁止将运行时 JUnit 和历史大目录混入 |
| V1.108 | 2026-04-14 16:40 CST | 技术架构师 | 新增 ADR-107，明确 F9 证据补提交必须 docs-only，提交前必须清理非白名单 staged 文件 |

## ADR-002：BOM 写接口鉴权、权限动作与敏感操作审计

- 决策时间：2026-04-12 00:00 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001A BOM 鉴权权限审计整改
- 审计来源：审计意见书第 4 份，BOM 写接口无鉴权、无真实操作者、无敏感操作审计
- 结论：BOM 写接口必须使用真实当前用户、权限动作校验和敏感操作审计日志

### 背景

审计发现 BOM 写接口仍存在以下问题：

1. `create_bom`、`update_bom_draft` 使用 `operator="system"`。
2. 发布、停用、设默认接口没有当前用户依赖。
3. BOM 写接口没有动作级权限校验。
4. 敏感写操作没有审计日志。

该问题会绕过 ERPNext/衣算云权限语义，不允许进入验收。

### 决策

1. BOM 写接口统一接入 `get_current_user()`。
2. 业务操作者必须来自真实登录用户或受控服务账号。
3. 禁止业务路径使用 `operator="system"` 代表用户。
4. BOM 写接口必须校验动作权限。
5. BOM 敏感写操作必须写入 `ly_schema.ly_operation_audit_log`。

### 权限动作

| 动作 | 权限码 |
| --- | --- |
| 创建 BOM | `bom:create` |
| 更新 BOM | `bom:update` |
| 发布 BOM | `bom:publish` |
| 停用 BOM | `bom:deactivate` |
| 设置默认 BOM | `bom:set_default` |

### 审计动作

以下动作必须审计：

1. `bom:create`
2. `bom:update`
3. `bom:publish`
4. `bom:deactivate`
5. `bom:set_default`

### 验收

1. 无登录态调用 BOM 写接口返回 `AUTH_UNAUTHORIZED`。
2. 无权限调用 BOM 写接口返回 `AUTH_FORBIDDEN`。
3. BOM 写接口不再出现 `operator="system"`。
4. 创建和更新写入真实 `created_by/updated_by`。
5. 发布、停用、设默认生成审计日志。

## ADR-003：权限来源临时方案与生产方案

- 决策时间：2026-04-12 09:49 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001B 权限来源升级 ERPNext
- 审计来源：审计意见书第 5 份，权限动作来源仍是 FastAPI 静态角色映射和环境变量
- 结论：Sprint 1 允许静态权限映射作为临时方案，生产前必须升级为 ERPNext 权限权威来源

### 背景

TASK-001A 已接入当前用户、权限动作和敏感操作审计，但审计指出权限动作来源仍是 FastAPI 静态角色映射和环境变量，不是 ERPNext `Role / User Permission` 或 `/api/auth/` 权限聚合结果。

### 决策

1. `permissions.py` 静态角色映射只允许作为 Sprint 1 临时方案。
2. 生产环境必须使用 ERPNext `Role / User Permission / Workflow` 作为权限权威来源。
3. FastAPI 必须提供 `/api/auth/me` 和 `/api/auth/actions` 聚合接口。
4. Vue3 前端按钮权限必须从 FastAPI 权限动作接口读取。
5. 后端写接口继续强制校验动作权限，不能只依赖前端按钮显示。

### 环境约束

| 环境 | 允许权限来源 |
| --- | --- |
| development | `static` 或 `erpnext` |
| sprint | `static` 或 `erpnext` |
| production | 仅 `erpnext` |

当 `APP_ENV=production` 且 `LINGYI_PERMISSION_SOURCE!=erpnext` 时，应用必须启动失败。

### 验收

1. `GET /api/auth/me` 返回当前用户和角色。
2. `GET /api/auth/actions?module=bom` 返回 BOM 动作权限。
3. 前端 BOM 按钮从权限接口读取。
4. 生产环境禁用静态权限来源。

## ADR-004：BOM 读接口和展开接口必须执行后端资源级鉴权

- 决策时间：2026-04-12 10:05 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001C BOM 读接口资源级鉴权
- 审计来源：审计意见书第 6 份，BOM 读接口和展开接口未做后端鉴权
- 结论：BOM 读接口、详情接口、展开接口必须执行 `bom:read` 后端资源级鉴权

### 背景

BOM 用料、工价、展开结果属于敏感经营数据。前端 `canRead` 只能控制显示，不能替代后端权限校验。

### 决策

1. `GET /api/bom/` 必须校验 `bom:read`。
2. `GET /api/bom/{bom_id}` 必须校验 `bom:read` 和目标 BOM 的 `item_code` 资源权限。
3. `POST /api/bom/{bom_id}/explode` 必须校验 `bom:read` 和目标 BOM 的 `item_code` 资源权限。
4. BOM 列表只能返回当前用户有权读取的 BOM。
5. 无登录态返回 `AUTH_UNAUTHORIZED`。
6. 无读权限返回 `AUTH_FORBIDDEN`。

### 验收

1. 未登录用户不能调用 BOM 读接口。
2. 无 `bom:read` 用户不能调用 BOM 读接口。
3. 有 `bom:read` 但无目标 `item_code` 权限的用户不能读取该 BOM。
4. 展开接口与详情接口使用同一资源级读权限。

## ADR-005：ERPNext User Permission 查询失败必须 Fail Closed

- 决策时间：2026-04-12 10:23 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001D ERPNext 权限 Fail Closed 整改
- 审计来源：审计意见书第 7 份，ERPNext User Permission 查询失败时 fail open
- 结论：ERPNext User Permission 查询失败不得返回空权限列表，必须 fail closed

### 背景

审计发现 `get_user_permissions()` 在 ERPNext 权限查询失败时返回 `[]`。后续资源级权限判断会把空列表理解成“用户没有 Item 限制”，导致有角色动作权限的用户可能访问全部 BOM。

该问题会泄露 BOM 用料、工价、损耗率和展开结果等敏感经营数据，必须阻断。

### 决策

1. ERPNext `User Permission` 查询失败、超时、数据库异常、REST API 异常或返回结构异常时，必须拒绝访问。
2. `get_user_permissions()` 禁止在异常时返回 `[]`。
3. 必须明确区分“ERPNext 查询成功但无 User Permission 限制”和“ERPNext 权限查询失败”。
4. 权限来源不可用时统一返回 `PERMISSION_SOURCE_UNAVAILABLE`。
5. BOM 列表、详情、展开接口必须继承 fail closed。
6. 生产环境 `/api/auth/actions` 不得在 ERPNext 权限失败时用静态权限兜底。

### 结果语义

| 场景 | 处理方式 |
| --- | --- |
| ERPNext 查询成功，返回 0 条 User Permission | 允许按“无资源限制”继续判断动作权限 |
| ERPNext 查询成功，返回 N 条 User Permission | 按返回记录限制 `item_code` / `company` |
| ERPNext 查询失败 | 返回 `PERMISSION_SOURCE_UNAVAILABLE`，不返回业务数据 |

### 验收

1. 模拟 ERPNext 权限查询失败时，`GET /api/bom/` 不返回 BOM 数据。
2. 模拟 ERPNext 权限查询失败时，`GET /api/bom/{bom_id}` 不返回 BOM 详情。
3. 模拟 ERPNext 权限查询失败时，`POST /api/bom/{bom_id}/explode` 不返回展开结果。
4. 代码中不存在用 `except Exception: return []` 掩盖权限查询失败的逻辑。

## ADR-006：BOM 权限拒绝必须形成安全审计闭环

- 决策时间：2026-04-12 10:35 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001E BOM 权限拒绝安全审计整改
- 审计来源：审计意见书第 8 份，401/403 权限拒绝未形成安全审计闭环
- 结论：BOM 模块认证失败、权限拒绝、资源级越权和权限来源不可用事件必须写入安全审计日志或持久化审计记录

### 背景

审计确认 TASK-001D 已修复 fail open 高危问题，但指出当前 401/403 权限拒绝只返回客户端，未形成审计闭环。

BOM 用料、工价、损耗率和展开结果属于敏感经营数据，越权尝试必须可追踪、可复查、可统计。

### 决策

1. BOM 模块所有 `AUTH_UNAUTHORIZED` 必须记录安全审计事件。
2. BOM 模块所有 `AUTH_FORBIDDEN` 必须记录安全审计事件。
3. `PERMISSION_SOURCE_UNAVAILABLE` 必须记录安全审计事件。
4. 资源级越权必须记录 `resource_type`、`resource_id`、`resource_no`。
5. 审计记录必须包含 `request_id`、`user_id`、`action`、`deny_reason`、`ip_address`、`user_agent`。
6. 审计日志禁止记录完整 Token、Cookie、密码、ERPNext API Secret。
7. 审计写入失败不得导致权限放行。

### 持久化方案

优先新增 `ly_schema.ly_security_audit_log` 独立记录安全审计事件，避免与业务操作审计 `ly_operation_audit_log` 混用。

如工程实现阶段决定复用现有审计表，必须保证字段可覆盖以下信息：

1. `event_type`
2. `module`
3. `action`
4. `resource_type`
5. `resource_id`
6. `resource_no`
7. `user_id`
8. `deny_reason`
9. `request_id`
10. `ip_address`
11. `user_agent`
12. `created_at`

### 验收

1. 未登录访问 `GET /api/bom/` 返回 401 且产生安全审计记录。
2. 无 `bom:read` 访问 `GET /api/bom/` 返回 403 且产生安全审计记录。
3. 无资源权限访问 `GET /api/bom/{bom_id}` 返回 403 且产生资源级安全审计记录。
4. ERPNext 权限来源不可用返回 503 且产生安全审计记录。
5. 安全审计日志不包含完整 Token、Cookie、密码、ERPNext API Secret。

## ADR-007：BOM 写接口异常必须分类，`AUDIT_WRITE_FAILED` 不得作为通用兜底

- 决策时间：2026-04-12 11:01 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001F BOM 写接口异常分类整改
- 审计来源：审计意见书第 9 份，BOM 写接口把所有未知异常统一返回为 `AUDIT_WRITE_FAILED`
- 结论：`AUDIT_WRITE_FAILED` 只能用于审计日志写入失败，业务异常、数据库异常、未知异常必须使用独立错误码

### 背景

审计发现 BOM 写接口仍把所有未知异常统一返回为 `AUDIT_WRITE_FAILED`，会误导排障和审计结论。

如果业务校验失败、数据库写入失败、未知程序异常都显示为审计失败，工程师无法判断真实故障点，审计官也无法确认审计链路是否真的失败。

### 决策

1. `AUDIT_WRITE_FAILED` 只能由审计服务写入失败产生。
2. 业务校验异常必须返回 BOM 业务错误码。
3. 主业务数据库写入异常返回 `DATABASE_WRITE_FAILED`。
4. 主业务数据库读取异常返回 `DATABASE_READ_FAILED`。
5. 未知程序异常返回 `BOM_INTERNAL_ERROR`。
6. 唯一索引冲突如能识别为默认 BOM 冲突，返回 `BOM_DEFAULT_CONFLICT`。
7. 客户端错误响应不得暴露 Python traceback、SQL 原文、数据库连接串、Token、Cookie。

### 错误码边界

| 错误码 | 使用边界 |
| --- | --- |
| `AUDIT_WRITE_FAILED` | 仅审计日志写入失败 |
| `DATABASE_WRITE_FAILED` | BOM 主业务数据库写入失败 |
| `DATABASE_READ_FAILED` | BOM 主业务数据库读取失败 |
| `BOM_INTERNAL_ERROR` | 未知程序异常 |
| `BOM_DEFAULT_CONFLICT` | 默认 BOM 唯一约束冲突 |

### 验收

1. 模拟审计写入失败时，BOM 写接口返回 `AUDIT_WRITE_FAILED`。
2. 模拟数据库写入失败时，BOM 写接口返回 `DATABASE_WRITE_FAILED`。
3. 模拟未知异常时，BOM 写接口返回 `BOM_INTERNAL_ERROR`。
4. 代码中不存在把所有 `Exception` 统一转换为 `AUDIT_WRITE_FAILED` 的逻辑。

## ADR-008：BOM 提交阶段数据库异常必须归类为 `DATABASE_WRITE_FAILED`

- 决策时间：2026-04-12 11:22 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001G BOM 提交阶段数据库异常分类整改
- 审计来源：审计意见书第 10 份，`session.commit()` 阶段数据库写入失败仍落到 `BOM_INTERNAL_ERROR`
- 结论：`flush/commit/事务退出` 阶段的数据库异常均属于主业务数据库写入失败，必须返回 `DATABASE_WRITE_FAILED`

### 背景

审计确认 `AUDIT_WRITE_FAILED` 已限定为审计写入失败专用错误码，但发现 `session.commit()` 阶段的数据库写入失败仍落到 `BOM_INTERNAL_ERROR`。

提交失败是主业务数据库写失败，不是未知异常。若归类为未知异常，会影响排障、告警分级和审计判断。

### 决策

1. `session.flush()`、`session.commit()`、事务上下文退出时触发的数据库异常，统一归类为 `DATABASE_WRITE_FAILED`。
2. `IntegrityError` 如能识别为默认 BOM 部分唯一索引冲突，返回 `BOM_DEFAULT_CONFLICT`。
3. 提交阶段数据库异常不得归类为 `BOM_INTERNAL_ERROR`。
4. 提交阶段数据库异常不得归类为 `AUDIT_WRITE_FAILED`。
5. 提交失败后必须执行 `rollback`，且 `rollback` 失败不得覆盖原始错误码。
6. `snapshot_resource()` 必须纳入统一异常处理，禁止绕过错误信封。
7. 测试环境变量必须在应用模块 import 前完成设置，避免 import-time 顺序脆弱性。

### 错误码边界

| 场景 | 错误码 |
| --- | --- |
| `session.commit()` 数据库写失败 | `DATABASE_WRITE_FAILED` |
| `session.flush()` 数据库写失败 | `DATABASE_WRITE_FAILED` |
| 默认 BOM 唯一索引冲突 | `BOM_DEFAULT_CONFLICT` |
| `snapshot_resource()` 数据库读取失败 | `DATABASE_READ_FAILED` |
| `snapshot_resource()` 未知异常 | `BOM_INTERNAL_ERROR` |

### 验收

1. 模拟 `session.commit()` 抛 `SQLAlchemyError` 时，BOM 写接口返回 `DATABASE_WRITE_FAILED`。
2. 模拟默认 BOM 唯一索引冲突时，返回 `BOM_DEFAULT_CONFLICT`。
3. `snapshot_resource()` 异常返回统一 `{code, message, data}` 错误信封。
4. 测试连续执行两次结果一致，不依赖 import-time 环境变量顺序。

## ADR-009：服务端普通日志必须脱敏，测试环境变量必须强制隔离

- 决策时间：2026-04-12 11:37 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001H 服务端日志脱敏与测试环境隔离整改
- 审计来源：审计意见书第 11 份，普通错误日志泄露 SQLAlchemy 异常原文，测试 `setdefault()` 对外部生产变量不免疫
- 结论：普通应用日志和审计日志不得记录 SQL 原文、SQL 参数、Token、Cookie、密码、Secret；测试环境变量必须强制覆盖并发生在应用 import 前

### 背景

审计复验时确认客户端响应未泄露敏感信息，但服务端普通错误日志仍直接记录 `str(exc)`，出现 `[SQL: UPDATE ...]` 和 SQLAlchemy 异常原文。

同时，测试环境变量使用 `setdefault()`，当外部已存在 `APP_ENV=production`、`LINGYI_PERMISSION_SOURCE=static` 时无法覆盖，导致测试导入阶段失败。

### 决策

1. 普通应用日志禁止直接记录 `str(exc)`。
2. SQLAlchemy 异常不得在普通日志中输出 SQL statement、SQL parameters、数据库连接串。
3. 认证、权限、审计相关日志不得记录完整 Token、Cookie、密码、ERPNext API Secret。
4. 普通日志只记录安全元数据：`error_code`、`exception_type`、`request_id`、`module`、`action`、资源标识和安全数据库错误码。
5. 如无法安全脱敏，错误明细统一写为 `internal error, detail redacted`。
6. 原始异常详情如需保留，只允许在非生产环境通过显式开关进入受限调试日志，生产环境强制禁止。
7. 测试环境变量必须强制覆盖，不允许用 `os.environ.setdefault()` 设置关键测试变量。
8. 测试环境变量设置必须发生在应用模块 import 前。
9. 生产保护逻辑不得删除：非测试入口下 `APP_ENV=production` 且 `LINGYI_PERMISSION_SOURCE=static` 必须启动失败。

### 验收

1. 数据库写失败、回滚失败、审计写入失败场景下，普通日志不包含 `[SQL:]`、`[parameters:]`、SQL statement、SQL parameters。
2. 普通日志和审计日志不包含完整 Token、Cookie、密码、Secret。
3. 外部注入 `APP_ENV=production LINGYI_PERMISSION_SOURCE=static` 时，测试入口仍能覆盖为 test 环境并通过。
4. 非测试入口的生产保护仍有效。

## ADR-010：`X-Request-ID/request_id` 必须白名单规范化

- 决策时间：2026-04-12 11:54 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001I Request ID 白名单校验与脱敏整改
- 审计来源：审计意见书第 12 份，`X-Request-ID/request_id` 未做统一白名单校验或脱敏
- 结论：外部传入的 `X-Request-ID` 不可信，进入日志、审计表和响应头前必须统一白名单规范化

### 背景

审计发现 `X-Request-ID/request_id` 会原样进入错误日志和审计上下文，位置包括 `main.py`、`logging.py`、`audit_service.py`。

如果请求头中包含换行、SQL 片段、Token、Cookie、密码或脚本片段，会造成日志注入、审计污染和敏感信息落库风险。

### 决策

1. 新增统一函数 `normalize_request_id(raw_request_id)`。
2. 白名单字符为 `A-Z`、`a-z`、`0-9`、`_`、`-`、`.`。
3. 长度限制为 1 到 64 个字符。
4. 缺失、超长或不符合白名单的 `request_id` 必须整体丢弃并重新生成。
5. 非法 `request_id` 禁止截断或部分清洗后继续使用。
6. `main.py` 请求中间件是唯一允许读取原始 `X-Request-ID` 的位置。
7. 后续日志、审计和业务模块只能使用 `request.state.request_id`。
8. 响应头 `X-Request-ID` 只返回规范化后的值。
9. 普通日志、`ly_security_audit_log`、`ly_operation_audit_log` 禁止记录非法 `request_id` 原文。

### 验收

1. 合法 `X-Request-ID` 被保留。
2. 缺失、超长、包含换行、SQL 片段、Token、Cookie、密码、脚本片段的 `X-Request-ID` 被替换为安全生成值。
3. 普通日志和审计表不包含非法 `request_id` 原文。
4. 除 `main.py` 请求中间件外，业务代码不得直接读取 `request.headers.get(\"X-Request-ID\")`。

## ADR-011：`request_id` 必须拦截语义敏感词

- 决策时间：2026-04-12 12:07 CST
- 决策类型：架构审计整改
- 关联模块：TASK-001J Request ID 语义敏感词拦截与单测隔离整改
- 审计来源：审计意见书第 13 份，`normalize_request_id()` 仅做字符白名单导致语义敏感 request_id 被放行
- 结论：`request_id` 必须同时满足字符白名单和语义安全校验，命中敏感关键词时整体丢弃并重新生成

### 背景

审计发现 `X-Request-ID: token-password-secret-cookie-authorization` 和 `X-Request-ID: Bearer.abc123_token_secret` 虽然字符合法，但语义上包含凭证、认证、Cookie、密码等敏感关键词，会原样进入响应头和安全审计表。

字符白名单只能防止格式注入，不能防止语义敏感数据落日志。因此必须增加语义敏感词拦截。

### 决策

1. `normalize_request_id()` 必须同时执行字符白名单、长度限制和语义敏感词校验。
2. 语义敏感词校验大小写不敏感。
3. 必须拦截 `authorization`、`bearer`、`token`、`cookie`、`set-cookie`、`password`、`passwd`、`secret`、`session`、`sessionid`、`api-key`、`api_key`、`access-token`、`access_token`、`refresh-token`、`refresh_token`。
4. 命中敏感词时必须整体丢弃原 request_id 并生成新值。
5. 禁止将敏感词 mask、截断或替换后继续作为 request_id。
6. 响应头、普通日志、`ly_security_audit_log`、`ly_operation_audit_log` 只能使用规范化后的 request_id。
7. 单文件 `unittest` 不得依赖 pytest `conftest.py` 自动加载；需要在业务模块 import 前强制配置测试环境。

### 验收

1. `token-password-secret-cookie-authorization` 被替换，不进入响应头、日志和审计表。
2. `Bearer.abc123_token_secret` 被替换，不进入响应头、日志和审计表。
3. 合法业务 request_id 未命中敏感词时可保留。
4. 外部生产变量注入下，`python -m unittest tests.test_request_id_sanitization` 可通过。
5. 非测试入口的生产保护仍有效。

## ADR-012：工票为本地工资事实源，Job Card 为 ERPNext 生产执行同步对象

- 决策时间：2026-04-12 12:21 CST
- 决策类型：模块架构决策
- 关联模块：TASK-003 工票/车间管理
- 结论：工票和工资结算事实保存在 `ly_schema`，ERPNext `Job Card` 仅作为生产工序卡和进度同步对象；工票创建时必须快照计件工价

### 背景

车间工票涉及计件工资、撤销、批量导入、PDA/MES 幂等和日薪汇总，属于服装工厂特有执行层业务。ERPNext `Job Card` 可承载生产工序卡，但不适合承载工票幂等、撤销和工资结算全部细节。

### 决策

1. `ly_schema.ys_workshop_ticket` 是工票事实源。
2. `ly_schema.ys_workshop_daily_wage` 是日薪汇总数据。
3. `ly_schema.ly_operation_wage_rate` 是本厂工序计件工价来源。
4. ERPNext `Job Card` 只作为工序执行对象和进度同步目标。
5. FastAPI 不直接写 ERPNext 数据库表，只通过 ERPNext API 同步 Job Card。
6. 工票创建时必须快照 `unit_wage`，历史工资不随工价档案后续调整自动变化。
7. Job Card 同步失败不得删除或回滚已成功落库的本地工票，应标记 `sync_status=failed` 并支持重试。

### 验收

1. 工票登记后 `ys_workshop_ticket.unit_wage` 有快照值。
2. 修改工价档案后，历史工票工资不变化。
3. Job Card 同步失败时，本地工票保留且有同步失败记录。
4. 工票撤销通过新增 reversal 记录完成，不物理删除原工票。

## ADR-013：工票写接口必须按 Job Card 派生资源做权限校验

- 决策时间：2026-04-12 13:17 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003A 工票写接口资源级权限整改
- 审计来源：审计意见书第 14 份，车间写接口缺少资源级 Item / Job Card 权限校验
- 结论：工票写接口必须同时校验动作权限和由 ERPNext `Job Card / Work Order` 派生出的 `item_code/company` 资源权限

### 背景

审计探针复现：仅授权 `ITEM-B` 的用户可以成功登记 `ITEM-A` 工票。该问题说明工票写接口只校验了 `workshop:*` 动作权限，未校验目标业务资源权限。

工票会影响员工工资、生产进度和 Job Card 同步，属于高敏业务写入。前端传入的 `item_code` 不可信，不能作为权限判断依据。

### 决策

1. 工票登记、撤销、批量导入、Job Card 同步必须先读取 ERPNext `Job Card`。
2. 必须由 `Job Card.work_order` 继续读取 ERPNext `Work Order`，并派生真实 `item_code/company`。
3. 前端传入的 `item_code` 只能做一致性校验，不能作为权限依据。
4. 用户必须同时具备对应 `workshop:*` 动作权限和目标 `item_code/company` 资源权限。
5. 仅授权 `ITEM-B` 的用户不得写入 `ITEM-A` 工票。
6. 资源权限不足时不得写工票、不得更新日薪、不得同步 Job Card。
7. 批量导入必须逐行校验资源权限；越权行进入 `failed_items` 并写安全审计。
8. 工价维护涉及具体 `item_code` 时必须校验该 `item_code` 资源权限；通用工价必须要求全局工价维护权限。
9. ERPNext 权限来源不可用时必须 fail closed，返回 `PERMISSION_SOURCE_UNAVAILABLE`。

### 验收

1. 仅授权 `ITEM-B` 的用户登记 `ITEM-A` 工票返回 `AUTH_FORBIDDEN`。
2. 越权登记不得新增 `ys_workshop_ticket`。
3. 越权登记不得更新 `ys_workshop_daily_wage`。
4. 越权登记不得调用 ERPNext Job Card 同步。
5. 批量导入中越权行失败，授权行可成功。
6. 所有资源级拒绝写入 `ly_security_audit_log`。

## ADR-014：工价读取和维护必须按 Item/Company 资源权限闭环

- 决策时间：2026-04-12 13:52 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003B 工价资源权限闭环整改
- 审计来源：审计意见书第 15 份，工价读取和创建资源权限未闭环
- 结论：工价属于工资/成本敏感数据，读取、创建、停用必须同时满足动作权限和 `item_code/company` 资源权限；Company-only 不得替代 Item 权限

### 背景

审计复现：仅授权 `ITEM-B` 的用户可以读取 `ITEM-A` 工价；只有 Company 权限、没有 Item 权限的用户也能创建指定 `ITEM-Z` 的工价。

工价直接影响员工工资和款式成本，属于敏感经营数据。只校验 Company 或动作权限不足以证明用户可访问某个款式工价。

### 决策

1. `GET /api/workshop/wage-rates` 必须在数据库查询层按 `item_code/company` 做资源过滤。
2. 显式查询未授权 `item_code` 时返回 `AUTH_FORBIDDEN`。
3. 创建或停用指定 `item_code` 工价，必须校验目标 Item 权限。
4. Company-only 权限不得替代 Item 权限。
5. `POST /api/workshop/wage-rates` 必须完成 `item_code -> company` 解析。
6. 无法解析 company 时返回 `WORKSHOP_WAGE_RATE_COMPANY_REQUIRED`，禁止 fail open。
7. 通用工价必须使用独立全局权限：读取需 `workshop:wage_rate_read_all`，维护需 `workshop:wage_rate_manage_all`。
8. 权限来源不可用时返回 `PERMISSION_SOURCE_UNAVAILABLE`。
9. 工价资源级拒绝必须写入 `ly_security_audit_log`。

### 验收

1. 仅授权 `ITEM-B` 的用户读取工价列表时，不返回 `ITEM-A` 工价。
2. 仅授权 `ITEM-B` 的用户显式查询 `ITEM-A` 工价时，返回 `AUTH_FORBIDDEN`。
3. 只有 Company 权限、没有 Item 权限的用户创建 `ITEM-Z` 工价时，返回 `AUTH_FORBIDDEN`。
4. 无法解析 `item_code -> company` 时，返回 `WORKSHOP_WAGE_RATE_COMPANY_REQUIRED`。
5. 通用工价读取和维护必须具备全局工价权限。

## ADR-015：工票批量导入系统级异常不得包装为行级失败

- 决策时间：2026-04-12 14:22 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003C 工票批量导入系统级异常边界整改
- 审计来源：审计意见书第 16 份，批量导入将 `DatabaseWriteFailed` 包装成 `200/code=0` 的普通失败行
- 结论：批量导入必须区分行级业务失败和系统级失败；数据库、权限源、审计、ERPNext 服务、未知异常必须向上抛出标准错误响应

### 背景

审计指出 `POST /api/workshop/tickets/batch` 会把 `DatabaseWriteFailed` 这类系统级异常包装成 `failed_items`，并返回 `200/code=0`。这会让调用方误判为批次请求成功，也会让运维监控无法感知系统故障。

### 决策

1. 行级业务失败可进入 `failed_items`，批次可继续处理其他合法行。
2. 系统级失败必须中断批次，返回非 200 标准错误响应。
3. `DATABASE_WRITE_FAILED`、`DATABASE_READ_FAILED`、`PERMISSION_SOURCE_UNAVAILABLE`、`AUDIT_WRITE_FAILED`、`ERPNEXT_SERVICE_UNAVAILABLE`、`WORKSHOP_INTERNAL_ERROR` 禁止进入 `failed_items`。
4. 缺少批量导入动作权限时，整个请求返回 `AUTH_FORBIDDEN`，不进入行处理。
5. 未登录时，整个请求返回 `AUTH_UNAUTHORIZED`，不进入行处理。
6. 单行资源级 `AUTH_FORBIDDEN` 可进入 `failed_items`，但必须写安全审计。
7. 系统级异常响应不得包含 `success_count/failed_count`，避免调用方误判为业务行失败。

### 验收

1. 模拟数据库写失败时，批量导入返回 `DATABASE_WRITE_FAILED`，不是 `200/code=0`。
2. 模拟权限来源不可用时，批量导入返回 `PERMISSION_SOURCE_UNAVAILABLE`，不是 `failed_items`。
3. 模拟审计写入失败时，批量导入返回 `AUDIT_WRITE_FAILED`，不是 `failed_items`。
4. 行级业务错误仍可进入 `failed_items` 并允许合法行成功。
5. 行级资源越权进入 `failed_items` 且写安全审计。

## ADR-016：Job Card 同步必须采用 Outbox / After-Commit / 异步重试

- 决策时间：2026-04-12 14:38 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003D Job Card 同步 Outbox 整改
- 审计来源：审计意见书第 17 份，Job Card 同步发生在本地事务提交前，存在 ERPNext 已更新但本地工票/审计回滚风险
- 结论：工票写接口不得在本地事务提交前调用 ERPNext Job Card 更新，必须先持久化本地工票、日薪、审计和 outbox，再由 after-commit/worker 异步同步 ERPNext

### 背景

工票写入和 ERPNext Job Card 更新属于跨系统写入。如果在本地事务提交前先调用 ERPNext，可能出现 ERPNext 更新成功但本地工票事务回滚的情况，造成生产进度和工资事实不一致。

### 决策

1. `ys_workshop_ticket`、`ys_workshop_daily_wage`、操作审计、`ys_workshop_job_card_sync_outbox` 必须在同一本地事务内写入。
2. ERPNext Job Card 更新必须发生在本地事务提交之后。
3. 本地事务提交失败、操作审计失败、outbox 写入失败时，禁止调用 ERPNext。
4. outbox 是待同步任务事实，`ys_workshop_job_card_sync_log` 是每次尝试日志。
5. Worker 必须使用最终态覆盖方式同步 Job Card 完成数量，不得增量累加。
6. Worker 失败必须记录 attempts、next_retry_at、last_error_code，并支持重试和 dead 状态。
7. 服务账号必须最小权限，只允许读取 Job Card / Work Order 和更新 Job Card 完成数量。
8. 服务账号凭证不得进入日志、审计表或 sync_log。

### 验收

1. 工票登记、撤销、批量导入在本地事务提交前不调用 ERPNext。
2. 本地 commit 失败时不调用 ERPNext。
3. 审计写入失败时不调用 ERPNext。
4. 本地事务成功后产生 pending outbox。
5. Worker 成功后 outbox 变为 `succeeded` 并写 sync_log。
6. Worker 重试不会导致 ERPNext Job Card 完成数量重复累加。

## ADR-017：Outbox `event_key` 必须采用短前缀 + 完整 Hash，禁止截断 Digest

- 决策时间：2026-04-12 15:09 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003E Outbox Event Key 截断碰撞整改
- 审计来源：审计意见书第 18 份，`event_key` 先拼 `job_card:digest` 再截断导致长 Job Card 截掉 digest
- 结论：outbox `event_key` 必须采用固定短前缀 + 完整 SHA-256 摘要，禁止任何截断 digest 的实现

### 背景

审计发现 outbox `event_key` 当前先拼接 `job_card:digest` 再截断到 140 字符。当 `job_card` 长度达到 138/139/140 字符时，digest 可能被截掉，不同同步事件会生成相同 key，导致 outbox 误判任务已存在，最终漏同步 ERPNext Job Card 完成数量。

### 决策

1. `event_key` 禁止使用 `f\"{job_card}:{digest}\"[:140]` 或任何先拼接后截断模式。
2. `event_key` 必须采用固定短前缀 + 完整 SHA-256 摘要。
3. 推荐格式：`wjc:{sha256_hex}`，总长度 68。
4. hash 输入必须包含 `job_card`、`local_completed_qty`、`source_type`、排序后的 `source_ids`。
5. `source_ids` 必须排序，保证同一集合生成同一 key。
6. `local_completed_qty` 必须数值规范化，保证 `1`、`1.0`、`1.000000` 语义相同时 key 相同。
7. `event_key` 不保存完整 `job_card` 明文；排查使用 outbox 独立字段 `job_card`。
8. 旧 pending/failed outbox 不得因迁移被删除。

### 验收

1. 138/139/140/200 字符 Job Card 下，不同同步事件生成不同 `event_key`。
2. `event_key` 长度稳定小于等于 140。
3. `event_key` 不包含完整 `job_card` 明文。
4. 旧算法会碰撞的样例在新算法下不碰撞。
5. 重复同一业务事件仍返回同一 `event_key`。

## ADR-018：内部 Job Card 同步 Worker 接口必须服务账号化

- 决策时间：2026-04-12 15:21 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003F 内部 Job Card 同步 Worker 接口权限整改
- 审计来源：审计意见书第 19 份，`/api/workshop/internal/job-card-sync/run-once` 可能被普通车间管理角色触发
- 结论：内部 Worker 批量写同步接口必须与普通业务同步权限分离，只允许服务账号、系统级集成账号或 System Manager 调用

### 背景

TASK-003D 已将 ERPNext Job Card 写同步从工票登记事务内移到 outbox/worker 模式，TASK-003E 已修复 outbox `event_key` 截断碰撞。审计继续指出，内部 Worker 触发接口如果可由普通车间管理角色调用，会扩大批量写 ERPNext Job Card 的权限面，破坏内部调度和服务账号边界。

### 决策

1. `POST /api/workshop/internal/job-card-sync/run-once` 定义为内部接口，不是普通前端业务接口。
2. 内部 Worker 接口必须要求 `workshop:job_card_sync_worker`。
3. `workshop:job_card_sync_worker` 只能授予服务账号、系统级集成账号或 `System Manager`。
4. 普通 `Workshop Manager / Workshop User / Production Manager` 不得调用内部 Worker。
5. `workshop:job_card_sync` 仅用于用户手动重试单个 Job Card 同步，不得替代内部 Worker 权限。
6. 普通用户的 `/api/auth/actions?module=workshop` 不得返回 `workshop:job_card_sync_worker`。
7. 生产环境保留 HTTP 内部 Worker 接口时，必须支持 `ENABLE_INTERNAL_WORKER_API` 开关；未开启时返回 `INTERNAL_API_DISABLED`。
8. 未登录、权限不足、权限来源不可用、内部接口关闭时，不得锁定、更新或重试 outbox，也不得调用 ERPNext。
9. 权限拒绝必须写安全审计，成功执行 Worker 必须写操作审计。
10. 服务账号凭证、Authorization、Cookie、secret、password 不得进入日志或审计表。

### 验收

1. 普通 `Workshop Manager` 调用内部 Worker 返回 `AUTH_FORBIDDEN`。
2. 只有 `workshop:job_card_sync` 的用户调用内部 Worker 返回 `AUTH_FORBIDDEN`。
3. 未登录调用内部 Worker 返回 `AUTH_UNAUTHORIZED`。
4. 权限来源不可用时返回 `PERMISSION_SOURCE_UNAVAILABLE`。
5. 生产环境内部接口未开启时返回 `INTERNAL_API_DISABLED`。
6. 未授权调用不得修改 outbox，不得写 sync_log，不得调用 ERPNext。
7. 服务账号具备 `workshop:job_card_sync_worker` 时可以处理 pending outbox。
8. 普通用户动作权限列表不包含 `workshop:job_card_sync_worker`。

## ADR-019：服务账号必须按 ERPNext User Permission 执行最小资源权限

- 决策时间：2026-04-12 15:40 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003G 服务账号最小权限策略整改
- 审计来源：审计意见书第 21 份，服务账号仍是“全模块全资源”硬编码授权并跳过 item/company 资源校验
- 结论：服务账号不得默认全模块全资源；内部 Worker 每处理一条 outbox 前，必须按 ERPNext User Permission 校验 `company/item_code`

### 背景

TASK-003F 已将内部 Worker API 限定为服务账号或系统级集成账号调用。但如果服务账号本身被硬编码为全模块全资源，仍会绕过最小权限原则。服务账号一旦被误用或凭证泄露，就可能批量处理全部公司、全部款式的 Job Card 同步。

### 决策

1. 删除或废弃服务账号默认全模块全资源授权。
2. 服务账号身份必须来自 ERPNext `User + Role`，不得由请求参数声明。
3. 服务账号动作权限必须具备 `workshop:job_card_sync_worker`。
4. 服务账号资源权限必须来自 ERPNext `User Permission`。
5. 服务账号必须明确具备目标 `Company` 和 `Item` 权限，才能处理对应 outbox。
6. 没有明确 Company 或 Item 授权时，默认 fail closed。
7. 禁止用 `*`、`all`、空数组表示全资源。
8. ERPNext 权限来源不可用时返回 `PERMISSION_SOURCE_UNAVAILABLE`，不得处理 outbox。
9. outbox 缺少 `company/item_code` 时，不得调用 ERPNext。
10. 资源越权 outbox 不得标记为 `succeeded`，不得写成功 sync_log。
11. 服务账号资源越权必须写安全审计。
12. 服务账号成功处理 outbox 必须写操作审计，并记录 `principal/outbox_id/job_card/item_code/company/request_id`。

### 验收

1. 代码中不存在服务账号默认 `all modules/all resources/*` 硬编码授权。
2. 服务账号只有 `workshop:job_card_sync_worker` 但没有 Company/Item 权限时，不处理 outbox。
3. 服务账号授权 `COMPANY-A + ITEM-B` 时，不能处理 `COMPANY-A + ITEM-A`。
4. 服务账号授权 `COMPANY-A + ITEM-B` 时，不能处理 `COMPANY-B + ITEM-B`。
5. 服务账号授权 `COMPANY-A + ITEM-B` 时，可以处理匹配 outbox。
6. 资源越权 outbox 不调用 ERPNext，不写成功 sync_log，不标记 `succeeded`。
7. 权限来源不可用时返回 `PERMISSION_SOURCE_UNAVAILABLE`。
8. 安全审计和操作审计均不泄露服务账号凭证。

## ADR-020：Outbox Worker 必须先按服务账号 Scope 过滤再 Limit

- 决策时间：2026-04-12 16:02 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003H Outbox 跳过行队头阻塞整改
- 审计来源：审计意见书第 22 份，越权 pending outbox 配合 `list_due(limit)` 固定窗口导致后续有权任务被长期阻塞
- 结论：内部 Worker 主处理查询必须先按服务账号 `company/item_code` scope 做数据库层过滤，再执行排序和 limit；缺 scope 行必须脱离 due pending 队列

### 背景

审计复现：第 1 条 outbox 越权、第 2 条 outbox 有权，`limit=1` 连续两轮都只跳过第 1 条越权行，第 2 条有权行始终未处理，ERPNext 调用 0 次。根因是 Worker 先按 `id asc limit` 取 due pending 窗口，再在 Python 中逐行跳过越权行，导致越权行长期占据队头。

### 决策

1. 新增或改造主处理查询为 `list_due_for_service_account(policy, limit)`。
2. 主处理查询必须先限定 `status/next_retry_at`。
3. 主处理查询必须在数据库层追加 `company in allowed_companies` 和 `item_code in allowed_items`。
4. `order by id asc` 和 `limit batch_size` 必须发生在资源过滤之后。
5. 禁止先 `limit` 再逐行权限过滤。
6. 当前服务账号越权 outbox 不得被锁定、增加 attempts、更新 `locked_by/locked_at` 或标记成功。
7. 缺少 `company/item_code` 的 outbox 必须转为 `dead` 或 `blocked_scope`，并写 `SERVICE_ACCOUNT_RESOURCE_SCOPE_REQUIRED` 安全审计。
8. 服务账号空 scope 不得查询全量 outbox。
9. 权限来源不可用时不得执行 outbox 查询锁定或 ERPNext 调用。
10. 如果保留 `skipped_forbidden_count`，只能来自独立诊断扫描，不得消费主处理 limit。

### 验收

1. 第 1 条 outbox 越权、第 2 条 outbox 有权、`batch_size=1` 时，第一轮 Worker 必须处理第 2 条有权 outbox。
2. 连续两轮 Worker 不得重复只跳过同一条越权队头行。
3. 主处理查询在 SQLAlchemy 查询层包含 `company/item_code` scope 过滤。
4. 越权 outbox 不被当前服务账号锁定或增加 attempts。
5. 缺 scope outbox 不会长期停留在 due pending 队头。
6. 空 scope 服务账号不触发全量扫描。
7. 权限来源不可用时不锁定 outbox、不调用 ERPNext。

## ADR-021：越权 Outbox 诊断必须节流并对安全审计去重

- 决策时间：2026-04-12 16:15 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003I Outbox 越权诊断节流与审计去重整改
- 审计来源：审计意见书第 23 份，越权 outbox 每轮 run-once 重复枚举并写安全审计，存在审计表放大和 Worker 事务变重风险
- 结论：默认 Worker 不扫描越权 outbox；越权诊断必须显式启用、限量扫描，并按 `outbox_id + principal + reason_code + scope_hash` 做冷却期去重

### 背景

TASK-003H 已关闭队头阻塞高危问题，但越权 outbox 仍可能在每轮 run-once 中被重复枚举并写安全审计。多公司、多服务账号场景下，这会造成安全审计表快速膨胀，也会让同步事务承担与实际同步无关的重复诊断负担。

### 决策

1. 默认 `run-once` 只处理当前服务账号有权的 outbox，不扫描越权 outbox。
2. 越权诊断必须显式启用，不能作为主处理默认路径。
3. 越权诊断必须设置单次扫描上限，默认不超过 50 行。
4. 同一 `outbox_id + principal + reason_code + scope_hash` 在冷却期内只能写 1 条安全审计。
5. 推荐冷却期为 6 小时，通过 `WORKSHOP_OUTBOX_DENIAL_AUDIT_COOLDOWN_SECONDS` 配置。
6. 冷却期内重复发现只更新 denial 记录的 `seen_count/last_seen_at`。
7. 冷却期到期、scope 变化或 reason 变化时，才允许再次写安全审计。
8. 越权诊断不得锁定 outbox，不得增加 attempts，不得更新 `next_retry_at`，不得调用 ERPNext。
9. 缺 scope outbox 已转为 `dead/blocked_scope` 后，不得继续进入越权诊断扫描。
10. 去重 key 和审计上下文不得包含 Authorization、Cookie、token、secret、password 明文。

### 验收

1. 连续 3 次默认 `run-once` 不新增同一越权 outbox 的安全审计。
2. 显式诊断首次发现越权 outbox 时写 1 条安全审计。
3. 冷却期内重复诊断同一拒绝事件不重复写安全审计。
4. 冷却期内重复诊断只更新 `seen_count/last_seen_at`。
5. 冷却期到期后再次诊断同一拒绝事件最多新增 1 条安全审计。
6. 越权诊断不锁定 outbox、不增加 attempts、不调用 ERPNext。
7. TASK-003H 的队头阻塞回归用例继续通过。

## ADR-022：内部 Worker Dry-Run 必须可追溯且生产默认禁用

- 决策时间：2026-04-12 16:37 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003J 内部 Worker Dry-Run 操作审计整改
- 审计来源：TASK-003I 审计意见，内部 Worker `dry_run=true` 路径不写操作审计，敏感内部诊断接口存在不可追溯调用
- 结论：dry-run 属于敏感内部诊断行为，授权成功必须写操作审计；生产环境默认禁用，除非显式开启

### 背景

`dry_run=true` 虽不修改 outbox、不调用 ERPNext，但会读取内部同步候选、服务账号 scope、诊断结果和生产同步状态。该能力如果没有操作审计，服务账号或运维调用将不可追溯。

### 决策

1. 生产环境默认禁用 dry-run，除非 `WORKSHOP_ENABLE_WORKER_DRY_RUN=true`。
2. dry-run 被禁用时返回 `WORKSHOP_DRY_RUN_DISABLED`，写安全审计，不查询 outbox。
3. dry-run 授权成功必须写操作审计。
4. dry-run 操作审计失败时返回 `AUDIT_WRITE_FAILED`，不得静默成功。
5. dry-run 不得锁定 outbox，不得增加 attempts，不得更新 `next_retry_at`，不得调用 ERPNext，不得写成功 sync_log。
6. dry-run 操作审计必须记录 `principal/dry_run/batch_size/include_forbidden_diagnostics/would_process_count/request_id`。
7. dry-run 操作审计不得包含 Authorization、Cookie、token、secret、password 明文。
8. dry-run 与 forbidden diagnostics 联用时，仍必须遵守 TASK-003I 的诊断节流和安全审计去重规则。

### 验收

1. 生产环境未开启 dry-run 时返回 `WORKSHOP_DRY_RUN_DISABLED`。
2. dry-run 被禁用时不查询 outbox、不调用 ERPNext、不写操作审计，但写安全审计。
3. 授权 dry-run 成功时新增 1 条操作审计。
4. dry-run 操作审计写入失败时返回 `AUDIT_WRITE_FAILED`。
5. dry-run 不修改 outbox 状态、attempts、锁字段、重试时间。
6. dry-run 不新增成功 sync_log，不调用 ERPNext。
7. dry-run + forbidden diagnostics 仍遵守冷却期去重。

## ADR-023：生产 Dry-Run 禁用判断必须早于 ERPNext 资源权限读取

- 决策时间：2026-04-12 16:53 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003K 生产 Dry-Run 禁用判断前置整改
- 审计来源：TASK-003J 审计意见，生产 dry-run 禁用且 ERPNext 权限源超时时返回 `PERMISSION_SOURCE_UNAVAILABLE`，说明禁用判断晚于权限源读取
- 结论：生产 dry-run 禁用是本地配置门禁；完成身份、动作权限、服务账号主体校验后，必须立即返回 `WORKSHOP_DRY_RUN_DISABLED`，不得读取 ERPNext User Permission 或 outbox

### 背景

生产环境默认禁用 dry-run。该禁用状态应由本地配置决定，不应依赖 ERPNext 权限源是否可用。若禁用判断晚于 ERPNext User Permission 查询，权限源故障会导致禁用 dry-run 返回 `PERMISSION_SOURCE_UNAVAILABLE`，既暴露外部依赖状态，也让禁用门禁不稳定。

### 决策

1. `dry_run=true` 请求先完成 request_id 规范化和当前用户解析。
2. 未登录返回 `AUTH_UNAUTHORIZED`。
3. 缺少 `workshop:job_card_sync_worker` 返回 `AUTH_FORBIDDEN`。
4. 非服务账号、非系统级集成账号、非 `System Manager` 返回 `AUTH_FORBIDDEN`。
5. 通过身份和主体校验后，若生产 dry-run 未开启，立即返回 `WORKSHOP_DRY_RUN_DISABLED`。
6. 返回 `WORKSHOP_DRY_RUN_DISABLED` 前不得读取 ERPNext User Permission。
7. 返回 `WORKSHOP_DRY_RUN_DISABLED` 前不得解析服务账号资源策略。
8. 返回 `WORKSHOP_DRY_RUN_DISABLED` 前不得查询 outbox 或 forbidden diagnostics。
9. dry-run 开启后仍必须读取 ERPNext User Permission，并按服务账号资源策略 fail closed。
10. dry-run disabled 不得掩盖未登录、无动作权限或非服务账号主体错误。

### 验收

1. 生产 dry-run 禁用且 ERPNext User Permission 超时时，返回 `WORKSHOP_DRY_RUN_DISABLED`。
2. 上述场景 ERPNext User Permission 查询调用次数为 0。
3. 上述场景 outbox 查询调用次数为 0。
4. 上述场景写安全审计，不写操作审计。
5. 未登录、无动作权限、非服务账号主体仍返回对应鉴权错误。
6. dry-run 开启且 ERPNext 权限源不可用时，仍返回 `PERMISSION_SOURCE_UNAVAILABLE`。

## ADR-024：历史 Item 工价必须补齐 Company，计薪匹配必须与工价权限过滤同口径

- 决策时间：2026-04-12 17:07 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003L 历史工价 Company 补数与计薪一致性整改
- 审计来源：TASK-003K 审计意见，历史 item 工价 `company=NULL` 迁移策略未闭环，存在页面权限过滤不可见但登记计薪仍命中的风险
- 结论：`item_code IS NOT NULL AND company IS NULL` 的 active 工价属于历史脏数据，必须补数或阻断；工票计薪匹配必须带 `company`

### 背景

工价列表已按 `item_code/company` 做资源过滤。如果历史款式工价缺少 company，页面可能因 company 过滤看不到该工价；但登记计薪若只按 `item_code/process_name/effective_date` 匹配，则仍可能命中该历史工价，造成权限可见性和工资计算口径不一致。

### 决策

1. 款式专属工价必须绑定明确 `company`。
2. `item_code IS NOT NULL AND company IS NULL` 的 active 工价不得参与计薪。
3. `company=NULL` 只允许用于明确的全局通用工价，且必须满足 `item_code IS NULL` 和全局工价权限。
4. 历史 item 工价必须通过迁移或补数脚本补齐 company。
5. 可唯一解析 company 的历史工价允许自动补数。
6. 多 company 或无法解析 company 的历史工价必须停用或标记 `blocked_scope`，不得保持 active 可用。
7. 工票登记计薪必须使用 ERPNext `Job Card -> Work Order` 派生 company 匹配工价。
8. 工价列表、工价维护、工票计薪必须使用同一套 `item_code/company` scope 口径。
9. 创建款式专属工价时 company 为空必须返回 `WORKSHOP_WAGE_RATE_COMPANY_REQUIRED`。
10. 补数、阻断、冲突和失败必须写补数日志或等价审计记录。

### 验收

1. 迁移后不存在 `item_code IS NOT NULL AND company IS NULL AND status='active'` 的工价。
2. 可唯一解析 company 的历史 item 工价被补齐 company。
3. 无法解析或多 company 冲突的历史 item 工价被停用或标记 `blocked_scope`。
4. 工票登记不会命中历史 `company=NULL` 款式工价。
5. 工票登记使用 Job Card / Work Order 派生 company 匹配同 company 工价。
6. 工价列表可见集合和计薪可命中集合在 `item_code/company` 口径上一致。
7. 补数脚本可重复执行，且不会重复放大日志。

## ADR-025：工价 Company 缺失口径统一为 NULL / 空字符串 / 空白字符串

- 决策时间：2026-04-12 17:31 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003M 工价 Company 空字符串口径统一整改
- 审计来源：TASK-003L 审计意见，`company=''` active item 工价仍出现在列表中，计薪错误码与缺 scope 场景不一致
- 结论：`company IS NULL OR trim(company) = ''` 必须统一视为缺失 company；迁移、列表过滤、补数扫描、计薪阻断和写入校验必须共用同一口径

### 背景

TASK-003L 解决了 `company=NULL` 的历史工价问题，但空字符串和空白字符串仍可能作为脏数据存在。若列表过滤、计薪匹配、补数扫描只判断 SQL NULL，会出现空字符串工价页面可见、计薪错误码不一致等问题。

### 决策

1. `NULL`、`''`、`'   '` 在工价 company 语义上全部等价为“缺失 company”。
2. 必须提供或统一使用 `is_missing_company(company)` / `normalize_company(company)` 等价逻辑。
3. 迁移补数扫描必须覆盖 `company IS NULL OR btrim(company) = ''`。
4. 空字符串和空白字符串必须先 normalize 为 NULL，再进入补数或阻断流程。
5. 工价列表不得返回 active `item_code IS NOT NULL AND is_missing_company(company)` 记录。
6. 工票登记计薪不得命中 active `item_code IS NOT NULL AND is_missing_company(company)` 记录。
7. 创建或更新款式专属工价时，normalize 后 company 为空必须返回 `WORKSHOP_WAGE_RATE_COMPANY_REQUIRED`。
8. 计薪发现唯一候选是缺失 company 历史 item 工价时，必须返回 `WORKSHOP_WAGE_RATE_SCOPE_REQUIRED`。
9. `WORKSHOP_WAGE_RATE_NOT_FOUND` 只用于确实不存在任何有效工价候选的场景。
10. 迁移后不得存在 active `item_code IS NOT NULL AND (company IS NULL OR btrim(company) = '')` 工价。

### 验收

1. `company=NULL`、`company=''`、`company='   '` 全部被判定为缺失。
2. 创建 item 工价时 company 为 `NULL/''/'   '` 均返回 `WORKSHOP_WAGE_RATE_COMPANY_REQUIRED`。
3. 列表不返回 active 空字符串或空白字符串 company 的 item 工价。
4. 计薪不命中空字符串或空白字符串 company 的 item 工价。
5. 唯一候选为缺失 company 历史工价时返回 `WORKSHOP_WAGE_RATE_SCOPE_REQUIRED`。
6. 迁移后不存在 active `item_code IS NOT NULL AND (company IS NULL OR btrim(company) = '')` 工价。

## ADR-026：工价 Company 补数 Dry-Run 必须零写入且不挂载 ORM 对象

- 决策时间：2026-04-12 17:59 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003N 工价补数 Dry-Run 只读整改
- 审计来源：TASK-003M 审计意见，`backfill_wage_rate_company_scope(dry_run=True)` 会把补数日志对象挂入 session，调用方后续 commit 仍会持久化日志
- 结论：工价 company 补数 dry-run 必须是真正只读预演；不得向 session 挂载 ORM 对象，调用方 commit 后也不得产生补数日志、工价变更或审计记录

### 背景

补数 dry-run 用于迁移前预演和管理入口预览。如果 dry-run 路径仍将补数日志 ORM 对象加入当前 session，即使函数本身不 commit，调用方后续 commit 也会产生持久化日志，破坏 dry-run 的只读语义。

### 决策

1. `backfill_wage_rate_company_scope(dry_run=True)` 不得调用 `session.add/add_all/merge/delete/flush/commit`。
2. dry-run 不得修改已加载 ORM 实例字段。
3. dry-run 不得创建并挂载补数日志 ORM 对象。
4. dry-run 报告必须使用 plain dict、dataclass、Pydantic schema 或不可持久化 DTO。
5. dry-run 结束后 `session.new/session.dirty/session.deleted` 必须为空。
6. dry-run 后调用方即使执行 `session.commit()`，也不得新增补数日志、不得修改工价、不得新增审计。
7. 推荐将补数流程拆分为 `build_plan` 与 `apply_plan`；dry-run 只 build plan，execute 才 apply plan。
8. `dry_run=False` 正式执行仍必须写补数日志并更新工价 company/status。

### 验收

1. dry-run 后 `session.new/session.dirty/session.deleted` 均为空。
2. dry-run 后调用 `session.commit()`，补数日志新增 0 条。
3. dry-run 后调用 `session.commit()`，工价 company/status 无变化。
4. dry-run 后调用 `session.commit()`，审计表无 dry-run 副作用记录。
5. dry-run 报告行不是 SQLAlchemy ORM 持久化对象。
6. execute 正式执行仍能写补数日志并更新工价。

## ADR-027：工价补数解析数据库读取失败必须 Fail Closed

- 决策时间：2026-04-12 18:17 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003O 工价补数读取失败 Fail Closed 整改
- 审计来源：TASK-003N 审计意见，`_resolve_backfill_companies()` 吞掉 `SQLAlchemyError` 并继续生成补数计划
- 结论：补数链路中数据库读取失败是系统级失败，必须返回 `DATABASE_READ_FAILED` 并中断计划生成；不得降级为业务多义或无法解析

### 背景

工价 company 补数依赖数据库中的 scoped 工价、历史引用和 ERPNext 派生关系。如果某一步数据库读取失败，继续生成补数计划会基于不完整事实做数据修复判断，可能错误停用、错误补数或漏补数。

### 决策

1. `_resolve_backfill_companies()` 捕获 `SQLAlchemyError` 时必须转换为 `DATABASE_READ_FAILED`。
2. 数据库读取失败不得返回空候选、`ambiguous_company` 或 `company_unresolved`。
3. `build_wage_rate_company_backfill_plan()` 遇到 `DATABASE_READ_FAILED` 必须立即中断。
4. dry-run 读取失败不得返回成功报告，不得挂载 ORM 对象。
5. execute 读取失败必须 rollback，不得更新工价、不得写补数日志。
6. `ambiguous_company/company_unresolved` 只允许在数据库读取成功后作为业务判断结果出现。
7. 错误日志必须脱敏，不得记录 SQL 原文、参数、连接串或敏感凭证。

### 验收

1. 模拟 scoped 工价候选查询抛出 `SQLAlchemyError` 时返回 `DATABASE_READ_FAILED`。
2. 读取失败不返回 `ambiguous_company` 或 `company_unresolved`。
3. 读取失败不返回成功 dry-run 报告。
4. dry-run 读取失败后 session 无 new/dirty/deleted。
5. execute 读取失败后无工价变更、无补数日志。
6. 查询成功但业务多义时仍返回 `WORKSHOP_WAGE_RATE_COMPANY_AMBIGUOUS`。
7. 查询成功但业务无法解析时仍返回 `WORKSHOP_WAGE_RATE_COMPANY_UNRESOLVED`。

## ADR-028：ERPNext Item 候选查询不可用必须 Fail Closed

- 决策时间：2026-04-12 18:30 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003P 工价补数 ERPNext Item 候选查询 Fail Closed 整改
- 审计来源：TASK-003O 审计意见，ERPNext Item 候选查询不可用时仍静默当成“无候选”，execute 会把历史工价停用为 `WORKSHOP_WAGE_RATE_COMPANY_UNRESOLVED`
- 结论：ERPNext Item 候选查询不可用是外部主数据源不可用，必须 fail closed；不得降级为无候选、不得停用历史工价、不得写补数日志

### 背景

工价 company 补数需要从 ERPNext Item 主数据解析候选 company。如果 ERPNext Item 候选查询失败，系统无法确认“确实没有候选”还是“主数据源暂时不可用”。继续执行会把系统故障误判为业务无法解析，导致 active 历史工价被错误停用。

### 决策

1. ERPNext Item 候选查询超时、连接失败、HTTP 非成功、响应解析失败或权限源不可用时，必须 fail closed。
2. 如果候选查询通过 ERPNext REST API 执行，不可用时返回 `ERPNEXT_SERVICE_UNAVAILABLE` 或等价主数据源不可用错误。
3. 如果候选查询通过共享 PostgreSQL `public.tabItem` 执行，SQLAlchemy 读取失败时返回 `DATABASE_READ_FAILED`。
4. 不可用场景不得返回空候选集合。
5. 不可用场景不得返回 `WORKSHOP_WAGE_RATE_COMPANY_UNRESOLVED`。
6. 不可用场景不得停用历史工价、不得标记 `blocked_scope`、不得写补数日志。
7. dry-run 不得返回成功预演报告。
8. execute 必须 rollback，不得更新 `ly_operation_wage_rate.company/status`。
9. 只有 ERPNext Item 查询成功且确认无候选时，才允许返回 `WORKSHOP_WAGE_RATE_COMPANY_UNRESOLVED`。
10. 错误日志必须脱敏，不得记录 ERPNext token、Cookie、Authorization、响应原文中的敏感字段。

### 验收

1. 模拟 ERPNext Item 候选查询超时时返回 `ERPNEXT_SERVICE_UNAVAILABLE` 或 `DATABASE_READ_FAILED`。
2. ERPNext Item 候选查询不可用时不返回 `WORKSHOP_WAGE_RATE_COMPANY_UNRESOLVED`。
3. ERPNext Item 候选查询不可用时不返回成功 dry-run 报告。
4. execute 遇到 ERPNext Item 候选查询不可用时不修改工价 company/status。
5. execute 遇到 ERPNext Item 候选查询不可用时不新增补数日志。
6. ERPNext Item 查询成功且确认无候选时，仍按业务规则返回 `WORKSHOP_WAGE_RATE_COMPANY_UNRESOLVED`。

## ADR-029：内部 Worker 响应中 skipped_forbidden 字段必须废弃或标注诊断语义

- 决策时间：2026-04-12 19:15 CST
- 决策类型：架构审计整改
- 关联模块：TASK-003Q 内部 Worker skipped_forbidden 兼容字段语义整改
- 审计来源：TASK-003P 审计意见，内部 Worker 响应仍保留 `skipped_forbidden_count/skipped_forbidden` 兼容字段，且字段值等于 `forbidden_diagnostic`，可能被误读为实际跳过的越权 outbox 数
- 结论：`forbidden_diagnostic_count` 是越权诊断唯一推荐字段；`skipped_forbidden_count/skipped_forbidden` 必须废弃或明确标注为 diagnostics-only 兼容别名

### 背景

TASK-003H 到 TASK-003I 已把越权 outbox 从主处理窗口中移除，并改为显式诊断扫描。此后内部 Worker 主处理不再“跳过越权 outbox”，而是默认只查询当前服务账号有权处理的 outbox。继续暴露 `skipped_forbidden_count/skipped_forbidden` 会让前端或运维误以为主处理流程跳过了越权行，影响排障判断。

### 决策

1. 主处理指标只使用 `would_process_count/processed_count/succeeded_count/failed_count/blocked_scope_count/dead_count`。
2. 越权诊断指标只推荐使用 `forbidden_diagnostic_count`。
3. `skipped_forbidden_count/skipped_forbidden` 是历史兼容字段，不得作为新契约继续推荐。
4. 如立即删除会破坏兼容，本阶段允许保留，但必须在 Pydantic Field、OpenAPI description、接口文档中标注 `deprecated`、`diagnostics-only`、`alias of forbidden_diagnostic_count`。
5. 默认主处理模式未启用诊断时，`forbidden_diagnostic_count/skipped_forbidden_count/skipped_forbidden` 必须均为 0。
6. 显式启用诊断时，兼容字段如保留只能等于 `forbidden_diagnostic_count`，不得描述为主处理跳过数。
7. 推荐新增 `forbidden_diagnostics_enabled` 或等价字段，明确本次响应是否执行了越权诊断扫描。
8. 操作审计 `after_data` 的判读以 `forbidden_diagnostic_count` 为准。
9. 下一次破坏性 API 版本升级时删除 `skipped_forbidden_count/skipped_forbidden`。

### 验收

1. OpenAPI schema 中 `skipped_forbidden_count/skipped_forbidden` 标注 deprecated 和 diagnostics-only。
2. 接口文档不再把 `skipped_forbidden_count/skipped_forbidden` 描述为主处理跳过数。
3. 默认 `run-once` 不启用诊断时三个相关字段均为 0。
4. `include_forbidden_diagnostics=true` 时兼容字段与 `forbidden_diagnostic_count` 相等，并有清晰诊断说明。
5. 前端和测试只断言推荐字段 `forbidden_diagnostic_count`，不新增对兼容字段的业务依赖。

## ADR-030：外发模块 V1.1 生产级契约冻结

- 决策时间：2026-04-12 20:08 CST
- 决策类型：架构审计整改 / 开发前门禁
- 关联模块：TASK-002A 外发模块设计契约冻结
- 审计来源：TASK-002A 任务单/架构审阅意见，要求补齐写接口幂等契约、对账结算边界、内部库存同步 worker 安全契约
- 结论：外发模块进入工程实现前，必须以 V1.1 契约为唯一基线；未满足幂等、结算锁定和 worker 安全契约，不得进入 TASK-002B

### 背景

外发加工链路会同时写本地业务事实和 ERPNext 库存单据。若写接口无幂等，重复提交会产生重复发料、重复回料、重复验货和重复 Stock Entry outbox。若对账边界不清，TASK-006 加工厂对账会无法锁定结算金额。若内部库存同步 worker 缺少安全契约，普通账号或越权服务账号可能触发库存落账。

### 决策

1. 外发写接口必须支持 `idempotency_key` 或业务唯一键。
2. 相同幂等键 + 相同 payload 必须返回第一次结果，不新增本地事实、不新增 outbox、不新增 ERPNext Stock Entry。
3. 相同幂等键 + 不同 payload 必须返回 `SUBCONTRACT_IDEMPOTENCY_CONFLICT`。
4. 外发主单必须包含 `statement_id/settlement_no/settled_by/settled_at`。
5. `settlement_status=settled` 后禁止修改金额、发料、回料、验货、取消。
6. 已结算外发单调整必须通过 TASK-006 调整单或反冲单，不得直接改原始金额。
7. 内部库存同步 worker 生产默认关闭，必须通过显式开关启用。
8. 内部库存同步 worker 必须使用服务账号最小权限，并按 `company/item_code/supplier/warehouse` 做资源级过滤。
9. dry-run 必须可审计且生产默认禁用。
10. 越权 outbox 不得进入主处理窗口，诊断必须显式启用、限流、去重。

### 验收

1. TASK-002 V1.1 设计文档包含写接口幂等契约。
2. TASK-002 V1.1 设计文档包含对账结算字段和锁定规则。
3. TASK-002 V1.1 设计文档包含内部库存同步 worker 安全契约。
4. TASK-002A 任务单包含上述三类契约的可检查验收标准。
5. 当前 sprint 任务清单标注 TASK-002A 为 TASK-002B~TASK-002H 的开发前门禁。

## ADR-031：TASK-002B 阶段回料与验货旧演示成功路径必须 Fail Closed

- 决策时间：2026-04-12 21:25 CST
- 决策类型：架构审计整改 / 阶段门禁
- 关联模块：TASK-002B1 回料验货旧演示路径 Fail Closed 整改
- 审计来源：TASK-002B 审计意见，回料和验货接口仍保留旧演示成功路径，越过 TASK-002B 边界
- 结论：TASK-002B 阶段只做权限、审计、统一错误和事务边界；回料和验货不得继续旧演示成功路径，必须在鉴权和资源权限通过后 fail closed

### 背景

TASK-002B 不实现回料 Stock Entry outbox，也不实现验货扣款正式金额口径。若继续允许 `receive()` 写本地回料事实并推进状态，或允许 `inspect()` 使用旧公式完成验货，会在后续 outbox 和金额口径未落地前制造错误事实，甚至为 TASK-006 对账和库存同步留下脏数据。

### 决策

1. `POST /api/subcontract/{id}/receive` 在 TASK-002B 阶段必须 fail closed。
2. 回料接口必须先完成认证、动作权限、资源权限校验，再返回阶段门禁错误。
3. 回料接口不得新增 `ly_subcontract_receipt`，不得推进状态，不得伪造 `stock_entry_name`。
4. `POST /api/subcontract/{id}/inspect` 在 TASK-002B 阶段必须 fail closed。
5. 验货接口不得新增验货事实，不得推进 `completed`，不得写金额字段。
6. 验货接口不得继续使用旧公式 `net_amount = inspected_qty - deduction_amount`。
7. `draft` 直接回料通道必须删除。
8. 回料接口必须鉴权优先于 payload 字段校验。
9. 数据库写入、flush、commit 失败必须归类为 `DATABASE_WRITE_FAILED`。
10. TASK-002C/D/E/F 未审计通过前，不得恢复回料/验货成功路径。

### 验收

1. 回料鉴权通过后返回 fail closed 错误，且不新增回料事实。
2. 回料不修改外发状态，不新增成功状态日志，不返回伪 `stock_entry_name`。
3. 验货鉴权通过后返回 fail closed 错误，且不新增验货事实。
4. 验货不修改外发状态，不写金额，不使用旧金额公式。
5. 未登录、无权限、权限源不可用优先于 payload 字段校验。
6. DB 写失败返回 `DATABASE_WRITE_FAILED`。
7. 全量测试通过，且 `rg "STE-ISS|net_amount = .*inspected_qty"` 不命中外发正向实现。

## ADR-032：外发资源权限必须以本地 company 事实字段为准

- 决策时间：2026-04-12 21:51 CST
- 决策类型：架构审计整改 / 数据模型门禁
- 关联模块：TASK-002C 外发数据模型与迁移
- 审计来源：TASK-002B1 审计通过后进入 TASK-002C 的门禁意见，要求补齐外发单本地 `company` 字段、迁移、回填和索引，让资源权限从“推断”升级为“本地事实字段校验”
- 结论：外发主单 `ly_subcontract_order.company` 是外发资源权限的本地权威字段；TASK-002C 完成后，列表、详情、写接口和后续 outbox 查询必须以本地 `company` 做 fail closed 权限校验

### 背景

外发加工横跨公司、加工厂、款式、仓库和 ERPNext 库存单据。若外发单没有本地 `company` 事实字段，后端只能在每次请求时通过 Item、Supplier、BOM 或请求上下文推断公司归属。该方式在多公司、多候选、ERPNext 主数据不可用或历史脏数据场景下容易误判资源范围，导致用户访问或操作不属于自己的外发单。

### 决策

1. `ly_subcontract_order.company` 是外发资源权限的本地权威字段。
2. TASK-002C 必须新增或补齐 `ly_subcontract_order.company`，并建立 `company/status`、`company/supplier/status`、`company/item_code/status` 索引。
3. `company` 缺失口径统一为 `NULL/空字符串/空白字符串`。
4. 新建外发单必须写入非空 `company`；无法唯一解析时不得创建。
5. 历史外发单回填只接受唯一公司候选；候选多义、候选为空、ERPNext 主数据不可用、数据库读取失败必须 fail closed。
6. 不得在公司无法解析时写入默认公司、放开全公司权限或降级为“无资源限制”。
7. `ly_subcontract_material`、`ly_subcontract_receipt`、`ly_subcontract_inspection`、`ly_subcontract_status_log`、`ly_subcontract_stock_outbox`、`ly_subcontract_stock_sync_log` 的 `company` 必须从主单派生。
8. 外发列表必须在数据库层按本地 `company` 做资源过滤。
9. 外发详情和写接口必须先读取主单本地 `company`，再做 `company/item_code/supplier/warehouse` 资源校验。
10. `resource_scope_status='blocked_scope'` 或 company 缺失的历史外发单，普通业务动作必须禁止。
11. dry-run 回填必须真正只读，不得挂载 ORM 对象、不得产生迁移日志、不得写审计、不得修改业务数据。
12. TASK-002C 不得恢复发料、回料、验货成功路径，不得创建 ERPNext `Stock Entry`。

### 验收

1. 新建外发单返回体包含后端解析并校验后的 `company`。
2. `company=NULL/''/'   '` 使用同一缺失判断。
3. 历史外发单唯一候选能正确回填 company。
4. 历史外发单候选多义返回 `SUBCONTRACT_COMPANY_AMBIGUOUS`，不改数据。
5. 历史外发单候选为空返回 `SUBCONTRACT_COMPANY_UNRESOLVED`，不写默认公司。
6. ERPNext 主数据不可用返回 `ERPNEXT_SERVICE_UNAVAILABLE`，不改数据。
7. 数据库读取失败返回 `DATABASE_READ_FAILED`，不改数据。
8. dry-run 后 `session.new/session.dirty/session.deleted` 均为 0，后续 commit 不产生副作用。
9. 授权 `Company B` 的用户无法通过列表、详情、回料、验货访问 `Company A` 外发单。
10. `blocked_scope` 外发单不能发料、回料、验货、取消、库存同步重试。
11. `receive/inspect` 在 TASK-002C 完成后仍保持 TASK-002B1 fail closed。

## ADR-033：外发迁移必须自洽且创建单据必须校验 BOM 款式一致

- 决策时间：2026-04-12 22:21 CST
- 决策类型：架构审计整改 / 阻断修复
- 关联模块：TASK-002C1 外发迁移自洽与 BOM 一致性整改
- 审计来源：TASK-002C 审计意见书第 37 份，迁移链缺少外发基础表创建、创建外发单未校验 BOM item 与 payload item 一致
- 结论：外发迁移必须能从空库自洽升级；创建外发单必须以 BOM item 作为款式事实校验边界，禁止 `payload.item_code` 与 `bom_id` 脱钩

### 背景

TASK-002C 已补本地 `company` 事实字段，但迁移脚本只对外发基础表执行 `add_column`，当前迁移目录没有创建 `ly_subcontract_order/material/receipt/status_log` 的迁移。新库从迁移链升级会直接失败，说明迁移不可独立落地。

同时，审计复现 `item_code=ITEM-A` 但 `bom_id` 指向 `ITEM-B` BOM 时，接口仍能创建外发单。这会污染外发主单事实，绕过 BOM item 一致性和潜在资源权限边界，后续发料 outbox 会使用错误 BOM 物料计划。

### 决策

1. TASK-002C1 必须修复迁移链自洽性。
2. 新库从空库执行 Alembic 迁移链，必须能创建 `ly_schema` 和外发基础表。
3. 迁移链必须创建或补齐 `ly_subcontract_order/material/receipt/status_log`，不得只对不存在的表执行 `add_column`。
4. 迁移验收不得使用 ORM `metadata.create_all()` 替代 Alembic 迁移。
5. 已有库升级必须只补缺失字段、索引和约束，不得破坏已有数据。
6. 创建外发单必须校验 `bom.item_code == payload.item_code`。
7. BOM item 与 payload item 不一致时必须返回 `SUBCONTRACT_BOM_ITEM_MISMATCH`。
8. BOM item 不一致时不得新增外发主单、状态日志、成功操作审计或子表事实。
9. 显式传入 company 时，仍必须校验 Item/BOM 事实一致性。
10. company 解析、权限读取、资源校验、service 调用必须纳入统一异常处理范围。
11. `company=null/''/'   '`、候选多义、候选为空、ERPNext 不可用、权限源不可用必须返回统一错误信封，不得裸 500。
12. 回填 execute 写失败必须 rollback，调用方后续 `commit()` 不得落半更新。
13. Company-only 权限允许作为公司内范围权限，但必须由权限源明确返回 Item/Supplier/Warehouse 无显式限制；权限源不可用不得解释为无显式限制。
14. TASK-002C1 不得恢复发料、回料、验货成功路径，不得创建 ERPNext `Stock Entry`。
15. TASK-002D 不得启动，直到 TASK-002C1 审计通过。

### 验收

1. 空库迁移链能创建 `ly_schema`。
2. 空库迁移链能创建 `ly_subcontract_order/material/receipt/status_log`。
3. 空库迁移链能完成 TASK-002C 的 company 字段、索引和约束。
4. 迁移测试不依赖 `metadata.create_all()`。
5. `payload.item_code` 与 BOM item 不一致时返回 `SUBCONTRACT_BOM_ITEM_MISMATCH`。
6. BOM item mismatch 不新增外发主单、不写成功操作审计。
7. `company=null/''/'   '` 返回 `SUBCONTRACT_COMPANY_REQUIRED`，不是裸 500。
8. 公司候选多义、候选为空、ERPNext 不可用、权限源不可用均返回统一错误信封。
9. 回填子表传播写失败或 flush 写失败时 rollback，后续 commit 不落半更新。
10. Company-only 用户只能在权限源明确无 Item/Supplier/Warehouse 限制时访问该 company 外发单。
11. `receive/inspect` 仍保持 TASK-002B1 fail closed。

## ADR-034：外发发料必须通过 outbox 异步创建 ERPNext Material Issue

- 决策时间：2026-04-13 09:24 CST
- 修订时间：2026-04-13 10:58 CST
- 决策类型：架构审计整改 / 库存一致性决策
- 关联模块：TASK-002D 发料 Stock Entry Outbox
- 审计来源：TASK-002C1 审计通过后的风险项，要求 TASK-002D 补齐 outbox 最终字段、幂等键、事件键、Stock Entry worker 查询索引，不能把 TASK-002C scope 骨架当成最终库存同步表结构
- 结论：外发发料写接口只写本地事实和 `stock_action='issue'` outbox；ERPNext `Stock Entry / Material Issue` 必须由内部 worker 在本地事务提交后异步创建并提交；`event_key` 以幂等事件为单位生成，不包含 `issue_batch_no`

### 背景

外发发料会影响 ERPNext 库存台账。若在发料接口本地事务内直接调用 ERPNext，或重复提交时重复生成 Stock Entry，会造成跨系统不一致和库存重复落账。TASK-002C 已建立 outbox scope 骨架，但该结构不足以支撑生产级库存同步，必须在 TASK-002D 补齐幂等键、事件键、payload hash、重试字段、worker 查询索引和 ERPNext event_key 查重。

### 决策

1. `POST /api/subcontract/{id}/issue-material` 本地事务内只写发料事实、状态日志、操作审计和 `ly_subcontract_stock_outbox`。
2. 发料接口本地事务内禁止调用 ERPNext 写接口。
3. 一次发料请求生成一个 `issue_batch_no`、一条 `stock_action='issue'` outbox、多条 `ly_subcontract_material`。
4. 发料必须支持 `idempotency_key`。
5. 相同 `idempotency_key` + 相同 `payload_hash` 返回第一次结果，不新增事实、不新增 outbox。
6. 相同 `idempotency_key` + 不同 `payload_hash` 返回 `SUBCONTRACT_IDEMPOTENCY_CONFLICT`。
7. outbox `event_key` 必须使用完整稳定 hash，禁止截断 digest；推荐 `sio:<64位sha256>`。
8. outbox `event_key` 必须以幂等事件为单位生成，推荐输入为 `stock_action + subcontract_id + idempotency_key + stable_payload_hash`。
9. outbox `event_key` 不得包含易变 `issue_batch_no/request_id/outbox_id/created_at/operator`。
10. `issue_batch_no` 是本地发料批次号，只用于业务展示和 material 分组，不参与库存同步幂等事件键。
11. outbox 必须增加 due 查询索引和资源 scope 查询索引。
12. Worker 本阶段只处理 `stock_action='issue'`，不得处理 `receipt`。
13. Worker 必须先按服务账号 `company/item_code/supplier/warehouse` scope 过滤，再 limit。
14. Worker 必须使用行级锁或租约字段，避免并发重复处理同一 outbox。
15. Worker 创建 ERPNext Stock Entry 时必须写入 `custom_ly_subcontract_no/custom_ly_subcontract_outbox_id/custom_ly_outbox_event_key/custom_ly_stock_action`。
16. ERPNext Stock Entry 必须创建并提交，提交成功后才能标记 outbox `succeeded`。
17. 重试前必须按 `custom_ly_outbox_event_key` 查询 ERPNext 是否已有 Stock Entry；已存在则补本地成功回写，不得重复创建。
18. ERPNext 创建成功但本地回写失败时，下一次 worker 必须通过 event_key 找回原单。
19. Worker 未开启、dry-run 未开启、权限源不可用、服务账号越权必须 fail closed。
20. `receive/inspect` 在 TASK-002D 完成后仍保持 fail closed。

### 验收

1. 发料接口能创建本地 material 和 pending issue outbox。
2. 发料接口不调用 ERPNext。
3. 同一幂等键同 payload 不重复生成 material/outbox。
4. 同一幂等键不同 payload 返回 `SUBCONTRACT_IDEMPOTENCY_CONFLICT`。
5. outbox `event_key` 使用完整 hash，长输入不碰撞。
6. outbox `event_key` 不包含 `issue_batch_no`，同一幂等请求必须稳定命中同一 outbox。
7. Worker 主查询只处理 issue outbox，并按服务账号资源 scope 过滤。
8. Worker dry-run 不锁定 outbox、不增加 attempts、不调用 ERPNext，并写操作审计。
9. Worker 正式执行能创建并提交 ERPNext `Stock Entry / Material Issue`。
10. ERPNext 成功后回写真正 `stock_entry_name`，禁止伪造 `STE-ISS-*`。
11. ERPNext 已存在相同 event_key 的 Stock Entry 时，不重复创建，直接补本地成功回写。
12. Worker 并发执行不会重复处理同一 outbox。
13. `receive/inspect` 仍保持 fail closed。

## ADR-035：发料幂等必须前置且 ERPNext event_key 找回必须校验 docstatus

- 决策时间：2026-04-13 10:33 CST
- 决策类型：架构审计整改 / 库存一致性决策
- 关联模块：TASK-002D1 发料幂等与 Worker 库存一致性整改
- 审计来源：TASK-002D 审计意见书第 39 份，发料幂等重试被剩余数量校验误拒、Worker 找回 ERPNext Stock Entry 未校验 docstatus、Worker 持事务调用 ERPNext、关键测试缺失
- 结论：发料写接口必须先做幂等命中再做剩余数量校验；Worker 按 event_key 找回 ERPNext Stock Entry 时必须校验 docstatus；Worker 必须使用短事务，ERPNext 网络调用期间不得持有数据库事务或行锁

### 背景

外发发料是库存敏感动作，真实业务中前端重试、网关重试、PDA 重发都很常见。如果第一次全量发料已成功，第二次相同幂等键请求先执行剩余数量校验，就会把合法重试误判为超发，导致调用方以为发料失败。

同时，ERPNext Stock Entry 只有 `docstatus=1` 才代表库存已提交入账。若 Worker 只按 event_key 找到 draft 单据就本地标记成功，会造成本地显示库存同步成功，但 ERPNext Stock Ledger 实际未入账。

### 决策

1. `issue-material` 必须在剩余数量校验之前执行幂等查询。
2. 幂等查询维度为 `subcontract_id + stock_action='issue' + idempotency_key`。
3. 相同幂等键且 `stable_payload_hash` 一致，必须返回第一次 outbox 结果。
4. 相同幂等键但 `stable_payload_hash` 不一致，返回 `SUBCONTRACT_IDEMPOTENCY_CONFLICT`。
5. 全量发料 `items=[]` 的 hash 基于请求语义，不得受发料后剩余数量变化影响。
6. `event_key` 正式以幂等事件为单位生成，不包含易变 `issue_batch_no`。
7. `ly_subcontract_stock_outbox` 是幂等权威表；`ly_subcontract_material` 通过 `stock_outbox_id` 关联 outbox 查询幂等上下文，不强制冗余 `idempotency_key/payload_hash`。
8. `ERPNextStockEntryService.find_by_event_key()` 必须返回结构化结果，至少包含 `name/docstatus`。
9. `docstatus=1` 才可直接补本地 `succeeded`。
10. `docstatus=0` 必须 submit 原 draft 单据，submit 成功后才能本地 `succeeded`。
11. `docstatus=2` 或未知状态不得本地成功。
12. 相同 event_key 查到多张 ERPNext Stock Entry 时必须 fail closed。
13. Worker 必须拆成 claim 短事务、ERPNext 网络调用、result 回写短事务。
14. Worker 调用 ERPNext 时不得持有数据库事务或行锁。
15. result 回写失败后，下一轮 worker 必须能通过 event_key 恢复本地状态。
16. TASK-002D1 不得恢复回料、验货成功路径。

### 验收

1. 第一次全量发料成功后，相同幂等键重试返回第一次 outbox 结果，不返回超发错误。
2. 同幂等键重试不新增 material、不新增 outbox、不增加 issued_qty。
3. `event_key` 不包含 `issue_batch_no`，同一幂等事件稳定不变。
4. 找到 ERPNext `docstatus=1` 单据时补本地 succeeded。
5. 找到 ERPNext `docstatus=0` draft 时 submit 原单，成功后补本地 succeeded。
6. 找到 ERPNext `docstatus=2` 或未知状态时不得本地成功。
7. 相同 event_key 多单据时 fail closed。
8. Worker 调用 ERPNext 时不持有数据库事务或行锁。
9. 并发 Worker 不重复处理同一 outbox。
10. 失败、重试、dead、event_key 恢复和日志脱敏测试齐全。

## ADR-036：外发回料必须通过 outbox 异步创建 ERPNext Material Receipt

- 决策时间：2026-04-13 11:01 CST
- 决策类型：架构审计整改 / 库存一致性决策
- 关联模块：TASK-002E 回料 Stock Entry Outbox
- 审计来源：TASK-002D1 审计通过后，审计官确认 TASK-002E 可进入但边界必须锁死在回料链路
- 结论：外发回料写接口只写本地回料事实和 `stock_action='receipt'` outbox；ERPNext `Stock Entry / Material Receipt` 必须由内部 worker 在本地事务提交后异步创建并提交；TASK-002E 不得引入验货、对账、结算逻辑

### 背景

回料是外发链路中第二个库存动作，会把加工结果回入本厂仓库。该动作必须进入 ERPNext Stock Ledger，但不能在 FastAPI 本地事务内直接调用 ERPNext，否则会产生跨系统不一致和重复入账风险。TASK-002D1 已验证发料 outbox 的幂等、event_key、docstatus 和短事务规则，TASK-002E 必须复用同一库存一致性标准到回料链路。

### 决策

1. `POST /api/subcontract/{id}/receive` 本地事务内只写回料事实、状态日志、操作审计和 `stock_action='receipt'` outbox。
2. 回料接口本地事务内禁止调用 ERPNext 写接口。
3. 本地回料事实与 receipt outbox 必须同事务提交；commit 失败不得调用 ERPNext。
4. 一次回料请求生成一个 `receipt_batch_no`、一条 receipt outbox、一组 `ly_subcontract_receipt`。
5. 回料必须支持 `idempotency_key`。
6. 回料幂等检查必须前置到剩余可回数量校验之前。
7. 回料 `event_key` 必须以幂等事件为单位生成，推荐输入为 `stock_action + subcontract_id + idempotency_key + stable_payload_hash`。
8. 回料 `event_key` 不得包含易变 `receipt_batch_no/request_id/outbox_id/created_at/operator`。
9. `receipt_batch_no` 只用于本地回料批次展示和 receipt 分组，不参与库存同步幂等事件键。
10. 回料 item 默认为外发单 `item_code`，禁止前端任意指定 item 成为回料事实。
11. 默认禁止超收，超收策略如需放开必须另出 ADR。
12. Worker 必须明确区分 `stock_action='issue'` 与 `stock_action='receipt'`，禁止 handler 混用。
13. Worker 处理 receipt 时必须创建并提交 ERPNext `Stock Entry / Material Receipt`。
14. ERPNext Material Receipt payload 必须使用 `t_warehouse`。
15. Worker 必须按 event_key 查询 ERPNext Stock Entry 并校验 `docstatus`；draft 必须 submit，cancelled/未知状态不得本地成功。
16. Worker 必须使用 claim 短事务、ERPNext 网络调用、result 回写短事务。
17. `inspect` 在 TASK-002E 完成后仍必须 fail closed，不得写验货事实、金额、对账或结算数据。
18. `waiting_inspection` 状态允许继续分批回料；该状态下只能继续写 receipt/outbox，不得在 TASK-002E 阶段执行验货成功路径。

### 验收

1. 回料接口能创建本地 receipt 和 pending receipt outbox。
2. 回料接口不调用 ERPNext。
3. 回料事实和 receipt outbox 同事务提交；commit 失败不调用 ERPNext。
4. 同一幂等键同 payload 不重复生成 receipt/outbox。
5. 同一幂等键不同 payload 返回 `SUBCONTRACT_IDEMPOTENCY_CONFLICT`。
6. 回料 `event_key` 不包含 `receipt_batch_no`。
7. Worker 处理 receipt 时创建并提交 ERPNext `Stock Entry / Material Receipt`。
8. Worker 使用 `t_warehouse`，不得使用 issue 的 `s_warehouse`。
9. Worker 按 `docstatus` 区分已提交、草稿、已取消和异常状态。
10. Worker 并发执行不会重复处理同一 receipt outbox。
11. `inspect` 仍保持 fail closed。
12. `waiting_inspection` 状态可继续登记后续分批回料。

## ADR-037：外发库存同步重试必须按 outbox_id + idempotency_key + stock_action 精确定位

- 决策时间：2026-04-13 12:06 CST
- 决策类型：架构审计整改 / 库存一致性决策
- 关联模块：TASK-002E1 回料同步重试精确定位整改
- 审计来源：TASK-002E 审计意见书第 41 份，retry 接口按订单取最新 outbox，误重置已成功 receipt outbox
- 结论：外发库存同步 retry 必须精确定位目标 outbox；禁止按订单选择最新 outbox；禁止重置已成功、处理中或待处理 outbox

### 背景

外发单会同时存在发料 issue outbox 和回料 receipt outbox。TASK-002E 审计复现了一个高危场景：同一订单下 issue outbox 已失败，后续 receipt outbox 已成功，调用 retry 时系统按“最新 outbox”选择了 receipt，并把已成功 receipt 重置为 pending，真正失败的 issue 仍未重试。该行为会造成已成功库存同步重新入队，存在重复库存落账和本地状态污染风险。

### 决策

1. `/api/subcontract/{id}/stock-sync/retry` 必须接收 `outbox_id`。
2. retry 请求必须同时提供 `stock_action` 与原始 `idempotency_key`。
3. 服务端必须按 `outbox_id` 精确读取目标 outbox。
4. 目标 outbox 必须满足 `subcontract_id=路径 id`。
5. 目标 outbox 必须满足 `stock_action=请求 stock_action`。
6. 目标 outbox 必须满足 `idempotency_key=请求 idempotency_key`。
7. 不满足订单、动作或幂等键匹配时必须 fail closed，不得降级到最新 outbox。
8. retry 只允许 `failed/dead` 状态进入 pending。
9. `succeeded/processing/pending/blocked_scope` 禁止重置。
10. retry 资源权限以目标 outbox 的 `company/item_code/supplier/warehouse/stock_action` 为准。
11. 权限源不可用返回 `PERMISSION_SOURCE_UNAVAILABLE`，不得修改 outbox。
12. retry 接口不得调用 ERPNext。
13. retry 成功写操作审计；401/403/503 写安全审计。
14. `stable_payload_hash` 必须对发料和回料数量字段做 Decimal 语义归一化。
15. `waiting_inspection` 状态允许继续分批回料，但验货接口继续 fail closed。
16. Worker submit ERPNext draft 后必须确认最终 `docstatus=1`，否则不得本地 succeeded。

### 验收

1. issue failed + receipt succeeded 场景下，重试 issue 只重置 issue outbox。
2. 上述场景中 receipt outbox 保持 succeeded。
3. receipt failed 可按精确 outbox 重试。
4. succeeded/processing/pending outbox 调用 retry 不改变状态。
5. stock_action/idempotency_key/order 任一不匹配均返回明确错误码。
6. retry 接口不调用 ERPNext。
7. 无目标 outbox 资源权限或权限源不可用时不修改 outbox。
8. `10/10.0/10.000000` 生成相同 payload hash。
9. submit draft 后未确认 `docstatus=1` 不得标记 succeeded。

## ADR-038：外发验货金额必须以本地 inspection 事实固化 gross / deduction / net

- 决策时间：2026-04-13 12:31 CST
- 决策类型：架构审计整改 / 金额口径决策
- 关联模块：TASK-002F 验货扣款金额口径
- 审计来源：TASK-002E1 审计通过后允许进入 TASK-002F；历史 inspect 路径曾存在 `net_amount = inspected_qty - deduction_amount` 数量减金额错误
- 结论：外发验货从 TASK-002F 开始写本地 inspection 金额事实，金额公式固定为 `gross_amount - deduction_amount = net_amount`，并禁止验货接口调用 ERPNext 财务或库存写接口

### 背景

外发验货决定加工厂扣款和后续对账金额。如果金额公式混用数量和金额，会直接污染加工厂对账单和应付数据。TASK-002B~E 阶段一直保持 inspect fail closed，就是为了避免旧演示路径继续写入错误金额。TASK-002F 开始恢复验货成功路径，但必须先冻结金额口径、幂等规则、状态流转和与 ERPNext 的边界。

### 决策

1. `POST /api/subcontract/{id}/inspect` 必须写入 `ly_subcontract_inspection` 本地事实。
2. 验货必须绑定 `receipt_batch_no`。
3. 目标回料批次必须已完成 ERPNext Material Receipt 同步。
4. 验货接口不调用 ERPNext 写接口。
5. 验货接口不创建 `Stock Entry / Purchase Invoice / Payment Entry / GL Entry`。
6. `accepted_qty = inspected_qty - rejected_qty`。
7. `rejected_rate = rejected_qty / inspected_qty`。
8. `gross_amount = inspected_qty × subcontract_rate`。
9. `deduction_amount = rejected_qty × deduction_amount_per_piece`。
10. `net_amount = gross_amount - deduction_amount`。
11. `deduction_amount` 不得大于 `gross_amount`。
12. 金额字段使用 Decimal，保留 2 位小数，ROUND_HALF_UP。
13. 禁止 `net_amount = inspected_qty - deduction_amount`。
14. 验货写接口必须支持幂等。
15. 幂等检查必须前置到剩余可验数量校验之前。
16. 验货成功必须更新外发主单验货与金额汇总。
17. 状态流转按累计回料和累计验货决定：待验、待回料、完成。
18. 旧 `get_retry_target()` 最新 outbox 选择逻辑必须删除或显式废弃，避免 TASK-002E1 修复回潮。

### 验收

1. 验货成功写入 inspection 并返回金额字段。
2. 未同步成功的 receipt batch 不允许验货。
3. 相同幂等键相同 payload 不重复扣款。
4. 相同幂等键不同 payload 返回冲突。
5. `inspected_qty=100,rejected_qty=5,subcontract_rate=10,deduction_amount_per_piece=2` 时，`gross_amount=1000.00,deduction_amount=10.00,net_amount=990.00`。
6. 旧错误公式扫描无命中。
7. 验货接口不调用 ERPNext 写接口。
8. 并发验货不得超过批次可验数量。
9. 删除或废弃旧 `get_retry_target()` 后，retry 仍只按精确 outbox 定位。

## ADR-039：验货幂等必须以 receipt_batch_no 作为业务身份字段

- 决策时间：2026-04-13 13:05 CST
- 决策类型：架构审计整改 / 幂等与对账金额一致性决策
- 关联模块：TASK-002F1 验货幂等批次隔离与详情闭环整改
- 审计来源：TASK-002F 审计意见书第 43 份，同一 idempotency_key 换回料批次返回第一次验货结果，导致第二批漏验
- 结论：验货 inspection 的 payload hash 必须包含 `receipt_batch_no`；stock outbox 的易变字段排除规则不得复用于 inspection 幂等；外发详情必须返回 inspections；验货字段必须通过独立迁移补齐

### 背景

外发验货是按回料批次确认质量和扣款的业务事实。`receipt_batch_no` 对 stock outbox event_key 来说是本地展示批号，可以不参与库存同步事件键；但对 inspection 来说，`receipt_batch_no` 是验货对象身份。如果 inspection hash 排除该字段，同一幂等键切换到另一个回料批次会被误判为重复请求并返回第一次验货结果，造成第二批未验货但接口返回成功，后续对账金额少计。

### 决策

1. inspection payload hash 必须使用独立构造函数。
2. inspection payload hash 必须包含 `receipt_batch_no`。
3. inspection payload hash 不得复用 stock outbox 的易变字段排除清单。
4. 同一 `subcontract_id + idempotency_key + 同一 receipt_batch_no + 同一语义 payload` 返回第一次验货结果。
5. 同一 `subcontract_id + idempotency_key + 不同 receipt_batch_no` 返回 `SUBCONTRACT_IDEMPOTENCY_CONFLICT`。
6. 同一 `subcontract_id + idempotency_key + 同一 receipt_batch_no + 不同数量/扣款/备注 payload` 返回 `SUBCONTRACT_IDEMPOTENCY_CONFLICT`。
7. Decimal 数量和金额字段继续做语义归一化。
8. `GET /api/subcontract/{id}` 必须返回 `inspections[]`。
9. 验货金额事实源为 `ly_subcontract_inspection` 和主单汇总字段。
10. `ly_subcontract_receipt` 上的历史金额字段不得作为 TASK-002F 后的金额事实源。
11. `gross_amount = inspected_qty × subcontract_rate` 是唯一有效加工费公式。
12. `gross_amount = accepted_qty × subcontract_rate` 废弃。
13. TASK-002F 验货字段必须通过独立 Alembic migration 补齐，不能只依赖已存在的 TASK-002C revision。

### 验收

1. 同 key 不同 receipt batch 返回幂等冲突。
2. 同 key 不同 receipt batch 不新增 inspection，不更新主单金额。
3. 同 key 同 batch 同语义 payload 返回第一次结果。
4. 外发详情返回 inspections 和金额快照。
5. Alembic 空库升级到 head 后包含验货字段。
6. 已执行 TASK-002C revision 的库可通过 TASK-002F 独立迁移补齐验货字段。
7. 代码和设计无有效 `gross_amount = accepted_qty × subcontract_rate` 口径。

## ADR-040：外发验货必须通过并发回归测试证明不超量

- 决策时间：2026-04-13 13:26 CST
- 决策类型：架构审计整改 / 并发一致性与接口安全顺序决策
- 关联模块：TASK-002F2 并发验货不超量回归测试
- 审计来源：TASK-002F1 审计意见书第 44 份，缺少两个请求同时验同一回料批次时不超量的回归测试
- 结论：外发验货必须用数据库事务锁和并发回归测试证明不会超过批次已同步回料数量；外发详情必须先资源鉴权再读取子表明细；TASK-002F 验货字段必须通过独立 migration 补齐

### 背景

验货直接决定加工费和扣款金额。如果两个请求同时验同一回料批次，任何读旧剩余数量后分别写入的竞态都会导致超验、超扣或对账金额污染。当前实现依赖生产数据库 `FOR UPDATE`，但主测试环境是 SQLite，无法真实验证行锁语义，因此必须补充明确的并发回归测试或 PostgreSQL 集成测试。

### 决策

1. 两个请求同时验同一 `receipt_batch_no` 时，最终累计验货数量不得超过该批次已同步回料数量。
2. 验货剩余可验数量必须在数据库锁内重新计算。
3. 插入 inspection、更新主单汇总、写状态日志必须与剩余可验校验在同一事务内完成。
4. 禁止用进程内全局锁替代数据库事务锁。
5. 禁止用 sleep 作为唯一并发保护。
6. 必须补充两个独立 session/client 的并发回归测试。
7. SQLite 无法证明行锁时，必须补 PostgreSQL 集成测试或明确 skip，并保留服务层防回归测试。
8. `GET /api/subcontract/{id}` 必须先做订单级资源权限校验，再读取 `inspections[]/receipts[]/status_logs[]/latest_outbox`。
9. 无权限或权限源不可用时不得读取子表明细。
10. TASK-002F 验货字段必须通过独立 Alembic migration 补齐，不能只依赖已执行过的 TASK-002C revision。

### 验收

1. 并发验货测试使用两个独立 session/client。
2. 两个请求数量之和大于批次已回料数量时，最终累计验货数量不超过已回料数量。
3. 第二个请求返回 `SUBCONTRACT_INSPECTION_QTY_EXCEEDED` 或明确并发/锁冲突错误信封。
4. 并发失败不写超量 inspection，不超量更新主单汇总。
5. 无权限详情请求不读取 inspections 明细。
6. 权限源不可用详情请求不读取子表明细。
7. 独立 TASK-002F migration 可补齐已执行 TASK-002C revision 的数据库。

## ADR-041：PostgreSQL 并发验货测试必须真实执行并进入交付门禁

- 决策时间：2026-04-13 13:41 CST
- 决策类型：架构审计整改 / 测试门禁决策
- 关联模块：TASK-002F3 PostgreSQL 并发验货集成测试门禁整改
- 审计来源：TASK-002F2 审计意见书第 45 份，`test_inspect_concurrent_requests_do_not_overinspect` 无条件 skip 且 `postgresql` marker 未注册
- 结论：PostgreSQL 并发验货测试必须在提供 `POSTGRES_TEST_DSN` 时真实执行；`postgresql` marker 必须注册；TASK-002F3 复审必须提供 PostgreSQL 并发测试运行结果；inspect 审计快照必须资源鉴权后读取

### 背景

验货并发安全依赖 PostgreSQL 行锁语义，SQLite 默认测试无法证明生产环境双事务竞争不会超验。无条件 skip 的占位测试会让并发风险长期无门禁保护，unknown marker warning 也会淹没真实测试配置问题。

### 决策

1. PostgreSQL 并发验货测试不得无条件 skip。
2. 无 `POSTGRES_TEST_DSN` 时可以明确 skip。
3. 有 `POSTGRES_TEST_DSN` 时必须真实执行双连接或双 Session 并发测试。
4. 并发测试必须证明最终累计验货数量不超过批次已同步回料数量。
5. 必须注册 `postgresql` pytest marker。
6. TASK-002F3 交付复审必须提供带 `POSTGRES_TEST_DSN` 的测试运行结果。
7. 后续合并门禁应包含 PostgreSQL job；短期没有 CI 时，以本地 PostgreSQL 测试输出作为最低门禁。
8. `POST /api/subcontract/{id}/inspect` 的 `before_data/get_order_snapshot()` 必须在资源权限校验之后读取。
9. 无权限或权限源不可用时，inspect 不得读取 latest outbox 或 inspections 子表快照。

### 验收

1. `pytest.ini` 注册 `postgresql` marker。
2. 全量 pytest 无 unknown marker warning。
3. 无 DSN 时 PostgreSQL 测试明确 skip。
4. 有 DSN 时 PostgreSQL 测试真实执行并通过。
5. 两个并发请求不会造成累计验货数量超过批次已回料数量。
6. inspect 无权限和权限源不可用路径不读取子表快照。

## ADR-042：外发前端按钮必须以后端权限动作和单据状态共同驱动

- 决策时间：2026-04-13 13:58 CST
- 决策类型：前端权限与状态联动决策
- 关联模块：TASK-002G 前端状态与权限联动
- 审计来源：TASK-002F3 通过后进入外发前端闭环；现有前端仍有 receive/retry/inspect 旧契约残留
- 结论：外发前端 UI 只能展示后端授权且当前状态允许的操作；前端 API 类型必须与后端现行契约一致；内部 worker API 禁止进入普通前端

### 背景

外发后端已完成权限、资源权限、发料/回料 outbox、验货金额和库存同步重试。前端如果继续使用旧入参或只按本地状态展示按钮，会出现“页面可点但后端拒绝”或误调用旧接口的情况。特别是 retry 已经修复为精确 outbox 定位，前端无 body 调用会直接回到高危历史问题。

### 决策

1. 外发前端按钮权限以 `/api/auth/actions` 的 `button_permissions` 为权威来源。
2. 前端按钮必须同时检查后端权限、单据状态、资源范围状态和结算状态。
3. `subcontract:stock_sync_worker` 是内部权限，不得展示普通 UI 按钮。
4. 前端不得调用 `/api/subcontract/internal/stock-sync/run-once`。
5. 回料前端 payload 不得包含验货字段。
6. 验货前端 payload 必须使用 `deduction_amount_per_piece`。
7. retry 前端 payload 必须包含 `outbox_id + stock_action + idempotency_key`。
8. 写操作成功后必须重新加载详情。
9. 详情页必须展示回料批次和验货明细。
10. 如后端详情缺少前端必要只读字段，可在 TASK-002G 补只读字段，但不得新增业务写逻辑。

### 验收

1. 无 read 权限不展示业务列表。
2. 无 create 权限不展示新建按钮。
3. 发料、回料、验货、retry、取消按钮均按权限和状态显示。
4. 前端 receive/retry/inspect 入参与后端契约一致。
5. 前端不调用内部 worker API。
6. 详情页展示真实回料批次、验货明细和金额快照。
7. 401/403/503 错误提示友好且不泄露敏感信息。

## ADR-043：外发前端 API 必须统一鉴权，列表同步摘要不得逐行拉详情

- 决策时间：2026-04-13 14:20 CST
- 决策类型：架构审计整改 / 前端请求安全与列表性能决策
- 关联模块：TASK-002G1 前端外发 API 统一鉴权与列表摘要整改
- 审计来源：TASK-002G 审计意见书第 47 份，`src/api/subcontract.ts` 使用裸 `fetch()`，列表页逐行调用详情接口获取同步状态
- 结论：外发前端所有 `/api/subcontract/` 请求必须走统一鉴权请求封装；外发列表同步摘要纳入 list API 轻量字段，不允许前端逐行拉详情

### 背景

外发模块后端已经以当前用户、动作权限、资源权限和审计为安全边界。前端 API 如果直接 `fetch()`，生产登录态下会缺少 `Authorization`、Cookie 凭证和统一错误信封处理，导致真实环境下 401/403 或错误提示失真。另一方面，列表页逐行拉详情会把一次列表查询放大为 N+1 次 HTTP 请求，并重复触发资源权限、子表读取和错误处理，后续数据量扩大后会成为性能和审计噪声风险。

### 决策

1. `src/api/subcontract.ts` 禁止直接调用 `fetch()`。
2. 外发前端 API 必须使用统一 `request<T>()` 或等价封装。
3. 统一请求封装必须包含 `credentials: 'include'`。
4. 统一请求封装必须按项目现有 token 约定写入标准 `Authorization` 头。
5. 统一请求封装必须解析 `{code,message,data}` 成功和失败信封。
6. 401、403、503 必须转成前端友好错误提示。
7. 前端错误不得展示 Authorization、Cookie、token、password、secret、SQL 原文或堆栈。
8. 外发列表页禁止逐行调用 `GET /api/subcontract/{id}` 获取同步状态。
9. 外发列表同步状态正式纳入 `GET /api/subcontract/` list item 轻量摘要字段。
10. list 摘要字段包含最新 issue/receipt outbox id、sync status、stock entry name、idempotency key 和 error code。
11. list 摘要必须在资源权限过滤后计算。
12. 后端必须用批量查询或窗口函数计算 list 摘要，不得循环查详情。
13. 如果 list 摘要暂不可用，列表页只能提示“进入详情查看”，不得用 N+1 兜底。
14. `allowed_actions_for_current_order` 允许作为详情接口只读增强字段，但不替代后端写接口鉴权。
15. TASK-002H 不得启动，直到 TASK-002G1 通过审计。

### 验收

1. `src/api/subcontract.ts` 无直接 `fetch(`。
2. 所有 `/api/subcontract/` 请求带登录凭证和统一错误处理。
3. 401/403/503 提示友好且不泄露敏感信息。
4. 列表页无 `fetchSubcontractOrderDetail(row.id)` 或等价逐行详情请求。
5. list item 可展示 latest issue/receipt 同步摘要。
6. 后端 list 摘要不返回 payload_json、原始异常和敏感凭证。
7. retry 仍按 `outbox_id + stock_action + idempotency_key` 精确提交。
8. 前端不调用内部 worker run-once API。

## ADR-044：外发列表库存同步状态采用后端轻量摘要字段封版

- 决策时间：2026-04-13 14:31 CST
- 决策类型：架构审计整改 / 前后端列表契约封版决策
- 关联模块：TASK-002G2 外发列表同步摘要字段封版整改
- 审计来源：TASK-002G1 审计意见书第 48 份，前端列表读取 latest issue/receipt sync status，但后端 list 接口未返回字段
- 结论：保留外发列表“库存同步状态”列，后端 `GET /api/subcontract/` 补齐 latest issue/receipt 轻量同步摘要字段；禁止恢复 N+1 详情查询

### 背景

TASK-002G1 已经修复裸 `fetch()` 和列表 N+1 高危问题，但前端列表库存同步状态仍没有稳定数据来源。如果简单删除该列，会降低外发单列表的运营可见性；如果恢复逐行拉详情，又会回到审计已经关闭的 N+1 和权限检查放大问题。因此本次封版选择后端 list item 返回轻量只读摘要字段。

### 决策

1. 外发列表继续展示库存同步状态。
2. `GET /api/subcontract/` list item 必须返回 latest issue/receipt 轻量摘要字段。
3. 摘要字段包括 outbox id、sync status、stock entry name、idempotency key、error code。
4. 摘要字段只读，不触发 retry、worker、ERPNext 调用或 outbox 状态变化。
5. 最新 outbox 以 `subcontract_id + stock_action` 分组，按 `created_at desc, id desc` 取 1 条。
6. issue 和 receipt 摘要必须分开计算，不得串扰。
7. 摘要计算必须发生在订单资源权限过滤之后。
8. 摘要查询必须批量完成，不得前端或后端逐订单调用详情。
9. list item 不得返回 `payload_json`、`last_error_message`、ERPNext 原始异常或敏感凭证。
10. 前端列表读取 list item 字段展示同步状态；字段为空显示“暂无同步任务”。
11. `latest_*_idempotency_key` 可用于精确 retry payload，但不得作为普通文本展示。
12. TASK-002H 不得启动，直到 TASK-002G2 通过审计。

### 验收

1. `GET /api/subcontract/` 返回 `latest_issue_sync_status/latest_receipt_sync_status`。
2. 同一订单 issue/receipt 摘要互不串扰。
3. 多条 outbox 返回最新一条。
4. 无 outbox 时返回 null 字段。
5. 无权限订单不泄露摘要。
6. 前端无逐行详情请求。
7. 前端库存同步状态列不再长期显示“进入详情查看”。
8. 后端和前端均不恢复裸 fetch 或内部 worker run-once 调用。

## ADR-045：外发列表摘要字段必须以自动化测试锁定

- 决策时间：2026-04-13 14:41 CST
- 决策类型：架构审计整改 / 测试门禁决策
- 关联模块：TASK-002G3 外发列表摘要自动化测试补齐
- 审计来源：TASK-002G2 审计意见书第 49 份，列表同步摘要功能已通过但缺少自动化测试覆盖
- 结论：外发列表 latest issue/receipt 摘要字段和 latest outbox 选择规则必须补后端自动化测试后才能封版

### 背景

外发列表同步摘要是前端取消 N+1 详情请求后的稳定数据来源。该字段虽然是只读展示字段，但它直接影响库存同步失败提示和后续精确 retry 的入口判断。如果没有测试，后续重构 list service、outbox 查询或 schema 时，容易再次出现字段丢失、issue/receipt 串扰、最新 outbox 选择错误等回归。

### 决策

1. 必须补齐 list item 返回 latest issue/receipt 摘要字段的后端自动化测试。
2. 必须补齐 issue/receipt 分开按 `created_at desc, id desc` 取最新 outbox 的后端自动化测试。
3. 测试必须断言 issue 和 receipt 摘要互不串扰。
4. 测试必须断言 list item 包含 outbox_id、sync_status、stock_entry_name、idempotency_key、error_code。
5. 测试不得调用 ERPNext、worker、retry 或内部 run-once。
6. 测试不得依赖真实外部服务。
7. 测试不得引入敏感凭证或原始异常明文。
8. 本任务原则上只改测试；如测试证明实现不符合 V1.16 契约，只允许最小修复。
9. TASK-002H 不得启动，直到 TASK-002G3 通过审计。

### 验收

1. 新增列表摘要字段返回测试。
2. 新增 latest outbox 选择规则测试。
3. 定向测试通过。
4. 全量 pytest 通过。
5. unittest discover 通过。
6. py_compile 通过。
7. 静态扫描确认前端未恢复 N+1 详情请求或裸 fetch。

## ADR-046：外发对账数据出口以验货明细为结算事实源

- 决策时间：2026-04-13 14:51 CST
- 决策类型：模块边界 / 对账数据出口决策
- 关联模块：TASK-002H 对账数据出口；TASK-006 加工厂对账单
- 审计来源：TASK-002G3 审计意见书第 50 份通过，允许进入 TASK-002H；第 50 份风险项 `_latest_outbox_by_order_ids()` 后续优化不阻塞本任务
- 结论：外发对账数据出口以 `ly_subcontract_inspection` 为最小结算明细事实源；TASK-002H 只提供候选、预览和锁定服务，TASK-006 才创建对账单和应付

### 背景

外发验货已经形成 `gross_amount/deduction_amount/net_amount` 金额事实。加工厂对账需要按照供应商和期间汇总这些金额，但一个外发单可能多批回料、多次验货，订单级汇总无法表达部分批次先结算、后续批次再结算的场景。因此，结算出口必须以 inspection 行为明细粒度。

### 决策

1. `ly_subcontract_inspection` 是外发结算明细事实源。
2. `ly_subcontract_order` 金额字段仅作为订单汇总展示和校验辅助。
3. 一条 inspection 只能进入一个未释放/未取消的 statement。
4. 对账候选必须只包含已验货、回料已同步成功、未结算、资源权限允许的 inspection。
5. 对账候选金额读取 inspection 金额事实，不重新按工价公式计算。
6. TASK-002H 新增 `settlement_status/statement_id/statement_no/settlement_line_key/settlement_locked_by/settlement_locked_at/settled_by/settled_at/settlement_request_id` 等字段到 inspection。
7. `statement_id` 当前阶段 nullable 且不强制 FK，正式 FK 由 TASK-006 创建对账单表后接入。
8. 锁定服务必须使用事务和行级锁，防止重复结算。
9. 同一 statement 重复锁定同一批 inspection 返回幂等成功。
10. 不同 statement 锁定同一 inspection 必须返回冲突。
11. release 只允许释放 `statement_locked` 行，不允许释放 `settled` 行。
12. TASK-002H 不创建 `factory_statement` 表，不创建 ERPNext Purchase Invoice，不创建 Payment Entry，不写 GL。
13. TASK-006 负责对账单主表、对账编号、确认、取消、应付草稿和打印。
14. 列表同步状态只展示最新发料/最新回料 outbox 摘要；多批次同步流水不进入外发列表。

### 验收

1. 可结算候选接口按 inspection 行返回。
2. 未同步回料、未验货、已锁定、已结算、取消和 blocked_scope 明细不返回。
3. 候选接口和锁定接口均做动作权限与资源权限校验。
4. 预览汇总金额来自 inspection 金额事实。
5. 锁定同一批 inspection 具备幂等性。
6. 不同 statement 重复锁定同一 inspection 返回冲突。
7. 失败 rollback，不产生半锁定。
8. 不调用 ERPNext 写接口。
9. 不创建对账单主表或财务单据。

## ADR-047：结算锁定与释放幂等必须使用 append-only 操作表

- 决策时间：2026-04-13 15:26 CST
- 决策类型：架构审计整改 / 幂等与结算状态安全决策
- 关联模块：TASK-002H1 结算幂等重放漏洞整改
- 审计来源：TASK-002H 审计意见书第 51 份，锁定/释放共用 `settlement_request_id` 导致旧锁定请求可在释放后重放
- 结论：结算锁定/释放幂等历史必须使用 append-only 操作表；inspection 可变字段不得作为幂等权威来源；候选/预览 summary 定义为筛选条件全量汇总

### 背景

结算锁定和释放是对账前置财务状态。如果幂等历史记录在 inspection 行的单个可变字段上，释放操作会覆盖锁定操作的幂等 key，导致旧锁定请求在网络重试时被误判为新请求，从而把已经释放的明细重新锁定。这种状态重放会直接污染对账单候选和结算锁定边界。

### 决策

1. 新增 `ly_subcontract_settlement_operation` append-only 操作表。
2. `operation_type + idempotency_key` 必须唯一。
3. `request_hash` 必须包含 operation_type、statement_id、statement_no 和排序后的 inspection_ids。
4. 同 key 同 hash 返回首次成功响应，不再次修改 inspection。
5. 同 key 不同 hash 返回 `SUBCONTRACT_SETTLEMENT_IDEMPOTENCY_CONFLICT`。
6. `lock K -> release R -> retry lock K` 不得重新锁定已释放行。
7. `release R -> retry release R` 不得再次修改行。
8. operation 记录写入与 inspection 状态变更必须在同一事务内完成。
9. operation 写入失败必须 rollback inspection 状态变更。
10. `inspection.settlement_request_id` 不再作为幂等历史权威来源。
11. `idempotency_key` 在 API Schema、模型和迁移中统一长度 128。
12. `settlement-candidates.summary` 和 `settlement-preview.summary` 默认表示筛选条件全量汇总；分页汇总必须另命名 `page_summary`。
13. TASK-002H1 不得新增对账单主表、应付、付款或 GL。

### 验收

1. append-only 操作表存在并有唯一约束。
2. 旧锁定请求在 release 后重试不会重新锁定。
3. 同 key 不同 payload 返回冲突。
4. operation 写失败会 rollback 业务状态。
5. 128 字符幂等键可用，超过 128 字符被拒绝。
6. summary 语义明确且有测试覆盖。
7. 不调用 ERPNext 写接口。

## ADR-048：结算 operation 唯一冲突必须转为幂等 Replay

- 决策时间：2026-04-13 15:43 CST
- 决策类型：架构审计整改 / 并发幂等决策
- 关联模块：TASK-002H2 结算并发幂等 Replay 整改
- 审计来源：TASK-002H1 审计意见书第 52 份，同 key 并发重复请求可能在 operation 唯一约束处返回 `DATABASE_WRITE_FAILED`
- 结论：同 `operation_type + idempotency_key` 并发唯一冲突必须转为 replay 首次响应或幂等冲突；lock/release 响应必须显式返回 `idempotent_replay`

### 背景

append-only operation 表解决了释放后旧锁定请求重放的高危问题，但并发场景仍有窗口：两个相同幂等请求同时进入，第二个可能在首次读取 operation 时未命中，随后在写 operation 时撞唯一约束。该情况是正常幂等并发，不应暴露为数据库写失败。

### 决策

1. lock/release 必须在获得 inspection 行锁后再次读取 operation。
2. operation 二次读取命中且 hash 相同，直接 replay 首次响应。
3. operation 二次读取命中但 hash 不同，返回幂等冲突。
4. operation insert/update 遇到 `operation_type + idempotency_key` 唯一冲突时，必须 reload operation。
5. reload 后 hash 相同，返回首次响应，且 `idempotent_replay=true`。
6. reload 后 hash 不同，返回 `SUBCONTRACT_SETTLEMENT_IDEMPOTENCY_CONFLICT`。
7. replay 不得新增第二条 operation，不得再次修改 inspection。
8. 首次 lock/release 成功返回 `idempotent_replay=false`。
9. lock/release 成功响应包含 `operation_id/idempotency_key/idempotent_replay`。
10. `settlement_request_id` 保留为兼容/最近请求标识，不是幂等历史事实源。
11. PostgreSQL 有 `POSTGRES_TEST_DSN` 时必须执行同 key 并发 lock/release 测试。
12. TASK-002H2 不得创建对账单主表、应付、付款或 GL。

### 验收

1. 同 key 同 payload 并发 lock 不返回 `DATABASE_WRITE_FAILED`。
2. 同 key 同 payload 并发 release 不返回 `DATABASE_WRITE_FAILED`。
3. replay 返回 `idempotent_replay=true`。
4. 首次执行返回 `idempotent_replay=false`。
5. 同 key 不同 payload 返回幂等冲突。
6. operation 表不追加重复记录。
7. inspection 不被 replay 再次修改。
8. 不调用 ERPNext 写接口。

## ADR-049：TASK-006 前必须补跑 PostgreSQL 结算并发集成验证

- 决策时间：2026-04-13 15:57 CST
- 决策类型：测试门禁 / 数据库并发语义验证决策
- 关联模块：TASK-002H3 PostgreSQL 结算并发集成验证；TASK-006 加工厂对账单
- 审计来源：TASK-002H2 审计意见书第 53 份通过，剩余风险为 `POSTGRES_TEST_DSN` 未提供导致真实 PostgreSQL 并发语义未非 skip 验证
- 结论：进入 TASK-006 前必须在专用 PostgreSQL 测试库上验证 settlement lock/release 同 key 并发 replay；无 DSN 不算完成

### 背景

结算锁定/释放依赖 PostgreSQL 的行锁和唯一约束等待语义。SQLite 和 mock 测试能覆盖服务逻辑，但不能证明真实数据库并发等待、唯一冲突识别和事务提交后的 replay 行为。加工厂对账单会建立在这些锁定结果之上，因此必须在 TASK-006 前补一次非 skip 集成验证。

### 决策

1. 必须提供专用 `POSTGRES_TEST_DSN`。
2. 禁止连接生产或准生产数据库。
3. 必须注册并使用 `postgresql` pytest marker。
4. 同 key 并发 lock 必须两个请求成功，且只有一条 lock operation。
5. 同 key 并发 release 必须两个请求成功，且只有一条 release operation。
6. replay 响应必须 `idempotent_replay=true`。
7. 首次响应必须 `idempotent_replay=false`。
8. 同 key 不同 payload 必须返回幂等冲突。
9. PostgreSQL migration 必须确认 operation 唯一约束存在并生效。
10. 无 `POSTGRES_TEST_DSN` 时允许明确 skip，但 TASK-002H3 不算完成。
11. TASK-002H3 通过审计后，才允许进入 TASK-006。

### 验收

1. `-m postgresql` 非 skip 运行通过。
2. 同 key lock/release 并发测试通过。
3. operation 表唯一约束验证通过。
4. 常规全量 pytest/unittest/py_compile 继续通过。
5. 审计记录补记 PostgreSQL 非 skip 结果。

## ADR-050：PostgreSQL 破坏性集成测试必须双重安全门禁

- 决策时间：2026-04-13 16:06 CST
- 决策类型：测试安全 / 数据保护决策
- 关联模块：TASK-002H4 PostgreSQL 测试库破坏性操作门禁整改
- 审计来源：TASK-002H3 审计意见书第 54 份，PostgreSQL 集成测试仅凭 `POSTGRES_TEST_DSN` 即执行 `DROP SCHEMA ly_schema CASCADE`
- 结论：任何 destructive PostgreSQL 测试必须同时满足 `POSTGRES_TEST_ALLOW_DESTRUCTIVE=true` 和数据库名测试库白名单；否则不得执行 drop schema

### 背景

结算并发集成测试需要清理 `ly_schema` 后构造独立测试数据。这个操作只能在一次性测试库中执行。如果 DSN 被误配到开发、预发或共享库，`DROP SCHEMA ly_schema CASCADE` 会删除业务数据。因此 destructive 测试必须有显式授权和数据库目标白名单。

### 决策

1. `POSTGRES_TEST_DSN` 只表示“可连接 PostgreSQL”，不表示允许破坏性操作。
2. destructive SQL 前必须检查 `POSTGRES_TEST_ALLOW_DESTRUCTIVE=true`。
3. destructive SQL 前必须连接数据库并读取 `current_database()`。
4. 数据库名必须匹配 `*_test`、`test_*`、`lingyi_test_*` 或 `tmp_lingyi_*`。
5. 两项门禁必须同时满足。
6. 不满足门禁时必须 skip 或 fail，且不得执行 drop schema。
7. skip/fail 文案不得泄露完整 DSN、账号、密码。
8. PostgreSQL 测试输出允许显示脱敏数据库名和 schema 名。
9. TASK-002H4 不修改结算业务逻辑。
10. TASK-002H4 通过后，才能重新实跑 TASK-002H3。

### 验收

1. 无 allow env 时不执行 drop schema。
2. 非测试库名时不执行 drop schema。
3. allow=true 且测试库名匹配时才允许 destructive SQL。
4. 门禁测试覆盖通过。
5. 常规 pytest/unittest/py_compile 通过。

## ADR-051：TASK-006 前必须提供 PostgreSQL 非 Skip 集成验证证据

- 决策时间：2026-04-13 16:15 CST
- 决策类型：测试门禁 / 上线前置验证决策
- 关联模块：TASK-002H5 PostgreSQL 非 Skip 实跑验证；TASK-006 加工厂对账单
- 审计来源：TASK-002H4 审计意见书第 55 份通过，剩余风险为当前环境没有真实 `POSTGRES_TEST_DSN`，4 条 PostgreSQL 并发/唯一约束集成测试仍为 skip
- 结论：TASK-006 开发前必须使用一次性 PostgreSQL 测试库和 `POSTGRES_TEST_ALLOW_DESTRUCTIVE=true` 非 skip 跑通结算并发集成测试；全部 skip 不得视为通过

### 背景

TASK-002H1/H2 已修复结算锁定/释放幂等重放和同 key 并发 replay 问题。TASK-002H3 已补真实 PostgreSQL 集成测试，TASK-002H4 已补 destructive 测试双重安全门禁。当前剩余缺口不是代码实现，而是缺少真实 PostgreSQL 非 skip 执行证据。加工厂对账单会依赖结算锁定结果，必须先证明 PostgreSQL 行锁、唯一约束等待和事务 replay 行为符合预期。

### 决策

1. TASK-006 前必须创建一次性 PostgreSQL 测试库。
2. 测试库名称必须匹配 `*_test`、`test_*`、`lingyi_test_*` 或 `tmp_lingyi_*`。
3. 必须设置 `POSTGRES_TEST_ALLOW_DESTRUCTIVE=true`。
4. 必须设置 `POSTGRES_TEST_DSN`，且只允许连接一次性测试库。
5. `-m postgresql` 必须非 skip 运行通过，不能以 skip 结果替代。
6. 必须验证同 key 并发 lock replay。
7. 必须验证同 key 并发 release replay。
8. 必须验证同 key 不同 payload 返回幂等冲突。
9. 必须验证 `ly_subcontract_settlement_operation` 的 `operation_type + idempotency_key` 唯一约束在 PostgreSQL 上存在并生效。
10. 回报和日志中不得泄露完整 DSN、账号或密码。
11. 如真实 PostgreSQL 非 skip 测试暴露业务缺陷，必须停止进入 TASK-006，由架构师另出整改任务。
12. TASK-002H5 通过审计后，才允许进入 TASK-006。

### 验收

1. `.venv/bin/python -m pytest -q -m postgresql tests/test_subcontract_settlement_postgresql.py` 非 skip 通过。
2. PostgreSQL marker 用例没有全部 skip。
3. 同 key lock/release 并发测试通过。
4. 同 key 不同 payload 冲突测试通过。
5. operation 唯一约束测试通过。
6. 常规结算定向测试通过。
7. 全量 pytest/unittest/py_compile 通过。
8. 审计记录补记 PostgreSQL 非 skip 结果。

## ADR-052：PostgreSQL 非 Skip 集成测试必须成为 CI 硬门禁

- 决策时间：2026-04-13 16:35 CST
- 决策类型：CI 门禁 / 测试可靠性决策
- 关联模块：TASK-002H6 PostgreSQL 非 Skip CI 硬门禁；TASK-006 加工厂对账单
- 审计来源：TASK-002H5 审计意见书第 56 份通过，剩余风险为缺 DSN、缺 allow env 或缺驱动时 PostgreSQL marker 会安全 skip，CI 若无执行数量断言可能误绿
- 结论：PostgreSQL marker 集成测试必须在 CI 中硬断言 `tests=4` 且 `skipped=0`；全部 skip 或执行数量不符必须失败

### 背景

TASK-002H5 已证明结算 lock/release 并发语义可在一次性 PostgreSQL 测试库上非 skip 通过。但本地开发环境无 DSN 时仍会安全 skip，这是保护开发环境和避免误删数据的正确策略。CI 场景不同：CI 是发布前质量门禁，不能把 skip 当成功。因此必须增加专用 CI job 和报告断言，确保 PostgreSQL 并发语义持续被真实执行。

### 决策

1. CI 必须提供一次性 PostgreSQL 测试库。
2. CI 数据库名必须匹配 `*_test`、`test_*`、`lingyi_test_*` 或 `tmp_lingyi_*`。
3. CI 必须设置 `POSTGRES_TEST_ALLOW_DESTRUCTIVE=true`。
4. CI 必须设置 `POSTGRES_TEST_DSN`。
5. CI 必须安装 PostgreSQL 驱动依赖。
6. CI 必须运行 `pytest -q -m postgresql tests/test_subcontract_settlement_postgresql.py`。
7. CI 必须生成 JUnit XML 或等效测试报告。
8. CI 必须断言 PostgreSQL marker `tests=4`。
9. CI 必须断言 `skipped=0`。
10. CI 必须在 failures/errors 大于 0 时失败。
11. CI 缺 DSN、缺 allow env、缺驱动、报告缺失、测试数量不等于 4 或 skipped 大于 0 时必须失败。
12. 本地开发可继续安全 skip，但 CI job 不允许 skip 后通过。
13. CI 日志不得泄露完整 DSN、账号或密码。
14. TASK-002H6 通过审计后，才允许进入 TASK-006。

### 验收

1. CI hard gate 脚本存在并可解析 pytest JUnit XML。
2. skipped=4 时断言失败。
3. tests=0、tests=3、tests=5 时断言失败。
4. tests=4 且 skipped=0 时断言通过。
5. CI workflow 或等效 CI job 配置一次性 PostgreSQL 测试库。
6. CI workflow 执行 PostgreSQL marker 并调用 hard gate 断言。
7. 全量 pytest/unittest/py_compile 通过。
8. 未修改结算业务逻辑。

## ADR-053：Backend PostgreSQL Hard Gate 必须成为主干 Required Check

- 决策时间：2026-04-13 16:46 CST
- 决策类型：分支保护 / 发布门禁决策
- 关联模块：TASK-002H7 GitHub Actions 实跑与 Required Check 固化；TASK-006 加工厂对账单
- 审计来源：TASK-002H6 审计意见书第 57 份通过，剩余风险为尚未确认真实 GitHub Actions hosted runner artifact 和 required check 配置
- 结论：`Backend PostgreSQL Hard Gate` 必须在 GitHub hosted runner 上实跑通过，并作为主干分支 required check；未纳入 required check 时，TASK-006 不得进入主干

### 背景

TASK-002H6 已经建立 PostgreSQL marker 的 CI hard gate，能防止 `4 skipped` 被误判为通过。但 hard gate 只有在 GitHub 平台真实运行并纳入分支保护后，才具备主干准入意义。如果 workflow 只存在于代码中，或只作为可选 check，后续 PR 仍可能绕过结算并发语义验证。

### 决策

1. 必须使用 GitHub Actions hosted runner 实跑 `Backend PostgreSQL Hard Gate`。
2. 必须上传 JUnit artifact。
3. JUnit artifact 必须显示 `tests=4`。
4. JUnit artifact 必须显示 `skipped=0`。
5. JUnit artifact 必须显示 `failures=0` 和 `errors=0`。
6. workflow log 和 artifact 不得泄露完整 DSN、账号或密码。
7. 主干分支必须配置 `Backend PostgreSQL Hard Gate / subcontract-postgresql-gate` 或 GitHub 实际等效 check 为 required check。
8. 如果使用 GitHub ruleset，ruleset 也必须包含该 required check。
9. 无仓库管理员权限时，工程师必须回报权限阻塞和管理员配置步骤，不得宣称完成。
10. 后续新增 PostgreSQL marker 用例时，必须同步维护 hard gate expected-tests 或测试名清单。
11. TASK-002H7 通过审计后，允许进入 TASK-006。

### 验收

1. 提供 GitHub Actions run URL。
2. 提供 artifact 名称和脱敏结果摘要。
3. JUnit artifact 为 `tests=4/skipped=0/failures=0/errors=0`。
4. 主干 required check 已配置。
5. 无管理员权限时，提供明确阻塞说明和 exact check 名称。
6. README 记录 required check 名称和维护规则。
7. 未修改外发结算业务逻辑。

## ADR-054：GitHub 平台门禁权限阻塞必须由仓库管理员闭环

- 决策时间：2026-04-13 16:55 CST
- 决策类型：平台权限 / 主干保护决策
- 关联模块：TASK-002H7A Hosted Runner 与 Required Check 管理员闭环；TASK-006 加工厂对账单
- 审计来源：TASK-002H7 审计意见书第 58 份有条件通过，P1 阻断项为 Hosted Runner 实跑 URL、artifact 核验和 required check 配置仍未完成
- 结论：GitHub Actions hosted runner 实跑和主干 required check 配置必须由仓库管理员闭环；无管理员权限时不得宣称 TASK-002H7 完成，TASK-006 不得进入主干

### 背景

TASK-002H7 已完成本地 hard gate 与 workflow 配置层面的验证，但 GitHub 平台层门禁还没有真实生效。由于 branch protection/ruleset 需要仓库管理员权限，普通工程师无法完成最终闭环。该问题属于平台准入权限阻塞，而不是业务代码问题，但它直接决定 PostgreSQL 并发语义验证是否能强制作用于主干。

### 决策

1. TASK-002H7A 必须由仓库管理员或具备 branch protection/ruleset 权限的负责人执行。
2. 必须真实触发 `Backend PostgreSQL Hard Gate` workflow。
3. 必须提供 GitHub Actions hosted runner run URL。
4. 必须核验 GitHub artifact，而不是本地 `.pytest-postgresql.xml`。
5. artifact 必须显示 `tests=4`、`skipped=0`、`failures=0`、`errors=0`。
6. workflow log 和 artifact 不得泄露完整 DSN、账号、密码或 token。
7. 主干分支必须配置 `Backend PostgreSQL Hard Gate / subcontract-postgresql-gate` 或 GitHub 实际等效 check 为 required check。
8. 使用 ruleset 时，ruleset 必须包含同一 required check。
9. 无管理员权限时只能回报“权限阻塞”，不得宣称完成。
10. 必须更新 `CI门禁证据/README.md` 作为审计证据索引。
11. TASK-002H7A 通过审计后，允许进入 TASK-006。

### 验收

1. 提供 hosted runner run URL。
2. 提供 artifact 名称和 JUnit 摘要。
3. JUnit artifact 为 `tests=4/skipped=0/failures=0/errors=0`。
4. required check 已配置到主干 branch protection 或 ruleset。
5. `CI门禁证据/README.md` 已记录完整证据索引。
6. 未修改外发结算业务逻辑。

## ADR-055：TASK-006 前必须提交 GitHub 平台最终闭环证据

- 决策时间：2026-04-13 17:02 CST
- 决策类型：审计证据 / 平台门禁决策
- 关联模块：TASK-002H7B GitHub 平台最终闭环证据提交；TASK-006 加工厂对账单
- 审计来源：TASK-002H7A 审计意见书第 59 份通过，但 Hosted Runner Run URL、artifact 核验和 required check 配置仍为 `<pending>`
- 结论：TASK-006 前必须提交 hosted runner run URL、JUnit artifact 摘要和 required check 配置证据；仅模板通过或本地 JUnit 通过不得放行 TASK-006

### 背景

TASK-002H7A 已确认管理员操作模板、证据字段和敏感信息扫描要求合格，但审计仍未看到真实 GitHub hosted runner 的 artifact，也未看到主干 required check 已配置。该缺口是外发结算进入加工厂对账单前最后的平台门禁证据缺口。

### 决策

1. TASK-002H7B 必须提交 GitHub Actions hosted runner run URL。
2. 必须提交 JUnit artifact 名称。
3. 必须提交 JUnit 摘要 `tests=4/skipped=0/failures=0/errors=0`。
4. 必须提交 4 条 PostgreSQL marker 用例名称。
5. 必须提交 required check 目标分支。
6. 必须提交 required check 的 GitHub 实际显示名称。
7. 必须提交配置方式：Branch protection 或 Ruleset。
8. 必须提交配置人和配置时间。
9. 必须确认 workflow log、artifact、README 和回报中没有完整 DSN、账号、密码或 token。
10. `CI门禁证据/README.md` 不得保留关键 `<pending>` 字段。
11. 本地 `.pytest-postgresql.xml` 只能作为辅助证据，不能替代 hosted runner artifact。
12. TASK-002H7B 通过审计后，允许进入 TASK-006。

### 验收

1. Hosted runner run URL 可追溯。
2. Artifact 可追溯。
3. JUnit 四项为 `tests=4/skipped=0/failures=0/errors=0`。
4. Required check 已配置为主干保护。
5. 证据 README 无关键 `<pending>` 字段。
6. 敏感信息扫描通过。
7. 未修改外发结算业务逻辑。

## ADR-056：TASK-006 放行前 GitHub 平台证据必须全部清零 Pending

- 决策时间：2026-04-13 17:10 CST
- 决策类型：最终放行门禁 / 审计证据决策
- 关联模块：TASK-002H7C GitHub 平台 Pending 清零与最终复审；TASK-006 加工厂对账单
- 审计来源：TASK-002H7B 审计意见书第 60 份通过，但 GitHub 平台闭环仍未完成，三份证据文档仍需真实回填
- 结论：TASK-006 前必须完成 GitHub hosted runner artifact、Required Check 和三份证据文档 pending 清零；模板通过、本地证据通过或部分回填均不得放行

### 背景

TASK-002H7B 已把管理员最终证据拆分成 README、JUnit summary 和 Required Check 配置记录三份模板，并通过审计。但审计结论明确指出 GitHub 平台闭环仍未完成，管理员需要触发 hosted runner、核验 artifact，并设置 required check。为了避免继续以模板替代平台事实，必须设置 H7C 作为最终放行门禁。

### 决策

1. TASK-002H7C 必须提交真实 hosted runner run URL。
2. 必须提交 hosted runner artifact 名称。
3. JUnit 摘要必须为 `tests=4/skipped=0/failures=0/errors=0`。
4. 必须确认 4 条 PostgreSQL marker 用例均被 artifact 覆盖。
5. Required Check 必须已经配置到主干 Branch Protection 或 Ruleset。
6. Required Check 名称必须使用 GitHub 实际显示名称。
7. `README.md`、`postgresql-gate-junit-summary.md`、`required-check-config.md` 的关键 `<pending>` 字段必须全部清零。
8. workflow log、artifact、证据文档和回报不得泄露完整 DSN、账号、密码或 token。
9. 本地 `.pytest-postgresql.xml` 只能作为辅助证据，不能替代 hosted runner artifact。
10. TASK-002H7C 通过审计后，允许进入 TASK-006。

### 验收

1. Hosted runner run URL 可追溯。
2. Artifact 可追溯。
3. JUnit 四项为 `tests=4/skipped=0/failures=0/errors=0`。
4. Required Check 已配置。
5. 三份证据文档无关键 `<pending>` 字段。
6. 敏感信息扫描通过。
7. 未修改外发结算业务逻辑。

## ADR-057：H7C 模板通过不等于 TASK-006 放行

- 决策时间：2026-04-13 17:17 CST
- 决策类型：放行口径修正 / 平台门禁决策
- 关联模块：TASK-002H7C GitHub 平台 Pending 清零与最终复审；TASK-006 加工厂对账单
- 审计来源：TASK-002H7C 审计结论为通过，但明确指出 GitHub hosted runner 实跑、artifact 核验、required check 配置和三份证据 `<pending>` 清零仍需管理员完成
- 结论：TASK-002H7C 的模板通过不等于 TASK-006 放行；只有管理员完成真实 GitHub 平台闭环并经审计最终确认后，才允许进入 TASK-006

### 背景

H7C 已将最终证据字段、验收项和禁止事项整理为可执行模板，并通过审计。但审计同时明确，GitHub 平台真实动作仍未完成，三份证据文档仍需管理员清零 pending。为了避免误读为 TASK-006 已可启动，必须修正放行口径。

### 决策

1. H7C 模板通过仅表示任务单和证据结构合格。
2. TASK-006 不得因 H7C 模板通过而启动。
3. 管理员必须真实触发 hosted runner。
4. 管理员必须核验 JUnit artifact 为 `tests=4/skipped=0/failures=0/errors=0`。
5. 管理员必须配置 `Backend PostgreSQL Hard Gate / subcontract-postgresql-gate` 或实际等效 check 为主干 required check。
6. 三份 CI 证据文档关键 `<pending>` 字段必须全部清零。
7. 敏感信息扫描必须通过。
8. 上述真实平台证据必须经审计最终确认。
9. 最终确认前，TASK-006 保持阻塞。

### 验收

1. Sprint 状态明确 TASK-006 仍阻塞。
2. 外发模块设计明确 H7C 模板通过不等于放行。
3. 架构师日志记录本次放行口径修正。

## ADR-058：生产计划创建 Work Order 必须采用 Outbox / Worker

- 决策时间：2026-04-13 17:28 CST
- 决策类型：跨系统一致性 / 生产计划集成决策
- 关联模块：TASK-004A 生产计划后端基线与 Work Order Outbox
- 架构来源：TASK-002 Stock Entry outbox 和 TASK-003 Job Card outbox 的一致性经验；生产计划同样涉及 FastAPI 本地事务与 ERPNext `Work Order` 跨系统写入
- 结论：生产计划写接口不得在本地事务提交前调用 ERPNext 创建 `Work Order`；必须先落本地计划、审计、outbox，再由 worker 短事务 claim 后调用 ERPNext

### 背景

生产计划集成需要把 ERPNext `Sales Order` 转换为 `Work Order` 和 `Job Card`。如果请求事务内直接调用 ERPNext，一旦 ERPNext 已创建工单但 FastAPI 本地事务回滚，会出现本地计划、审计、工单映射不一致。TASK-002 和 TASK-003 已证明跨系统写入必须采用 outbox/worker 才能保证失败可重试、状态可追溯、审计可闭环。

### 决策

1. ERPNext `Sales Order` 是销售订单事实源。
2. FastAPI `ly_production_plan` 是本地生产计划事实源。
3. 创建 `Work Order` 必须先写 `ly_production_work_order_outbox`，不得在请求事务内直接调用 ERPNext。
4. Worker 必须采用短事务：先 claim 并提交，再调用 ERPNext，再短事务回写结果。
5. `event_key` 必须稳定，不得包含 `outbox_id/request_id/created_at/operator` 等易变字段。
6. ERPNext `Work Order` 必须使用 `custom_ly_plan_id/custom_ly_plan_no` 或等效自定义字段辅助幂等恢复。
7. Worker 找回 ERPNext `Work Order` 时必须校验 `docstatus` 和取消状态，不得把草稿、取消或异常状态误判为成功。
8. `Job Card` 同步只读取 ERPNext 并写入本地 `ly_production_job_card_link` 映射，完成数量仍由 TASK-003 工票链路负责。
9. BOM 中外发工序只在 TASK-004A 标记和映射，不自动创建外发单；外发单仍由 TASK-002 链路控制。
10. 权限资源来源必须来自 ERPNext `Sales Order`、BOM、Item、Company 和 ERPNext User Permission 聚合结果，不能信任前端传入 company。
11. 权限源、ERPNext 主数据源或数据库源不可用时必须 fail closed。
12. TASK-006 仍受 GitHub 平台门禁 pending 阻塞；TASK-004A 可以独立启动，但不得进入 TASK-006 对账开发。

### 验收

1. 生产计划创建接口只落本地计划和审计，不调用 ERPNext 创建 `Work Order`。
2. 创建 `Work Order` 接口只写 outbox 并返回 outbox 状态。
3. Worker claim 与 ERPNext API 调用不在同一个数据库事务内。
4. 相同计划和相同幂等请求稳定命中同一 outbox。
5. ERPNext 草稿、取消、异常 `Work Order` 不会被本地标记为成功。
6. Job Card 同步只写本地映射，不改工票计薪逻辑。
7. 权限、审计、错误信封和日志脱敏延续 TASK-001/TASK-003/TASK-002 标准。

## ADR-059：生产计划接口契约和 Work Order Worker Claim 必须作为复审门禁

- 决策时间：2026-04-13 18:13 CST
- 决策类型：审计整改 / 接口契约 / 并发一致性决策
- 关联模块：TASK-004A1 生产计划审计阻断整改
- 审计来源：审计意见书第 62 份，TASK-004A 不通过，高危 2 / 中危 3
- 结论：TASK-004A1 必须同时补齐请求体契约、仓库字段、Work Order outbox payload、原子 claim、lease 恢复、映射表约束和测试门禁；未闭环前不得进入 TASK-004B、TASK-005 或 TASK-006

### 背景

TASK-004A 已完成生产计划主链路骨架，但审计指出 `material-check` 与 `create-work-order` 的请求体契约没有落地，导致物料检查不能按仓库表达，Work Order outbox payload 缺少 ERPNext 必要字段。同时，Worker claim 不是原子认领，可能在并发下重复调用 ERPNext 创建 `Work Order`，也可能因 `processing` 任务无 lease 恢复而永久卡死。

### 决策

1. `material-check` 必须接收并校验 `warehouse`。
2. `create-work-order` 必须接收并校验 `fg_warehouse/wip_warehouse/start_date/idempotency_key`。
3. `fg_warehouse/wip_warehouse/start_date` 必须进入 outbox payload 和 payload hash。
4. 同一 `plan_id + idempotency_key` 相同 payload 必须 replay 首次 outbox，不同 payload 必须返回 `PRODUCTION_IDEMPOTENCY_CONFLICT`。
5. Work Order outbox worker 必须使用原子 claim。
6. PostgreSQL 下必须采用 `SELECT ... FOR UPDATE SKIP LOCKED` 或等价原子 `UPDATE ... RETURNING`。
7. Worker 必须支持 `processing` lease 过期恢复。
8. Worker 不得在持有数据库行锁或未提交 claim 事务时调用 ERPNext。
9. `ly_production_work_order_link.plan_id` 必须有唯一约束，保证一张生产计划只对应一个本地 Work Order 映射。
10. `ly_production_job_card_link` 必须持久化 `company/item_code/operation_sequence/synced_at`，避免后续生产页面和工票模块只能绕回计划表推断资源范围。
11. TASK-004A1 复审前必须补齐仓库契约、DB/AUDIT 失败、Closed/Cancelled SO、BOM mismatch、worker 并发和 lease 恢复测试。
12. 本次整改不得进入前端、生产入库、外发自动创建、TASK-005 或 TASK-006。

### 验收

1. `material-check` 缺少仓库返回 `PRODUCTION_WAREHOUSE_REQUIRED`。
2. `create-work-order` 缺少成品仓或 WIP 仓返回 `PRODUCTION_WAREHOUSE_REQUIRED`。
3. Work Order outbox payload 含 `fg_warehouse/wip_warehouse/start_date`。
4. 相同 idempotency_key 相同 payload 能 replay，相同 idempotency_key 不同 payload 返回冲突。
5. 并发 worker 不重复调用 ERPNext。
6. lease 过期的 `processing` outbox 可以恢复。
7. `ly_production_work_order_link.plan_id` 唯一约束生效。
8. Job Card 映射写入 `company/item_code/operation_sequence/synced_at`。
9. 全量 pytest、unittest、py_compile 通过。
10. 未修改外发结算、工票计薪、TASK-005、TASK-006。

## ADR-060：生产计划前端只能调用 FastAPI 业务接口，不得触发内部 Worker 或直连 ERPNext

- 决策时间：2026-04-13 18:57 CST
- 决策类型：前端架构 / 权限边界 / 集成边界决策
- 关联模块：TASK-004B 生产计划前端联动
- 审计来源：审计意见书第 63 份确认 TASK-004A1 通过；后端接口契约已冻结，可进入前端联动
- 结论：生产计划前端只允许调用 `/api/production/` 普通业务接口，必须复用统一请求层和权限按钮；不得调用内部 worker、不得直连 ERPNext、不得让前端传入 company 作为业务事实或权限依据

### 背景

TASK-004A1 已完成后端接口契约、Work Order outbox、Job Card 本地映射和权限审计。下一步前端联动需要把生产计划员的日常操作接入这些接口。历史外发前端审计曾发现裸 `fetch()`、鉴权头缺失和接口字段漂移风险，因此生产计划前端必须从任务单阶段锁死统一请求层、权限按钮和内部接口边界。

### 决策

1. `src/api/production.ts` 必须复用 `src/api/request.ts`。
2. production 前端代码不得出现裸 `fetch()`。
3. production 前端不得调用 `/api/production/internal/work-order-sync/run-once`。
4. production 前端不得直接调用 ERPNext `/api/resource` 或其他 ERPNext REST API。
5. 创建生产计划表单不得包含 `company` 输入项。
6. `company` 只能由后端从 ERPNext Sales Order 派生并返回给前端展示。
7. 前端按钮权限必须来自 `/api/auth/actions?module=production`。
8. UI 必须过滤 `production:work_order_worker`，不得暴露内部 worker 运维入口。
9. `material-check` 前端请求必须传 `warehouse`。
10. `create-work-order` 前端请求必须传 `fg_warehouse/wip_warehouse/start_date/idempotency_key`。
11. 物料可用量展示必须标注为后端快照；未接 ERPNext 库存快照前不得宣传为实时可用库存或锁定库存。
12. 本任务不得修改 production 后端业务逻辑，不得进入 TASK-005/TASK-006。

### 验收

1. 新增 `src/api/production.ts`，且所有请求使用统一 `request()`。
2. 新增生产计划列表页和详情页。
3. 新增 `/production/plans` 与 `/production/plans/detail` 路由。
4. 无 `read` 权限时不加载业务数据。
5. 无对应权限时不展示创建计划、物料检查、创建 Work Order、同步 Job Card 按钮。
6. 前端静态扫描未发现裸 `fetch()`、内部 worker 调用、ERPNext 直连或敏感信息泄露。
7. 创建计划 payload 不包含 `company`。
8. 未修改 production 后端业务逻辑。
9. 未进入 TASK-005/TASK-006。

## ADR-061：`planned_start_date` 是生产计划事实字段，Work Order `start_date` 是工单同步参数

- 决策时间：2026-04-13 19:14 CST
- 决策类型：前后端契约 / 数据事实边界决策
- 关联模块：TASK-004B1 生产计划前端契约整改
- 审计来源：审计意见书第 64 份，TASK-004B 有条件通过，中危 3 / 低危 1
- 结论：`planned_start_date` 必须作为本地生产计划事实字段保存和返回；Work Order `start_date` 仅作为创建 ERPNext Work Order outbox 的工单开工参数，两者允许默认联动但不得混用或互相替代

### 背景

TASK-004B 前端已经实现生产计划页面，但审计发现用户在新建计划弹窗中选择 `planned_start_date` 后，payload 没有提交，后端也没有返回该字段，列表和详情会长期显示空值。这会让生产计划员误判计划开工信息。与此同时，`create-work-order` 已有 `start_date` 字段，它服务于 ERPNext Work Order 创建，不应替代本地生产计划事实字段。

### 决策

1. `planned_start_date` 是 `ly_production_plan` 的业务事实字段。
2. `planned_start_date` 必须进入创建生产计划请求 DTO。
3. `planned_start_date` 必须由后端保存。
4. `planned_start_date` 必须在生产计划列表 DTO 中返回。
5. `planned_start_date` 必须在生产计划详情 DTO 中返回。
6. 创建计划幂等 `request_hash` 必须包含 `planned_start_date`。
7. 相同 idempotency_key 但不同 `planned_start_date` 必须返回幂等冲突。
8. Work Order `start_date` 是创建 ERPNext Work Order outbox 的请求字段。
9. 前端创建 Work Order 时可以默认把 `planned_start_date` 带入 `start_date`，但提交字段必须仍叫 `start_date`。
10. 前端不得删除 `planned_start_date` 输入来规避契约问题。
11. Work Order 映射详情必须展示后端返回的 `erpnext_docstatus/erpnext_status/sync_status/last_synced_at`。
12. 物料快照详情必须展示后端返回的 `checked_at`。
13. 前端必须在 actions 和 button_permissions 两层过滤内部 worker 权限。
14. 本次整改不得进入 TASK-005/TASK-006。

### 验收

1. 创建计划提交 `planned_start_date`。
2. 后端保存并在列表/详情返回 `planned_start_date`。
3. 不同 `planned_start_date` 触发幂等冲突。
4. Work Order `start_date` 仍作为独立字段提交。
5. `work_order_worker` 按钮权限被前端强制清零。
6. Work Order 映射和物料快照展示真实 DTO 字段。
7. 状态中文标签覆盖任务单要求。
8. 前端无内部 worker 调用、无 ERPNext 直连、无裸 `fetch()`。
9. 后端 production 定向和全量回归通过。
10. 未进入 TASK-005/TASK-006。

## ADR-062：生产计划前端必须具备最小 typecheck/build 与契约扫描门禁

- 决策时间：2026-04-13 19:32 CST
- 决策类型：前端工程质量 / 契约校验 / 上线门禁决策
- 关联模块：TASK-004C 前端最小构建与契约校验基建
- 审计来源：审计意见书第 65 份，TASK-004B1 通过，但保留“前端无 package.json，无法 typecheck/build，DTO 漂移仍可能回潮”的高概率风险
- 结论：TASK-004 进入后续上线或更大范围联调前，`/06_前端/lingyi-pc` 必须具备可执行的 `npm run typecheck`、`npm run build` 和 `npm run check:production-contracts`

### 背景

生产计划前端已完成列表、详情、物料检查、Work Order outbox、Job Card 同步等页面联动，并通过 TASK-004B1 复审。但当前前端目录缺少基础工程文件，审计只能依赖静态扫描，无法通过 TypeScript 编译发现 DTO 字段漂移。TASK-004B 已经出现过 `planned_start_date/checked_at` 类字段漂移，因此必须补齐最小前端构建和契约扫描基建。

### 决策

1. 前端目录必须新增 `package.json`。
2. 前端目录必须新增 `tsconfig.json`、`tsconfig.node.json` 和 `vite.config.ts`。
3. 前端目录必须新增 `index.html`、`src/main.ts`、`src/App.vue` 和 `src/env.d.ts`。
4. `package.json` 必须提供 `typecheck/build/check:production-contracts`。
5. `typecheck` 必须执行 Vue + TypeScript 类型检查。
6. `build` 必须先 typecheck 再 Vite build。
7. `check:production-contracts` 必须扫描 production 前端契约边界。
8. 契约扫描必须拦截裸 `fetch()`、内部 worker 调用、ERPNext 直连、创建计划 payload 包含 `company`、planned_start_date 缺失、create-work-order payload 字段缺失、内部 worker button permission 未清零。
9. 本任务只补工程基建和类型/契约问题，不新增业务页面，不修改 production 后端业务逻辑。
10. 不得提交 `node_modules`、生产 token、Cookie、账号密码、完整 DSN。
11. TASK-004C 通过前，不建议继续扩大生产计划前端范围。
12. TASK-005/TASK-006 仍不得因本任务启动。

### 验收

1. `npm install` 成功。
2. `npm run check:production-contracts` 通过。
3. `npm run typecheck` 通过。
4. `npm run build` 通过。
5. production 前端无裸 `fetch()`、内部 worker 调用、ERPNext 直连。
6. production 创建计划 payload 不包含 `company`。
7. planned_start_date 契约被脚本检查。
8. 前端构建产物不包含明文 token、Cookie、Secret、完整 DSN。
9. 后端 production 定向和全量回归通过。
10. 未修改 production 后端业务逻辑，未进入 TASK-005/TASK-006。

## ADR-063：生产计划前端契约脚本必须覆盖 router/stores 防止内部入口回潮

- 决策时间：2026-04-13 20:08 CST
- 决策类型：前端契约门禁 / 安全边界决策
- 关联模块：TASK-004C1 前端契约扫描盲区整改
- 审计来源：审计意见书第 66 份，TASK-004C 通过，但保留“contract 脚本未覆盖 router/stores，内部 worker 入口可从非页面文件绕回”的风险
- 结论：`check-production-contracts.mjs` 必须把 `src/router`、`src/stores` 纳入禁线扫描；内部 worker、ERPNext 直连、裸 fetch、敏感关键字不得从非 production 页面路径回潮

### 背景

TASK-004C 已补齐前端 package、typecheck、build 和 production 契约扫描，但脚本主要覆盖 production API 与 production views。生产计划内部 worker 的入口也可能从 router、store 或权限状态层被误暴露。为了让门禁真正覆盖 UI 入口链路，必须将 router/stores 纳入同一脚本校验。

### 决策

1. 契约脚本必须扫描 `src/api/production.ts`。
2. 契约脚本必须扫描 `src/views/production/**/*`。
3. 契约脚本必须扫描 `src/router/**/*`。
4. 契约脚本必须扫描 `src/stores/**/*`。
5. 上述范围内必须拦截裸 `fetch()`。
6. 上述范围内必须拦截 `/api/production/internal`。
7. 上述范围内必须拦截 `work-order-sync/run-once`。
8. 上述范围内必须拦截 ERPNext `/api/resource` 直连。
9. 上述范围内必须拦截非白名单 `Authorization/Cookie/token/secret/password`。
10. `src/stores/permission.ts` 可以保留内部动作 denylist，但必须验证内部按钮权限清零逻辑存在。
11. 禁止把内部 worker API 或 ERPNext 直连加入白名单。
12. 本任务只补契约脚本和验证，不修改生产计划后端业务逻辑，不进入 TASK-005/TASK-006。

### 验收

1. `npm run check:production-contracts` 覆盖 router/stores。
2. 脚本可拦截 router/stores 中的内部 production API。
3. 脚本可拦截 router/stores 中的 ERPNext `/api/resource`。
4. 脚本可拦截 router/stores 中的裸 `fetch()`。
5. planned_start_date、Work Order payload、company 禁止和 last_synced_at 原规则仍保留。
6. `npm run typecheck` 与 `npm run build` 通过。
7. 后端 production 定向和全量回归通过。
8. 未修改 production 后端业务逻辑，未进入 TASK-005/TASK-006。

## ADR-064：生产计划前端契约脚本必须具备自动反向测试

- 决策时间：2026-04-13 20:21 CST
- 决策类型：质量门禁自证 / 前端契约测试决策
- 关联模块：TASK-004C2 前端契约脚本自动反向测试
- 审计来源：审计意见书第 67 份，TASK-004C1 通过，但保留“契约脚本靠人工反向验证，脚本被改弱后无人发现”的风险
- 结论：`check-production-contracts.mjs` 必须配套自动反向测试，使用临时 fixture 注入坏样例，断言脚本非 0 退出并命中预期错误信息

### 背景

TASK-004C1 已把 router/stores 纳入 production 契约扫描，并通过审计。但如果未来有人削弱 `check-production-contracts.mjs` 的规则，当前项目真实源码仍可能通过，人工反向验证也容易漏跑。契约脚本是质量门禁自身，必须有自动化测试证明“坏样例会失败、好样例会通过”。

### 决策

1. 新增 `scripts/test-production-contracts.mjs`。
2. 测试必须创建临时 fixture 项目，不得修改真实 `src`。
3. `check-production-contracts.mjs` 必须支持对临时 fixture projectRoot 执行。
4. 自动反向测试必须覆盖裸 `fetch()`。
5. 自动反向测试必须覆盖 `/api/resource`。
6. 自动反向测试必须覆盖 `/api/production/internal/work-order-sync/run-once`。
7. 自动反向测试必须覆盖 `ProductionPlanCreatePayload.company`。
8. 自动反向测试必须覆盖缺少 `planned_start_date`。
9. 自动反向测试必须覆盖缺少 Work Order payload 字段。
10. 自动反向测试必须覆盖 `latestJobCardSyncedAt` 回潮。
11. 自动反向测试必须覆盖内部按钮清零缺失。
12. 自动反向测试必须覆盖最小合法 fixture 通过。
13. `package.json` 必须新增 `test:production-contracts`。
14. `npm run verify` 必须串联 `check:production-contracts && test:production-contracts && typecheck && build`。
15. 本任务不得修改生产计划后端业务逻辑，不得进入 TASK-005/TASK-006。

### 验收

1. `npm run test:production-contracts` 通过。
2. 每个坏样例均断言退出码非 0。
3. 每个坏样例均断言错误关键词命中。
4. 最小合法 fixture 通过。
5. `npm run verify` 包含反向测试并通过。
6. `npm run check:production-contracts/typecheck/build` 通过。
7. 后端 production 定向和全量回归通过。
8. 未修改 production 后端业务逻辑，未进入 TASK-005/TASK-006。

## ADR-065：契约脚本关键规则必须有独立反向 fixture

- 决策时间：2026-04-13 20:32 CST
- 决策类型：质量门禁自证 / 反向测试粒度决策
- 关联模块：TASK-004C3 前端契约反向测试独立用例补齐
- 审计来源：审计意见书第 68 份，TASK-004C2 通过，但保留“`work-order-sync/run-once` 和敏感关键字缺少独立 fixture”的风险
- 结论：契约脚本每条关键规则必须具备独立反向 fixture，避免某条规则被删弱后，坏样例仍因其他规则失败而误判为覆盖充分

### 背景

TASK-004C2 已新增自动反向测试，但 `work-order-sync/run-once` 用例同时命中 `/api/production/internal`，无法证明 run-once 专项规则独立有效；敏感关键字规则尚无独立坏样例。为了避免组合用例掩盖单条规则失效，必须补独立 fixture。

### 决策

1. `work-order-sync/run-once` 必须有独立坏样例。
2. 该坏样例不得包含 `/api/production/internal`。
3. 该坏样例不得包含 `/api/resource`。
4. 该坏样例不得包含裸 `fetch()`。
5. 该坏样例必须因 run-once 专项规则失败。
6. 敏感关键字必须有独立坏样例。
7. 敏感关键字坏样例不得包含裸 `fetch()`、`/api/production/internal`、`work-order-sync/run-once`、`/api/resource`。
8. 敏感关键字坏样例必须因敏感关键字规则失败。
9. TASK-004C2 已有 10 个场景必须全部保留。
10. 新增后 `test:production-contracts` 至少覆盖 12 个场景。
11. 本任务不得修改真实 production 页面/API/router/store 业务逻辑。
12. 本任务不得修改生产计划后端业务逻辑，不得进入 TASK-005/TASK-006。

### 验收

1. run-once 独立坏样例失败且命中 run-once 错误关键词。
2. 敏感关键字独立坏样例失败且命中敏感关键字错误关键词。
3. `npm run test:production-contracts` 至少 12 个场景通过。
4. `npm run check:production-contracts` 通过。
5. `npm run verify/typecheck/build` 通过。
6. 后端 production 定向和全量回归通过。
7. 未修改 production 后端业务逻辑，未进入 TASK-005/TASK-006。

## ADR-066：前端 Verify 必须进入 CI 并锁定 Node/npm 版本

- 决策时间：2026-04-13 20:43 CST
- 决策类型：CI 质量门禁 / 前端运行时版本决策
- 关联模块：TASK-004C4 前端 Verify CI 与 Node 版本锁定
- 审计来源：审计意见书第 69 份，TASK-004C3 通过，但保留“`npm run verify` 未固化进 CI，Node/npm 版本未统一”的风险
- 结论：生产计划前端 `npm run verify` 必须进入 GitHub Actions 前端硬门禁，且 Node/npm 版本必须在 CI、本地版本文件和 `package.json` 中一致锁定

### 背景

TASK-004C3 已补齐 production 契约脚本的独立反向测试，证明 `work-order-sync/run-once` 和敏感关键字规则单独有效。但如果这些门禁只在本地执行，或者不同工程师使用不同 Node/npm 版本，合并链路仍可能出现“本地通过、CI 未跑、他人环境失败”的质量漂移。

### 决策

1. 新增 `.github/workflows/frontend-verify.yml`。
2. workflow 名称固定为 `Frontend Verify Hard Gate`。
3. job 名称固定为 `lingyi-pc-verify`。
4. workflow 必须支持 `workflow_dispatch`。
5. workflow 必须在前端目录或 workflow 文件变更时触发 `pull_request`。
6. workflow 必须在 `main` 分支前端目录或 workflow 文件变更时触发 `push`。
7. Node 固定为 `22.22.1`，禁止使用 `22.x/latest/lts/*`。
8. npm 固定为 `10.9.4`。
9. CI 必须断言 `node -v = v22.22.1`。
10. CI 必须断言 `npm -v = 10.9.4`。
11. CI 必须使用 `npm ci`，禁止使用 `npm install`。
12. CI 必须执行 `npm run verify`。
13. CI 必须执行 `npm audit --audit-level=high`。
14. 前端目录必须新增 `.nvmrc` 和 `.npmrc`，并在 `package.json` 中声明 `packageManager` 与 `engines`。
15. 本任务不得修改生产计划前端业务逻辑，不得修改后端业务逻辑，不得进入 TASK-005/TASK-006。

### 验收

1. `.github/workflows/frontend-verify.yml` 存在且 workflow/job 名称符合约定。
2. workflow 触发范围覆盖前端目录与自身 workflow 文件。
3. workflow 执行 `npm ci`、`npm run verify`、`npm audit --audit-level=high`。
4. `.nvmrc`、`.npmrc`、`package.json` 版本声明一致。
5. 本地 `npm run verify`、`npm audit --audit-level=high` 通过。
6. production 禁线扫描无业务文件命中。
7. 后端 production 定向和全量回归通过。
8. 未修改 production 页面/API/router/store 业务逻辑，未进入 TASK-005/TASK-006。

## ADR-067：前端 CI 必须完成 GitHub 平台 Required Check 闭环

- 决策时间：2026-04-13 20:51 CST
- 决策类型：平台门禁 / 分支保护决策
- 关联模块：TASK-004C5 前端 CI 平台 Required Check 闭环
- 审计来源：审计意见书第 70 份，TASK-004C4 通过，但保留“GitHub 平台侧 required check 尚未闭环”的风险
- 结论：仓库内 workflow 合格不等于团队合并门禁生效；必须由管理员完成 hosted runner 实跑、日志核验和 main 分支 required check 配置

### 背景

TASK-004C4 已把 `npm run verify` 写入 GitHub Actions，并完成 Node/npm 版本锁定。但如果 workflow 没有在 GitHub hosted runner 上实跑，或者没有被配置成 main 分支 required check，团队仍可能合并未经过前端契约门禁的代码。因此需要单独冻结平台闭环任务。

### 决策

1. 必须在 GitHub hosted runner 上实跑 `Frontend Verify Hard Gate`。
2. 必须确认 job `lingyi-pc-verify` 通过。
3. 必须记录 Run URL、Commit SHA、Branch。
4. 必须核验 `node -v = v22.22.1`。
5. 必须核验 `npm -v = 10.9.4`。
6. 必须核验 `npm ci`、`npm run verify`、`npm audit --audit-level=high` 均成功。
7. 必须在 branch protection 或 ruleset 中将 `Frontend Verify Hard Gate / lingyi-pc-verify` 配置为 main 分支 required check。
8. 必须创建平台闭环证据文档。
9. 证据文档不得包含 token、password、secret、cookie、私钥。
10. 本任务不得修改前端业务代码、后端业务代码，不得进入 TASK-005/TASK-006。

### 验收

1. 平台证据文档存在。
2. 证据文档包含 Run URL、Commit SHA、Branch。
3. 证据文档确认 hosted runner 上 `lingyi-pc-verify` 通过。
4. 证据文档确认 Node/npm 版本与 ADR-066 一致。
5. 证据文档确认 `npm run verify` 与 `npm audit --audit-level=high` 在 hosted runner 上通过。
6. 证据文档确认 `Frontend Verify Hard Gate / lingyi-pc-verify` 已配置为 main required check。
7. 敏感信息扫描通过。
8. 未修改前后端业务代码，未进入 TASK-005/TASK-006。

## ADR-068：生产系统 CI 必须位于真实 GitHub 仓库根内

- 决策时间：2026-04-13 21:02 CST
- 决策类型：仓库治理 / CI 可见性决策
- 关联模块：TASK-004C6 Git 仓库根与 CI 可见性整改
- 审计来源：审计意见书第 71 份，TASK-004C5 有条件通过但平台闭环未完成；最高风险为 workflow 不在真实 git root 内
- 结论：生产系统 CI 文件、前端代码、后端代码必须处于同一个真实 GitHub 仓库根；workflow 位于 git root 外时，不得宣称 GitHub Actions 或 required check 已闭环

### 背景

审计指出当前本地可检测 git root 是 `/Users/hh/Desktop/领意服装管理系统/02_源码`，但 `.github/workflows/frontend-verify.yml` 位于 `/Users/hh/Desktop/领意服装管理系统/.github/workflows/`，不在该 git root 内。同时当前前端目录 `/06_前端/lingyi-pc` 和后端目录 `/07_后端/lingyi_service` 也不在 git repository 内。该状态会导致 GitHub 远端看不到 workflow 或看不到被 workflow 验证的源码。

### 决策

1. 生产系统 GitHub 仓库根必须同时包含 `.github/workflows/frontend-verify.yml`、`.github/workflows/backend-postgresql.yml`、`06_前端/lingyi-pc/package.json`、`07_后端/lingyi_service/requirements.txt`。
2. 推荐仓库根为 `/Users/hh/Desktop/领意服装管理系统`。
3. 如果管理员决定继续使用 `/Users/hh/Desktop/领意服装管理系统/02_源码` 作为 GitHub 仓库根，则必须把前端、后端和 workflow 迁入该 git root，并同步修正 workflow 路径。
4. 必须用 `git rev-parse --show-toplevel`、`git remote -v`、`git ls-files` 三类证据确认仓库根和 CI 文件可见性。
5. `.gitignore` 不得忽略 `.github/workflows/**`、`06_前端/lingyi-pc/**`、`07_后端/lingyi_service/**` 关键源码路径。
6. 在 GitHub Actions 页面能看到 `Frontend Verify Hard Gate` 前，不得宣称前端 CI 平台闭环。
7. 在 hosted runner 实跑和 required check 完成前，TASK-005/TASK-006 不得放行。
8. 本任务不得修改前后端业务逻辑，不得进入 TASK-005/TASK-006。

### 验收

1. 证据文档记录真实 git root、remote、branch、commit SHA。
2. `git ls-files` 能看到 frontend workflow、前端 package、后端 requirements。
3. GitHub Actions 页面能看到 `Frontend Verify Hard Gate`。
4. Hosted runner 实跑 `Frontend Verify Hard Gate / lingyi-pc-verify` 通过。
5. main 分支 required check 已配置为 `Frontend Verify Hard Gate / lingyi-pc-verify`。
6. 证据文档无 token/password/secret/cookie/私钥。
7. 未修改前后端业务代码，未进入 TASK-005/TASK-006。

## ADR-069：项目根作为唯一生产 GitHub 仓库根

- 决策时间：2026-04-13 21:08 CST
- 决策类型：仓库治理 / CI 根目录决策
- 关联模块：TASK-004C7 项目根 GitHub 仓库根策略落地
- 审计来源：审计意见书第 72 份，TASK-004C6 通过，但保留“仓库根策略未定，Hosted Runner 和 required check 都只是纸面门禁”的风险
- 结论：采用 `/Users/hh/Desktop/领意服装管理系统` 作为唯一生产 GitHub 仓库根；不采用把 `.github`、`06_前端`、`07_后端` 迁入 `/02_源码` 的方案

### 背景

TASK-004C6 证据已经确认：项目根不是 git repository，`/02_源码` 是当前唯一可检测 git root，但 workflow、前端、后端正式交付目录都在项目根下。若继续以 `/02_源码` 作为生产仓库根，GitHub 将无法直接看到当前 `.github`、`06_前端`、`07_后端`，CI 和 required check 无法形成真实门禁。

### 决策

1. `/Users/hh/Desktop/领意服装管理系统` 定为唯一生产 GitHub 仓库根。
2. `/Users/hh/Desktop/领意服装管理系统/02_源码` 降级为历史源码目录，不再作为生产 CI 仓库根。
3. 不把 `.github`、`06_前端`、`07_后端` 迁入 `/02_源码`。
4. `/02_源码/.git` 必须先备份并记录 SHA256，再移出项目目录；不得直接删除。
5. 项目根必须初始化 git，并配置 GitHub remote。
6. 项目根 `.gitignore` 必须排除 `node_modules/dist/.venv/__pycache__/.env` 等生成物与敏感文件。
7. 项目根 `.gitignore` 不得忽略 `.github/workflows/**`、`06_前端/lingyi-pc/**`、`07_后端/lingyi_service/**`、`03_需求与设计/**`。
8. `git ls-files` 必须能看到 frontend workflow、backend workflow、前端 package、后端 requirements、生产计划设计文档。
9. 项目根推送到 GitHub 后，必须确认 `Frontend Verify Hard Gate` 在 Actions 页面可见并实跑通过。
10. main 分支必须配置 required check：`Frontend Verify Hard Gate / lingyi-pc-verify`。
11. 本任务不得修改前后端业务逻辑，不得进入 TASK-005/TASK-006。

### 验收

1. 证据文档记录 `/02_源码/.git` 备份路径与 SHA256。
2. `git rev-parse --show-toplevel` 输出项目根。
3. `git remote -v` 记录 GitHub 远端。
4. `git ls-files` 能看到关键 workflow、前端、后端、设计文件。
5. GitHub Actions 页面能看到 `Frontend Verify Hard Gate`。
6. Hosted runner 实跑 `Frontend Verify Hard Gate / lingyi-pc-verify` 通过。
7. main required check 已配置。
8. 证据文档无 token/password/secret/cookie/私钥。
9. 未修改前后端业务代码，未进入 TASK-005/TASK-006。

## ADR-070：首个生产提交必须通过仅 git tracked 文件的离线 CI 模拟

- 决策时间：2026-04-13 21:20 CST
- 决策类型：仓库治理 / CI 可复现性决策
- 关联模块：TASK-004C8 根仓库首个提交清单与离线 CI 模拟
- 审计来源：审计意见书第 73 份，TASK-004C7 有条件通过；最高风险为根仓库 index 只有 6 个文件，GitHub runner 看不到 CI 必需文件
- 结论：首个生产提交前，必须明确 git 跟踪清单，并用 `git ls-files` 生成离线 snapshot 执行前端和后端 CI 等价验证；原工作区通过不等于 GitHub runner 可复现通过

### 背景

项目根 git root 已经建立，但当前 index 只有 workflow、`.gitignore`、生产计划设计文档、前端 `package.json`、后端 `requirements.txt` 等 6 个文件。前端 CI 需要 `package-lock.json`、`scripts/**`、`tsconfig*.json`、`vite.config.ts`、`src/**`；后端回归需要 `requirements-dev.txt`、`pytest.ini`、`app/**`、`tests/**`、`scripts/**` 等。如果这些文件未被 git 跟踪，GitHub hosted runner checkout 后无法复现本地验证。

### 决策

1. 首个生产提交必须纳入 `.github/workflows/**`。
2. 首个生产提交必须纳入前端 `package-lock.json`、`scripts/**`、`tsconfig*.json`、`vite.config.ts`、`index.html`、`src/**`。
3. 首个生产提交必须纳入后端 `requirements-dev.txt`、`pytest.ini`、`app/**`、`tests/**`、`scripts/**`、`migrations/**`。
4. 首个生产提交必须纳入当前架构设计、开发计划和审计记录必要文档。
5. 首个生产提交不得纳入 `node_modules/dist/.vite/.venv/__pycache__/*.pyc/.env/lingyi_service.db/.pytest-postgresql.xml`。
6. 首个生产提交暂不纳入 `02_源码/**`，该目录作为历史源码归档后续单独决策。
7. 必须用 `git ls-files` 生成临时 snapshot。
8. 必须在 snapshot 中执行前端 `npm ci && npm run verify && npm audit --audit-level=high`。
9. 必须在 snapshot 中执行后端 production 定向、全量 pytest、unittest、py_compile。
10. snapshot 验证失败时禁止提交和推送。
11. 本任务不得修改前后端业务逻辑，不得进入 TASK-005/TASK-006。

### 验收

1. 证据文档记录 staged 文件数和 commit SHA。
2. `package-lock.json/scripts/tsconfig/vite/src` 已被 git 跟踪。
3. `requirements-dev/pytest.ini/app/tests/scripts/migrations` 已被 git 跟踪。
4. 生成物、敏感文件、`02_源码/**` 未进入 index。
5. 离线 snapshot 来源为 `git ls-files`。
6. snapshot 前端 CI 等价验证通过。
7. snapshot 后端回归通过。
8. 未修改前后端业务代码，未进入 TASK-005/TASK-006。

## ADR-071：前端 CI 平台闭环必须完成 origin、hosted runner 与 required check

- 决策时间：2026-04-13 21:33 CST
- 决策类型：平台门禁 / GitHub required check 决策
- 关联模块：TASK-004C9 GitHub 远端推送与前端 Required Check 闭环
- 审计来源：审计意见书第 74 份，TASK-004C8 通过；剩余风险为尚未配置 `origin`、push `main`、触发 Hosted Runner、配置 required check
- 结论：根仓库 tracked snapshot 通过后，必须接入 GitHub 平台并完成 `Frontend Verify Hard Gate / lingyi-pc-verify` required check；本地可复现不等于平台合并门禁生效

### 背景

TASK-004C8 已把根仓库 tracked 文件补齐到 236 个，并通过只包含 `git tracked` 文件的离线 CI snapshot 验证。此时本地“GitHub runner 可复现”的地基已经具备，但还没有配置远端、推送 main、触发 hosted runner，也没有将前端 verify job 配置为 main required check。

### 决策

1. 必须配置 `origin` remote。
2. remote URL 不得包含 token、password、secret、cookie。
3. 必须 push `main` 到 GitHub。
4. 如果远端已有历史导致 push 失败，必须停止并提交冲突/合并方案，禁止强推覆盖。
5. GitHub Actions 页面必须能看到 `Frontend Verify Hard Gate`。
6. 必须在 hosted runner 上执行 `Frontend Verify Hard Gate / lingyi-pc-verify`。
7. hosted runner 必须确认 Node `v22.22.1` 与 npm `10.9.4`。
8. hosted runner 必须通过 `npm ci`、`npm run verify`、`npm audit --audit-level=high`。
9. main 分支必须配置 required check：`Frontend Verify Hard Gate / lingyi-pc-verify`。
10. 如果 GitHub 实际显示名称不同，必须记录实际名称并提交架构师确认。
11. 必须创建平台闭环证据文档，记录 Run URL、Commit SHA、Branch、required check 配置。
12. 本任务不得修改前后端业务逻辑，不得进入 TASK-005/TASK-006。

### 验收

1. `origin` 已配置且 remote 输出无敏感信息。
2. `main` 已成功推送到 GitHub。
3. GitHub Actions 页面可见 `Frontend Verify Hard Gate`。
4. Hosted runner `Frontend Verify Hard Gate / lingyi-pc-verify` 通过。
5. Hosted runner 日志确认 Node/npm 版本、`npm ci`、`npm run verify`、`npm audit` 均通过。
6. main required check 已配置。
7. 证据文档无 token/password/secret/cookie/私钥。
8. 未修改前后端业务代码，未进入 TASK-005/TASK-006。

## ADR-072：平台闭环证据必须先做 docs-only 补提交

- 决策时间：2026-04-13 21:40 CST
- 决策类型：仓库治理 / 平台证据提交决策
- 关联模块：TASK-004C10 C9 证据与审计记录补提交
- 审计来源：审计意见书第 75 份，TASK-004C9 有条件通过；最高风险为 C9 证据文件和最新审计记录未跟踪，直接 push `e4a3e4b` 会丢失平台阻塞证据
- 结论：在配置 remote 和 push 前，必须先将 C9 证据、第 75 份审计记录、架构记录和 sprint 状态做成 docs-only commit；禁止夹带前后端业务代码或未审计目录

### 背景

TASK-004C9 复审确认本地基线属实，但当前没有 remote，也无法触发 hosted runner 或配置 required check。同时，C9 证据文件与最新审计记录仍是未跟踪文件。如果直接 push 当前基线提交，远端仓库不会包含平台闭环阻塞证据，后续审计无法从远端复原决策链路。

### 决策

1. 必须先形成 docs-only commit，再进入 remote/push。
2. docs-only commit 必须包含 C9 任务单、C9 证据、第 75 份审计记录、审计官日志、架构师日志、当前 sprint、生产计划模块设计、技术决策记录和 C10 任务单。
3. 必须显式 `git add` 白名单文件。
4. 禁止 `git add .`。
5. 禁止提交前端业务代码、后端业务代码、`02_源码/**`、`04_测试与验收/**`、`05_交付物/**`。
6. 禁止提交 node_modules、dist、.venv、__pycache__、.env、db、xml。
7. 必须执行 cached 文件清单阻断检查。
8. 必须执行敏感信息扫描。
9. commit message 固定为 `docs: record frontend platform gate blocker`。
10. 本任务不得配置 remote，不得 push，不得进入 TASK-005/TASK-006。

### 验收

1. docs-only commit 形成。
2. C9 任务单与 C9 证据已入库。
3. 第 75 份审计记录和审计官日志已入库。
4. 生产计划模块设计、ADR、架构师日志、当前 sprint 已入库。
5. 未提交前后端业务代码和未审计大目录。
6. 敏感信息扫描通过。
7. 未配置 remote，未 push，未进入 TASK-005/TASK-006。

## ADR-073：TASK-004 前端 CI 平台闭环完成标准

- 决策时间：2026-04-13 21:49 CST
- 决策类型：平台门禁 / 最终闭环标准
- 关联模块：TASK-004C11 GitHub 平台最终闭环
- 审计来源：审计意见书第 76 份，TASK-004C10 通过；剩余风险为 GitHub 平台闭环仍未完成
- 结论：TASK-004 前端 CI 平台闭环必须同时满足 docs 准备提交、GitHub remote/push、hosted runner 成功、main required check 配置、平台证据 docs commit 五项条件；缺任一项不得放行 TASK-005/TASK-006

### 背景

TASK-004C10 已将 C9 证据和第 75 份审计记录补进 docs-only commit，当前本地基线为 `b32585c docs: record frontend platform gate blocker`。但平台动作仍未完成：没有 GitHub remote，没有 push main，没有 hosted runner 实跑，没有 required check 配置。

### 决策

1. 配置 remote 前，必须先提交 C11 任务单、第 76 份审计记录、最新架构设计和 ADR。
2. GitHub remote 必须由管理员提供，禁止在 remote URL 中包含 token/password/secret/cookie。
3. 必须 push main 到 GitHub。
4. 禁止强推覆盖远端历史；远端已有历史时必须停下提交合并方案。
5. 必须在 GitHub hosted runner 上实跑 `Frontend Verify Hard Gate / lingyi-pc-verify`。
6. Hosted runner 必须确认 Node `v22.22.1`、npm `10.9.4`、`npm ci`、`npm run verify`、`npm audit --audit-level=high` 均通过。
7. main 分支必须配置 required check：`Frontend Verify Hard Gate / lingyi-pc-verify`，或记录 GitHub 实际显示名称并由架构师确认。
8. 必须创建 TASK-004C11 平台闭环证据，记录 Run URL、Commit SHA、Branch、required check 实际名称。
9. 平台证据必须通过 docs-only commit 推送到 GitHub。
10. 本任务不得修改前后端业务逻辑，不得进入 TASK-005/TASK-006。

### 验收

1. C11 文档准备 commit 已形成。
2. `origin` 已配置且 remote 输出无敏感信息。
3. `main` 已 push 到 GitHub。
4. Hosted runner `Frontend Verify Hard Gate / lingyi-pc-verify` 成功。
5. main required check 已配置。
6. TASK-004C11 平台证据已创建、提交并 push。
7. 证据文档无 token/password/secret/cookie/私钥。
8. 未修改前后端业务代码，未进入 TASK-005/TASK-006。

## ADR-074：C11 平台证据 SHA 口径必须区分准备提交、证据提交和待推送 HEAD

- 决策时间：2026-04-13 21:59 CST
- 决策类型：平台证据 / SHA 口径治理
- 关联模块：TASK-004C12 C11 证据 SHA 口径修正
- 审计来源：审计意见书第 77 份，TASK-004C11 有条件通过；最高风险为 C11 证据 SHA 口径不清，需明确 `fc0dc2c`、`62e70bd` 与“待推送 HEAD”的关系
- 结论：C11 平台证据必须明确区分 C11 文档准备提交、C11 平台证据提交、C12 修正提交和最终远端 `main` HEAD；缺少该口径不得进入 GitHub 平台实跑和 TASK-005/TASK-006

### 背景

TASK-004C11 尝试推进 GitHub 平台最终闭环，但当前本地仍没有 remote，无法完成 push、hosted runner 和 required check。审计意见书第 77 份指出，C11 证据中对 `fc0dc2c`、`62e70bd` 和“待推送 HEAD”的关系描述不够清楚，管理员后续可能把准备提交、证据提交或待推送 HEAD 混用，导致 hosted runner 证据无法与真实远端 commit 对齐。

### 决策

1. `b32585c` 定义为 TASK-004C10 前基线提交，用于记录 C9 blocker docs。
2. `fc0dc2c` 定义为 TASK-004C11 文档准备提交，包含 C11 任务单、第 76 份审计后续架构记录等。
3. `62e70bd` 定义为 TASK-004C11 平台证据 docs-only 提交，是第 77 份审计时的本地待推送 HEAD。
4. TASK-004C12 若形成新的 docs-only 修正提交，则 C12 新提交成为新的本地待推送 HEAD；`62e70bd` 只能作为其父提交记录。
5. GitHub Hosted Runner 的最终证据必须以实际 push 到 GitHub 的 `main` HEAD 为准，Run URL 里的 Commit SHA 必须等于远端 `main` HEAD。
6. TASK-004C12 只能修正文档证据和架构记录，禁止配置 remote、禁止 push、禁止修改前后端业务代码。
7. TASK-004C12 docs-only commit message 固定为 `docs: clarify frontend ci platform sha lineage`。
8. TASK-005/TASK-006 在 GitHub platform closure 和复审通过前继续阻塞。

### 验收

1. `TASK-004C11_GitHub平台最终闭环证据.md` 新增 SHA 口径修正章节。
2. `TASK-004C12_C11证据SHA口径修正证据.md` 记录提交链、当前 HEAD、是否配置 remote 和最终远端 HEAD 判定规则。
3. `git log --oneline -6` 与证据文档中的 SHA 关系一致。
4. staged 文件清单仅包含白名单文档。
5. 敏感信息扫描通过。
6. 未配置 remote、未 push、未进入 TASK-005/TASK-006。

## ADR-075：TASK-004 GitHub 平台闭环必须由管理员按非强推流程完成

- 决策时间：2026-04-13 22:10 CST
- 决策类型：平台门禁 / 仓库治理 / 管理员执行标准
- 关联模块：TASK-004C13 GitHub 平台闭环管理员执行
- 审计来源：审计意见书第 78 份，TASK-004C12 通过；剩余阻塞为 GitHub 平台闭环未完成
- 结论：TASK-004 前端平台门禁的最终闭环必须由管理员完成 GitHub URL、origin、push、Hosted Runner、required check 和证据回填；远端历史存在分叉时禁止强推，必须回到架构师确认合并方案

### 背景

TASK-004C12 已修正 C11 平台证据 SHA 口径，当前本地待推送 HEAD 为 `64fdfe4 docs: clarify frontend ci platform sha lineage`。但当前仓库仍未配置 GitHub remote，无法证明 GitHub Hosted Runner 真实执行，也无法配置 main required check。平台闭环已经不是普通工程实现问题，而是需要仓库管理员权限的发布门禁问题。

### 决策

1. GitHub 仓库 URL 必须由管理员提供，remote URL 不得包含 token/password/secret/cookie/Authorization。
2. 配置 remote 前，必须先形成 docs-only 准备提交，纳入第 78 份审计结果、TASK-004C13 任务单、架构记录和 sprint 状态。
3. push 前必须检查远端 `main` 是否存在。
4. 若远端 `main` 不存在，允许首次 `git push -u origin main`。
5. 若远端 `main` 已存在且为本地 `main` 的祖先，允许普通 push。
6. 若本地 `main` 不包含远端 `main`，必须停止，禁止 `--force` 和 `--force-with-lease`，提交合并方案给架构师。
7. Hosted Runner 必须实跑 `Frontend Verify Hard Gate / lingyi-pc-verify`。
8. Hosted Runner 必须确认 Node `v22.22.1`、npm `10.9.4`、`npm ci`、`npm run verify`、`npm audit --audit-level=high` 均通过。
9. main required check 必须配置为 `Frontend Verify Hard Gate / lingyi-pc-verify`；若 GitHub UI 显示名称不同，必须记录实际名称并由架构师确认。
10. 必须创建 `TASK-004C13_GitHub平台闭环证据.md`，记录 remote、push、Run URL、Commit SHA、required check 和敏感信息扫描结果。
11. 平台证据必须通过 docs-only commit 推送到 GitHub。
12. 审计官复审通过前，TASK-005/TASK-006 继续阻塞。

### 验收

1. `origin` 已配置，且 remote URL 无敏感凭据。
2. `main` 已成功 push 到 GitHub，且未使用 force push。
3. Hosted Runner `Frontend Verify Hard Gate / lingyi-pc-verify` 通过。
4. Hosted Runner Commit SHA 等于远端 `main` HEAD。
5. Node/npm 版本和三条前端命令均在 hosted runner 日志中可验证。
6. main required check 已配置并记录实际名称。
7. `TASK-004C13_GitHub平台闭环证据.md` 无 `<pending>`，无敏感凭据。
8. 平台证据 docs-only commit 已 push。
9. 未修改前端、后端、workflow 和 TASK-005/TASK-006。

## ADR-076：TASK-005 在平台闭环前只允许开发前基线盘点

- 决策时间：2026-04-13 23:14 CST
- 决策类型：任务边界 / 实现门禁
- 关联模块：TASK-005A 款式利润报表开发前基线盘点
- 审计来源：TASK-004C13 仍等待无凭据 GitHub 仓库 URL，GitHub 平台闭环未完成，TASK-005/TASK-006 工程实现继续阻塞
- 结论：允许启动 TASK-005A 只读基线盘点；禁止启动 TASK-005B 及任何利润报表工程实现，直到 TASK-004C13 平台闭环和审计复审通过

### 背景

TASK-004C13 当前停在 GitHub URL 等待点，`origin` 未配置，`main` 未 push，Hosted Runner 未实跑，required check 未配置。按前序 ADR，TASK-005/TASK-006 工程实现不得放行。但款式利润报表依赖 BOM、外发、工票、生产计划和 ERPNext 多个事实源，若等平台闭环后再盘点，会延后关键口径识别。因此允许先做只读开发前基线盘点，不产生业务代码和数据库变更。

### 决策

1. TASK-005A 定义为只读基线盘点任务。
2. TASK-005A 允许读取文档、读取前后端代码、输出基线报告。
3. TASK-005A 允许新建 `TASK-005_款式利润报表_开发前基线盘点.md`。
4. TASK-005A 禁止新增后端模型、schema、router、service。
5. TASK-005A 禁止新增 Alembic 迁移。
6. TASK-005A 禁止新增前端 api、view、router、store。
7. TASK-005A 禁止注册 `/api/reports/style-profit/` 路由。
8. TASK-005A 禁止调用 ERPNext 写接口。
9. TASK-005A 必须明确收入、材料、工票、外发、扣款、费用分摊和利润率口径的待决策项。
10. TASK-005A 必须明确 TASK-004C13 平台闭环未完成前，TASK-005B 工程实现不得启动。
11. TASK-006 继续阻塞。

### 验收

1. `TASK-005_款式利润报表_开发前基线盘点.md` 已创建。
2. 报告覆盖现有实现、上游依赖、利润口径、数据模型草案、API 草案、权限审计缺口、前端缺口、风险清单和 TASK-005B 拆分建议。
3. `git diff --name-only -- 06_前端 07_后端 .github 02_源码` 无业务代码变更。
4. 未新增迁移文件。
5. 未注册新路由。
6. 未进入 TASK-006。

## ADR-077：TASK-005A 盘点证据必须 docs-only 入库且不放行工程实现

- 决策时间：2026-04-13 23:29 CST
- 决策类型：文档证据链 / 实现门禁
- 关联模块：TASK-005A1 利润盘点文档证据链补提交
- 审计来源：审计意见书第 80 份，TASK-005A 有条件通过；阻塞点为 TASK-005A 关键文档未进入 git 提交链，且不得误解为 TASK-005B 可开工
- 结论：TASK-005A 必须通过 docs-only 白名单提交进入 git 证据链；该提交只代表只读盘点证据入库，不代表 TASK-005B/TASK-006 放行

### 背景

TASK-005A 已完成只读开发前基线盘点，审计未发现前端/后端业务代码越界，也未进入 TASK-005B 实现。但审计发现关键文档尚未进入 git 提交链。若后续直接进入平台闭环或工程实现，远端仓库可能缺少 TASK-005A 盘点证据，且团队可能误把“盘点通过”理解为“可以开始利润报表实现”。因此需要单独做 TASK-005A1 文档证据链补提交。

### 决策

1. TASK-005A1 只允许做 docs-only 白名单提交。
2. 允许提交 TASK-005A 任务单、TASK-005A 基线盘点报告、款式利润报表模块设计、技术决策记录、当前 sprint、架构师日志、工程师日志、第 80 份审计记录和 TASK-005A1 任务单。
3. 禁止提交 `06_前端/**`、`07_后端/**`、`.github/workflows/**`、`02_源码/**`。
4. 禁止新增迁移、后端路由、模型、schema、service、前端 api、view、router、store。
5. 必须执行 staged 白名单检查和业务代码路径检查。
6. 必须执行敏感信息扫描。
7. commit message 固定为 `docs: record style profit baseline evidence`。
8. 该 commit 不得被解释为 TASK-005B 放行条件。
9. TASK-004C13 平台闭环和审计复审通过前，TASK-005B/TASK-006 继续阻塞。

### 验收

1. TASK-005A 基线盘点报告已存在。
2. TASK-005A 相关文档已进入 docs-only commit。
3. staged 文件不包含前端、后端、workflow、02_源码、测试交付大目录。
4. 敏感信息扫描无真实凭据。
5. 输出 `TASK_005A_EVIDENCE_HEAD`。
6. 未进入 TASK-005B/TASK-006。

## ADR-078：无 GitHub 项目按本地仓库门禁替代平台闭环

- 决策时间：2026-04-13 23:58 CST
- 决策类型：交付门禁 / 本地仓库治理
- 关联模块：TASK-004C14 本地仓库门禁替代 GitHub 平台闭环
- 触发原因：项目当前没有 GitHub 仓库，继续等待无凭据 GitHub URL 会造成流程死循环
- 结论：当前阶段废止“等待 GitHub URL”作为硬阻塞，改用本地仓库、本地验证命令、本地提交链和审计记录作为交付门禁；GitHub/Hosted Runner/Required Check 作为未来接入远端仓库时的增强项

### 背景

TASK-004C13 原本要求配置 GitHub `origin`、push `main`、运行 Hosted Runner、配置 required check。用户确认项目一直是本地模式，没有 GitHub 仓库。因此继续要求 GitHub URL 会造成管理流程空转，且阻塞后续合理的本地开发节奏。为了保持审计可追溯，同时避免死循环，需要将当前门禁切换为本地可执行、可复验的门禁。

### 决策

1. 当前项目交付模式定义为本地仓库交付。
2. `origin` 为空是当前事实，不再作为阻塞项。
3. GitHub URL、Hosted Runner、Required Check 不再作为当前阶段硬门禁。
4. 若未来引入 GitHub 或其他远端平台，必须另开任务恢复远端平台门禁，不得直接套用旧 C13 证据。
5. 当前门禁改为本地 `main` 分支、本地 HEAD、本地验证命令、本地 docs-only 提交和审计记录。
6. 必须创建 `TASK-004C14_本地仓库门禁闭环证据.md`，记录本地 root、branch、HEAD、remote 为空、本地验证结果和业务代码未变更结论。
7. 本地验证必须覆盖后端 pytest、unittest、py_compile 和前端 npm ci、npm run verify、npm audit；如缺依赖或无法运行，必须记录真实原因，不得伪造通过。
8. TASK-004C14 只允许 docs-only 提交，commit message 固定为 `docs: replace github gate with local delivery gate`。
9. TASK-005B 是否放行，必须等待 TASK-004C14 审计复审结论。
10. TASK-006 仍需单独架构放行，不因本地门禁替代自动启动。

### 验收

1. TASK-004C14 任务单已输出。
2. 生产计划模块设计已更新本地门禁替代口径。
3. 技术决策记录已追加本 ADR。
4. 当前 sprint 明确不再等待 GitHub URL，但仍需 TASK-004C14 本地门禁复审。
5. 本地门禁证据已创建。
6. docs-only 提交形成，未包含前端、后端、workflow、02_源码变更。
7. 未进入 TASK-005B/TASK-006。

## ADR-079：款式利润报表 V1 利润口径冻结

- 决策时间：2026-04-14 00:11 CST
- 决策类型：业务口径 / 报表计算 / 实现前冻结
- 关联模块：TASK-005B 款式利润口径设计冻结
- 审计来源：审计意见书第 83 份，TASK-004C14 通过；TASK-005B 只能进入利润口径设计冻结，不得进入模型、迁移、API 或前端实现
- 结论：冻结款式利润报表 V1 的收入、标准成本、实际成本、外发扣款、费用分摊、快照、幂等、权限和审计口径，作为 TASK-005C 后续数据模型与迁移的唯一依据

### 背景

TASK-005A 基线盘点确认，利润报表当前无现成后端模型、路由、服务、迁移和前端页面。上游 BOM、外发、工票和生产计划已有可复用事实源，但利润口径存在 P1 分歧：收入采用 Sales Order 还是 Sales Invoice，实际材料成本采用 Purchase Receipt 还是 Stock Ledger Entry，外发成本采用验货事实还是结算锁定事实。若不先冻结口径直接实现，会导致报表结果与财务、库存和对账事实不一致。

### 决策

1. 收入冻结为双口径：`estimated_revenue_amount` 来自已提交 Sales Order，`actual_revenue_amount` 来自已提交 Sales Invoice。
2. V1 默认展示实际收入；无已提交 Sales Invoice 时展示预计收入，并标记 `revenue_status=estimated`。
3. 标准材料成本 = BOM 展开需求量 × 标准单位成本。
4. 标准单位成本来源优先级为 ERPNext Item Price 有效采购价 -> ERPNext Item valuation_rate -> unresolved。
5. 标准工序成本 = BOM 工序单价 × 计划数量。
6. 实际材料成本冻结为 ERPNext Stock Ledger Entry 口径：`sum(abs(stock_value_difference))`；Purchase Receipt 仅作为参考和排查来源。
7. 实际工票成本 = 工票净数量 × 工价快照；工票净数量 = 登记数量 - 撤销数量。
8. 实际外发成本采用结算优先、验货兜底；已结算/锁定用结算净额，未结算但已验货用验货净额并标记 provisional。
9. 扣款金额只作为明细展示；已使用 `net_amount` 时不得重复扣减。
10. V1 默认不纳入制造费用/管理费用分摊，`allocated_overhead_amount = 0`，并显示 `allocation_status=not_enabled`。
11. 利润公式：`profit_amount = revenue_amount - actual_total_cost`。
12. 利润率公式：`profit_rate = profit_amount / revenue_amount`；收入为 0 时返回 null。
13. 利润快照不可变；重算必须生成新快照，不覆盖旧快照。
14. 生成快照必须要求 `idempotency_key`，唯一键为 `company + idempotency_key`。
15. `request_hash` 不得包含 created_at、operator、request_id 等易变字段。
16. 权限动作冻结为 `style_profit:read`、`style_profit:snapshot_create`、`style_profit:export`。
17. 资源权限维度冻结为 `company + item_code + sales_order`。
18. 权限来源沿用 ERPNext Role / User Permission 聚合，权限源不可用必须 fail closed。
19. 401、403、503 必须写安全审计；快照生成、幂等 replay、幂等冲突、unresolved 快照必须写操作审计。
20. TASK-005B 只允许输出口径冻结说明和 ADR，不得创建模型、迁移、API 或前端页面。

### 验收

1. `TASK-005B_款式利润口径冻结说明.md` 已创建。
2. 收入、标准材料、标准工序、实际材料、工票、外发、扣款、费用分摊和利润公式均已冻结。
3. 快照不可变、幂等、权限、审计和 unresolved 状态已冻结。
4. TASK-005C 输入清单已明确。
5. 未新增后端模型、schema、router、service、迁移。
6. 未新增前端 api、view、router、store。
7. 未进入 TASK-006。

## ADR-080：TASK-005C 仅允许模型、迁移与来源映射设计

- 决策时间：2026-04-14 00:30 CST
- 决策类型：开发边界 / 数据模型 / 来源映射
- 关联模块：TASK-005C 利润模型迁移与来源映射设计
- 审计来源：审计意见书第 85 份，TASK-005B1 通过；审计建议 TASK-005C 先限定为“模型 / 迁移 / 来源映射设计”
- 结论：TASK-005C 只允许创建利润报表数据模型、迁移、schema 和来源映射骨架，不得注册 API、不得实现完整利润计算服务、不得修改前端、不得进入 TASK-006

### 背景

TASK-005B/B1 已冻结款式利润 V1 口径，旧 `Purchase Receipt` 直接计实际材料成本、旧利润公式和固定示例已清理。下一步需要把口径落到可审计的数据结构和来源映射上，但如果直接实现完整 API 或前端，仍可能绕过 Stock Ledger Entry 纳入/排除规则、Sales Invoice / Sales Order 行级映射和快照幂等边界，导致利润结果不可复验。因此 TASK-005C 必须先把模型、迁移和来源映射设计做稳。

### 决策

1. TASK-005C 允许新建 `app/models/style_profit.py`、`app/schemas/style_profit.py`、`app/services/style_profit_source_service.py`。
2. TASK-005C 允许新建 Alembic 迁移，创建 `ly_style_profit_snapshot`、`ly_style_profit_detail`、`ly_style_profit_source_map`、`ly_cost_allocation_rule`。
3. `ly_style_profit_snapshot` 必须包含 `snapshot_no`、`company`、`item_code`、`sales_order`、`formula_version`、`revenue_status`、收入金额、标准成本、实际成本、利润金额、利润率、unresolved 数量、`idempotency_key`、`request_hash`。
4. `ly_style_profit_snapshot` 必须有 `snapshot_no` 唯一约束，以及 `company + idempotency_key` 唯一约束。
5. `ly_style_profit_detail` 必须按 `detail_type` 区分 `revenue/material/workshop/subcontract/overhead/adjustment`。
6. `ly_style_profit_source_map` 必须记录 `source_system`、`source_doctype`、`source_name`、`source_line_no`、`source_status`、`item_code`、`qty`、`amount`，用于审计追溯。
7. `ly_cost_allocation_rule` V1 仅建表保留，默认 `status=disabled`，启用费用分摊前必须另行 ADR。
8. 收入来源映射必须同时支持 ERPNext `Sales Order` 预计收入和 `Sales Invoice` 实际收入。
9. 已提交 `Sales Invoice` 优先于 `Sales Order`；无已提交发票时才允许使用已提交 `Sales Order` 作为预计收入。
10. `Sales Invoice` 与 `Sales Order` 必须做行级映射，保存 `doctype/name/line_no/item_code/qty/rate/amount`，不得重复计入收入。
11. 实际材料成本来源必须为 ERPNext `Stock Ledger Entry`，公式为 `sum(abs(stock_value_difference))`。
12. `Stock Ledger Entry` 纳入规则必须至少校验 `company`、目标物料或目标工单归属、提交状态、目标仓库/工单链路；无法归属时标记 unresolved，不得静默纳入。
13. `Stock Ledger Entry` 排除规则必须覆盖取消、冲销、非目标公司、非目标仓库、无法关联目标 Sales Order/Work Order/Production Plan 的记录。
14. `Purchase Receipt` 只允许作为参考来源记录，不得直接写入 `actual_material_cost`。
15. TASK-005C 只能实现来源定位和映射骨架，不得计算最终 `profit_amount`。
16. TASK-005C 不得修改 `app/main.py`，不得新建或注册 `/api/reports/style-profit/` router。
17. TASK-005C 不得新增 Vue `api/view/router/store`。
18. TASK-005C 不得进入 TASK-006 加工厂对账单。

### 验收

1. 迁移可从空库创建 4 张利润相关表，并包含唯一约束、外键和查询索引。
2. 模型字段覆盖 ADR-079 的快照、明细、来源、幂等和 unresolved 状态。
3. 来源映射测试覆盖 Sales Invoice 优先、Sales Order 兜底、多发票汇总、draft/cancelled 排除。
4. 来源映射测试覆盖 Stock Ledger Entry 纳入、排除和 `abs(stock_value_difference)` 汇总。
5. 来源映射测试确认 Purchase Receipt 仅作为参考，不直接进入实际材料成本。
6. 静态扫描确认未注册利润 API、未修改前端、未进入 TASK-006。
7. TASK-005C 通过审计后，才允许下发 TASK-005D 利润快照计算服务任务单。

## ADR-081：TASK-005C1 利润来源映射整改必须先于 TASK-005D

- 决策时间：2026-04-14 01:28 CST
- 决策类型：审计整改 / 数据追溯 / fail closed / 材料成本口径
- 关联模块：TASK-005C1 利润来源映射审计字段与状态 Fail Closed 整改
- 审计来源：TASK-005C 审计结论有条件通过，高危 2 / 中危 3
- 结论：TASK-005C1 必须先修复 source_map 审计字段、来源状态 fail closed、snapshot 复核字段、费用分摊默认状态和 SLE 款式/材料编码语义；审计通过前不得进入 TASK-005D/TASK-006

### 背景

TASK-005C 已完成模型、迁移和来源映射骨架，但审计发现两个高危问题：第一，`ly_style_profit_source_map` 缺少 `source_system/source_doctype/source_status/qty/amount` 等审计追溯字段，也缺少明确的 snapshot/detail 关联策略；第二，`_is_submitted()` 在缺少 `docstatus/status` 时默认放行，会把未知状态来源当作已提交事实纳入利润口径。审计同时指出 snapshot 复核字段、费用分摊默认状态和 SLE 款式/材料编码语义仍需修正。因此 TASK-005D 不能直接启动。

### 决策

1. `ly_style_profit_source_map` 必须关联 `snapshot_id`，并可选关联 `detail_id`。
2. `snapshot_id` 必须为非空 FK；`detail_id` 可空，用于 excluded/unresolved 来源。
3. source_map 必须补齐 `source_system`、`source_doctype`、`source_status`、`qty`、`unit_rate`、`amount`、`currency`、`warehouse`、`posting_date`、`raw_ref`。
4. source_map 必须拆分 `style_item_code` 和 `source_item_code`，禁止继续用一个 `item_code` 同时表达款式和来源物料。
5. source_map 唯一约束必须为 `snapshot_id + source_system + source_doctype + source_name + source_line_no`。
6. 旧的全局 `company + source_type + source_name + source_line_id` 唯一约束必须删除或废弃。
7. `_is_submitted()` 必须 fail closed：缺少 `docstatus/status` 时返回 false。
8. `docstatus == 1` 才可判定为已提交；`docstatus` 非 1 必须返回 false。
9. `docstatus` 缺失时，仅允许显式 status 白名单通过；unknown、空字符串、draft、cancelled、canceled、closed、void、return 必须返回 false。
10. `is_cancelled=true` 必须返回 false。
11. snapshot 必须补齐 `from_date`、`to_date`、`revenue_mode`、`unresolved_count`。
12. `revenue_mode` 默认 `actual_first`，限定 `actual_first/actual_only/estimated_only`。
13. `request_hash` 必须包含 `from_date/to_date/revenue_mode`，继续排除 `created_at/operator/request_id`。
14. `ly_cost_allocation_rule.status` 默认值必须为 `disabled`。
15. SLE 材料成本映射中，利润报表款式编码必须称为 `style_item_code`。
16. SLE 行 `item_code` 必须视为来源物料编码，映射到 `source_item_code` 或 `material_item_code`。
17. 不得因 `SLE.item_code != style_item_code` 直接排除材料成本。
18. SLE 行必须通过 `work_order/production_plan_id/sales_order` 桥接，或通过 BOM 物料集合验证后才可纳入。
19. 既无桥接也无 BOM 物料范围验证时，必须 unresolved，不得静默纳入。
20. `Purchase Receipt` 仍只允许作为参考来源，不得进入 `actual_material_cost`。
21. TASK-005C1 不得注册 API，不得实现完整利润计算服务，不得修改前端，不得进入 TASK-006。

### 验收

1. source_map 字段、FK、唯一约束和索引符合本 ADR。
2. `_is_submitted({})`、`_is_submitted({'status': ''})`、`_is_submitted({'status': 'Unknown'})` 均返回 false。
3. `_is_submitted({'docstatus': 1})` 返回 true，`docstatus != 1` 或 `is_cancelled=true` 返回 false。
4. snapshot 字段包含 `from_date/to_date/revenue_mode/unresolved_count`。
5. cost allocation rule 默认 `disabled`。
6. SLE 材料编码不等于款式编码但工单归属匹配时，可纳入材料成本。
7. 无桥接、无 BOM 物料范围验证的 SLE 不得纳入。
8. Purchase Receipt 不直接计入 `actual_material_cost`。
9. 静态扫描确认未注册利润 API、未修改前端、未进入 TASK-005D/TASK-006。
10. TASK-005C1 审计通过后，才允许下发 TASK-005D。

## ADR-082：利润来源默认不纳入与 status-only 白名单收口

- 决策时间：2026-04-14 01:59 CST
- 决策类型：审计整改 / fail closed / 字段契约 / 状态白名单
- 关联模块：TASK-005C2 利润来源默认不纳入与字段契约收口整改
- 审计来源：TASK-005C1 审计结论有条件通过，高危 1 / 中危 3
- 结论：`include_in_profit` 默认必须为 false；source_map 字段 nullable、长度、枚举、索引必须统一；status-only 仅允许 doctype 级白名单，不允许 Stock Ledger Entry 通过 status-only 放行

### 背景

TASK-005C1 已完成主要整改，但审计发现 `ly_style_profit_source_map.include_in_profit` 默认仍是 true。该字段是利润来源是否计入成本或收入的 fail-closed 开关，如果默认为 true，后续任何漏填、未解析或未知状态来源都有可能被误纳入利润。同时 source_map 的字段契约、复合索引和 status-only 白名单还需要架构明确，防止后续实现漂移。

### 决策

1. `ly_style_profit_source_map.include_in_profit` 模型、迁移、DTO 默认值必须为 false。
2. 新构造来源行初始 `include_in_profit` 必须为 false。
3. 只有来源通过提交状态校验、资源归属校验、材料范围校验和排除规则后，才允许显式设置 `include_in_profit=true`。
4. `mapping_status` 默认必须为 `unresolved` 或等价未解析状态，不得默认为 `mapped`。
5. `mapping_status != 'mapped'` 时，`include_in_profit` 必须为 false。
6. source_map 必须包含并约束 `snapshot_id/detail_id/company/sales_order/style_item_code/source_item_code/source_system/source_doctype/source_name/source_line_no/source_status/qty/unit_rate/amount/currency/warehouse/work_order/job_card/posting_date/include_in_profit/mapping_status/unresolved_reason/raw_ref/created_at`。
7. `source_system` 限定为 `erpnext/fastapi/manual`。
8. `source_status` 非空，缺失时写 `unknown`，且 `unknown` 不允许纳入利润。
9. source_map 唯一约束为 `snapshot_id + source_system + source_doctype + source_name + source_line_no`。
10. source_map 必须补索引：`snapshot_id + mapping_status`、`company + sales_order + style_item_code`、`source_system + source_doctype + source_name`、`detail_id`。
11. `_is_submitted(row, source_doctype=None)` 必须优先判断 `docstatus`。
12. `docstatus` 存在时，只有 `int(docstatus) == 1` 返回 true；非 1 直接 false。
13. `is_cancelled=true` 直接 false。
14. `docstatus` 缺失时，必须识别 `source_doctype`；未知 doctype 一律 false。
15. `Stock Ledger Entry` 不允许 status-only 放行，必须有 `docstatus=1`。
16. `Purchase Receipt` 不允许通过 status-only 进入利润成本。
17. `Sales Invoice` status-only 白名单只允许 `paid/unpaid/overdue/partly paid`。
18. `Sales Order` status-only 白名单只允许 `to deliver and bill/to bill/to deliver/completed`。
19. `closed/draft/cancelled/canceled/void/return/unknown/空字符串` 一律 false。
20. TASK-005C2 不得注册 API，不得实现完整利润计算服务，不得修改前端，不得进入 TASK-005D/TASK-006。

### 验收

1. 数据库默认值和 DTO 默认值均证明 `include_in_profit=false`。
2. `mapping_status` 默认不为 `mapped`。
3. source_map 字段 nullable、长度、枚举、非空约束符合本 ADR。
4. source_map 复合索引和唯一约束符合本 ADR。
5. status-only 测试覆盖 Sales Invoice、Sales Order、Stock Ledger Entry、Purchase Receipt 和未知 doctype。
6. `docstatus=0,status=Paid` 必须 false。
7. `is_cancelled=true,docstatus=1` 必须 false。
8. 静态扫描确认未注册利润 API、未修改前端、未进入 TASK-005D/TASK-006。
9. TASK-005C2 审计通过后，才允许下发 TASK-005D。

## ADR-083：利润快照必须补齐公司款式期间查询索引

- 决策时间：2026-04-14 06:58 CST
- 决策类型：审计整改 / 数据库索引 / 查询契约
- 关联模块：TASK-005C3 利润快照期间索引补齐
- 审计来源：TASK-005C2 审计结论有条件通过，中危 1
- 结论：`ly_style_profit_snapshot` 必须补齐 `idx_ly_style_profit_snapshot_company_item_period(company, item_code, from_date, to_date)`；审计通过前不得进入 TASK-005D/TASK-006

### 背景

TASK-005C2 已完成利润来源默认不纳入、字段契约、复合索引和 status-only 白名单整改，回归测试通过。但审计发现模块设计在 TASK-005C1 已要求 `company + item_code + from_date + to_date` 期间查询索引，当前模型和迁移仍只有 `company + item_code + sales_order` 等索引。后续利润快照查询会按公司、款式和期间复核历史结果，该索引缺失会造成契约不完整和潜在性能问题。

### 决策

1. `ly_schema.ly_style_profit_snapshot` 必须新增索引 `idx_ly_style_profit_snapshot_company_item_period`。
2. 索引字段顺序必须固定为 `company, item_code, from_date, to_date`。
3. 不得用 `sales_order` 替代 `from_date/to_date`。
4. 不得删除现有 `company + item_code + sales_order` 索引。
5. 如果 TASK-005C 建表迁移尚未正式应用，可在原迁移中补索引。
6. 如果迁移已应用或审计要求保持历史，必须新增增量迁移创建该索引。
7. 增量迁移 downgrade 必须能删除该索引。
8. TASK-005C3 只允许补索引和测试，不得注册 API，不得实现利润计算服务，不得修改前端，不得进入 TASK-006。

### 验收

1. SQLAlchemy 模型存在 `idx_ly_style_profit_snapshot_company_item_period`。
2. 索引字段顺序为 `company, item_code, from_date, to_date`。
3. Alembic 迁移创建同名索引，schema 为 `ly_schema`。
4. 测试覆盖索引存在性和字段顺序。
5. 静态扫描确认未注册利润 API、未修改前端、未进入 TASK-005D/TASK-006。
6. TASK-005C3 审计通过后，才允许下发 TASK-005D。

## ADR-084：TASK-005C~C3 必须纳入本地仓库基线后才允许 TASK-005D

- 决策时间：2026-04-14 07:21 CST
- 决策类型：交付基线 / 本地仓库治理 / 放行门禁
- 关联模块：TASK-005C4 利润模型本地仓库基线提交
- 审计来源：TASK-005C3 审计结论通过，审计建议新增利润模型、服务、迁移和测试文件纳入本地仓库基线/提交
- 结论：TASK-005D 启动前，必须先将 TASK-005C~C3 的利润模型、schema、来源映射服务、迁移、测试和配套文档按白名单纳入本地 git commit；未形成可复验提交前不得进入 TASK-005D/TASK-006

### 背景

TASK-005C3 已通过审计，表示利润模型与来源映射阶段的阻断问题已经关闭。但当前本地工作区仍存在 TASK-005C~C3 的新增利润模型、schema、来源映射服务、迁移和测试文件未纳入本地仓库基线。如果直接进入 TASK-005D，后续利润快照计算服务会依赖未提交文件，造成审计、回滚和交付基线不完整。

### 决策

1. TASK-005C4 只做本地仓库白名单提交和证据记录。
2. 允许纳入提交的后端文件仅限 `style_profit.py` 模型、schema、source service、TASK-005C 迁移、style profit 测试和必要 `models/__init__.py`。
3. 允许纳入提交的文档仅限 TASK-005C~C4 任务单、款式利润报表模块设计、技术决策记录、当前 sprint、架构师日志、审计记录、必要工程师日志和 C4 证据文件。
4. 禁止 `git add .`、`git add -A`、`git add --all`。
5. 必须使用精确白名单路径执行 `git add -- <path>`。
6. staged 文件不得包含 `06_前端`、`.github`、`02_源码`、`app/main.py`、`app/routers/**`、`style_profit_service.py`、TASK-005D 或 TASK-006 文件。
7. 提交前必须通过 style_profit 定向 pytest、全量 pytest、unittest 和 py_compile。
8. 提交前必须通过禁改边界扫描。
9. 必须创建 `TASK-005C4_利润模型本地仓库基线提交证据.md`，记录验证结果、staged 文件、提交前 HEAD、提交后 HEAD。
10. commit message 固定为 `feat: add style profit model source mapping baseline`。
11. 本任务不允许 push，不允许配置 remote。
12. TASK-005C4 审计通过后，才允许下发 TASK-005D 利润快照计算服务。

### 验收

1. 本地 commit 已形成。
2. commit 内容仅限白名单。
3. TASK-005C~C3 新增利润模型、schema、来源映射服务、迁移和测试文件均已纳入 commit。
4. C4 证据文件记录验证结果、禁改扫描和提交前后 HEAD。
5. 未 push，未配置 remote。
6. 未修改前端、`.github`、`02_源码`、`app/main.py`、`app/routers/**`。
7. 未进入 TASK-005D/TASK-006。

## ADR-085：TASK-005D 只实现利润快照计算服务

- 决策时间：2026-04-14 07:59 CST
- 决策类型：开发边界 / 利润计算 / 幂等快照 / 审计追溯
- 关联模块：TASK-005D 利润快照计算服务
- 审计来源：TASK-005C4 审计意见书第 90 份通过，当前本地基线 HEAD `67a995c2933fac3bb269e3f668252a03b93d1238`
- 结论：TASK-005D 只允许实现后端 `StyleProfitService` 利润快照计算服务和测试；不得注册 API、不得修改前端、不得新增迁移、不得进入 TASK-006

### 背景

TASK-005C~C3 已完成利润模型、schema、来源映射服务、迁移和测试，TASK-005C4 已将这些交付纳入本地仓库基线。下一步需要把 ADR-079 冻结的收入、标准成本、实际成本、费用分摊、利润公式、幂等和 unresolved 规则落到服务层。但在计算服务经过审计前，不应直接暴露 API 或前端入口，否则会扩大未验收计算口径的调用面。

### 决策

1. TASK-005D 允许新建 `app/services/style_profit_service.py`。
2. TASK-005D 允许新增 `tests/test_style_profit_service.py`、`tests/test_style_profit_snapshot_calculation.py`、`tests/test_style_profit_snapshot_idempotency.py`。
3. TASK-005D 允许修改 `app/schemas/style_profit.py`，仅补服务级入参和出参 DTO。
4. TASK-005D 允许修改 `app/services/style_profit_source_service.py`，仅补只读来源聚合 helper，不得放宽状态校验、`include_in_profit=false` 默认值、SLE 纳入/排除规则和 Purchase Receipt 参考规则。
5. TASK-005D 不得修改 `app/main.py`。
6. TASK-005D 不得新增或修改 `app/routers/**`。
7. TASK-005D 不得注册 `/api/reports/style-profit/`。
8. TASK-005D 不得新增 Alembic 迁移。
9. TASK-005D 不得修改 Vue 前端。
10. TASK-005D 不得进入 TASK-006。
11. `StyleProfitService.create_snapshot()` 必须以 `company + idempotency_key` 作为幂等唯一键。
12. `request_hash` 必须包含 `company / item_code / sales_order / from_date / to_date / revenue_mode / include_provisional_subcontract / formula_version`。
13. `request_hash` 必须排除 `created_at / operator / request_id / snapshot_no`。
14. 相同 `company + idempotency_key + request_hash` 必须 replay 首次快照，不得新增 snapshot/detail/source_map。
15. 相同 `company + idempotency_key` 但不同 `request_hash` 必须返回或抛出 `STYLE_PROFIT_IDEMPOTENCY_CONFLICT`。
16. 服务内不得调用 `session.commit()`；只允许 `flush()`，由上层调用方控制事务提交和回滚。
17. 创建 snapshot、detail、source_map 必须在同一数据库事务内完成。
18. 收入模式必须支持 `actual_first / actual_only / estimated_only`。
19. `actual_first` 优先已提交 `Sales Invoice`，无已提交发票时才使用已提交 `Sales Order`。
20. `actual_only` 无已提交发票时收入为 0，并写 unresolved。
21. `estimated_only` 只使用已提交 `Sales Order`。
22. draft、cancelled、unknown 来源不得纳入利润。
23. 实际材料成本只允许来自 ERPNext `Stock Ledger Entry`，公式为 `sum(abs(stock_value_difference))`。
24. `Purchase Receipt` 只允许作为参考来源，不得计入 `actual_material_cost`。
25. 实际工票成本公式为 `actual_workshop_cost = sum((register_qty - reversal_qty) * wage_rate_snapshot)`。
26. 外发成本优先使用结算锁定净额；未结算验货只有在 `include_provisional_subcontract=true` 时才允许 provisional 纳入。
27. V1 不启用费用分摊，`allocated_overhead_amount=0`，`allocation_status=not_enabled`。
28. `actual_total_cost = actual_material_cost + actual_workshop_cost + actual_subcontract_cost + allocated_overhead_amount`。
29. `profit_amount = revenue_amount - actual_total_cost`。
30. `profit_rate = profit_amount / revenue_amount`；收入为 0 时 `profit_rate=null`。
31. 每个纳入利润的收入/成本项必须生成 detail 和 source_map。
32. source_map 初始 `include_in_profit=false`，只有 `mapping_status=mapped` 且最终纳入利润的来源才允许设为 true。
33. `source_status=unknown` 不得纳入利润。
34. `raw_ref` 必须脱敏，不得保存 Authorization、Cookie、token、password、secret 或原始 SQL。
35. `unresolved_count` 必须等于可复核的 unresolved detail/source_map 数量。
36. 任意数据库写入失败必须归类为 `DATABASE_WRITE_FAILED` 或可被上层映射到该错误码的异常，不得吞错返回成功对象。

### 验收

1. `StyleProfitService.create_snapshot()` 可创建 snapshot/detail/source_map，且服务内不 commit。
2. 利润公式、收入模式、实际材料、工票、外发、费用分摊全部符合 ADR-079。
3. 幂等 replay 和幂等冲突有测试覆盖。
4. unresolved 来源会写 detail/source_map，并影响 `snapshot_status/unresolved_count`。
5. source_map 遵守 `include_in_profit` fail closed、`source_status` fail closed 和 `raw_ref` 脱敏规则。
6. 定向 pytest、全量 pytest、unittest、py_compile 通过。
7. 静态扫描确认未注册 API、未修改前端、未新增迁移、未进入 TASK-006。
8. TASK-005D 审计通过后，才允许评估 TASK-005E API 层或报表查询层任务单。

## ADR-086：TASK-005D1 必须修复利润快照幂等输入与 fail closed 缺口

- 决策时间：2026-04-14 10:15 CST
- 决策类型：审计整改 / 幂等一致性 / fail closed / 审计追溯
- 关联模块：TASK-005D1 利润快照计算服务审计整改
- 审计来源：审计意见书第 92 份，TASK-005D 有条件通过但仍有 3 个 P1、3 个 P2 必改项
- 结论：TASK-005D1 必须先修复 request_hash 来源输入、收入 unresolved 明细、异常 SLE 追溯、缺工价 unresolved、收入真实 source_status 和 idempotency_key 长度校验；审计通过前不得进入 TASK-005E/TASK-006

### 背景

TASK-005D 已实现利润快照计算服务主干，但审计复核发现幂等 hash 只包含 header，不包含实际影响利润的来源行，导致同幂等键下来源金额变化仍可能 replay 旧快照。同时，无有效收入、异常 SLE、缺工价等 fail-closed 场景没有稳定写入 unresolved detail/source_map，可能让不完整快照显示为 complete。这些问题会直接影响利润快照不可变性、可追溯性和财务口径可信度。

### 决策

1. `request_hash` 必须包含所有影响利润结果的规范化来源输入。
2. `request_hash` 必须纳入 `sales_invoice_rows`、`sales_order_rows`、`bom_material_rows`、`bom_operation_rows`、`stock_ledger_rows`、`workshop_ticket_rows`、`subcontract_rows`、`allowed_material_item_codes`、`work_order`。
3. `request_hash` 必须排除 `operator`、`request_id`、`created_at`、`updated_at`、`snapshot_no`、`audit_id`、数据库自增 `id` 和敏感字段。
4. 来源行参与 hash 前必须按稳定键排序，保证列表顺序不影响 hash。
5. Decimal、date、datetime、None 必须规范化，避免同语义输入产生不同 hash。
6. 同 `company + idempotency_key`、同 header、不同来源金额必须返回 `STYLE_PROFIT_IDEMPOTENCY_CONFLICT`。
7. 所有 `revenue_status=unresolved` 场景必须写 unresolved detail/source_map。
8. 无 Sales Invoice 且无 Sales Order、`actual_only` 无发票、`estimated_only` 无订单、收入来源全部 draft/cancelled/unknown，均必须使 `snapshot_status=incomplete`。
9. 无真实收入来源行时，允许生成 synthetic source_map，但必须记录 `source_doctype=Revenue Resolution`、`source_name=UNRESOLVED_REVENUE`、`mapping_status=unresolved`、`include_in_profit=false`。
10. SLE 状态未知、缺 `docstatus`、未提交、取消或无法判断提交状态时，必须作为 unresolved 来源持久化，不得静默丢弃。
11. SLE 无法通过 `work_order / production_plan / sales_order / BOM 物料范围` 归属时，必须作为 unresolved 来源持久化，不得纳入 `actual_material_cost`。
12. `Purchase Receipt` 继续只作为 reference/excluded 来源，不计入实际材料成本。
13. 标准工序工价缺失、空字符串、非法 Decimal 时，必须写 unresolved detail/source_map。
14. 工票净数量大于 0 且工价缺失、空字符串、非法 Decimal 时，必须写 unresolved detail/source_map。
15. 缺工价来源不得 `mapping_status=mapped`，不得 `include_in_profit=true`。
16. 合法 0 工价必须是字段显式存在且可解析为 Decimal(0)，并有测试证明。
17. `StyleProfitRevenueSourceDTO` 必须保留真实 `source_status`。
18. Sales Invoice 的 `paid/unpaid/overdue/partly paid` 状态必须写入 source_map。
19. Sales Order 的 `to deliver and bill/to bill/to deliver/completed` 状态必须写入 source_map。
20. `docstatus=1` 但原状态缺失时，才允许写 `submitted`。
21. `idempotency_key` 必须在 DTO 或服务层校验长度不超过 128。
22. 超长 `idempotency_key` 必须返回业务异常，建议错误码为 `STYLE_PROFIT_INVALID_IDEMPOTENCY_KEY`，不得进入数据库写入。
23. TASK-005D1 不得注册 API，不得修改前端，不得新增迁移，不得进入 TASK-005E/TASK-006。

### 验收

1. 同 header 不同来源金额触发 `STYLE_PROFIT_IDEMPOTENCY_CONFLICT`。
2. 同来源行不同列表顺序生成相同 `request_hash`。
3. 无有效收入来源时写 unresolved detail/source_map，`snapshot_status=incomplete`。
4. unknown/缺 docstatus/无法归属 SLE 写 unresolved source_map，不计入 `actual_material_cost`。
5. 缺标准工序工价和缺工票工价均 unresolved，不 mapped，不 `include_in_profit`。
6. 收入 source_map 保留真实 `source_status`。
7. 超长 `idempotency_key` 在服务层拒绝，且不落库。
8. 定向 pytest、全量 pytest、unittest、py_compile 通过。
9. 静态扫描确认未注册 API、未修改前端、未新增迁移、未进入 TASK-005E/TASK-006。
10. TASK-005D1 审计通过后，才允许评估 TASK-005E API 层任务单。

## ADR-087：TASK-005D2 必须修复实际成本归属与事务兜底

- 决策时间：2026-04-14 10:36 CST
- 决策类型：审计整改 / 成本归属 / 事务一致性 / 入参契约
- 关联模块：TASK-005D2 利润实际成本归属与事务兜底整改
- 审计来源：审计意见书第 93 份，TASK-005D1 有条件通过但仍有 2 个 P1、1 个 P2 必改项
- 结论：TASK-005D2 必须修复 workshop/subcontract 实际成本归属校验、`create_snapshot()` 保存点回滚兜底，并将 V1 `sales_order` 固化为强制非空；审计通过前不得进入 TASK-005E/TASK-006

### 背景

TASK-005D1 已修复第 92 份审计中的 request_hash 来源输入、收入 unresolved、异常 SLE、缺工价、收入真实状态和 idempotency_key 长度校验。但第 93 份复审发现，工票和外发实际成本仍可跨公司、跨订单、跨款式计入当前利润快照，这是利润报表可信度的核心风险。同时，计算异常后已 flush 的半快照仍可能被调用方误提交。审计还要求架构师明确 `sales_order` 口径，V1 决策为强制非空。

### 决策

1. TASK-005D V1 中 `sales_order` 强制非空。
2. 空 `sales_order` 包括 `None`、空字符串和空白字符串，必须在 DTO 或服务校验阶段拒绝。
3. 空 `sales_order` 建议错误码为 `STYLE_PROFIT_SALES_ORDER_REQUIRED`。
4. 空 `sales_order` 不得生成 snapshot/detail/source_map。
5. `StyleProfitSourceService.resolve_revenue_sources()` 不得在 expected_order 为空时把无 sales_order 来源行当作有效收入。
6. workshop 来源必须按 `company + item_code + sales_order` 做归属校验。
7. subcontract 来源必须按 `company + item_code + sales_order` 做归属校验。
8. 来源行 `company` 存在且不匹配时，必须排除，不得纳入利润。
9. 来源行 `item_code/style_item_code` 存在且不匹配时，必须排除，不得纳入利润。
10. 来源行 `sales_order` 存在且不匹配时，必须排除，不得纳入利润。
11. `work_order/job_card` 只能作为辅助归属桥，不得覆盖明确冲突的 company、item_code 或 sales_order。
12. 来源缺少 `sales_order` 且没有可验证工单/工序卡桥接时，必须 unresolved，不得纳入利润。
13. 错范围来源可以写 excluded source_map。
14. 无法确认归属但可能相关的来源必须写 unresolved source_map。
15. 错范围和 unresolved 来源不得影响 `actual_workshop_cost`、`actual_subcontract_cost`、`actual_total_cost`。
16. `create_snapshot()` 首次写入路径必须使用 `session.begin_nested()` 保存点或等价保存点机制。
17. replay 路径不得新增行。
18. 幂等冲突路径不得新增行。
19. 首次写入过程中任意 `BusinessException`、`SQLAlchemyError` 或未知异常，都必须回滚本次保存点。
20. 保存点回滚后，调用方即使误执行 `session.commit()`，也不得留下本次 snapshot/detail/source_map。
21. 服务内仍不得调用外层 `session.commit()`。
22. TASK-005D1 已修复项不得回退。
23. TASK-005D2 不得注册 API，不得修改前端，不得新增迁移，不得进入 TASK-005E/TASK-006。

### 验收

1. 工票错 company、错 item_code、错 sales_order 不计入 `actual_workshop_cost`。
2. 外发错 company、错 item_code、错 sales_order 不计入 `actual_subcontract_cost`。
3. 缺归属桥的工票和外发来源 unresolved，并使 snapshot incomplete。
4. company/item/sales_order 全匹配的工票和外发来源可计入成本。
5. 计算异常后调用方误 `commit()` 也不会留下 snapshot/detail/source_map。
6. 幂等 replay 和幂等冲突不受保存点改造影响，不新增行。
7. `sales_order=None/''/'   '` 均返回业务错误且不落库。
8. 定向 pytest、全量 pytest、unittest、py_compile 通过。
9. 静态扫描确认未注册 API、未修改前端、未新增迁移、未进入 TASK-005E/TASK-006。
10. TASK-005D2 审计通过后，才允许评估 TASK-005D3 本地基线提交或 TASK-005E API 层任务单。

## ADR-088：实际成本来源缺关键归属字段必须 fail closed

- 决策时间：2026-04-14 10:56 CST
- 决策类型：审计整改 / 成本归属 / fail closed
- 关联模块：TASK-005D3 利润实际成本关键归属字段 Fail Closed 整改
- 审计来源：审计意见书第 94 份，TASK-005D2 有条件通过但仍有 1 个 P1 必改项
- 结论：workshop/subcontract 实际成本来源必须能验证 `company + item_code + sales_order` 三者全部匹配才允许纳入利润；缺 `company` 或缺 `item_code/style_item_code` 必须 unresolved 或通过可信桥接补全后再 mapped；不得仅凭 `sales_order` 匹配纳入利润

### 背景

TASK-005D2 已修复错公司、错订单、错款式的明确冲突场景，但审计发现，当工票或外发行缺 `company` 或缺 `item_code/style_item_code` 时，当前实现仍可只凭 `sales_order` 匹配将成本纳入利润。在一个 Sales Order 包含多个款式，或上游推送缺关键字段时，这会把无法确认归属的成本写入单款式利润快照，破坏报表可信度。

### 决策

1. workshop/subcontract 来源必须能验证 `company + item_code + sales_order` 三者全部匹配，才允许 `mapping_status=mapped`。
2. 缺 `company` 的 workshop/subcontract 来源，默认 `mapping_status=unresolved`。
3. 缺 `item_code/style_item_code` 的 workshop/subcontract 来源，默认 `mapping_status=unresolved`。
4. 缺 `company` 或缺 `item_code/style_item_code` 时，不得仅凭 `sales_order` mapped。
5. 缺 `company` 或缺 `item_code/style_item_code` 时，不得计入 `actual_workshop_cost` 或 `actual_subcontract_cost`。
6. 缺 `company` 时 source_map 必须写 `include_in_profit=false`，reason 建议为 `company_scope_missing`。
7. 缺 `item_code/style_item_code` 时 source_map 必须写 `include_in_profit=false`，reason 建议为 `item_scope_missing`。
8. 只有通过可信桥接补全并验证 `company + item_code + sales_order` 三者全部匹配后，才允许 mapped。
9. 可信桥接限定为可解析出三字段的 `work_order`、`job_card`、`production_plan_id`、`subcontract_order`。
10. 当前无真实桥接查询能力时，不得把桥接字段存在本身视为可信补全。
11. scope helper 必须先校验硬字段完整性，再判断桥接匹配。
12. 硬字段存在但冲突时，必须 excluded。
13. 硬字段缺失时，必须 unresolved。
14. 桥接只允许补全缺失字段或辅助验证，不得覆盖明确冲突字段。
15. TASK-005D1/D2 已修复项不得回退。
16. TASK-005D3 不得注册 API，不得修改前端，不得新增迁移，不得进入 TASK-005E/TASK-006。

### 验收

1. 工票缺 company 时，不计入 `actual_workshop_cost`，并写 unresolved source_map。
2. 工票缺 item_code/style_item_code 时，不计入 `actual_workshop_cost`，并写 unresolved source_map。
3. 外发缺 company 时，不计入 `actual_subcontract_cost`，并写 unresolved source_map。
4. 外发缺 item_code/style_item_code 时，不计入 `actual_subcontract_cost`，并写 unresolved source_map。
5. 缺关键归属字段时 `include_in_profit=false`，reason 明确。
6. company + item_code + sales_order 三者全部可验证匹配时，才允许 mapped。
7. 保存点回滚、sales_order 强制非空、request_hash 来源输入等 TASK-005D1/D2 修复项未回退。
8. 定向 pytest、全量 pytest、unittest、py_compile 通过。
9. 静态扫描确认未注册 API、未修改前端、未新增迁移、未进入 TASK-005E/TASK-006。
10. TASK-005D3 审计通过后，才允许评估 TASK-005D4 本地基线提交或 TASK-005E API 层任务单。

## ADR-089：TASK-005D4 必须在 API 前完成本地基线提交

- 决策时间：2026-04-14 11:08 CST
- 决策类型：交付基线 / 本地仓库治理 / API 前置门禁
- 关联模块：TASK-005D4 利润快照服务本地仓库基线提交
- 审计来源：审计意见书第 95 份，TASK-005D3 通过；审计建议进入 TASK-005E/API 前先做本地基线提交
- 结论：进入 TASK-005E/API 前，必须先将 TASK-005D~D3 的利润快照服务、DTO、来源映射调整、测试、任务文档、审计记录和证据文件按白名单纳入本地 git commit；未形成稳定基线前不得进入 TASK-005E/TASK-006

### 背景

TASK-005D3 已通过审计，说明利润快照服务层当前阻断问题已关闭。但 D 系列新增的服务、测试和任务文档仍处于未提交或未跟踪状态。若直接进入 TASK-005E/API 层，API 权限、审计、错误信封和服务调用会建立在不稳定工作区上，后续审计难以界定 API 问题还是服务层基线漂移。因此必须先执行 D4 本地白名单基线提交。

### 决策

1. TASK-005D4 只允许做本地白名单提交和证据记录。
2. 允许纳入后端文件：`app/schemas/style_profit.py`、`app/services/style_profit_source_service.py`、`app/services/style_profit_service.py`、`tests/test_style_profit_source_mapping.py`、`tests/test_style_profit_service.py`、`tests/test_style_profit_snapshot_calculation.py`、`tests/test_style_profit_snapshot_idempotency.py`。
3. 允许纳入文档文件：TASK-005D~D4 任务单、当前 Sprint 清单、工程师日志、款式利润报表模块设计、技术决策记录、架构师日志、审计记录、审计官日志和 D4 证据文件。
4. 禁止 `git add .`、`git add -A`、`git add --all`。
5. 必须使用 `git add -- <精确文件路径>`。
6. 禁止 stage `06_前端/**`、`.github/**`、`02_源码/**`、`app/main.py`、`app/routers/**`、`migrations/**`。
7. 禁止新增或修改 TASK-005E 文件。
8. 禁止新增或修改 TASK-006 文件。
9. 禁止 push。
10. 禁止配置 remote/origin。
11. 提交前必须通过 style profit 定向 pytest、全量 pytest、unittest 和 py_compile。
12. 提交前必须通过禁改扫描。
13. 必须创建 `TASK-005D4_利润快照服务本地仓库基线提交证据.md`。
14. commit message 固定为 `feat: add style profit snapshot service baseline`。
15. TASK-005D4 不允许修改业务逻辑，不允许注册 API，不允许修改前端。

### 验收

1. 本地 commit 已形成。
2. commit 内容仅限 D4 白名单。
3. D4 证据文件记录提交前 HEAD、提交后 HEAD、验证结果、暂存文件清单、白名单核对结论和禁改扫描结果。
4. TASK-005D~D3 的利润服务、DTO、来源映射调整和测试文件已纳入 commit。
5. 未 push，未配置 remote。
6. 未注册 API，未修改前端，未新增迁移，未进入 TASK-005E/TASK-006。
7. TASK-005D4 审计通过后，才允许评估 TASK-005E API 权限与审计基线任务单。

## ADR-090：TASK-005D5 必须修正 D4 证据提交后 HEAD 占位

- 决策时间：2026-04-14 11:25 CST
- 决策类型：证据链修正 / docs-only / API 前置门禁
- 关联模块：TASK-005D5 D4 证据提交后 HEAD 占位修正
- 审计来源：审计意见书第 96 份，TASK-005D4 有条件通过，唯一问题为 D4 证据提交后 HEAD 仍是占位符
- 结论：进入 TASK-005E/API 前，必须先以 docs-only 方式将 D4 证据文件中的“提交后 HEAD”修正为 `47c1728a4eb2ca16549f6478d3bdb5af95b12b1a`；未清零占位符前不得下发 API 实现任务

### 背景

TASK-005D4 已完成利润快照服务本地基线提交，commit `47c1728a4eb2ca16549f6478d3bdb5af95b12b1a` 已存在，且审计确认利润服务与新增测试已纳入 git 跟踪。审计仅发现 D4 证据文件中的“提交后 HEAD”仍保留占位符。虽然该问题不影响业务代码，但会削弱后续 API 审计对服务层基线的追溯能力，因此必须在进入 TASK-005E/API 前修正。

### 决策

1. TASK-005D5 只允许做 docs-only 证据修正。
2. D4 证据文件的“提交后 HEAD”必须改为 `47c1728a4eb2ca16549f6478d3bdb5af95b12b1a`。
3. 不得改写 D4 证据中的测试结果、扫描结果、提交前 HEAD 或其他事实字段。
4. 允许追加工程师会话日志说明本次修正。
5. 禁止修改后端、前端、`.github`、`02_源码`、迁移、API router 和 `app/main.py`。
6. 禁止新增或修改 TASK-005E 文件。
7. 禁止新增或修改 TASK-006 文件。
8. 禁止 push。
9. 禁止配置 remote/origin。
10. 必须用 grep 确认占位符已清零、真实 SHA 已写入。
11. 必须形成本地 docs-only commit。

### 验收

1. D4 证据文件不再存在 `<本次 commit 后回填于执行回报>`。
2. D4 证据文件不再存在 `<pending>` 或 `TODO` 占位。
3. D4 证据文件包含 `47c1728a4eb2ca16549f6478d3bdb5af95b12b1a`。
4. 禁改扫描确认未修改后端、前端、`.github`、`02_源码`、TASK-005E 和 TASK-006。
5. 本地 docs-only commit 已形成。
6. TASK-005D5 审计通过后，才允许下发 TASK-005E API 权限与审计基线任务单。
7. TASK-006 继续保持阻塞。

## ADR-091：TASK-005D6 必须将 D5 任务单纳入文档基线

- 决策时间：2026-04-14 11:33 CST
- 决策类型：证据链补齐 / docs-only / API 前置门禁
- 关联模块：TASK-005D6 D5 任务单文档基线提交
- 审计来源：审计意见书第 97 份，TASK-005D5 通过，但 D5 任务单文件仍未跟踪
- 结论：进入 TASK-005E/API 前，必须先将 `TASK-005D5_D4证据提交后HEAD占位修正_工程任务单.md`、`TASK-005D6_D5任务单文档基线提交_工程任务单.md` 和必要架构文档更新纳入本地 docs-only commit；未清理该文档基线缺口前不得下发 API 实现任务

### 背景

TASK-005D5 已通过审计，D4 证据提交后 HEAD 已回填为 `47c1728a4eb2ca16549f6478d3bdb5af95b12b1a`，D5 修正 commit `ff1f8dd` 仅修改 D4 证据文件，未触碰后端、前端、`.github` 或 `02_源码`。但 D5 任务单本身仍是未跟踪文件。如果直接进入 TASK-005E/API，审计记录能看到 D5 结果，却缺少 D5 任务边界的入库证据。因此必须先做 D6 文档基线提交。

### 决策

1. D5 任务单必须纳入本地 git 跟踪。
2. D6 任务单必须纳入本地 git 跟踪，避免再出现任务单未跟踪的递归缺口。
3. 当前 Sprint 清单、款式利润报表模块设计、技术决策记录、架构师日志可纳入同一 docs-only 提交。
4. 工程师日志、审计记录、审计官日志如有本轮新增变化，可纳入同一 docs-only 提交。
5. 禁止修改后端、前端、`.github`、`02_源码`、迁移、API router 和 `app/main.py`。
6. 禁止新增或修改 TASK-005E 文件。
7. 禁止新增或修改 TASK-006 文件。
8. 禁止 push。
9. 禁止配置 remote/origin。
10. 必须使用精确 `git add -- <文件路径>`，禁止 `git add .`、`git add -A`、`git add --all`。
11. commit message 建议为 `docs: baseline TASK-005D5 evidence task`。

### 验收

1. D5 任务单已纳入本地 git 跟踪。
2. D6 任务单已纳入本地 git 跟踪。
3. 本地 docs-only commit 已形成。
4. 提交内容仅限白名单文档。
5. 禁改扫描确认未修改后端、前端、`.github`、`02_源码`、TASK-005E 和 TASK-006。
6. TASK-005D6 审计通过后，才允许下发 TASK-005E API 权限与审计基线任务单。
7. TASK-006 继续保持阻塞。

## ADR-092：TASK-005E API 层必须先审计任务单再实现

- 决策时间：2026-04-14 11:42 CST
- 决策类型：API 前置门禁 / 权限审计 / 本地 PostgreSQL 验证
- 关联模块：TASK-005E 款式利润 API 权限与审计基线
- 审计来源：TASK-005D6 审计通过结论，允许准备 TASK-005E 任务单，但要求先审计任务单本身，不能直接进入 API 实现
- 结论：TASK-005E 必须先输出待审计版任务单，审计通过后才允许实现；API 设计必须明确鉴权、资源权限、安全审计、操作审计、统一错误信封和 PostgreSQL 验证门禁

### 背景

TASK-005D~D6 已完成利润模型、来源映射、快照计算服务和文档证据基线。下一步进入 API 层后，会暴露款式收入、材料成本、工票工资、外发加工费和利润率等敏感经营数据。如果 API 层缺少权限、审计或错误信封约束，已通过的服务层口径仍可能被越权读取、被客户端污染来源明细，或在异常场景中泄露敏感信息。因此 TASK-005E 必须先审任务单，再实现。

### 决策

1. TASK-005E 任务单状态为“待审计版”，审计通过前不得实现。
2. API 前缀固定为 `/api/reports/style-profit`。
3. V1 API 仅包含快照列表、快照详情、快照创建。
4. 创建快照 API 只允许接收 selector，不允许接收客户端来源明细。
5. 来源事实必须由服务端采集器读取 ERPNext/FastAPI 可信来源。
6. 新增权限动作 `style_profit:read` 和 `style_profit:snapshot_create`。
7. `/api/auth/actions?module=style_profit` 必须返回款式利润动作权限。
8. 所有接口必须接入 `get_current_user`。
9. 所有接口必须校验动作权限。
10. 所有接口必须按 `company + item_code` 校验资源权限。
11. ERPNext 权限源不可用必须 fail closed，返回 `PERMISSION_SOURCE_UNAVAILABLE`。
12. 401、403、503 权限相关拒绝必须写安全审计。
13. 创建快照成功/失败必须写操作审计。
14. 快照详情读取成功/失败必须写操作审计，审计失败不得返回详情。
15. 成功、分页、错误响应必须统一 `{code,message,data}`。
16. 禁止顶层 `detail`，禁止 `detail=str(exc)`。
17. 禁止敏感异常明文进入响应或普通日志。
18. 必须新增 PostgreSQL 集成测试门禁。
19. destructive PostgreSQL 测试必须要求 `POSTGRES_TEST_DSN`、`POSTGRES_TEST_ALLOW_DESTRUCTIVE=true` 和测试库命名约束。
20. 本地无 PostgreSQL DSN 时普通回归允许安全 skip，但进入前端联调或发布前必须补真实非 skip 证据。
21. 本项目不等待 GitHub Hosted Runner。
22. TASK-006 继续阻塞，不得混入 TASK-005E。

### 验收

1. TASK-005E 任务单经审计官审查通过。
2. API 实现只触碰白名单后端文件和测试文件。
3. 列表、详情、创建接口均存在。
4. `/api/auth/actions?module=style_profit` 可返回款式利润动作权限。
5. 动作权限和资源权限测试覆盖通过。
6. 安全审计和操作审计测试覆盖通过。
7. 错误信封测试覆盖通过。
8. PostgreSQL 集成测试文件存在并具备安全破坏性门禁。
9. 定向 pytest、全量 pytest、unittest、py_compile 通过。
10. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
11. TASK-005E 审计通过后，才允许评估 TASK-005F 前端或报表联调任务。

## ADR-093：允许预派发 TASK-005E1，但不得绕过 TASK-005E 审计门禁

- 决策时间：2026-04-14 11:50 CST
- 决策类型：开发排程 / 防空转 / 审计门禁
- 关联模块：TASK-005E1 款式利润 API 权限审计基线实现
- 审计来源：TASK-005E 待审计版任务单已输出，当前流程要求不陷入等待死循环
- 结论：允许预派发 TASK-005E1 工程实现任务单，以便 TASK-005E 任务单审计通过后工程师可立即执行；但 TASK-005E1 的执行前置条件必须写死为“TASK-005E 任务单审计通过”，不得用预派发绕过审计

### 背景

TASK-005E 是款式利润 API 层任务单，按审计要求必须先审任务单本身。如果在审计通过后再等待架构师另行下发实现单，会形成不必要的流程空转。为保证节奏连续，同时不破坏审计门禁，架构侧预先输出 TASK-005E1 实现任务单，但将执行前置条件写死。

### 决策

1. TASK-005E1 可以预派发。
2. TASK-005E1 不等于开工许可。
3. TASK-005E 任务单未审计通过前，工程师不得执行 TASK-005E1。
4. 如果审计官要求修改 TASK-005E 任务单，必须先整改 TASK-005E，再同步修正 TASK-005E1。
5. TASK-005E1 实现范围仅限款式利润 API 权限审计基线。
6. TASK-005E1 禁止修改利润公式。
7. TASK-005E1 禁止修改前端、`.github`、`02_源码`、migrations。
8. TASK-005E1 禁止进入 TASK-006。
9. TASK-005E1 必须覆盖鉴权、动作权限、`company + item_code` 资源权限、安全审计、操作审计、统一错误信封、PostgreSQL 门禁。
10. TASK-005E1 交付回报必须显式写明 TASK-005E 任务单审计已通过。

### 验收

1. TASK-005E1 任务单已落盘。
2. TASK-005E1 明确写出执行前置门禁。
3. TASK-005E1 未要求工程师绕过审计直接实现。
4. TASK-005E1 未放开 TASK-006。
5. TASK-005E1 审计通过后，才允许后续评估前端或报表联调任务。

## ADR-094：TASK-005E2 必须阻断空来源快照并修复 API 权限审计缺口

- 决策时间：2026-04-14 12:34 CST
- 决策类型：API 审计整改 / fail closed / 权限矩阵
- 关联模块：TASK-005E2 款式利润 API 审计阻断整改
- 审计来源：审计意见书第 99 份，TASK-005E1 不通过，共 4 个问题项（高 2 / 中 2）
- 结论：TASK-005E2 必须先修复 `StyleProfitApiSourceCollector` 空实现、角色权限矩阵偏差、鉴权顺序和早期失败操作审计；未关闭第 99 份问题前不得进入前端联调或 TASK-006

### 背景

TASK-005E1 已暴露款式利润 API，但审计发现默认来源采集器仍为空实现，可能把“采集未实现/不可用”误判为“业务确实无来源”，进而创建零值或不完整利润快照。审计还发现权限矩阵偏离冻结口径、无权限用户可先收到业务 400 错误、部分早期失败没有操作审计。这些问题都会破坏利润报表的可信度和权限语义。

### 决策

1. 默认 `StyleProfitApiSourceCollector` 不得空实现成功。
2. 真实来源采集未就绪或不可用时，必须返回 `STYLE_PROFIT_SOURCE_UNAVAILABLE` 或 `DATABASE_READ_FAILED`。
3. collector fail closed 时不得调用 `StyleProfitService.create_snapshot()`。
4. collector fail closed 时不得写入 snapshot/detail/source_map。
5. fake collector 仅允许测试注入，不得进入生产默认路径。
6. `System Manager` 和 `Finance Manager` 拥有 `style_profit:read`、`style_profit:snapshot_create`。
7. `Production Manager` 和 `Sales Manager` 仅拥有 `style_profit:read`。
8. `Viewer` 不得默认拥有款式利润权限。
9. static 与 ERP role 聚合必须同时按冻结矩阵修正。
10. 登录后必须先做动作权限校验，再做业务字段校验。
11. 无动作权限用户即使 payload 非法，也必须返回 `403 + AUTH_FORBIDDEN`。
12. 403 权限拒绝必须写安全审计。
13. 动作权限通过后的列表业务失败必须写操作失败审计。
14. 动作权限通过后的创建早期失败必须写创建失败操作审计。
15. 必需操作审计写入失败必须返回 `AUDIT_WRITE_FAILED`。
16. PostgreSQL 测试安全门禁必须保留，真实非 skip 证据作为前端联调或发布前门禁。
17. TASK-006 继续阻塞。

### 验收

1. 默认 collector 未配置真实来源时 fail closed。
2. 默认 collector 不再创建空来源零值快照。
3. collector 不可用时不调用 `StyleProfitService.create_snapshot()`。
4. 权限矩阵 static 与 ERP role 测试均通过。
5. 无权限用户在非法 payload 下优先返回 403。
6. 403 安全审计覆盖通过。
7. 列表业务失败操作审计覆盖通过。
8. 创建早期失败操作审计覆盖通过。
9. 必需审计失败返回 `AUDIT_WRITE_FAILED`。
10. API 定向 pytest、全量 pytest、unittest、py_compile 通过。
11. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
12. TASK-005E2 审计通过后，才允许评估 TASK-005E3 基线提交或 TASK-005F 前端联调任务。

## ADR-095：款式利润详情接口必须先鉴权再查询 snapshot

- 决策时间：2026-04-14 12:58 CST
- 决策类型：API 安全整改 / 存在性枚举防护 / 鉴权顺序
- 关联模块：TASK-005E3 款式利润详情接口鉴权前置整改
- 审计来源：TASK-005E2 复审有条件通过，保留 1 个中危问题
- 结论：`GET /api/reports/style-profit/snapshots/{snapshot_id}` 必须先校验 `style_profit:read` 动作权限，再查询 snapshot；无读权限用户访问存在或不存在 ID 均必须返回 `403 + AUTH_FORBIDDEN`

### 背景

TASK-005E2 已基本关闭第 99 份审计提出的来源采集、权限矩阵、鉴权顺序和早期失败审计问题。但审计发现详情接口仍然先查 snapshot，再做 `style_profit:read` 动作权限校验。这样无读权限用户可以通过存在 ID 返回 403、不存在 ID 返回 404 的差异推断利润快照是否存在。款式利润快照属于敏感经营数据，snapshot 是否存在本身也属于敏感信息，必须防止枚举。

### 决策

1. 详情接口登录后必须立即校验 `style_profit:read` 动作权限。
2. 动作权限未通过时，不得查询 snapshot。
3. 动作权限未通过时，存在 ID 和不存在 ID 都必须返回 `403 + AUTH_FORBIDDEN`。
4. 动作权限未通过时，必须写安全审计。
5. 只有动作权限通过后，才允许查询 snapshot。
6. 动作权限通过后，snapshot 不存在才返回 `404 + STYLE_PROFIT_NOT_FOUND`。
7. snapshot 存在后，必须继续按 `snapshot.company + snapshot.item_code` 做资源权限。
8. 资源权限通过后，才允许读取 detail/source_map。
9. 详情成功操作审计不得回退。
10. 详情操作审计失败必须返回 `AUDIT_WRITE_FAILED`，不得返回详情。
11. TASK-006 继续阻塞。

### 验收

1. 无读权限访问已存在 snapshot_id 返回 403。
2. 无读权限访问不存在 snapshot_id 返回 403。
3. 两类无读权限响应不能暴露 ID 是否存在。
4. 有读权限访问不存在 snapshot_id 返回 404。
5. 有读权限但无资源权限访问存在 snapshot_id 返回 403。
6. 详情接口仍在资源权限通过后才读取 detail/source_map。
7. 安全审计和操作审计测试通过。
8. 定向 pytest、全量 pytest、unittest、py_compile 通过。
9. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
10. TASK-005E3 审计通过后，才允许评估 TASK-005E4 本地基线提交或 TASK-005F 前端联调任务。

## ADR-096：TASK-005E4 必须将款式利润 API 权限审计基线纳入本地提交

- 决策时间：2026-04-14 13:07 CST
- 决策类型：交付基线 / 本地仓库治理 / 前端联调前置门禁
- 关联模块：TASK-005E4 款式利润 API 本地仓库基线提交
- 审计来源：TASK-005E3 审计通过，款式利润 API 详情存在性枚举风险已关闭
- 结论：进入前端联调、真实来源 adapter 或其他后续开发前，必须先将 TASK-005E~E3 的 API 代码、权限审计修复、测试、任务文档和审计记录按白名单纳入本地 git commit

### 背景

TASK-005E1~E3 已完成款式利润 API 权限审计基线的主要实现和整改：API 路由已落地，权限矩阵已回归冻结口径，默认来源采集已 fail closed，详情接口存在性枚举风险已关闭。当前这些 API 代码、测试与 E 系列任务文档仍处于未提交或未跟踪状态。若直接进入前端联调或真实来源 adapter，审计对象会继续漂移。因此必须先执行 E4 本地白名单基线提交。

### 决策

1. TASK-005E4 只允许做本地白名单提交和证据记录。
2. 允许纳入 style_profit API router、collector、权限映射、错误码、schema、main 注册和 API 测试。
3. 允许纳入 TASK-005E~E4 任务文档、当前 Sprint 清单、模块设计、技术决策记录、架构师日志、工程师日志、审计记录和审计官日志。
4. 必须创建 TASK-005E4 证据文件。
5. 禁止 `git add .`、`git add -A`、`git add --all`。
6. 必须使用精确 `git add -- <文件路径>`。
7. 禁止修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
8. 禁止 push。
9. 禁止配置 remote/origin。
10. 提交前必须通过 API 定向 pytest、全量 pytest、unittest、py_compile。
11. 提交前必须通过禁改扫描。
12. commit message 建议为 `feat: add style profit API permission audit baseline`。

### 验收

1. 本地 commit 已形成。
2. commit 内容仅限 E4 白名单。
3. E4 证据文件记录提交前 HEAD、提交后 HEAD、验证结果、暂存清单、白名单核对结论和禁改扫描结果。
4. E~E3 API 文件、测试和任务文档已纳入 commit。
5. 未 push，未配置 remote。
6. 未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
7. TASK-005E4 审计通过后，才允许评估 TASK-005F 前端联调或真实来源 adapter 任务。

## ADR-097：款式利润快照创建必须使用真实服务端来源 Adapter

- 决策时间：2026-04-14 13:20 CST
- 决策类型：利润来源可信性 / API 后续开发门禁 / fail closed
- 关联模块：TASK-005F 款式利润真实服务端来源 Adapter
- 审计来源：TASK-005E4 审计通过后保留风险：`StyleProfitApiSourceCollector` 当前仍为 fail-closed 基线，真实服务端来源采集尚未实现
- 结论：前端创建入口或生产试用前，必须先实现真实服务端来源 Adapter；利润快照创建不得信任前端 payload 中的收入、成本、工票、外发或库存明细

### 背景

TASK-005E4 已将款式利润 API 权限审计基线纳入本地稳定提交，登录鉴权、动作权限、资源权限、安全审计、操作审计和统一错误信封已经通过审计。但当前默认 `StyleProfitApiSourceCollector` 仍以 fail-closed 为主，未真正采集 ERPNext 和 FastAPI 本地事实。如果直接打开前端创建入口，要么一直失败，要么后续工程师可能误把客户端 payload 当作利润来源事实，这会破坏款式利润报表的可信性。

### 决策

1. TASK-005F 必须先实现真实服务端来源 Adapter。
2. 收入来源必须来自 ERPNext `Sales Invoice / Sales Invoice Item` 或 `Sales Order / Sales Order Item`。
3. 实际收入优先使用已提交 `Sales Invoice`；无已提交 SI 时，允许按冻结口径使用已提交或已审批 `Sales Order`。
4. 标准材料和标准工序来源必须来自本地 active/default BOM。
5. 实际材料成本必须来自 ERPNext `Stock Ledger Entry`，且必须受 BOM 允许物料编码约束。
6. `Purchase Receipt` 只能作为采购参考和异常追溯来源，不得直接计入实际材料成本。
7. 工票和外发实际成本必须具备可信 `company + item_code + sales_order/work_order/job_card` 归属；缺关键字段时不得仅凭 `sales_order` 纳入利润。
8. ERPNext 来源不可用、无有效收入来源、无 active/default BOM、SLE 查询不可用时必须 fail closed。
9. 客户端提交来源明细必须继续拒绝。
10. request_hash 必须包含真实来源输入，且不得包含 request_id、当前时间、operator、created_at、数据库自增 id 等易变字段。
11. TASK-005F 禁止修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
12. TASK-006 继续阻塞。

### 验收

1. 默认 collector 不再是空实现。
2. 可信 SI/SO + active/default BOM 存在时，快照创建可以生成非空来源。
3. ERPNext 来源不可用时返回 `STYLE_PROFIT_SOURCE_UNAVAILABLE`，且不创建 snapshot。
4. 无有效收入来源时返回 `STYLE_PROFIT_REVENUE_SOURCE_REQUIRED`。
5. 无 active/default BOM 时返回 `STYLE_PROFIT_BOM_REQUIRED`。
6. SLE 只纳入 BOM 允许物料编码。
7. 工票和外发缺关键归属字段时不纳入利润。
8. 客户端来源 payload 仍被拒绝。
9. 相同幂等键但来源输入变化时返回幂等冲突。
10. 定向 pytest、全量 pytest、unittest、py_compile 通过。
11. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
12. TASK-005F 审计通过后，才允许评估 TASK-005G 前端只读/创建入口联调。

## ADR-098：SLE 实际材料成本必须同时满足材料范围和销售/生产归属桥接

- 决策时间：2026-04-14 13:50 CST
- 决策类型：利润实际成本可信归属 / 审计阻断整改 / fail closed
- 关联模块：TASK-005F1 利润 SLE 归属与外发来源阻断整改
- 审计来源：审计意见书第 103 份，TASK-005F 不通过，共 4 个问题项（高 3 / 中 1）
- 结论：SLE 计入实际材料成本必须同时满足 BOM 物料范围和销售/生产归属桥接；Adapter 禁止伪造 SLE 提交状态；外发来源不得静默返回空成本

### 背景

TASK-005F 已将款式利润来源采集从空 collector 推进到真实 Adapter，但审计发现两个利润可信性高危问题：第一，Adapter 会给缺状态的 SLE 默认补 `docstatus=1/status=submitted`，把未知状态当成已提交；第二，来源映射层仅凭 BOM 物料白名单就把 SLE 计入利润，可能把同公司同物料但属于其他销售订单或生产工单的库存成本计入当前款式利润。审计还发现外发成本采集静默返回空列表，可能让利润系统性高估。

### 决策

1. BOM 物料白名单只能证明材料范围合法，不能证明库存流水归属当前订单或工单。
2. SLE 计入 `actual_material_cost` 必须同时满足材料范围合法和归属桥接可信。
3. 归属桥接可信包括：`sales_order/custom_ly_sales_order` 精确匹配、`work_order/custom_ly_work_order` 精确匹配或可映射到当前销售订单、`voucher_type/voucher_no` 指向可映射 Work Order、`production_plan/custom_ly_production_plan` 可映射、`job_card/custom_ly_job_card` 可经 Job Card/工票链路映射。
4. 以上桥接缺失、解析失败或指向其他订单/款式/公司时，SLE 必须 unresolved，`include_in_profit=false`。
5. Adapter 不得给 SLE 默认补 `docstatus=1`、`status=submitted` 或 `is_cancelled=false`。
6. SLE 缺可信状态字段时，不得计入利润，必须 unresolved 或 fail closed。
7. ERPNext 整体响应结构异常、非 JSON、缺 `data` 列表、分页异常时，必须返回 `STYLE_PROFIT_SOURCE_UNAVAILABLE`。
8. 外发来源不得静默返回空列表；存在候选事实但缺可信桥接时，必须写 source_map unresolved，不能把外发成本伪装为 0。
9. 外发成本计入利润必须满足 `company + item_code + sales_order/work_order` 可信归属，结算锁定金额优先，未结算验货净额受 `include_provisional_subcontract` 控制。
10. TASK-005F1 禁止修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
11. TASK-006 继续阻塞。

### 验收

1. SLE 缺 `docstatus/status/is_cancelled` 时不得计入利润。
2. SLE `docstatus != 1` 或取消态不得计入利润。
3. 同公司、同期间、同 BOM 物料但无销售/生产桥接时，不得计入利润。
4. SLE 明确桥接到当前 sales_order 或 work_order 时，才允许计入利润。
5. SLE 桥接到其他订单或其他工单时，不得计入利润。
6. BOM 白名单命中但桥接缺失时，source_map 必须 unresolved，snapshot 必须 incomplete 或 unresolved_count 增加。
7. 外发候选事实存在但缺桥接时，不得静默空返回；必须 unresolved 且不计入利润。
8. 外发有可信归属桥接时，才允许按冻结口径计入利润。
9. 补齐 SLE 状态、SLE 无桥接、外发候选、ERPNext 结构异常反向测试。
10. 定向 pytest、全量 pytest、unittest、py_compile 通过。
11. 禁改扫描确认未修改前端、`.github`、`02_源码`、migrations 和 TASK-006。
12. TASK-005F1 审计通过后，才允许评估下一步任务。

## ADR-099：外发成本完整计入款式利润必须补可信归属桥接字段

- 决策时间：2026-04-14 14:15 CST
- 决策类型：数据模型补强 / 利润成本完整性 / 外发桥接
- 关联模块：TASK-005F2 外发利润归属桥接字段与补数
- 审计来源：审计意见书第 104 份，TASK-005F1 通过但保留高风险：外发候选缺 `sales_order/work_order/production_plan_id` 可信桥接，无法完整计入利润
- 结论：外发成本要完整纳入款式利润，必须补齐外发单和外发验货事实的销售订单、生产计划、工单桥接字段，并通过安全补数把历史可证明数据回填；不能仅依赖前端提示或把外发成本长期 unresolved

### 背景

TASK-005F1 已修复 SLE 状态 fail closed、SLE BOM+桥接双门禁和外发来源不静默归零问题。但审计指出，当前外发来源虽然可见并会进入 unresolved，仍缺可信销售/生产桥接，导致外发成本无法完整计入利润。服装工厂外发加工费通常是款式利润的重要成本项，若长期 unresolved，利润会系统性偏高。

### 决策

1. 外发单 `ly_subcontract_order` 必须补 `sales_order`、`sales_order_item`、`production_plan_id`、`work_order`、`job_card`、`profit_scope_status`、`profit_scope_error_code`、`profit_scope_resolved_at`。
2. 外发验货事实 `ly_subcontract_inspection` 必须补对应桥接快照字段。
3. 新增验货记录时，必须从外发单复制桥接字段，形成事实快照。
4. 后续外发单桥接字段变化不得自动重写历史验货事实。
5. 新建外发单允许缺桥接字段，但必须标记 `profit_scope_status='unresolved'`，不得计入利润。
6. 传入 `production_plan_id/work_order/sales_order` 时，必须通过本地生产计划和 Work Order 映射校验，不得只信任前端字段。
7. 历史补数只允许在唯一可信匹配时回填；多匹配和无匹配必须保持 unresolved。
8. 补数 dry_run 必须真正只读。
9. 补数 execute 必须写操作审计；写失败必须 rollback。
10. 款式利润只允许纳入 `profit_scope_status='ready'` 且匹配当前 `company + item_code + sales_order/work_order` 的外发成本。
11. 未结算外发受 `include_provisional_subcontract` 控制；结算锁定金额优先。
12. TASK-005F2 禁止修改前端、`.github`、`02_源码` 和 TASK-006。
13. TASK-006 继续阻塞。

### 验收

1. 外发单和外发验货事实具备桥接字段。
2. 迁移可自举、幂等升级，不破坏历史数据。
3. 新建外发单缺桥接时 unresolved。
4. 新建外发单带合法生产计划或工单桥接时 ready。
5. 跨公司、跨款式、跨订单桥接不得落库或不得 ready。
6. 历史补数 dry_run 真正只读。
7. 历史补数 execute 只在唯一可信匹配时回填。
8. 款式利润只纳入 ready 且匹配当前销售/生产归属的外发成本。
9. unresolved 外发进入 source_map，让财务可见“存在外发候选但未计入”的原因。
10. 定向 pytest、全量 pytest、unittest、py_compile 通过。
11. 禁改扫描确认未修改前端、`.github`、`02_源码` 和 TASK-006。
12. TASK-005F2 审计通过后，才允许评估下一步任务。

## ADR-100：传入 Work Order 时外发成本必须严格匹配当前工单

- 决策时间：2026-04-14 14:55 CST
- 决策类型：利润报表粒度 / 外发成本归属 / 审计阻断整改
- 关联模块：TASK-005F3 外发 Work Order 严格匹配与补数审计整改
- 审计来源：审计意见书第 105 份，TASK-005F2 不通过，共 4 个问题项（高 1 / 中 3）
- 结论：款式利润 selector 带 `work_order` 时，外发成本必须严格匹配当前 Work Order；同销售订单、同款式但不同 Work Order 的外发成本不得计入当前 Work Order 快照

### 背景

TASK-005F2 已补齐外发利润桥接字段、迁移和补数服务，但审计发现外发成本匹配逻辑在 `company + item_code + sales_order` 匹配后提前 mapped，导致同一销售订单、同一款式下其他 Work Order 的外发成本可能被计入当前 Work Order 快照。该问题会直接污染工单级利润，属于财务口径高危问题。

### 决策

1. 款式利润快照分为两个粒度：销售订单 + 款式汇总、Work Order 快照。
2. selector 不带 `work_order` 时，按 `company + sales_order + item_code` 汇总 ready 外发成本。
3. selector 带 `work_order` 时，必须按 `company + sales_order + item_code + work_order` 严格匹配。
4. selector 带 `work_order` 时，不允许只凭同 SO 同款提前计入外发成本。
5. 外发 `profit_scope_status` 必须等于 `ready` 才能进入范围匹配；缺失、空白或未知状态一律 fail closed。
6. 外发期间过滤必须使用业务验货时间 `inspected_at`，不得使用系统 `created_at` 静默兜底。
7. `inspected_at` 为空时，必须 unresolved，原因 `SUBCONTRACT_INSPECTED_AT_REQUIRED`。
8. 历史补数 execute 必须写操作审计，审计失败必须 rollback 并返回/抛出 `AUDIT_WRITE_FAILED`。
9. `updated_count` 必须表示实际写入行数，dry_run 应为 0。
10. TASK-005F3 禁止修改前端、`.github`、`02_源码`、迁移和 TASK-006。
11. TASK-006 继续阻塞。

### 验收

1. selector 带 `work_order=WO-1`，外发行 `work_order=WO-1` 时可计入。
2. selector 带 `work_order=WO-1`，外发行 `work_order=WO-2` 时不得计入。
3. selector 带 `work_order=WO-1`，外发行缺 work_order 且无法反查时不得计入。
4. selector 不带 work_order 时，可按销售订单 + 款式汇总 ready 外发成本。
5. 外发行缺 `profit_scope_status`、空白或未知状态不得计入。
6. 外发期间过滤使用 `inspected_at`，`created_at` 不作为默认兜底。
7. 补数 execute 写操作审计，审计失败 rollback。
8. 定向 pytest、全量 pytest、unittest、py_compile 通过。
9. 禁改扫描确认未修改前端、`.github`、`02_源码`、迁移和 TASK-006。
10. TASK-005F3 审计通过后，才允许评估下一步任务。

## ADR-101：外发来源 inspected_at 期间过滤必须下推到数据库

- 决策时间：2026-04-14 15:15 CST
- 决策类型：查询性能 / 财务封版门禁 / PostgreSQL 证据
- 关联模块：TASK-005F4 外发利润查询下推与 PostgreSQL 证据
- 审计来源：审计意见书第 106 份，TASK-005F3 通过但保留最优先风险：外发来源先拉取 company/item 候选再由 Python 过滤 `inspected_at`
- 结论：外发来源正常成本候选必须在数据库层按 `inspected_at` 过滤期间，并补 PostgreSQL 非 skip 集成证据；Python 层不得承担主要期间过滤

### 背景

TASK-005F3 已关闭 Work Order 严格匹配、外发状态 fail closed、`inspected_at` 业务日期口径和补数操作审计问题。但审计指出，当前外发来源仍先按 `company/item_code` 拉取候选，再在 Python 层用 `inspected_at` 过滤期间。随着外发验货量增长，利润快照会变成大查询和内存过滤，影响财务报表可用性。

### 决策

1. 外发来源正常查询必须在数据库层过滤 `company`、`item_code` 和 `inspected_at` 期间。
2. `from_date` 非空时，数据库查询必须包含 `inspected_at >= from_date`。
3. `to_date` 非空时，数据库查询必须包含 `inspected_at <= to_date` 的日期结束边界。
4. `inspected_at IS NULL` 不得进入正常成本候选查询。
5. `created_at` 不得作为利润期间默认 fallback。
6. 缺 `inspected_at` 的诊断集合必须单独查询，并限制返回数量。
7. 超过诊断限制时必须写聚合诊断，不得拉全量。
8. PostgreSQL 集成测试必须覆盖外发 `inspected_at` 期间过滤、Work Order 过滤、查询条件/计划和缺日期诊断限制。
9. CI/JUnit 门禁必须识别 PostgreSQL 测试全 skip 并失败。
10. TASK-005F4 禁止修改前端、`.github`、`02_源码`、迁移和 TASK-006。
11. TASK-006 继续阻塞。

### 验收

1. 外发正常查询不再按 company/item 拉全量后 Python 过滤期间。
2. `created_at` 不再作为利润期间默认过滤字段。
3. 缺 `inspected_at` 不计入利润，诊断集合有限制。
4. selector 带 work_order 时，数据库查询或最终结果只保留当前 work_order。
5. 新增至少 4 条 PostgreSQL marker 用例。
6. JUnit 非 skip 断言能识别全 skip 并失败。
7. 定向 pytest、全量 pytest、unittest、py_compile 通过。
8. 禁改扫描确认未修改前端、`.github`、`02_源码`、迁移和 TASK-006。
9. TASK-005F4 审计通过后，才允许评估下一步任务。

## ADR-102：新增 PostgreSQL 门禁不得覆盖既有外发结算门禁

- 决策时间：2026-04-14 15:40 CST
- 决策类型：CI 门禁治理 / 财务风险防回退 / PostgreSQL hard gate
- 关联模块：TASK-005F5 PostgreSQL 门禁目标恢复与双 JUnit 断言
- 审计来源：审计意见书第 107 份，TASK-005F4 不通过，共 3 个问题项（高 1 / 中 2）
- 结论：新增款式利润 PostgreSQL 测试不得替换 TASK-002H 外发结算 PostgreSQL 门禁；如继续使用单一 `run_postgresql_ci_gate.sh`，必须分别执行并断言 settlement 与 style-profit 两组 JUnit

### 背景

TASK-005F4 为款式利润外发来源增加 PostgreSQL 测试，但实现过程中把 `run_postgresql_ci_gate.sh` 改成只执行款式利润测试。该脚本仍由 `subcontract-postgresql-gate` required check 调用，导致原本用于 TASK-002H 外发结算并发、幂等和唯一约束的 PostgreSQL 门禁被静默覆盖。外发结算和款式利润都属于财务相关链路，任何一个门禁都不能被另一个替代。

### 决策

1. `run_postgresql_ci_gate.sh` 必须保留 TASK-002H 外发结算 PostgreSQL 测试目标。
2. `run_postgresql_ci_gate.sh` 可以新增 TASK-005F4 款式利润 PostgreSQL 测试目标。
3. 如果使用单一脚本，必须分别生成 settlement JUnit 与 style-profit JUnit。
4. settlement JUnit 必须断言 `tests=4, skipped=0`。
5. style-profit JUnit 必须断言 `tests=4, skipped=0`。
6. 不允许只用总量 `tests=8` 断言替代分组断言。
7. `tests/test_ci_postgresql_gate.py` 必须覆盖脚本目标一致性，防止任一目标被移除。
8. README 必须说明当前 PostgreSQL gate 是后端财务组合门禁，同时覆盖外发结算和款式利润外发来源。
9. 无真实 DSN 时可以记录 pending，但不得伪装非 skip 已通过。
10. TASK-005F5 禁止修改前端、`.github`、`02_源码`、迁移和 TASK-006。
11. TASK-006 继续阻塞。

### 验收

1. 脚本包含 `tests/test_subcontract_settlement_postgresql.py`。
2. 脚本包含 `tests/test_style_profit_subcontract_postgresql.py`。
3. 脚本生成 `.pytest-postgresql-subcontract-settlement.xml`。
4. 脚本生成 `.pytest-postgresql-style-profit-subcontract.xml`。
5. 两组 JUnit 分别断言 `tests=4, skipped=0`。
6. CI gate 目标一致性测试能发现 settlement 目标被移除。
7. README 和证据文件与实际脚本一致。
8. 定向 pytest、全量 pytest、unittest、py_compile 通过。
9. 禁改扫描确认未修改前端、`.github`、`02_源码`、迁移和 TASK-006。
10. TASK-005F5 审计通过后，才允许评估下一步任务。

## ADR-103：Workflow 必须上传 PostgreSQL 双 JUnit 证据

- 决策时间：2026-04-14 15:55 CST
- 决策类型：CI 证据治理 / PostgreSQL hard gate / 审计证据闭环
- 关联模块：TASK-005F6 Workflow 双 JUnit 上传与 PostgreSQL 证据回填
- 审计来源：审计意见书第 108 份，TASK-005F5 有条件通过但 workflow 仍上传旧 `.pytest-postgresql.xml`
- 结论：PostgreSQL hard gate workflow 必须上传 settlement 与 style-profit 两份 JUnit 证据；旧 `.pytest-postgresql.xml` 不再作为有效审计 artifact

### 背景

TASK-005F5 已恢复 `scripts/run_postgresql_ci_gate.sh` 的双目标门禁：外发结算 PostgreSQL 测试与款式利润外发来源 PostgreSQL 测试。但 workflow 仍上传旧 `.pytest-postgresql.xml`，与脚本实际生成的两份 JUnit 文件不一致。该问题会导致审计或本地 workflow 证据缺失，即使脚本执行了正确测试，也无法从 artifact 侧证明两组门禁均非 skip 通过。

### 决策

1. `.github/workflows/backend-postgresql.yml` 必须上传 `.pytest-postgresql-subcontract-settlement.xml`。
2. `.github/workflows/backend-postgresql.yml` 必须上传 `.pytest-postgresql-style-profit-subcontract.xml`。
3. 推荐拆成两个 artifact：
   - `postgresql-settlement-junit`
   - `postgresql-style-profit-junit`
4. 如使用单 artifact，也必须在 `path` 中同时包含两份 JUnit。
5. 旧 `.pytest-postgresql.xml` 不再作为 PostgreSQL hard gate 审计证据。
6. workflow 必须继续调用 `bash scripts/run_postgresql_ci_gate.sh`。
7. workflow 必须继续保留 PostgreSQL service，不得通过删 service 或改触发路径规避门禁。
8. `tests/test_ci_postgresql_gate.py` 必须静态校验 workflow artifact 路径，防止只上传旧文件或只上传其中一组 JUnit。
9. 无真实 `POSTGRES_TEST_DSN` 时，证据文件只能标记 pending，不得伪装非 skip 通过。
10. TASK-005F6 可修改 `.github/workflows/backend-postgresql.yml`，但仅限 PostgreSQL JUnit 上传路径；不得修改业务代码、前端、迁移、`02_源码` 或 TASK-006。

### 验收

1. workflow 上传 settlement JUnit。
2. workflow 上传 style-profit JUnit。
3. workflow 不再只上传旧 `.pytest-postgresql.xml`。
4. 静态测试能发现 artifact path 回退。
5. README 明确两份 JUnit 文件名和 artifact 名称。
6. 非 skip 证据真实回填，或明确 pending。
7. 定向 pytest、全量 pytest、unittest、py_compile 通过。
8. 禁改扫描确认未修改前端、`02_源码`、迁移、业务代码和 TASK-006。
9. TASK-005F6 审计通过后，才允许评估 TASK-005G。

## ADR-104：TASK-005 财务封版前必须取得 PostgreSQL 真实非 Skip 证据

- 决策时间：2026-04-14 16:05 CST
- 决策类型：财务报表发布门禁 / PostgreSQL 证据闭环 / 测试库安全治理
- 关联模块：TASK-005F7 PostgreSQL 真实非 Skip 证据闭环
- 审计来源：审计意见书第 109 份，TASK-005F6 有条件通过但真实 PostgreSQL 非 skip 实跑证据仍未闭环
- 结论：TASK-005 财务封版前，必须在一次性 PostgreSQL 测试库下跑通 settlement 与 style-profit 两组 hard gate，并取得两份 JUnit 的 `tests=4, skipped=0, failures=0, errors=0` 证据

### 背景

TASK-005F6 已修复 workflow 双 JUnit 上传、本地静态门禁和无 DSN 安全跳过逻辑。但审计指出，当前 settlement 与 style-profit 两组 PostgreSQL marker 测试仍因缺少 `POSTGRES_TEST_DSN` 跳过，尚未证明真实 PostgreSQL 行锁、唯一约束和查询下推语义在真实数据库下成立。

### 决策

1. TASK-005 财务封版前必须运行 `scripts/run_postgresql_ci_gate.sh`。
2. 执行环境必须是一次性 PostgreSQL 测试库，不得使用生产库、准生产库或含真实业务数据的数据库。
3. 必须设置 `POSTGRES_TEST_ALLOW_DESTRUCTIVE=true`。
4. 数据库名必须匹配 `lingyi_test_*` 或 `*_test`。
5. settlement JUnit 必须为 `tests=4, skipped=0, failures=0, errors=0`。
6. style-profit JUnit 必须为 `tests=4, skipped=0, failures=0, errors=0`。
7. 证据文档必须记录脱敏 DSN、执行命令、JUnit 指标、清理动作和敏感信息扫描结果。
8. 如果真实 PostgreSQL 运行失败，必须记录失败证据并停止，不得在证据任务内顺手修改业务代码。
9. TASK-005F7 禁止修改前端、`02_源码`、后端业务代码、测试代码、脚本、迁移、`.github` 和 TASK-006。
10. TASK-006 继续阻塞。

### 验收

1. 使用一次性 PostgreSQL 测试库执行 hard gate。
2. settlement JUnit 非 skip 通过。
3. style-profit JUnit 非 skip 通过。
4. 证据文件不包含真实密码、token、Cookie、Authorization、私钥或未脱敏 DSN。
5. 定向 pytest、全量 pytest、unittest、py_compile 通过。
6. 禁改扫描确认未修改前端、`02_源码`、后端业务代码、测试代码、脚本、迁移、`.github` 和 TASK-006。
7. TASK-005F7 审计通过后，才允许评估 TASK-005G。

## ADR-105：外发利润桥接字段属于外发基础 Schema

- 决策时间：2026-04-14 16:20 CST
- 决策类型：数据库 schema 基线 / PostgreSQL 集成测试治理 / 跨模块字段契约
- 关联模块：TASK-005F8 PostgreSQL 外发 Schema 基线与双门禁复跑
- 审计来源：审计意见书第 110 份，TASK-005F7 不通过，真实 PostgreSQL settlement 测试缺 `ly_subcontract_order.sales_order`
- 结论：TASK-005F2 引入的外发利润桥接字段已经成为外发模块基础 schema，所有外发 PostgreSQL 集成测试必须纳入该迁移或完整 Alembic head

### 背景

TASK-005F7 在真实 PostgreSQL 一次性测试库中执行 hard gate，settlement 集成测试失败于 seed 阶段：`ly_subcontract_order.sales_order` 缺列。模型和业务代码已经使用 TASK-005F2 外发利润桥接字段，但 settlement PostgreSQL 测试迁移链仍停留在旧 schema，导致真实数据库无法复现当前模型契约。

### 决策

1. TASK-005F2 外发利润桥接字段属于外发基础 schema，不是款式利润模块私有字段。
2. 所有外发 PostgreSQL 集成测试必须使用包含 TASK-005F2 字段的 schema。
3. 优先方案是 PostgreSQL fixture 执行完整 Alembic head。
4. 如暂不能执行完整 Alembic head，必须显式纳入 `task_005f2_subcontract_profit_scope_bridge.py`。
5. 禁止用 `metadata.create_all()` 替代已存在表的迁移。
6. seed 数据前必须执行 schema assertion，明确检查 `sales_order/work_order/profit_scope_status` 等关键桥接字段。
7. schema assertion 失败必须输出缺失字段列表并让测试失败。
8. settlement 与 style-profit 两组 PostgreSQL JUnit 必须最终均为 `tests=4, skipped=0, failures=0, errors=0`。
9. TASK-005F8 可修改外发 PostgreSQL fixture、CI 静态测试和相关迁移，但不得进入前端、`.github`、`02_源码`、新业务功能或 TASK-006。

### 验收

1. settlement PostgreSQL fixture 纳入完整 Alembic head，或显式纳入 TASK-005F2 迁移。
2. schema assertion 覆盖 `ly_subcontract_order.sales_order/work_order/profit_scope_status`。
3. schema assertion 覆盖 `ly_subcontract_inspection.sales_order/work_order/profit_scope_status`。
4. 真实 PostgreSQL 下 settlement JUnit 非 skip 通过。
5. 真实 PostgreSQL 下 style-profit JUnit 非 skip 通过。
6. 定向 pytest、全量 pytest、unittest、py_compile 通过。
7. 禁改扫描确认未进入前端、`.github`、`02_源码`、TASK-006。
8. TASK-005F8 审计通过后，才允许评估 TASK-005G。

## ADR-106：TASK-005F 阶段进入 G 前必须完成白名单本地基线提交

- 决策时间：2026-04-14 16:26 CST
- 决策类型：本地仓库基线 / 交付可追溯 / 防止混入无关文件
- 关联模块：TASK-005F9 款式利润 F 阶段本地基线提交
- 审计来源：审计意见书第 111 份，TASK-005F8 通过但关键新增/修复文件尚未做本地基线提交
- 结论：TASK-005F~F8 完成后，进入 TASK-005G 前必须做一次显式白名单本地 commit；禁止使用 `git add .` 或 `git add -A`

### 背景

TASK-005F8 已证明 settlement 与 style-profit 两组真实 PostgreSQL JUnit 均 non-skip 通过。但当前工作区仍存在大量已修改、未跟踪和历史目录。若直接进入 TASK-005G，后续审计对象会漂移，关键迁移、测试、防回退和证据文件可能停留在未跟踪状态，也可能与无关历史资料混入同一提交。

### 决策

1. TASK-005F9 只做本地白名单基线提交，不做新功能。
2. 必须使用显式 `git add -- <paths>`，禁止 `git add .` 和 `git add -A`。
3. staged 文件必须通过 `git diff --cached --name-only` 复核。
4. 运行时 JUnit 文件 `.pytest-postgresql-*.xml` 不提交。
5. 历史未跟踪大目录不提交。
6. `02_源码` 不提交。
7. `06_前端` 不提交。
8. TASK-006 文件不提交。
9. 提交前必须复跑定向 pytest、全量 pytest、unittest、py_compile。
10. 提交后必须记录 commit hash、staged 文件清单、排除文件清单和测试结果。
11. TASK-005F9 审计通过后，才允许评估 TASK-005G。

### 验收

1. 本地 commit 成功。
2. commit 文件清单只包含白名单。
3. commit 不包含 `.pytest-postgresql-*.xml`。
4. commit 不包含历史未跟踪大目录。
5. commit 不包含 `02_源码`、`06_前端` 或 TASK-006。
6. 证据文件记录提交前 HEAD、提交后 HEAD、commit message、staged 清单、排除项和测试结果。
7. 审计官复审通过后，才允许进入 TASK-005G。

## ADR-107：F9 证据补提交必须 Docs-Only 且先清理非白名单 Staging

- 决策时间：2026-04-14 16:40 CST
- 决策类型：本地证据补提交 / Git staging 安全 / 防止历史目录混入
- 关联模块：TASK-005F10 F9 证据 Docs-Only 补提交
- 审计来源：审计意见书第 112 份，TASK-005F9 通过但 `TASK-005F9_本地基线提交证据.md` 仍未跟踪
- 结论：F9 证据补提交必须是 docs-only；如果当前存在非白名单 staged 文件，必须先非破坏性取消 staging，再按白名单提交

### 背景

TASK-005F9 已完成本地基线 commit，但 F9 证据文件仍未进入本地仓库。与此同时，工作区可能存在历史未跟踪目录、运行时 JUnit 产物或非本任务 staged 文件。若直接提交，可能把大量历史资料、前端、后端或运行时产物混入 docs-only 证据提交。

### 决策

1. TASK-005F10 只提交文档。
2. 允许提交 F9 证据、F10 任务单、架构文档、ADR、Sprint 清单、架构师日志、审计记录和审计官日志。
3. 禁止提交 `.github/**`。
4. 禁止提交 `07_后端/**`。
5. 禁止提交 `06_前端/**`。
6. 禁止提交 `02_源码/**`。
7. 禁止提交 `.pytest-postgresql-*.xml`。
8. 禁止提交 TASK-006。
9. 禁止提交历史未跟踪大目录。
10. 禁止 `git add .` 和 `git add -A`。
11. 如果已有非白名单 staged 文件，必须用 `git restore --staged <path>` 取消 staging；不得删除文件，不得 `git reset --hard`。
12. 提交前必须用 `git diff --cached --name-only` 验证 staged 文件只包含白名单。
13. TASK-005F10 审计通过后，才允许评估 TASK-005G。

### 验收

1. F9 证据文件进入本地 commit。
2. commit 为 docs-only。
3. commit 不包含 `.github/**`、`07_后端/**`、`06_前端/**`、`02_源码/**`、`.pytest-postgresql-*.xml` 或 TASK-006。
4. 提交后记录 HEAD 和文件清单。
5. 审计官复审通过后，才允许进入 TASK-005G。
