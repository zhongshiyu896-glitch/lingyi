# Z010 浏览器登录态人工前置操作说明

- `task_id=TASK-Z010B-05-IMPL`
- `source_head=1b2fbd8b8a8ddc48058fdeda8f142a7944816288`
- `full_browser_route_smoke_closed=false`
- `Z008-SCRIPT-005=SKIPPED_BY_BOUNDARY`
- `skipped_reason=login_state_env_missing:Z009_BROWSER_LOGIN_READY`

## 当前残余状态

- Z009 orchestrator 已达到 `SKIP_AWARE_LOCAL_CLOSED_WITH_RESIDUAL`，但浏览器链路仍未完整闭合。
- `Z008-SCRIPT-005` 当前状态为 `SKIPPED_BY_BOUNDARY`，原因是 `Z009_BROWSER_LOGIN_READY` 未满足。
- 当前不得外推为生产 readback、go-live 或项目完成。

## 用户人工前置条件

1. 本地 dev server 可用（仅本地开发环境）。
2. 用户在本地开发页面手工完成登录。
3. 仅在本地会话已准备就绪后设置 `Z009_BROWSER_LOGIN_READY=1`。

## 禁止项

- B 不输入密码/MFA。
- B 不读取或导出 cookie/localStorage/sessionStorage/token。
- 不使用生产账号。
- 不访问生产 ERPNext。
- 不执行写请求。

## 只读复跑边界

- 仅访问 `localhost` / `127.0.0.1`。
- 仅允许 `GET` 请求。
- 目标页固定为 7 个 `/fate/*` 页面：
  - `/fate/dashboard/overview`
  - `/fate/warehouse`
  - `/fate/factory-statements/list?z005_readback=1`
  - `/fate/factory-statements/detail?id=1`
  - `/fate/bom/list`
  - `/fate/production/plans`
  - `/fate/reports/catalog`
- 请求端点固定为 B07 allowlist：
  - `GET /api/warehouse/alerts`
  - `GET /api/warehouse/batches`
  - `GET /api/factory-statements/`
  - `GET /api/factory-statements/{statement_id}`
  - `GET /api/factory-statements/supplier-evaluations`
  - `GET /api/factory-statements/factory-evaluations`
  - `GET /api/factory-statements/customer-evaluations`
  - `GET /api/sales-inventory/customers`
  - `GET /api/sales-inventory/warehouses`
  - `GET /api/reports/catalog`

## 复跑后判定

- 仅当登录态满足且全部目标页 PASS，才可闭合 browser route smoke。
- 若登录态仍不可用，继续 `SKIPPED_BY_BOUNDARY`。
- 无论复跑结果如何，不得外推 `production_readback_ready=true`、`go_live_ready=true` 或 `project_completion_claimed=true`。

## 与主线状态的一致性

- `production_readback_ready=false`
- `go_live_ready=false`
- `project_completion_claimed=false`
- `remote_lifecycle_parked=true`
